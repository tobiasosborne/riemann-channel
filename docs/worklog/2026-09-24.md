# 2026-09-24 — The metric as an unknown state: observational channels, the contradiction pathway, the MaxEnt ansatz (record only)

TJO, after the explanation page *The Metric* (https://claude.ai/artifact/C1f9ZywZYyNc5vbXK6p7XZ): treat the
metric as an unknown, unnormalised quantum state; game rules: RH not assumed, the zeros themselves not used,
arithmetic data only as "observational" constraints; question: what kinds of observational data affect the
metric at all. Then: the Weil form looks like matrix elements of an unknown operator; assume it positive and
hunt for violations via matrix elements; RH is the absence of a contradiction; make a Jaynes MaxEnt ansatz
from the known constraints, keep adding constraints and updating; never finding a contradiction means
extremal or in the convex hull. "I just want that precisely recorded."

## What was done

A record, not a round: `notes/metric-tomography/metric-as-state.md`. No claim registered, no prover, numerics
or REFUTE lane. Contents:

- TJO's two messages verbatim, and the game rules as four numbered items.
- The sharpening: `W` is fully known as a functional (the explicit formula); the unknown is its positivity
  and, at Level 3, a construction making positivity manifest. Organising theorem `obs:prime-by-prime-blind`:
  single-place marginals are known and carry nothing, so informative observables are those that mix primes.
- Six channel families with their notebook anchors: dilation windows (CCM/Li, the `zst` learning laws
  `e^{-4 pi x}`, `10^{0.37 gamma}`, `N = 7.5 x`); prime-content windows (two-prime lattices, ledger item
  G1-T10-7, untried); short-range positivity (archimedean, no prime power below `log 2`); mollifiers and
  density theorems (allowed under the rules, not byte-cited); kinematic constraints (dilation, Weyl, real
  structure, grading, level tower, Gamma factor, Weyl law); family channels (Hodge index for curves, Haar for
  Selberg, the tensor-power channel empty for `GL_1`).
- What does not move the prior: symmetry alone (`thm:arithmetic-metric`), the stationary state's own metric
  (`obs:gl1-bond-no-metric`), bounded renormings (`thm:ks-not-deninger-h1`), single-place marginals, numerical
  zeros.
- The proposed first experiment: prime-content tomography on `zst` (lattices `{2}`, `{2,3}`, `{2,3,5}`,
  certified minimal eigenpair, contraction rate against primes added, compared with the dilation window at
  matched information).
- TJO's point made precise (section 7): the Weil form as the Gram table `G_ij = W(f_i * f~_j)`, GNS as the
  unknown operator, existence of GNS = positivity; a violation as a finite certificate from primes alone
  (refutation finite, proof not); the off-line `2 x 2` block (`thm:weil-positivity-finite`,
  `prop:weil-orbit-negative`) as the local shape of any violation; **the Jaynes ansatz for the window channel
  is already in the notebook**: the maximum-entropy (Burg) extension is the centre `c_K` of the disc of
  `prop:extension-disc`, the radius `r_K` is the ignorance measure, the update is the Schur step, a
  contradiction is the true next trace leaving the disc; for prime-content data it is the maximum-determinant
  completion of a non-Toeplitz pattern. "Extremal or in the convex hull": the extremals are the hermitian
  characters (the critical line); `W` in their closed convex hull is RH; `W` is not extremal (one atom per
  zero). Corrected during writing: naive Bochner–Schwartz does not apply (the prime side is not tempered in
  the log coordinate); Weil's block argument plays Bochner's role.
- Next steps if taken up: run the `r_K` diagnostic of shard 08g on `zst`; build the prime-content window;
  byte-cite Burg/Dempster, Bombieri/Yoshida, Selberg/Levinson/Conrey before any registration; REFUTE lane
  before registering 7.2–7.4.

Repository: note, this worklog, HANDOFF entry. No shard touched.
