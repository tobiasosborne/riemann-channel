# Worklog 2026-09-16

## The most consequential step: an infinite object whose odd-block form is derived from the letters

TJO asked what the most consequential step toward a full proof would be. Answer given: close the gap
between "graded spectral transfer" and a transfer whose odd-block unitarity is *derived* from the
letters, on an infinite object, Selberg first (the one case where the trace formula is a theorem, the
divisor is known, and the source of reality is known: self-adjointness of the Laplacian). TJO:
"investigate ... make a brief, hand it off to a codex exec gpt 6 astra xhigh prover, wait, report,
stop"; then "run a review with opus; when finished update labbook commit and push".

Orchestrator's draft `notes/selberg-letters/draft.md` (D1-D8), brief `astra-brief.md`, one codex
gpt-6-astra xhigh lane (~28 min, 2.0 MB log): `astra-proofs.md` (655 lines), 40-row ledger,
`finite/check_letters.py` (131 checks; promoted to `scripts/selberg_letters.py`, 4.7 s, in CI).
Tally: PROVED 0 / CORRECTED 7 / REFUTED 0 / OPEN 1 (D6). Opus REFUTE review
`notes/reviews/selberg-letters-2026-09-16.md`: four independent scripts (153 checks, none importing
the prover's), 31 citations byte-checked, no BLOCKER, no MAJOR; round 1: VALID D1, D2, D4, D7, D8, both
labelled-input theorems (7); MINOR (4) D3 (state the Poisson range), D5 (04f is sketched), D6 (order m at
rho/2 is inferred), ledger (6 of 40 rows answer questions rather than correct claims); cosmetic:
eta_+ lies in DFG's E_u^*; the D3 headline over-credits the letters. Applied (post-review section of
`astra-proofs.md`, shards 03e/03f); the reviewer fetched 1803.06016 (Booker-Lee-Strombergsson
Theorem 1.1 at main.tex:121-122), now `cit:bls-selberg-level-one`. Round 2: D2, D3, D5, D6, LEDGER all
VALID; three residual cosmetic items applied. Promoted to `proved` (reviewer claude:opus-5): D1, D2,
D3, D4, D5, D7, D8, both labelled-input theorems; D6 stays sketched (OPEN by content).

TJO's mid-session check ("the bond is where the Laplacian acts, the ring has physical letters
determined by jumps?"): yes, with the refinement that the Laplacian side is the adjacency and the
critical-line divisor lives on the flow side, tied by the continuous Ihara-Bass (DFG bands).

**Results (shard 03e, `def:stable-transverse-complex` in 02h).**
- D2: Z_S is the ring zeta of the *stable two-term transverse complex* (even -X, odd -X+1),
  Z_S(s) = D_tow(s-1)/D_tow(s), flat supertrace (1-e^t)F. Full transverse forms give the Ruelle
  zeta, whose even band is shifted by -1 and FAILS the notebook's FE and Ram even under coercivity.
  Exact trivial divisors (Ruelle net orders 1, c, 2c-1, 2c at 1, 0, -1, -n; tower cN^2+2; Selberg
  threshold 2 d_{1/4}). Closed geodesics fermionic because dim E_s = 1.
- D3: Omega = lambda(1+lambda) on Res^0 (my ordering sign was wrong); pushforward isomorphism is DFG
  analysis (cited by line), not algebra; reality from Haar; the form must be the ORTHOGONAL sum of
  branchwise pullbacks (the total pullback has Gram [[1,1],[1,1]] on partner branches); operator FE
  J X J^{-1} = 1 - X; at a Laplace eigenvalue 1/4 a Jordan block (algebraic 2d vs geometric d), so
  full operator HP needs the strict bound.
- D4: first-band operator form of the continuous Ihara-Bass proved (quadratic intertwining
  pi_*(A^2+A) = -Delta pi_* plus the horocyclic ladder); sampled adjacency is a cosine functional
  calculus of Delta, not -2 Delta; full-tower compression still not supplied.
- D5: no doubled-bond CP realisation: the flat supertraces are negative distributions and cannot be
  cMPS ring norms; adding the two dissipative jumps preserves neither the K-sector nor the first band.
- D6 (OPEN): Riemann zeros rho/2 are Selberg zeros of the modular surface (FJS divisor theorem,
  byte-checked); the first-band states push to Eisenstein Laurent coefficients, not L^2; my e^{t/2}
  rescaling can never unitarise that sector (its centre is -3/4 under RH; the Riemann model's -1/4
  belongs to -conj(rho)/2, not identified); Maass-Selberg truncated norm positive throughout the
  strip; Selberg's 1/4 for PSL_2(Z) is KNOWN (Booker-Lee-Strombergsson, reported, unverified here).
  Missing: H-CUSP-BRIDGE.
- D7: finite theorem for graphs and graded quantum Hashimoto lifts: Sigma R = R(T + q T^{-1}),
  inverse pushforward F_mu, companion model, letter-derived metric G_C = [[1, S/2],[S/2, q]] with
  C* G C = q G, positive iff STRICT band; endpoint Jordan blocks; exact counterexamples K_3 x Q_3 and
  a ten-letter Pauli channel. Settles the "Hermitian channel plus lift" open item (inverse-paired
  setting). Quantum pushforward uses Ad(U_i), not Ad(U_i^*).
- D8: K-type parity makes X odd (not a flow grading), is not the Hodge grading, and does not
  supercancel (theta factor > 0). obs:selberg-grading-choice corrected.

**Orchestrator corrected (ledger; 34 genuine rows, L04-L06/L20/L23/L26 answer posed questions):** L01 (map direction), L02-L03, L07-L09 (Ruelle/tower bookkeeping), L10
(Casimir sign), L12 (total pullback degenerate), L14 (FE sign), L28 (1/4 known for PSL_2(Z)), L30
(wrong rate on the Riemann sector), L32 (endpoint not an exception to the isomorphism), L35
(Ad(U_i)). Pattern held: drafting slightly too strong and letting one xhigh lane audit is productive.
