# 2026-09-20 — A finite graph with a cusp: resonances, the one-exit contraction, the rebound channel in discrete time (shards 04i, 04j)

TJO's theme, restated at the start: RH, the functional equation and the rest are best read as a
Lindblad process whose absorption spectrum (Connes) is the zeros; the ring norm is a supertrace
because the auxiliary system is Bose--Fermi; RH is the Ramanujan property of that process, i.e. the
cMPS is a continuous quantum expander; the Lindbladian should act on a state space tied to a
hyperbolic process; the jump operators are unclear. The corpus check (morning) put the jump shape at
shard 04h: cusp exit plus rebound, with the arithmetic identification of the rebound open. TJO asked
how to explore that on the simplest example closer to zeta. Answer: a finite graph with a cusp, the
ray attached with the weights of the tree quotient, where the arithmetic supplies the exit; then the
question "what do you mean by cusp" (answered: the ideal vertex of the fundamental domain, a funnel of
finite area through which only the angle-independent mode escapes, hence a one-dimensional exit;
the graph version is a ray).

## The round (protocol: astra prover, blind Opus numerics, Sonnet and Opus literature, Opus REFUTE)

Brief `notes/cusp-graph/astra-brief.md` (C1--C3, T1--T8, drafted slightly too strong). Lanes:
codex `gpt-6-astra` xhigh prover (`astra-proofs.md`, 809 lines, 34-row correction ledger, 138k
tokens, one network reconnection); Opus numerics blind from the brief (`scripts/cusp_graph.py`,
569 checks in 10 s, `numerics.md` with a 19-row ledger D1--D19); Sonnet literature lane
(`sources.md`, 8 arXiv TeX sources plus Efrat 1991 and Morgenstern's 1991 preprint fetched, manifest
green); Opus lane on TJO's recollection of Andrew Childs (`childs-lead.md`, 10 more TeX sources);
Opus REFUTE review (`notes/reviews/cusp-graph-2026-09-20.md`: 21 VALID, 3 MINOR (T1b, T1c, T8), 0 INVALID, 2880 independent checks).

## What was found

- **Prior art, decisive.** Arends--Peterson--Weich, arXiv:2603.26443 (2026), define resonances on
  geometrically finite graphs of groups by a resonance matrix that is exactly the rank-one perturbed
  Bass determinant `p` of the brief, compute two elliptic-curve tree quotients whose resonances are
  the square roots of the Hasse--Weil zeros on the circle `q^{-1/4}`, and leave the wave-equation /
  Lax--Phillips construction to follow-up work. Childs--Strouse, arXiv:1103.5077 (Levinson's theorem
  for graphs), contain the reflection coefficient of the brief verbatim, sign included, the
  bound / half-bound / confined classification, and the count via the winding of the phase. Kaneko--
  Koyama, arXiv:2303.09327, give the level-`A` constant term over `F_q[T]` as the zeta ratio; no
  source states that the `Gamma_0(N)` scattering matrix has Dirichlet `L`-zeros as poles (H-ARITH).
- **Corrections of the orchestrator's brief** (prover and numerics independently): the determinant
  form of `R` needs a minus sign (`R = -p/ptilde`, no power of `z`); the junction measure is
  `m_0 = 1`, `m_n = (q+1) q^{-n}`, and `c^2 = q+1` is right; cusp forms outside the band are `l^2` but
  invisible; threshold roots at `z = +-1` also cancel (Childs's half-bound states, on the critical
  line under `z = q^{s-1/2}`); the outgoing profile must start at `k >= 2`; the energy form is
  hyperbolic on bound-state data, and deleting them breaks the orthogonality of the projected
  radiation spaces (exact cross energy 3/32 on the pure `q = 2` cusp); the repair
  `D_+ = O_+ cap O_-^perp` gives `Theta = eta R B_bd`, the pole-removed inner part, `dim K = #res`,
  `spec Z` = resonances (not reflections); neither `R` nor `1/R` is the characteristic function with
  bound states present; `phi = 1/R`; uniqueness of the stationary density is unconditional and
  attraction needs aperiodicity, which fails iff `c = 1` when the rebound contains an eigenvector but
  can fail with nonreal resonances otherwise (`+-ir` example); the Gram identity
  `<k_n,k_m>(1 - conj z_n z_m) = conj(a_n) a_m` forces every mode to see the exit.
- **Structure.** RAM (Ramanujan diagram) and RH (all resonances on one circle) are independent
  already for one- and two-vertex cores; the product of all roots of `p` is exactly `1 - c^2`, so the
  common radius is fixed by the coupling and the visible bound states, never universally `q^{-1/4}`,
  and a full-weight cusp (`c^2 = q+1`) on a core whose only bound states are the Perron pair has no
  resonances at all (the pure cusp's emptiness is forced); the cusp sees only the Krylov space of the
  attachment vertex; `K_4` with `q = 2` satisfies RH (moduli 0.752259), the 3-cube and Petersen do not.
- **Transcription of 04h** to discrete time: CPTP renewal channel with explicit Kraus operators,
  finite mean always, mode-diagonal stationarity iff equal moduli with mean holding time
  `1/(1 - r^2)`, admissibility iff positive loss, flags realise every spectrum for every stable
  contraction, odd modes protected. A graph bound state is not a stationary even vacuum (time
  multipliers `beta^{+-1}`, indefinite block): the even exit is still missing.
- **Exposition.** `notes/cusp-graph/physics-picture.md` (Opus): the BSc scattering picture,
  Feshbach self-energy `c^2 z` vs `c^2/z`, quantum jumps = no-event contraction plus rebound, RH as
  equal linewidths with the functional equation as a width sum rule (paired widths add to 1/2, RH says
  no pair is split; equal width is not equal Q), the zeros as coherences; the primes live in the Wigner
  time delay. Upgraded to an interactive artifact (`physics-picture.html`) at TJO's request.

## Registered

Shards `04i_cusp_graph_scattering.tex` (definition, C1--C3, seven cited facts, T1, Gram lemma, T2) and
`04j_cusp_graph_renewal.tex` (definition, T3--T7, Krylov observation, numerics, H-ARITH). Bibliography:
Arends--Peterson--Weich, Childs--Strouse, Childs--Gosset, Kaneko--Koyama. Ten provenance rows,
byte-checked. Reserved-letter lesson applied again (`\beta`, `\mu`, `\tau`, `\ell`, `\Lambda`, `\xi`
all collide). Gate: 402 claims, 0 errors, 0 warnings; fresh build.

## Next

The arithmetic round on the elliptic-curve quotient of Arends--Peterson--Weich (`y^2 + y = x^3 + x + 1`
over `F_2`, Takahashi's Theorem 5 for the stabiliser data): build the diagram with its cusps and
junction weights, confirm the resonance determinant, construct the one-exit (or `h`-exit) contraction
and run the renewal channel with a rebound chosen from the arithmetic (Hecke-invariant, or the
function-field Bost--Connes state); test whether the arithmetic picks the rebound. In parallel,
H-ARITH for `Gamma_0(N)` over `F_q[T]` from Li 1979 / Kaneko--Koyama.

---

# 2026-09-20, evening — Two arithmetic cavities with cusps: the elliptic-curve diagrams, the zeta identification, the hedgehog, the arithmetic metric, and the vacuum (shards 04k, 04l, 04m)

TJO clarified the target: the Phantasm is a *system* (Bost–Connes, the modular surface, or something else)
whose resonances are the Riemann zeros; the physical picture is an arithmetic cavity coupled to cusps, "a
spiky object, like a hedgehog", and the modular surface may be being promoted too hard as its realisation.
Later in the session TJO added the picture of an arithmetic skeleton under the modular surface in which the
single cusp decomposes into many cusps talking only through the core, "a cusp per prime", the cusps
integrated out into probability sinks patched by exit spaces, and the cMPS as the minimal Stinespring
dilation of the core dynamics; and asked whether funnels are the even sector. Both were answered in the
conversation (the level tower Γ_0(N) is the honest cusp per prime, its diamond operators carry
(Z/N)^× whose inverse limit is the Bost–Connes symmetry group; a funnel flips the constant mode from a
bound state at z = q^{-1/2} to an outgoing state at the same value) and the funnel statement was put to
the prover.

## The round (protocol: one astra xhigh prover, blind Opus numerics, Opus literature, Opus REFUTE)

Brief `notes/elliptic-cavity/astra-brief.md` (C1–C3, T1–T6, drafted slightly too strong). The two
diagrams were transcribed from APW's figures (`serre_example.png`, `takahashi_example.png`) and checked
before the brief was written (degrees q+1 everywhere; APW's displayed polynomials reproduced as
z^2 · F_2 / 2 and z^4 · F_3 / 3). Lanes: codex `gpt-6-astra` xhigh (`astra-proofs.md`, 934 lines, 44-row
ledger, 216k tokens, one network reconnection, 63 min); Opus numerics blind from the brief
(`scripts/elliptic_cavity.py`, 192 checks in 51 s, `numerics.md`, ledger D1–D35); Opus literature
(`sources.md`, 62 kB; seven arXiv TeX sources and Sørensen's 2001 Aarhus preprint fetched, manifest
250 → 258 lines; Takahashi 1993, Li 1979, Gekeler, Efrat 1987, Hejhal not accessible); Opus REFUTE review
(`notes/reviews/elliptic-cavity-2026-09-20.md`: 19 VALID, 5 MINOR, 0 INVALID before fixes, 24 VALID after, 597 independent checks; three new results: the Euler–Poincaré mass Σ1/S(v) − Σ1/S(e) = −ζ_K(−1) on both diagrams, D3's scattering matrix as a Z/4 group matrix in Sørensen's literal form after the equal-depth cut, and the z = 0 delay sector as a cut artefact with the corrected genus law |[z^m]p| = q^{1−g−(h−1)(g−1)}).

## What was found

- **The diagrams.** Every arithmetic junction has c^2 = S(v)/S(e) = 1, not the Nagao q+1; so p(0) = 0 and
  both cavities have delay modes at z = 0 (double on D2, quadruple on D3; ord_0 p = 2 dim ker(1 − C)).
  The h-exit scattering matrix is S = (zΓ − 1)^{-1}(1 − Γ/z) = −Q(z)^{-1}Q(1/z) (Childs–Gosset), det S =
  (−1)^h p/p̃, and APW's μ is the notebook's z (det H = (−1)^n (2μ)^{-n} p). D2 is confirmed weight for
  weight by Lorscheid's independent class-number-one figure; D3 rests on APW's figure alone (Lorscheid's
  Hecke graph for the same curve is disconnected: it is not the tree quotient when h is even).
- **The zeta identification, exactly.** 1/R_2 = z^{-2} ζ_K(2s−1)/ζ_K(2s) for the F_2 curve; the four
  resonances are z^2 = 1/α on |z| = 2^{-1/4}; Z^2 on the Hasse–Weil sector is similar to Fr^{-1} ⊗ I_2
  and Z anticommutes with the bipartite sign. The nonzero-root product is 1 − ||b||^2 = −1 on both
  diagrams, so the Hasse–Weil product is q^{-1} and the equal-modulus hypothesis pins r = q^{-1/4}
  (numerics lane); the same coefficient at genus g would contradict Weil, the round's sharpest testable
  prediction. Review finding 7 of the previous round had a false premise (c^2 = q+1).
- **The hedgehog.** D3's Klein symmetry gives S = (−1) ⊕ z^2 ⊕ S_e with det S_e = z^2 ζ_K(2s)/ζ_K(2s−1);
  S_e has no constant eigenbasis, but the one-step height renormalisation diag(1,z) (the two Q-cusps start
  one step deeper) diagonalises it into (zeta block, z^2): one zeta channel and three monomial channels,
  exactly the genus-one class-character prediction (the numerics lane found this first; the brief leaned
  the other way). Sørensen's number-field theorem needs a simply transitive class-group action on cusps,
  which the graph does not have; the central multiplicities m_± = (h ± (h[2] − 2))/2 = 2, 2 match APW's
  ±1 resonances exactly, so the apparent tension with the trace formula dissolves.
- **The rebounds.** The canonical cusp rebound of D2 ("flux returns where it left") is a pure delay mode:
  Θ(0) = 0 forces Zj = 0, holding law δ_1, ρ_∞ = |j><j| absorbing, never charging the zeros. Modal
  Hasse–Weil rebounds are all stationary (Weil), mean 2 + √2, exit rate 1 − q^{-1/2}, Gram spectrum.
  The metrics making q^{1/4} Z unitary form a 3-parameter cone whose parity-and-reality-invariant part is a
  single ray; the Lax–Phillips metric is outside (angles 80.1°, 65.5°, 79.3°); in the arithmetic metric the
  Hasse–Weil defect has rank four, so an arithmetic renewal needs one exit per zero mode. The h-exit
  channel: stationary densities ↔ stationary exit densities of an h^2 × h^2 instrument; uniqueness is not
  irreducibility; the common-modulus theorem holds for fixed resets and fails for exit-dependent rebounds.
  Correction to 04j: every modal reset is aperiodic even at c = 1.
- **The vacuum.** On a finite-volume regular diagram the constant mode is a bound state with wave
  multipliers q^{±1/2}: never stationary, never leaking, for any number of cusps. A funnel enters p with
  self-energy C/(qz) (confirmed on APW's tree and modular-curve examples), and the constant function is
  outgoing on a funnel at z = q^{-1/2}, but cannot be outgoing on a cusp and a funnel at once; and lattice
  quotients have no funnels (Lubotzky, via APW), so the cusp-plus-funnel toy must be built by hand.

## Registered

Shards `04k_elliptic_cavity_scattering.tex`, `04l_elliptic_cavity_channel.tex`, `04m_elliptic_cavity_sources.tex`;
bibliography: Sørensen 2001, Lorscheid 1012.4825, 1012.3513; sixteen provenance rows; two definitions.
Reserved-letter lesson again (`\tau`, `\beta`, `\mu`, `\ell`, `\Sigma`, `\sigma`, `\xi`, `\alpha`, `\Phi`,
`\Delta`, `\upsilon` all collide; two warnings remain from verbatim APW quotes containing `\mu`, `\ell`).
Gate: 439 claims, 0 errors, 2 warnings (verbatim APW quotes containing `\mu`, `\ell`); fresh build. Corrected in 04j: the aperiodicity clause of `thm:cusp-renewal-discrete`(c). Registered the reviewer's two results as sketched propositions (`prop:d3-group-matrix`, `prop:euler-poincare-mass`) for the prover next round.

## Next

(i) The cusp-plus-funnel toy by hand. (ii) A genus-two (or Klein-class-group) diagram to test the
coefficient −1 and the 4h + 4 prediction. (iii) Γ_0(N) over F_q[T], N a product of two primes: the level
hedgehog with a cusp per divisor and Dirichlet L-functions (H-ARITH). (iv) The arithmetic-metric completion
with one exit per zero mode.
