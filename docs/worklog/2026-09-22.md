# 2026-09-22 — Astra day: four prover lanes on the Deninger / adelic-bond synergy (H-THETA-1, the LPS square complex, Deninger on the GL_1 bond, the Ihara square complex with a cusp)

TJO: "today is astra day"; use `codex gpt-6-astra` at xhigh as much as possible for proofs and ideation, up to four
lanes at once, Fable keeps the big picture, merges carefully and updates the state of knowledge; no Opus/Sonnet
subagents. Network patchy: lanes made resumable (`scripts/astra_lane.sh`: detached, session id captured, automatic
`codex exec resume` on failure, incremental writing of `astra-proofs.md` demanded by every brief).

## Inputs ingested

- `~/Documents/Codex/2026-09-21/in-x20/`: a Codex research note "Faces, grading, and positive dynamics" (21 pp.,
  121 exact SymPy assertions, PASS) on a finite Deninger-type package: Theorem 5.2 (an invariant positive
  polarization exists iff the normalised cohomological dynamics is semisimple with unimodular spectrum; certificate
  `A^T G A = qG`), Definition 6.1 / Theorem 6.2 (the finite package), Theorem 7.1 (conformal torus maps
  `q = a^2 + b^2`), Theorem 8.1 (reciprocal bouquets on two tori), Section 9 obstructions, Proposition 9.3 (Bass
  doubling: positivity iff `||T|| < 2 sqrt q`; `Omega_B` is not the face pairing). Staged under
  `notes/deninger-lps/src/` (tex, pdf, text, script, json).
- `~/Downloads/LPS_Deninger_worked_example.md` (the earlier Codex chat): the (13, 17; level 5) LPS square complex,
  120 vertices, 840 + 1080 edges, 7560 squares, total Betti `(1, 0, 5759)`, horizontal cohomology 721, transverse
  `T_1 = T_17`-transport, `chi_T` fully factored, `||T|| < 8`, Bass doubling on `K (+) K`, positive `G`, quarter-turn,
  suspension Hamiltonian, graded Euler product with the `(1 - u^2)^{5760}` correction. Staged as
  `notes/deninger-lps/src/lps-deninger-worked-example.md`.

## Lanes (briefs by claude:fable-5.1, maths worked out before dispatch; prover codex:gpt-6-astra xhigh)

| Lane | Brief | Session | Content |
|---|---|---|---|
| A | `notes/h-theta-1/astra-brief.md` | `01a0c805-114a-72d3-808d-224215ab6dce` | H-THETA-1 reduced to a Cauchy-kernel (Nevanlinna–Pick) criterion: rank-one of `-(d+d')G` iff the evaluators are Cauchy kernels up to scalars; in the de Branges structure of the Sonine space this is "`E_1^#/E_1` constant on the zeros"; expected refutation in Burnol's norm, rescue only by the model-space norm (tautology). Also: `K_bad` as a renewal channel with synthetic zeros; Burnol's `K_{v_+^2}` as the pole `s = 1` Jordan block vs the even sector. |
| B | `notes/deninger-lps/astra-brief.md` | `01a0c805-117f-78b2-b397-34d138f25a8d` | The LPS square complex is `Gamma(5) \ (T_14 x T_18)`; `K = H^1(X^{13,5})`; `T_1 = T_17`; spectrum = `a_17` of 13-Steinberg newforms (Jacquet–Langlands); Bass doubling = Frobenius at 17 on the etale `H^1`; `Omega_B`, `G` = Poincaré and Petersson; positivity = Eichler–Shimura; same object as the notebook's Weil–LPS (13; 17) channel at level 13; the symmetric-ray question non-vacuous at genus 721. |
| C | `notes/deninger-bond/astra-brief.md` | `01a0c805-114e-71d2-a23f-421f37d4e23f` | Deninger's package on the GL_1 bond: constant terms = `H^0, H^2` with reference rates, `q_t = e^{-t/2}`; the invariant pairing pairs `rho` with `1 - rho` (functional equation); positive compatible metric on the spectral side iff RH + simple zeros (Theorem 5.2 transcribed); NO bounded metric on `K_S` even under RH (Riesz failure); the canonical point is Weil's form (Deninger on the bond = Weil's criterion); the conformal torus map = the CM class-group bond of 04p. |
| D | `notes/deninger-cusp/astra-brief.md` | `01a0c805-1164-7f00-af5b-3d695d893c1d` | The Ihara square complex `Gamma_1(N)[1/p] \ (H x T_p)`: horizontal cohomology = Eichler–Shimura, Eisenstein classes = radical of the cup pairing = Bass-doubled even poles = the exit sector; the level-`N` scattering matrix as a diagonal matrix of GL_1 theta-channel edge quotients (Huxley transcribed; the twisted Siegel theta); the level-`N` Riemann channel = direct sum of character channels, one exit per class character (H-ARITH over Q). |

Parallel `codex exec` lanes from one cwd verified by a smoke test (two trivial lanes, both exit 0) before launch;
the earlier "one lane at a time" trap was the desktop app attaching, not `codex exec`.

Blind numerics lane B (Fable fork, may not read astra's output): `scripts/lps_square_complex.py` rebuilds the
complex from the quaternion sets and tests the worked example's numbers and the brief's finite claims.

Reviews today: Fable REFUTE reviews (reviewer `claude:fable-5.1`, author `codex:gpt-6-astra`; the gate requires
reviewer != author).

## Results (first wave, all four lanes landed by 10:07; reviews by Fable forks, numerics by Fable forks)

**Lane A, H-THETA-1 (shard 04s).** `conj:h-theta-1` REFUTED unconditionally: a one-exit realisation of stable modes in a
given norm is exactly the Cauchy-Gram condition (loss Gram positive of rank one); Burnol's level-one evaluators at
critical zeros have a real Gram (fixed by the cosine antiunitary), and three distinct critical zeros give a non-real
cyclic product of the factors -(d_i + conj d_j) (11.4772516480 - 37.3489700175i at the first three). The Riemann model
space passes identically (loss all ones). The augmented cosine space L_1 (not K_1) is a de Branges space after
multiplying by s(s-1); its evaluator Gram comes from one compact integral equation; at three zeros the loss spectrum
is (-0.0924, 0.469, 1.123), numerically, enclosure open. Every rescuing norm is a diagonally rescaled Cauchy norm
whose completion is K_S with full multiplicities. The bad-zero renewal channel needs a reset and a clock (finite-time
defects have full rank; one-zero mean 1/(sigma - 1/2)). Burnol's v_+^2 is one damped chain span{y^{-1/4}, log y
y^{-1/4}} on y > 1; the square is the quotient of his two radiation maps; the Eisenstein factor r has degree one.
28 corrections to the brief. Review 10 VALID / 0 / 0 (79 checks); numerics `scripts/h_theta_1.py` 48/48.

**Lane C, Deninger on the bond (shard 04t).** The theta constant terms are formal H^0, H^2 with reference rates 0, -1/2,
q_t = e^{-t/2}; the functional equation gives an explicit real alternating full-jet pairing (the brief's sgn(gamma)
was anti-real; the correct coefficient is -i sgn(gamma)); a positive invariant metric on a finite full-jet span exists
iff critical AND simple; J e_rho = -i sgn(gamma) e_rho. New theorems: no metric boundedly equivalent to the energy
metric can make C_t a similitude even under RH + simplicity (Riesz failure), and sup_t e^{t/4}||Z_t|| = infinity
unconditionally (exact two-mode norm sqrt((1+r)/(1-r)) along close pairs; 20.56 at the closest pair below 3000). A
uniformly bounded semigroup with dense unimodular eigenvectors would unitarise. The unit-coefficient form is an
unbounded closed form. Weil's form is RH but a trace does not select the metric (refutes the brief's "Weil form
canonical"). The two-prime orbit quotient is non-Hausdorff. The Codex note's conformal torus map is the CM curve
y^2 = x^3 - x with primary Frobenius pi = 1 mod (1+i)^3, h = |pi - 1|^2: the genus-one class-group bond. Review 7 VALID
/ 0 / 0 (51 checks); numerics `scripts/deninger_bond.py` 51/51.

**Lane B, LPS square complex (shard 04u).** X = Gamma(5) \ (T_14 x T_18) for the primary quaternion lattice (Mozes,
Rattaggi; B is a division algebra); K = H^1 of the LPS graph Lambda_13(5) \ T_14; T_1 = the degree-18 T_17
correspondence; the directed new space is K + K' with the bipartite sign intertwining T_17 with -T_17 (blind numerics
found it first); vertex functions are automorphic forms on the Hamilton algebra with level U_2 = image(1 + 2 O_2) at 2
and principal level at 5; K = the Steinberg-at-13 newspace (reversal -1), T = a_17, |a_17| <= 2 sqrt 17 is
Eichler-Shimura-Igusa; K = 6 Reg + 1 as a PGL_2(F_5)-module (seven irreducibles, not eight). The Bass doubling is
Frobenius at 17 as a one-operator system; the cup/Petersson comparison is NOT supplied (open); the certificate G
differs from the compatible metric G/r(T); the invariant metric cone has real dimension 16539. The Weil-LPS (13;17)
channel is a different Fourier selection of the same Hecke family (End(W_-) = 1+7+14+14, no Steinberg type; exact
odd polynomial shares only (x-3)^8(x+3)^3(x+5)^6 with chi_T). Exact graded CP realisation of the net divisor open
(direct sectors have Tr E^2 = -10940; scalar signs cancel under Ad). Review 8 VALID / 0 / 0 (40 checks); blind
numerics `scripts/lps_square_complex.py` 48/50 (the 2 failures are the brief's).

**Lane D, the cusp square complex and level N (shard 04v, pending review).** The literal square complex is refuted
(H x T is three-dimensional; two vertex types); the horizontal slice's H^1(Y_1(N)) splits by Eichler-Shimura and
Manin-Drinfeld with Eisenstein T_p-eigenvalues chi_1(p) + p chi_2(p) (not p + 1 in general); the four-fold identity
"Eisenstein = cup radical = even poles = exit" is REFUTED (ordinary cup on H^1(Y) is zero; the compact-support pairing
is perfect; dimensions h - 1, 1, h differ); the Bass companion of all of H^1 is not Frobenius. Positive results:
explicit prime-level scattering matrices with all constants (two cusp coordinates per even nebentypus,
A_psi = q^{1/2-s} Lambda_psi(2s-1)/Lambda_psi(2s); rational trivial block; residue 3/(pi(q+1)) all-ones; the full
Gamma_1(q) determinant), the twisted Siegel theta edges (the two edges are at different cusps; odd characters vanish
in weight zero), the conjugation-correct symbol Theta_chi pure Blaschke unconditionally, the prime-level model
decomposition with local factors and delays (K_AB = K_A + A K_B), exit rank exactly h at prime level (one exit per
scalar coordinate), GRH-plus-no-Jordan-chains criterion and the no-boundedly-equivalent-metric extension to
characters. asm:h-arith-gamma1 refuted as a Q statement. Numerics `scripts/deninger_cusp.py` 163/163.

**Lane E, genus two (landed 10:20, review/numerics running).** Counts (3, 31, 117, 619), Pic^0 = Z/15 generated by
[(1,1) - infinity], theta classes, special counts (1, 3, 20); all fourteen L(T, chi) and the genus-16 pointed class
field cover with P_{C'} = P prod L(T, chi); the trivial Frobenius block has a two-parameter positive cone (one ratio
modulo scale); the CM field is Q(sqrt 21, sqrt((-25 + 3 sqrt 21)/2)) with maximal order and reference-lattice ratio
(25 + 3 sqrt 21)/(2 sqrt 109); a rational marking with equal polarization weights is impossible (it would force an
imaginary quadratic subfield); the bond-to-H^1 comparison is open; the literal degree cut does not realise H^1
(infinite or nilpotent).

## Second wave (running): cerednik-drinfeld (the LPS horizontal cohomology as the character group of a Shimura
curve's toric reduction; monodromy pairing), level-local-cavity (the prime-level local scattering factor as the
notebook's finite cusp-graph cavity), connes-weighted-metric (the unbounded arithmetic metric of C4 versus Connes's
delta-weighted norm), h-arith-fqt (H-ARITH over F_q[T]: the Gamma_1(N) scattering matrix exactly).

## Second and third waves (landed 10:31–11:24), and the stop

- level-local-cavity (04x): reviewed 7 VALID; numerics 68/68. cerednik-drinfeld (04z), connes-weighted-metric (04y):
  prover done; reviews and numerics forks terminated by the Fable spend limit (numerics partial: 13/13 and 34 pass).
  deninger-cusp (04v) review likewise terminated. Their claims are registered as sketched with REVIEW PENDING notes.
- h-arith-fqt, genus-two-quotient-graph (H-CLASS proved at genus two: nucleus 366 vertices / 915 edges / 15 rays),
  lps-graded-cp: prover outputs saved, unreviewed.
- sonine-enclosure: attempt 1 died on "Selected model is at capacity"; the seven resumes failed with
  `error: unexpected argument '-s'` (`codex exec resume` takes `-c sandbox_mode="workspace-write"`, not `-s`); runner fixed.
- Then the codex quota was exhausted (TJO). Model switched to Opus 5 for the final save; no subagents used after that.
