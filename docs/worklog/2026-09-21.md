# 2026-09-21 — The adelic symplectic space as the bond: the class-group bond, the theta functional as its state, the cusps as the GL_1 bond, Riemann's theta for Q (shard 04p)

TJO restated the genesis of the campaign: attach a symplectic space to `Q^x \ (R_+ x prod_p Z)`, get a
Heisenberg–Weyl representation and hence an automorphism. Four Opus distillation lanes first collected what
the repository had already established (Weil representations and where unitarity came from; Bost–Connes,
adeles and where the zeros sit; the founding transcript's record; the energy-form and metric findings of the
cavity campaign). Reconsideration, in conversation: the naive quotient collapses to `R_+` with commuting
dilations and is the blind side A (`obs:prime-by-prime-blind`; the Weyl-covariance collapse and the symplectic
odd-sector counterexample of shard 04d); the symplectic space lives on the additive side, `A (+) A` with the
rational Lagrangian, the state is the theta functional, the automorphism group is `SL_2(Q)` through the Weil
representation, `Q^x` its torus, the functional equation Poisson summation; unitarity of a metaplectic operator
carries no information (Artin–Schreier: Parseval first, Weil implementer read off after), so the whole problem
is the canonical metric, where the notebook's `thm:arithmetic-metric` says the unitarising cone exists and the
physical metric is outside it. TJO then asked whether it is consistent to take the adelic symplectic space as
the *bond* (auxiliary) system rather than the bulk. Answer: yes, and the bulk reading is exactly the blind one;
the bond reading identifies the bond.

## The round (Fable brief and proofs, blind Opus numerics, Opus REFUTE review)

Brief `notes/gl1-bond/brief.md` (B1–B5), proofs `notes/gl1-bond/proofs.md` (author `claude:fable-5.1`, five-row
correction ledger), blind numerics `scripts/gl1_bond.py` (159 assertions in 5 s, `numerics.md` ledger D01–D31,
152 pass; the 7 failures are the brief's own defects: the theta Mellin constant, its strip of convergence, the
`theta(x^2)` normalisation, and two imprecisions), review `notes/reviews/gl1-bond-2026-09-21.md` (1 VALID / 4 MINOR / 0 INVALID before the fixes, 5 VALID / 0 MINOR / 0 INVALID after, 228 independent checks in four scratch scripts).

## What was found

- **The class-group bond.** For a function field with a rational point, the units-invariant part of the adelic
  bond is `l^2(Pic^0) (x) l^2(Z)` with the degree shift as transfer; its ring norms are the effective-divisor
  counts `a_n = sum_{Pic^n}(q^{h^0(D)}-1)/(q-1)`, generating `Z(T)`; Riemann–Roch makes `a_n` generic for
  `n > 2g-2` (the Perron pair, even) and the special degrees (the theta divisor) carry `P(T)`, the `H^1` factor.
  At genus one `Z = 1 + hT/((1-T)(1-qT))`, `P = 1 - (q+1-h)T + qT^2`, `h = P(1) = N_1`: the bond dimension is the
  zeta numerator at `T = 1` and fixes the Frobenius roots (D2: `1-2T+2T^2`; D3: `1+3T^2`).
- **The theta functional is the bond state.** `Theta(x 1_{O_A}) = q^{h^0(div x)}`; Tate's integral of the unit ball
  is `Z(T)`; Poisson summation on `A/K` for the unit ball is Riemann–Roch, so the Weyl element (adelic Fourier
  transform) acts on the bond state as `D -> K_C - D`; the two poles are the two constant terms of `Theta`.
- **The cusps are the GL_1 bond.** Cusps of `GL_2(R)\T` are `Pic(R) = Pic^0(K)`: one for D2, four for D3. The
  hedgehog's spikes are `l^2(Pic^0)`; H-CLASS is the Fourier transform on it; the three monomial channels of D3
  are the three nontrivial class characters, whose Hecke `L`-functions are `1` at genus one. `h = 4 = dim K_HW`
  at D3 is an accident (`P(1) = 4g`), not "one cusp per zero mode".
- **Over Q.** Bond `L^2(R_+^x)`, one cusp, bond state Jacobi's theta (`Theta(xf) = theta(x^2)`), even sector the
  constant terms `1, y^{-1/2}` exchanged by `y -> 1/y`, and `int_0^inf (theta - 1 - y^{-1/2}) y^{s/2} d^x y =
  2 pi^{-s/2} Gamma(s/2) zeta(s) = 4 xi(s)/(s(s-1))` in the strip only (the brief's `2 xi/(s(s-1))` and "for all
  `s`" were wrong; numerics F1, F2): Riemann's 1859 argument is the `Q` case. H-THETA (`conj:h-theta`): the
  compressed dilation on the odd theta's cyclic space, outgoing half removed, is the Riemann channel on `K_S`.
- **Not gained: the metric.** At genus one the roots are fixed by `h` and their modulus is Hasse; any
  Frobenius-normal metric is diagonal in the eigenbasis and the symmetries fix its ratios, so the
  symmetric-ray-is-Hodge test is vacuous at genus one (numerics D29, D30); the Kraus dichotomy stands.

## Registered

Shard `04p_gl1_bond.tex`: `def:class-group-bond`, `prop:class-group-bond`, `prop:theta-functional-bond-state`,
`prop:cusps-are-class-group-bond`, `prop:riemann-theta-dilation-bond`, `conj:h-theta`, `obs:gl1-bond-no-metric`,
`num:gl1-bond`, `obs:gl1-bond-next`; notation `\jtheta`; bibliography Serre, Weil (no local source). Gate: 467
claims, 0 errors; fresh build.

## Next

`obs:gl1-bond-next`: (i) H-THETA numerically through the first zeros; (ii) a genus-two class-group bond and the
first non-vacuous metric test; (iii) the Frobenius channel on the class-group bond in the Hodge metric; (iv) the
two cusps `0, infinity` of GL_1 against the one cusp of the modular surface.

# 2026-09-21, afternoon — H-THETA: the odd theta vector, its refutation, the two edge transforms, Burnol's causality (shards 04q, 04r)

TJO asked about H-THETA; it lived on the branch `claude/beautiful-bohr-e84nm9` (shards 04n, 04o, 04p), which was
merged into `claude/inspiring-ride-n5binf` first (commit `5e9c4c3`: report/README and report.pdf regenerated, the
Sørensen text re-fetched with matching hashes, gate 486 claims / 0 errors). Then "go hard on H-THETA, use astra
xhigh after working out the maths carefully".

## The maths worked out before dispatch (brief `notes/h-theta/astra-brief.md`, author claude:fable-5.1)

The odd part `phi = theta - 1 - y^{-1/2}` satisfies `phi(1/y) = y^{1/2} phi(y)` exactly; the weight `y^{1/4}` is the
only one making `g = y^{1/4} phi` square-integrable and inversion-invariant, and it is Connes's `|x|^{1/2}` (Burnol's
subtracted `E`). Its Mellin transform is `-xi(1/2 + 2i tau)/(tau^2 + 1/16)`, decaying like `e^{-pi|tau|/2}`, so it
violates the Szegő condition and its forward dilation orbit is dense: the literal H-THETA is degenerate. The
correct GL_1 statement is Burnol's causality theorem (2000): a Lax–Phillips system on the GL_1 bond whose outgoing
subspace is the orthocomplement of the theta images of unit-ball test functions satisfies every axiom except
causality, and causality is equivalent to RH. And the GL_2 symbol is a ratio of two GL_1 theta channels:
`Theta(tau) = [Lambda(1+2i tau)/Lambda(2i tau)] (tau - i/2)/(tau + i/2)`, the odd theta read on the two edge lines of
the strip, which are exactly the two constant-term coefficients of the completed Eisenstein series (Mellin transform
of the Siegel theta). Six identities checked with mpmath before dispatch. Sources fetched: Burnol math/0001013,
0203120, 0112254, 0407443, 0602425, 9911175, 9809119, 0208121; Meyer math/0311468; Uetake 2007 (Ann. Polon. Math.
92.2, pdftotext, `refs/src/uetake-2007/`).

## The round

Astra prover lane (xhigh, 35 min, `notes/h-theta/astra-proofs.md`: ledger T1–T6, 41-row correction ledger, three
restated conjectures); blind Opus numerics (`scripts/h_theta.py`, 98 checks, 98 pass, ledger D01–D68, findings F1–F16:
the brief's Poisson factor `y theta(1/(ty))` should be `t^{-1} theta(1/(ty))` (F1), the exchange involution is
`F(t) -> t^{-1} F(1/t)` not `t -> 1/(y^2 t)` (F2), the second Mellin identity's test point was outside its region
(F3), the pointwise asymptotic of `|ghat|` must keep `|zeta(1/2 + 2iu)|` (F5)); Opus REFUTE review
`notes/reviews/h-theta-2026-09-21.md`: 20 VALID / 4 MINOR / 0 INVALID before the fixes (T2c, T3d, T4a, H-GL1-BAD: no real zeros of xi named; the unweighted outgoing half covered; the Z = 1 and E = 2 sqrt x E_0 steps named; the transport R, v_- and B_bad(tau) = B(1/2 - 2i tau) in the statement), 24 VALID / 0 MINOR / 0 INVALID after (plus four further MINOR wording fixes of the registration read applied: the sign pairing of v_+-, the P'(1/2)/P(1/2) clause, the wording of the exhaustive readings, a missing region); 38 check groups / 169 evaluations in six scratch scripts, all 43 byte-cited spans opened, 0 misquoted.

## What was found

- **`conj:h-theta` is refuted** (`thm:theta-vector-cyclic`). The full and the forward dilation orbits of `g` are dense in
  the bond (Fourier uniqueness through a strip of analyticity of width `pi/2`); stronger than the brief, the outgoing
  half `g_>` is *outer*: `Lambda_>(s) != 0` on all of `Re s < 1`, by an explicit bound `Re[-(1-s)Lambda_>(s)/2] >= 3 - 2 theta(1)`.
  So every cyclic-and-cut construction gives the unitary dilation, the backward shift (whose generator has the whole
  left half-plane as point spectrum) or the isometric shift; none is `K_S`. This holds for every weight `y^a phi`.
- **H-THETA-EDGE** (`thm:symbol-edge-quotient`, unconditional): `Theta = [ghat(tau - i/4)/ghat(tau + i/4)] (tau-i/2)/(tau+i/2)`,
  `xi(1 +- 2i tau) = xi_crit(tau -+ i/4)`; `Theta = E^#/E` with `E = xi(1 - 2i tau)` Hermite–Biehler unconditionally
  (`K_S` is the de Branges space `H(E)`); `Theta` is a pure Blaschke product, proved by the paired Hadamard product
  of `xi` (`b = 0` from `xi'(1/2) = 0`) and `Theta(iv) ~ sqrt(pi/v)` excluding a delay: this proves the innerness of
  `prop:scattering-inner` without Phragmén–Lindelöf. The unshifted `xi_crit` is never Hermite–Biehler; HB for every
  translate `xi_crit(tau + ih)` is an RH equivalent.
- **H-GL1-BAD** (`thm:gl1-bad-zero-defect`): Burnol transcribed. His closure `V Delta = B H^2` is an *incoming*-half
  subspace in the notebook's orientation; the outgoing defect is the reflection, `K_bad = H^2(C_+) minus B_bad H^2(C_+)`,
  one Jordan chain per zero with `sigma > 1/2` at `gamma/2 + i(sigma - 1/2)/2`, zero iff RH; the brief's forward
  saturation of the theta images is all of `H`. Burnol's scattering multiplier is `v_+^2 B_bad^{-2}` (a bad zero of
  order `m` is a pole of order `2m`), under RH `v_+^2` with a two-dimensional causal model space: the GL_1 scattering
  is not trivial but elementary. `K_bad = 0 <=> RH <=> Im w_rho = 1/4` for every zero: the same statement seen from
  the two cavities, with no intertwining unitary.
- **The line zeros are absorption lines** of the theta vector's spectral density `|ghat|^2` (order twice the
  multiplicity), a cokernel only in Connes's weighted topology; no identification with the ring-norm trace.
  Meyer's virtual representation (poles in `pi_+`, zeros in `pi_-`, character the Weil distribution) is the
  Phantasm's grading on the idele class group.
- **Two GL_1 channels in the Eisenstein constant term** (`prop:siegel-constant-term`): `int_0^1 Theta_z dx = theta(t/y)
  + sqrt(y/t)(theta(ty) - 1)`, exchanged by the weighted involution; `E^* = (1/2) int (Theta_z - 1) t^s d^x t` with
  constant term `(1/2)[Lambda(2s) y^s + Lambda(2s-1) y^{1-s}]`; `phi_E(1/2 + i tau) = r(tau) S(tau)`; the notebook's `S`
  is Uetake's reduced causal factor `S_c0`, not simply "the wave scattering matrix with the cusp forms deleted"
  (`obs:symbol-is-reduced-causal-factor`, from the local text).
- **The model space from the absorption characters** (`thm:model-space-jets`): `K_S` is spanned unconditionally by
  the jets `(log y)^j y^{-(1-sigma)/2 - i gamma/2}` on `y > 1`; under RH, "cut the character to the outgoing half and
  damp by `y^{-1/4}`", with adjoint time. Complete and minimal, but under RH not a Riesz basis (gaps tending to zero
  are forced by the counting function against `log|xi_crit(iv)| = v log v + O(v)`): one kernel per zero is not one
  exit per mode (`prop:kernels-not-riesz`). An explicit outgoing-half vector with inner factor `Theta` generates the
  outgoing subspace (`prop:outgoing-generator`); it is not the theta vector.
- **Sonine**: Burnol's level-one evaluators are complete and minimal (one per zero with multiplicity); the diagonal
  Sonine filtration is not a dilation orbit (`U_t R L_{a,b} = R L_{a e^{t/2}, b e^{-t/2}}`, `prop:sonine-two-cutoff-covariance`);
  the one-exit contraction on the evaluators is `conj:h-theta-1`, open, with a finite Gram-matrix falsification test.

## Registered

Shards `04q_h_theta.tex` (refutation, edge theorem, GL_1 side) and `04r_h_theta_channels.tex` (GL_2 side, model
space, Sonine); `conj:h-theta` in 04p set to `refuted` with `thm:theta-vector-cyclic` as the refuting result;
notation `\thetaM` (`Lambda_J`, distinct from the von Mangoldt `\vM`) and `\gth`; definition
`def:theta-bond-vector`; thirteen provenance rows (Connes, Burnol ×2, Meyer, Uetake); bibliography Burnol 2001,
Burnol 2004, Meyer 2005, Uetake 2007; `asm:rh` for the RH-conditional Riesz statement. Gate: 509 claims, 0 errors, 2 warnings (the two verbatim APW quotes); fresh build.

## Next

`obs:h-theta-next`: (i) the Sonine evaluator Gram for three zeros against H-THETA-1; (ii) the GL_1 bad-zero channel
as a renewal channel with synthetic off-line zeros; (iii) the two-dimensional causal model `K_{v_+^2}` against the
even sector; (iv) the Eisenstein constant term at level `N` as a matrix of edge quotients (H-ARITH).
