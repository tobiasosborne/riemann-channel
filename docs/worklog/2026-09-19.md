# 2026-09-19 — RH decay-mode review and durable shutdown

The user requested subagent review of the repository and proof techniques, with
continuous on-disk work. Three initial agents covered cusp/scattering, positivity
and products, and graded channels. At the user's request, two additional agents
examined the precise Riemann/Selberg operator and physical cMPS differences.
Reports, scripts, results, source receipts and synthesis are all in
`notes/rh-strategy-2026-09-19/`.

The user ended the session by requesting shutdown, save, commit and push. Agents
were stopped immediately. Their substantive work had already been written to disk;
the second-round reports include completion sections. No further proof research
was performed after shutdown.

The principal constructive finding is a regular fermionic cMPS whose physical
correlation selects four equal-width modes despite additional odd transfer modes.
An independent review explains the selection by linear Majorana closure and
separates reflection from the coercivity inequality needed for equal widths.
Perturbation examples show which properties survive. The operator lane clarifies
cusp flux, wave/geodesic shifts and the distinct role of self-adjoint Laplace data;
Uetake's full 2007 paper and Bonthonneau–Weich's TeX were cached locally.
These are exploratory findings, not an RH proof or new registered theorem status.

Validation already completed: existing reference manifest passed; `make check`
passed with 357 claims and zero errors/warnings; six saved diagnostics reran
successfully; session recovery extraction/hash verification and deliberate archive
corruption rejection passed. Later comparison diagnostics have saved passing agent
outputs. The existing pre-commit hook performs its required full CI at commit.

Infrastructure: added atomic checksummed recovery snapshots including Git-ignored
sources, and excluded this maintenance utility from mathematical-evidence script
parity alongside the existing maintenance tools. `.recovery/` is Git-ignored;
source receipts, all research notes and calculation scripts are versionable.

Shutdown validation found excessive BLAS thread contention. Two threads reproduced
the recorded rounded numerical errors; one thread changed a displayed error from
5.5e-12 to 3.7e-12. A second platform-dependent fixture difference was only printed
signed zero. The graded Ramanujan script now adds positive zero after rounding its
display, and the captured output reflects that normalization. The original
mathematical assertions and their inputs are unchanged. Commit validation runs the
full existing hook with two BLAS threads; no checks are bypassed.

## Afternoon: the sidequest merged; the window as a compression and the extension disc (shard 08g)

TJO asked for the zeta-spectral-triples sidequest branch (`origin/claude/inspiring-ride-n5binf`,
26 commits: `zst/`, `ihz/`, `notes/zeta-spectral-triples/`, worklogs 2026-09-17/18) to be merged
into master keeping both HANDOFF blocks. Done as a merge commit; conflicts only in `HANDOFF.md` (both
session blocks kept, the sidequest's longer "Current state" header retained) and `.gitignore` (both
entries). The six arXiv TeX sources the sidequest added to the manifest were fetched into `refs/src/`
(hashes match), so the CI's manifest step passes here; the pre-commit CI ran green.

TJO then asked, from the CCM tutorial of an earlier session: the method seems to assume a form for
`tr(A^k)`, collect incomplete data, build a model for the higher traces, and use positivity and
Toeplitz structure on the minimal eigenvector; could one build better estimators for `tr(A^l)`,
`l > K`, from the traces up to `K`? Answer recorded in shard 08g
(`report/sections/08g_weil_window_extension.tex`, `scripts/weil_window_extension.py`, 45 checks):

- Step (3) is not something the chain does: the window form is the exact compression of the full
  Weil form (traces up to lag `K` for a graph; primes `<= lambda^2` with exact pole and archimedean
  terms for zeta), and the only implicit model of the higher traces is the Pisarenko extension
  (`K` atoms on the circle = the kernel roots of `W_K - eps_K` = the spectrum of the perturbed
  operator, plus a noise floor `eps_K` at lag zero).
- Given `n_0..n_K` with a definite window, the admissible `n_{K+1}` fills a closed disc: centre the
  Levinson/Yule-Walker one-step predictor (Burg's maximum-entropy extension), radius
  `det W_K / det W_{K-1}`; boundary points are the singular windows with `K+1` atoms on the circle
  (Caratheodory-Fejer, byte-cited from Connes-van Suijlekom 2511.23257:752). With exactly `K+1`
  distinct atoms the truth sits on the boundary, one lag before the critical window. Each further
  lag is one Schur parameter, so positivity alone cannot beat the disc.
- The counts add nonnegativity and a Newton-identity congruence `N_{K+1} = -sum c_i N_{K+1-i}
  mod (K+1)` (integer characteristic polynomial). For `(1 2)(3 4 5)` the candidates are
  `{0,2,4}, {0,3}, {2}` at `K = 1,2,3`: pinned one lag early, where Pisarenko alone gives `-0.5`.
  For the Petersen graph the rescaling multiplies the radius by `q^{(K+1)/2}` (35.5, 40.6, 49.4 in
  counts), so integrality pins nothing.
- For zeta the mean of the higher traces is the pole (already exact), higher powers of known primes
  are outside the window's support, and the unknown fluctuation is the zeros: a better estimator
  means more primes. The remaining freedom is the choice of extremal extension; its convergence
  rate is CCM's prolate step, which a finite divisor cannot exercise.
- Proposed, not run: the disc radius per window in `ihz` as a rigorous ignorance measure; the
  Schur-complement radius of the window Loewner form in `zst` versus the observed `e^{-4 pi x}`.

Gate green (363 claims, 0 errors, 0 warnings); fresh build; orchestrator alone, unreviewed.
