# 2026-09-26 — RTP round 2: the astra quota day

TJO: "codex usage is going to get reset, and I want to use my quota up ... deploy gpt 6 astra xhigh codex exec
subagents ... Our focus is 100% the RTP programme ... regularly check codex quota: as soon as it resets stop using
astra subagents. You may use up to 4 astras at a time." Then: "please set things up so that partial work from astra
subagents is not lost."

Morning orientation: all branches compared against master; a new branch `claude/project-familiarization-8gondy`
(the tutorial "Weil Positivity, Contracted", 3 commits, purely additive) is unmerged and fast-forwardable; it carries
its own `docs/worklog/2026-09-26.md`, to be concatenated with this one on merge.

## Infrastructure (so that nothing is lost, and the quota is watched)

- `scripts/codex_quota.py`: reads the codex account rate limits through `codex app-server` (JSON-RPC
  `account/rateLimits/read`); baseline 09:46Z: 26% of a 7-day window, reset 2026-10-01T08:14Z.
- `scripts/rtp2_watch.sh`: every 10 min commits every lane file (`--no-verify`; the gate carries the 8 known Yoshida
  errors) and logs the quota; on a reset it writes `notes/rtp-round-2/STOP`, stops this round's lanes only
  (`scripts/rtp2_stop.sh`, scoped by the brief text in the codex command line, since another session ran four
  Bennett.jl astras against the same quota at the same time) and exits.
- `scripts/astra_lane.sh` now RESUMES whenever `session.id` is present (before: attempt 1 always started fresh);
  `rtp2_adopt.sh` adopts an orphaned codex process; `rtp2_map.sh`, `rtp2_launch.sh`, `rtp2_monitor.sh`.
- Lesson paid for three times: `pkill -f <pattern>` from a shell whose own command line contains the pattern kills
  the shell (exit 144); background Bash tasks die with the shell; use script files and `setsid nohup`. The first four
  sessions (three minutes old) were lost in the relaunch; fresh sessions from 09:57Z.

## Wave 1 (four astra lanes, briefs under `notes/rtp-round-2/`)

- **P prover** (done 10:19Z; review 15 VALID / 2 MINOR / 0 INVALID): `lem:bordering-interval` proved in full with
  corrections (deficit not gain, `N >= 1`, empty/singleton sets allowed, exact midpoint iff condition, rational
  example offset `-3/sqrt 337`); the finite-prime language `L_S` of `finite-prime-language.md` **refuted** (the
  archimedean set contains `log` of every algebraic number, and even without that every `log q` is a rational
  combination of digamma values at rationals, so `L_S` is independent of `S`); point masses are not admissible test
  functions in the CCM class; the genuine grading lives on `span{1, i pi, log p}` and on the finite-place remainder of
  admissible bumps (PROVED-conditional on Baker); FNW exclusion PROVED-conditional on Tarski–Seidenberg (Coste's
  CAD theorem is on disk, removing H-BPR); an explicit Diophantine floor `e_d - e_0 >= B(d)` for the Heisenberg
  chain (conditional; needs an infimum-versus-limit lemma for Hulthén; "first" not asserted).
- **I impure bumps** (done 10:21Z; review 12 VALID / 4 MINOR / 0 INVALID): corrected all-width Kronecker lemma with a
  mixed-mask prime remainder entering with a minus sign (E/H/R classification); rank-one-per-pattern refuted;
  no Schmidt defect above 0.1 anywhere (36-point sweep, 401- and 4001-point scans, opposite-parity ground states at
  every gap minimum); the impure two-prime observable is a **pole-versus-third-prime cancellation** (mixed pole
  1.6–5.4 times the external-prime part along the ground vector), not a 2–3 correlation and not a third-prime
  channel; nothing bears on RH.
- **L lattice box** (done 10:40Z; review pending): recession cone trivial above a geometry-dependent cutoff (`N = 6`
  for the eleven atoms at `x = 13`; no `m`-only threshold; edge coordinates invisible); dual-pair width bound, LP is
  an outer relaxation of the SDP box; the `N -> oo` pinning statement **refuted in general**: a coercive fixed-window
  form (zeta under RH, by compact resolvent and the explicit formula) leaves an open neighbourhood of admissible
  weights, so "positivity pins the primes" is a finite-resolution effect; certified widths at `N = 20` reproduce the
  review's `2.98e-25`–`1.25e-4`; at saturated `N` the `n = 2` width contracts 3.48 digits per unit `x`, the edge 0.34,
  `eps` 5.27.
- **D Dirichlet** (running): driver `zst/tools/rtp2_dirichlet.c` (pilot 265 checks); ten characters at `x = 13, 25`
  give raw rates **0.21 to 1.78 digits per unit `x`** against zeta's 5.4, so a conductor-independent kinematic rate is
  refuted numerically; `x = 50` runs pending.

## Wave 2

- **G grid tomography** (launched 10:19Z, done 10:52Z; review pending): positions freed on a fine grid with `w >= 0`
  (H-SIGN): what the window sees is exactly the `2N+1` Loewner moments; the sparse true measure is unique from the
  exact moments for `N >= 16/26`; every prescribed pure uniform grid is certified EMPTY (the truth is off-grid), so the
  MaxEnt optimisers of the brief are undefined; the near-kernel support premise of the brief refuted, a dual
  localisation theorem replaces it; the union controls do not discover positions independently.
- **E elliptic curves** (launched 10:21Z, running): H-RATE (`sqrt(x/C_E)` as the window variable for weight two)
  numerically supported; the exact `8 pi` and the concentration transfer OPEN.
- **S rate at scale** (launched 10:40Z, running): plan and monotonicity written; certified runs pending.

Quota: 26% (09:46Z) → 87% (11:00Z) with eight astras on the window (four of them another session's).

## Afternoon: reviews, completions, follow-ups (Opus only; codex finished at 11:40Z)

- Codex window hit 100% at 11:40Z (`reached=rate_limit_reached`, not a reset); lanes D and S stopped mid-run; TJO:
  "codex is finished, please no more codex usage in this session"; `notes/rtp-round-2/STOP` written.
- Six Opus REFUTE reviews (`notes/reviews/rtp-round-2-*-2026-09-26.md`): prover 15/2/0, impure-bumps 12/4/0,
  lattice-box 10/5/0, grid-tomography 9/3/0, ellcurve 3/7/0, dirichlet 2/4/0 (on the partial). No INVALID anywhere.
- Reviewer findings that changed the reading: the impure two-prime signal is a pole-versus-third-prime cancellation;
  the full SDP lattice box at `x = 13, N = 20` is a two-vertex needle 2.4–2.6x inside the LP box and the lattice max-det
  point misses the truth by 23–49% of it; the empty pure grids are the positional signal, and a **blind** maximisation of
  `lambda_min` over nonnegative grid measures from pole and archimedean data alone recovers the prime-power positions
  (within `3e-7` after refinement) and the von Mangoldt weights (2%) at `x = 13, N = 20`; the elliptic conductor exponent
  was only bracketed (0.47–0.67) by two curves.
- Follow-ups (Opus, zst only): conductor sweep over 19 rank-zero curves from Cremona's ecdata: `alpha = 0.488 [0.473,
  0.502]` on a matched `sqrt(x/C)` window, rate 5.12 +- 0.05 digits per unit `S_max`; the five missing Dirichlet `x = 50`
  runs regenerated and the lane report completed with the review's corrections (pooled `c/(4 pi) = 1.0039`, rms 0.043);
  certified blind recovery with controls launched (`notes/rtp-round-2/blind-recovery/`).
- Discussion with TJO recorded in HANDOFF: the metric blocker after round 2 (finite windows measure a kinematic margin;
  route (1) global positivity theorem, route (2) infinite windows, route (3) rigid algebra with the Weil form as
  covariant state, corrected: rigidity gives a state not a sign, one state per Hecke character, GRH the natural target);
  theta as the stabiliser state of `Q x Q` in the adelic Weyl–Heisenberg group, RH as a Pontryagin-index-two statement
  on its dilation-orbit Gram; "Weil positivity = correctable errors" is vacuous for a one-dimensional code and, for
  Kraus families, is the Ramanujan (optimal-scrambler) bound `thm:kraus-weil-criterion`, the opposite of correctability.
- HANDOFF updated; shard 08j is the first next step (nothing registered today).
