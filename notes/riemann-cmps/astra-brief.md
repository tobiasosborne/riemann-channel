You are the prover in a mathematical research notebook (git repo, current directory). Author line for everything you write: codex:gpt-6-astra. Write ONLY the file notes/riemann-cmps/astra-proofs.md (create it; overwrite if present). Do not edit anything else. No web access; do not assume any. You may run python3 (numpy, mpmath, sympy) with `timeout` for sanity checks, writing scratch scripts only under notes/riemann-cmps/scratch/. The module scripts/artin_schreier_mps.py (import with sys.path.insert(0,'scripts')) provides irreducible(q,n), polymulmod, polypow, trace, S_bruteforce(q,n,a) and transfer(q,a); read it before T7.

CONTEXT (read first): notes/weil-positivity/astra-proofs.md (your own earlier file: the Lamport style, the H-* hypothesis discipline, the correction ledger; its T4.3 is the Dyson-expansion ring-norm theorem for Lindblad-type generators, its T6 the zeta dictionary with the distributional trace of the compressed semigroup), and report/sections/04_riemann_channel.tex (the Riemann channel: completed zeta xi(s) = (1/2) s (s-1) pi^{-s/2} Gamma(s/2) zeta(s); scattering symbol S(tau) = xi(1-2i tau)/xi(1+2i tau); model space K_S; compressed semigroup Z(t) with one mode per nontrivial zero rho_n = sigma_n + i gamma_n, eigenvalue exp(-sigma_n t/2 + i gamma_n t/2) = exp(-conj(rho_n) t/2); the explicit formula in the ring-length variable; the observation obs:ringnorm-sign-correction that the prime part of the distributional trace of Z(t) is -2 sum_n (Lambda(n)/n) delta(t - 2 log n) for t > 0). Also report/sections/06_artin_schreier_mps.tex (the Artin--Schreier transfer matrix E of the quadratic family g(x) = sum_{j=0}^J a_j x^{1+q^j}, a_J != 0, q an odd prime, on the bond space F_q^J, entries E_{(s_1..s_J) -> (s_2..s_J, x)} = psi(a_0 x^2 + sum_{j=1}^J a_j s_{J+1-j} x) with psi(c) = exp(2 pi i c/q); exponential sum S_n(g) = sum_{x in F_{q^n}} psi(tr_{q^n/q} g(x)); the affine count is q^n + sum_{a in F_q^*} S_n(a g) by Hilbert 90, the projective curve y^q - y = g(x) has one point at infinity, so N_n = 1 + q^n + sum_{a != 0} S_n(a g); L(g,T) = exp(sum_n S_n(g) T^n/n) = prod_i (1 - alpha_i T) is a polynomial of degree q^J; the notebook's prop:as-transfer-matrix claims alpha_i = -lambda_i(E) uniformly in n. THAT CLAIM IS FALSE IN GENERAL (see T7), which is one reason you are being asked.)

THE QUESTION. The notebook's central working picture (HANDOFF.md, "the Bost--Connes reframe") is: dilation time is the one-dimensional space of a continuous matrix product state (cMPS); the bond carries a "Riemann Lindbladian" whose unique fixed point is the pole of zeta at s = 1 and whose relaxation modes are the zeros; because the ring norms of a cMPS are sums of squares while the zeros enter the explicit formula with a minus sign, the bond must be Z_2-graded, the zeros fermionic (odd), the pole bosonic (even), and the ring norm a supertrace. We call the object being chased the Phantasm. We want every rigorous statement that can be made about it from the data we have: what is FORCED (spectrum, parities), what CANNOT exist (no-go theorems), what exists concretely (the Riemann channel as the odd sector; the vacuum-decay Lindbladian), what the prime-chain (Bost--Connes, side A) picture gives and does not give, and the finite-field prototype (Artin--Schreier) where everything is explicit, including the corrected sign law. I have drafted the statements below from numerics and heuristics. If any is false as drafted, or needs a different hypothesis, constant or sign, say so plainly, correct it, and prove the corrected version. Do not round up. Keep a CORRECTION LEDGER at the end.

CONVENTIONS. Nontrivial zeros rho = sigma + i gamma of zeta, counted with multiplicity m_rho, the multiset closed under rho -> conj(rho) and rho -> 1 - rho. Lambda is von Mangoldt. A GRADED SPECTRAL DATUM is a countable multiset Lambda_gr of pairs (lambda, epsilon) with lambda in C, epsilon in {+1, -1} (parity), multiplicities m(lambda, epsilon) in N, satisfying (G1) Re lambda <= c for some real c and (G2) sum m(lambda,epsilon) (1 + |lambda|)^{-N} < infinity for some N (finite order). Its SUPERTRACE DISTRIBUTION is str e^{t G} := sum epsilon m(lambda,epsilon) e^{lambda t}, meant as a distribution on (0, infinity). Its NET SIGNED MULTIPLICITY is nu(lambda) := m(lambda,+1) - m(lambda,-1). Two data are EQUIVALENT UP TO CANCELLING PAIRS if they have the same nu. A distribution on (0, infinity) is POSITIVE if it is >= 0 on nonnegative test functions (then it is a positive Radon measure; state this as H-SCHWARTZ). The POSITIVE-TIME PRIME MEASURE is P_+(t) := sum_{n >= 2} (Lambda(n)/n) delta(t - 2 log n) and the PRIME COMB is comb(u) := sum_{n >= 2} Lambda(n) delta(u - log n).

HYPOTHESES to state verbatim as H-* (you may add others; each with its standard name and exact statement): H-ZEF-T (explicit formula in trace form: for u > 0, as distributions on (0, infinity), sum_rho e^{rho u} = e^u - comb(u) - 1/(e^{2u} - 1), the zero sum taken in symmetric order |gamma| < T, T -> infinity; equivalently, from psi_0(x) = x - sum_rho x^rho/rho - log 2 pi - (1/2) log(1 - x^{-2}) for x > 1 by differentiating in x and substituting x = e^u; verify the derivation and the archimedean term 1/(e^{2u}-1) = sum_{k >= 1} e^{-2ku}); H-ZERO-COUNT (N(T) = (T/2 pi) log(T/2 pi e) + O(log T)); H-LIDSKII (for a trace-class operator on a separable Hilbert space, Tr E^n = sum_i lambda_i^n for n >= 1 with sum_i |lambda_i| < infinity, eigenvalues with algebraic multiplicity); H-LANDAU (the Laplace transform of a positive measure on (0, infinity) with finite abscissa of convergence sigma_c has a singularity at s = sigma_c); H-LP (Z(t) = e^{tB} is a strongly continuous contraction semigroup on K_S whose generator B has pure point spectrum {-conj(rho)/2}, one eigenvector per zero, and Z(t) -> 0 strongly; take as given); H-TRACE (the distributional trace Tr Z(t) := sum_rho e^{-conj(rho) t/2}, a distribution on (0, infinity) by H-ZERO-COUNT; this is a definition by fiat, say so); H-GAUSS (for q an odd prime and a quadratic form Q on F_q^n of rank r, sum_x psi(Q(x)) = q^{n-r} eta(det Q_nd) g(psi)^r with Q_nd the nondegenerate part, eta the quadratic character of F_q^*, g(psi) = sum_c psi(c^2), g(psi)^2 = eta(-1) q, conj(g(psi)) = eta(-1) g(psi)); H-STICK (Stickelberger: the discriminant of the trace form of F_{q^n}/F_q, q odd, is a square in F_q iff n is odd; equivalently eta(disc) = (-1)^{n-1} = the sign of the n-cycle by which Frobenius permutes the conjugates); H-NORMAL (normal basis theorem; the self-dual normal basis exists for n odd (q odd)); H-WEIL-REP (the Weil representation W of Sp(2J, F_q), q odd, on L^2(F_q^J): a genuine unitary representation, characterised up to a character by W(M) X_v W(M)^{-1} = (phase) X_{Mv} for the Weyl (Heisenberg) operators X_v, v in F_q^{2J}; its character satisfies |Tr W(M)|^2 = q^{dim ker(M - 1)}); H-BC (Bost--Connes: the Hamiltonian H = log N on l^2(N), N|n> = n|n>, generating sigma_t = Ad(N^{it}); the KMS_beta states for beta > 1 are the Gibbs states of H in the standard representation; for beta <= 1 there is a unique KMS_beta state and it is a factor state of type III_1 — take as given, cite as Bost--Connes 1995); H-MM1 (the birth--death generator on N with constant up-rate lambda and down-rate mu has spectrum {0} union [-(sqrt(lambda)+sqrt(mu))^2, -(sqrt(lambda)-sqrt(mu))^2] on l^2 with the stationary weight, when lambda < mu). State each precisely; if you can prove one from the others, do.

TASKS. For each give the statement as Theorem/Proposition/Lemma with a label, hypotheses listed explicitly, a Lamport-structured proof (<1>1, <1>2, ... with PROOF; sub-steps <2>k where needed), and a status line: proved-here | sketched | conditional-on <hypothesis>.

T1 (Rigidity: the supertrace determines the graded spectrum up to cancelling pairs). (a) Lemma: for a graded datum satisfying (G1),(G2) the series sum epsilon m e^{lambda t} converges in D'((0, infinity)) (pair with phi in C_c^infinity(0,infinity): integrate by parts N+2 times in t to get decay in |Im lambda|, uniform for Re lambda bounded). (b) Theorem: if two graded data satisfying (G1),(G2) have the same supertrace distribution then they have the same net signed multiplicity nu. Suggested route: for k >= N+1 and Re s > c, the pairing of the supertrace with t^k e^{-st} (justify the pairing with a non-compactly-supported test function, e.g. by a cutoff and dominated convergence using (G1)) equals k! sum epsilon m (s - lambda)^{-(k+1)}, which converges absolutely and locally uniformly on compact sets avoiding the lambda, hence defines a meromorphic function on C whose principal part at lambda is k! nu(lambda) (s - lambda)^{-(k+1)}; equality on Re s > c forces equality everywhere, hence equality of all nu(lambda). (c) Corollary (the forced spectrum): any graded datum whose supertrace on (0, infinity) equals comb(u) has nu(1) = +1, nu(rho) = -m_rho at each nontrivial zero, nu(-2k) = -1 for k >= 1, nu = 0 elsewhere (from H-ZEF-T and (b)). State also the rescaled/reflected version relevant to T4: any graded datum whose supertrace equals 2 P_+(t) has nu(0) = +1, nu(-conj(rho)/2) = -m_rho, nu(-(k+1/2)) = -1, nu = 0 elsewhere (derive by the substitution and the symmetry rho -> 1 - rho; be careful with the Jacobian: comb(t/2) = 2 sum Lambda(n) delta(t - 2 log n)).

T2 (Positivity forces the parities). Take the multiset {1} union {rho} union {-2k : k >= 1} with the zeta multiplicities and assign a parity epsilon(lambda) to each element freely (one parity per lambda, no cancelling pairs). Let F be the set on which epsilon differs from the reference assignment (1 even, everything else odd). (a) Lemma: the supertrace is a real distribution iff F is closed under complex conjugation. (b) Theorem (finite F): if F is finite and the supertrace is a positive distribution on (0, infinity), then F contains no nontrivial zero and does not contain 1; F may contain any set of trivial zeros (prove both directions: for F a subset of the trivial zeros the supertrace is comb + 2 sum_{-2k in F} e^{-2kt} >= 0). Route: the supertrace equals comb(u) + f(u) with f a real-analytic function (finite sum of exponentials); a positive distribution equal to (purely atomic positive measure) + (continuous function) forces f >= 0 pointwise; a finite real exponential sum containing a term 4 m e^{sigma_0 u} cos(gamma_0 u) with gamma_0 != 0, or the term -2 e^u, is negative somewhere (prove: compare growth rates and use the oscillation). (c) Infinite F: prove what you can. Suggested: the supertrace is then comb + f with f := 2 sum_{lambda in F} epsilon_ref(lambda)(-1) ... write it out; f is a distribution, not obviously a function. Route to try: since the supertrace and comb are Radon measures, f = supertrace - comb is a signed Radon measure whose diffuse part is >= 0; use H-LANDAU on the diffuse part or on the positive measure supertrace + comb, together with the meromorphic structure of the Laplace transform 2 sum_{rho in F} m_rho/(s - rho) + ... (poles at F only). If you cannot close the infinite case, say exactly what is missing and state the finite case as the theorem, the infinite case as conditional or open. Do not overclaim.

T3 (No-go for ungraded realisations; the numerator is the odd sector). Let N_n = sum_{i=1}^{P} b_i^n - sum_{j=1}^{Z} alpha_j^n for n >= 1, with all b_i, alpha_j nonzero complex numbers and the multisets {b_i}, {alpha_j} disjoint after cancelling common elements, Z >= 1. (a) Theorem: there is no finite matrix E with Tr E^n = N_n for all n >= 1. (b) Theorem: there is no trace-class operator E on a separable Hilbert space with Tr E^n = N_n for all n >= 1 (use H-LIDSKII; route: sum_n N_n u^n = sum_i b_i u/(1 - b_i u) - sum_j alpha_j u/(1 - alpha_j u) versus sum_i lambda_i u/(1 - lambda_i u), the latter meromorphic on C with locally finitely many poles because sum |lambda_i| < infinity; compare residues at u = 1/alpha_j; handle the possibility that some lambda_i coincide with some alpha_j by counting net multiplicities). (c) Proposition (criterion): a sequence (N_n)_{n >= 1} is the trace sequence of a trace-class operator iff Z(u) := exp(sum_n N_n u^n/n) equals 1/prod_i (1 - lambda_i u) for some absolutely summable family (lambda_i), i.e. iff 1/Z(u) is an entire function of genus zero with 1/Z(0) = 1 and absolutely summable reciprocal zeros; and (N_n) is the SUPERTRACE sequence str E^n of a finite Z_2-graded matrix iff Z(u) is a rational function with Z(0) = 1, in which case the even net spectrum is the multiset of reciprocal poles and the odd net spectrum the multiset of reciprocal zeros of Z. (d) Corollaries, stated explicitly: (i) for a smooth projective curve of genus g >= 1 over F_q, N_n = #C(F_{q^n}) = q^n + 1 - sum_{j=1}^{2g} alpha_j^n (Weil; take as H-WEIL) is not the trace sequence of any finite matrix or trace-class operator, and IS the supertrace sequence of the graded matrix diag(q, 1) (even) (+) Frob|H^1 (odd), the odd net spectrum being forced to be the Frobenius eigenvalues; (ii) consequently the ring norms <Psi_n|Psi_n> = Tr(E_A^n), E_A = sum_s A_s (x) conj(A_s), of a translation-invariant matrix product state with a fixed local tensor A (any finite bond dimension) can never equal N_n for such a curve; (iii) for a finite (q+1)-regular graph the non-backtracking counts N_l = Tr T^l are trivially a trace sequence and the Ihara zeta 1/det(1 - uT) has no zeros: the graph case needs no grading, the curve case does, and the difference is exactly whether the zeta function has a numerator ("the sign", Deligne's third ingredient in the notebook's language).

T4 (The Riemann channel is the odd sector; the graded generator whose supertrace is the prime measure). Define the graded Hilbert space B := C (even) (+) [K_S (+) l^2(N_{>=1})] (odd) and the generator G := 0 (+) B (+) diag(-(k + 1/2))_{k >= 1}. (a) Proposition: e^{tG} = 1 (+) Z(t) (+) diag(e^{-(k+1/2)t}) is a strongly continuous contraction semigroup on B whose fixed vectors are exactly C (+) 0 (+) 0 (under H-LP), and its supertrace (defined as 1 - Tr Z(t) - sum_{k >= 1} e^{-(k+1/2)t} with H-TRACE) equals 2 P_+(t) on (0, infinity): str e^{tG} = 2 sum_n (Lambda(n)/n) delta(t - 2 log n). Derive from H-ZEF-T: use the symmetry rho -> 1 - rho to write sum_rho e^{-rho t/2} = e^{-t/2} sum_rho e^{rho t/2}, substitute u = t/2, check comb(t/2) = 2 sum Lambda(n) delta(t - 2 log n) and e^{-t/2}/(e^t - 1) = sum_{k >= 1} e^{-(k+1/2)t}. Every sign and factor must be right; this is the corrected form of the notebook's prop:ringnorm-trace. (b) State the variant without the ladder: the supertrace over C (+) K_S alone is 2 P_+(t) + e^{-t/2}/(e^t - 1), also positive; and with the ladder counted even it is 2 P_+ + 2 e^{-t/2}/(e^t - 1): the ladder's parity is not fixed by positivity (T2). (c) Proposition: by T1(c), any graded datum with supertrace 2 P_+ has the net spectrum of G; so the Phantasm's single-particle graded spectrum is forced and is realised by G. (d) Remark (state, no proof): with respect to the grading, the fixed vector (the pole, the constant function on the modular surface, the residue of the Eisenstein series at s = 1) is even; RH is the statement that all odd modes of G in K_S have real part exactly -1/4.

T5 (The vacuum-decay Lindbladian: the zeros are its odd coherences). Finite model first. Let H be a finite-dimensional Hilbert space, B an operator on H with -(B + B^dagger) = |j><j| for some vector j (rank-one dissipation; the SHW exit space is one-dimensional), and suppose e^{tB} -> 0. On B(C (+) H) define L(x) = bB x + x bB^dagger + J x J^dagger with bB := 0 (+) B and J := |vac><j| (the jump from H into the vacuum vector of the even line C). Prove: (a) L is a GKLS generator, trace preserving, and |vac><vac| is its unique fixed point; (b) with respect to the block decomposition B(C (+) H) = C (+) (C (x) H^*) (+) (H (x) C^*) (+) B(H), L is block triangular, the coherence blocks evolve exactly by x -> x e^{tB^dagger} and x -> e^{tB} x, the population block B(H) evolves by x -> e^{tB} x e^{tB^dagger} plus the feed into the vacuum population; hence spec(L) = {0} union spec(B) union conj(spec B) union (spec B + conj(spec B)) as multisets, with the exact multiplicity bookkeeping; (c) with the grading of B(C (+) H) induced from vac even, H odd, the coherence blocks are the odd part of B(C (+) H) and the populations the even part, so the odd spectrum of L is spec(B) union conj(spec B) (each mode of B appearing once, plus its conjugate) and the even spectrum is {0} union (spec B + conj spec B); (d) formal corollary for H = K_S and B the generator of Z(t) (state as formal, conditional on H-LP; infinite dimensions, no trace class): the Riemann channel Z(t) is the odd coherence sector of the vacuum-decay Lindbladian on C (+) K_S; each zero appears once as an odd mode -conj(rho)/2 and once as its conjugate; the even populations carry the pair sums -(conj(rho_n) + rho_m)/2, whose formal trace is |Tr Z(t)|^2, i.e. the form factor sum_{n,m} e^{-(conj(rho_n)+rho_m) t/2} of the zeros (state as a remark; no claim about pair correlation is to be proved); (e) relation to the SHW renewal picture of HANDOFF item 0b: this is reinsertion at the rebound state Omega = |vac><vac| in the enlarged space, and the fixed-point equation rho_infty proportional to int_0^infty Z(t) Omega Z(t)^* dt is solved by Omega itself; the fixed point is PURE (rank one), so a cMPS with this bond has trivial entanglement spectrum. State this last sentence as a proposition about the finite model: the reduced state of the half-chain of the cMPS with bond C (+) H, no-event generator bB and jump J is the fixed point |vac><vac| (use the standard cMPS fact that the half-chain reduced density matrix is the fixed point of the transfer generator in canonical form; state it as H-CMPS).

T6 (What the prime chain gives: Gibbs fixed points exist only above the transition, and prime-by-prime Lindbladians have no zeros). (a) Proposition: for the dynamics alpha_t = Ad(N^{it}) on B(l^2(N)) (H-BC), a normal state omega = Tr(varrho .) is KMS_beta iff varrho = N^{-beta}/zeta(beta), which exists iff beta > 1; hence there is no normal KMS state for beta <= 1 in this representation (the type III_1 statement of H-BC is the non-normal completion). Its entanglement spectrum (the spectrum of -log varrho) is {beta log n + log zeta(beta)}: the ring lengths; its purification (thermofield double) factorises over the primes as a product state, i.e. the matrix product structure along the prime chain has bond dimension one. (b) Proposition: the detailed-balance Lindbladian L_beta(x) = sum_p r_p [mu_p x mu_p^dagger - (1/2){mu_p^dagger mu_p, x}] + sum_p r_p p^beta [mu_p^dagger x mu_p - (1/2){mu_p mu_p^dagger, x}] on B(l^2(N)) (mu_p |n> = |pn>, isometries; r_p > 0 summable, e.g. r_p = p^{-beta}), restricted to the diagonal, is the generator of the classical multiplicative birth--death process; the Gibbs state N^{-beta}/zeta(beta) is its fixed point for beta > 1; the process is a product of independent birth--death chains on N (one per prime, up-rate r_p, down-rate r_p p^beta), so its diagonal spectrum is {0} union the Minkowski sum of the M/M/1 bands of H-MM1, [-(sqrt(r_p p^beta) + sqrt(r_p))^2, -(sqrt(r_p p^beta) - sqrt(r_p))^2]; in particular the spectrum is a union of real intervals and contains no trace of the zeros (state precisely what is proved: the diagonal/classical part; say what you can about the off-diagonal part). (c) Corollary/remark: as beta -> 1^+ the fixed point delocalises (zeta(beta) -> infinity) and no normal fixed point survives; this is the precise content of "the Bost--Connes transition is ergodicity breaking" available at this level, and the reason the reframe must put the zeros in a different bond (T4), not in the prime chain.

T7 (Artin--Schreier prototype: the corrected sign law, the super-transfer matrix, and the Weil representation). q an odd prime, g(x) = sum_{j=0}^J a_j x^{1+q^j} with a_J != 0, E_g the transfer matrix defined above, E_{-g} the one with all a_j negated (so E_{-g} = conj(E_g) entrywise). Numerical data (brute force, 40 cases, all consistent) establishing what to prove: writing d_n := log_q(|S_n(g)|^2 / q^n) (always a nonnegative integer in the data), one has S_n(g) = -(-eta(-1))^n conj(Tr E_g^n) = -(-eta(-1))^n Tr(E_{-g}^n) for all n >= 1, equivalently S_n(g) = (-1)^{n+1} eta(-1)^{d_n} Tr(E_g^n). Data: q=3 (eta(-1) = -1): a=(0,1): n=1..7 sign of S_n/Tr E_g^n = +,-,+,-,+,-,+ with d_n = 0,0,0,2,0,0,0; a=(1,1): +,+,+,+,+,-,+ with d = 0,1,0,1,0,2,0; a=(0,1,1): +,+,+,+,+,+,+ with d = 0,1,0,1,0,3,0; q=5 (eta(-1)=+1): a=(0,1): +,-,+,-,+,- with d = 0,0,0,2,0,0; a=(1,2): +,-,+,-,+,- with d = 0,0,2,0,0,2; q=7 (eta(-1) = -1): a=(0,1): +,-,+,-,+ with d = 0,0,0,2,0. Also the Frobenius eigenvalues computed from the L-polynomial are exactly -eta(-1) conj(lambda_i(E_g)) in every case, and E_g/sqrt(q) conjugates every Weyl operator to a Weyl operator up to a phase (Clifford) in every case tested. Prove: (a) Lemma: Tr E_g^n = sum_{x in F_q^n} psi(Q^circ_g(x)) with Q^circ_g(x) = sum_j a_j sum_{i in Z/n} x_i x_{i+j} (the ring model; handle all n >= 1 including n <= J), and S_n(g) = sum_{x in F_q^n} psi(Q^tr_g(x)) with Q^tr_g the trace form in a normal basis theta (so Q^tr_g(x) = sum_j a_j sum_{i,k} x_i x_k c_{k+j-i}, c_m = tr(theta theta^{q^m}), i.e. the symmetric matrix of Q^tr is C M and that of Q^circ is M with C = circ(c) and M = P(S), P(z) = sum_j a_j (z^j + z^{-j})/2, S the cyclic shift; note C is invertible and commutes with M, det C = disc of the trace form up to squares). (b) Theorem (sign law): S_n(g) = -(-eta(-1))^n Tr(E_{-g}^n) for all n >= 1. Route: evaluate both Gauss sums by H-GAUSS; the ranks agree; the issue is the ratio of the discriminants of the nondegenerate parts, which is eta(det C) only when M is invertible (that case gives (-1)^{n-1} by H-STICK, the sign of the Frobenius n-cycle) and needs the decomposition of F_q[z]/(z^n - 1) into its components (field factors with the involution z -> z^{-1}, hyperbolic pairs, and the non-semisimple case q | n) in general; the conjugation in the drafted law is the phase of g(psi)^{rank}. Find the correct general statement; if the drafted law fails for some class of n (e.g. q | n), say so and give the corrected law with the numerics that decide it (you may run the module). (c) Corollary: L(g, T) = det(1 + eta(-1) T E_{-g}); the Frobenius eigenvalues of the psi-isotypic part are -eta(-1) conj(lambda_i(E_g)); the notebook's claim alpha_i = -lambda_i(E_g) holds iff spec(E_g) is closed under conjugation when eta(-1) = 1, and iff spec(E_g) = -conj(spec E_g) when eta(-1) = -1. (d) Theorem (super-transfer matrix): with E_0 the transfer matrix of the zero polynomial (all entries psi(0) = 1 on allowed transitions: the de Bruijn adjacency matrix; prove Tr E_0^n = q^n for all n >= 1), the graded matrix bbE := [E_0 (+) (1)] (even) (+) [-eta(-1) (+)_{a in F_q^*} E_{ag}] (odd) satisfies str bbE^n = N_n = #C(F_{q^n}) for all n >= 1, and its odd block F satisfies F F^dagger = q 1 (write out the index bookkeeping of the notebook's one-line argument for E_g E_g^dagger = q 1; this promotes prop:as-unitarity). By T3(d) the odd net spectrum of any graded realisation of N_n is Frob|H^1, so F is conjugate to the Frobenius on H^1 up to cancelling pairs; here there are none (check: the degree of prod_a L(ag,T) equals 2g). (e) Theorem (Weil representation): E_g/sqrt(q) is, up to a scalar phase, W(M_g) for an explicit M_g in Sp(2J, F_q): find M_g (route: E_g = (cyclic relabelling) o (quadratic phases) o (Fourier transform on the register being replaced, scaled by a_J), each a Clifford operation; compute its action on the Weyl operators X_v to read off the symplectic matrix, and verify it is symplectic). Then |Tr E_g^n|^2 = q^{n + dim ker(M_g^n - 1)} by H-WEIL-REP, so d_n = dim ker(M_g^n - 1), and S_n(g) saturates the Weil bound |S_n| <= q^J q^{n/2} iff M_g^n = 1. Check against the data: for q = 3, a = (0,1), M has order 4 (d_4 = 2 = 2J, d_2 = 0). (f) Corollary (the permutation sign): for n with d_n = 0, S_n(g) = (-1)^{n-1} Tr E_g^n = sgn(Frob_n) Tr E_g^n, where sgn(Frob_n) is the sign of the n-cycle: the fermionic sign of the count on a ring of n sites is the sign of the Frobenius permutation (H-STICK), and the general law (b) corrects it by eta(-1)^{d_n} on the fixed space of M_g^n.

T8 (Correction ledger and what is established). List every drafted statement you corrected, every H-* you introduced, and a final RESULT: line summarising which propositions are proved-here, which conditional, which sketched, and which questions you leave open (in particular T2(c) and anything in T7(b) you could not close).

FORMAT: Markdown with LaTeX math ($...$ and $$...$$), one section per task, Lamport steps, explicit hypotheses. Be complete rather than brief; this file will be reviewed adversarially by another model and then distilled into a lab-book section, so every constant, sign and multiplicity must be right and every step justified.
