You are the prover in a mathematical research notebook (git repo, current directory). Author line for everything you write: codex:gpt-6-astra. Write ONLY the file notes/selberg/astra-proofs.md (create it; overwrite if present). Do not edit anything else. No web access is needed; do not assume any. You may run python3 (numpy, mpmath, sympy) with `timeout` for sanity checks, writing scratch scripts only under notes/selberg/scratch/.

CONTEXT (read these first, they are short): notes/quantum-ihara-general.md (the style of Lamport structured proofs and status tags we use; Theorem 1 there is the discrete Ihara--Bass formula for a non-backtracking superoperator), notes/riemann-channel-note.md sections 3 and 5 (the compressed dilation semigroup Z(t) and its trace, which is a comb at t = 2 log n with weights Lambda(n) n^{-1/2}), docs/worklog/2026-09-12.md (the "circle flows" paragraph: the direct sum over primes of rotations on circles of circumference log p has trace sum_n Lambda(n) delta(s - log n) by Poisson summation and is blind to zeros).

THE PROGRAMME. The notebook reformulates RH as a Ramanujan property of a transfer operator. Discrete dictionary: edge space with the non-backtracking (Hashimoto) operator T; vertex space with the adjacency A; Ihara--Bass det(1-uT) = (1-u^2)^{|E|-|V|} det(1 - uA + q u^2); RH for the Ihara zeta iff Ramanujan. We now want the CONTINUOUS (Selberg) dictionary written down as concrete statements with complete proofs, at the level of rigour of a careful paper, with every input that is standard theory stated as an explicit hypothesis/cited fact (we will byte-check the sources separately). Conventions to use: H = upper half plane with the metric y^{-2}(dx^2+dy^2); Delta = -y^2(d_x^2 + d_y^2) is the POSITIVE Laplacian, eigenvalues lambda_j = 1/4 + r_j^2 (r_j real or i r_j in (0,1/2] for exceptional eigenvalues in (0,1/4)), lambda_0 = 0; Gamma a cocompact Fuchsian group unless stated; closed geodesics gamma with primitive lengths ell_gamma; the geodesic flow on Gamma\PSL(2,R) is g -> g a_t with a_t = diag(e^{t/2}, e^{-t/2}); the Selberg zeta Z(s) = prod_gamma prod_{k>=0} (1 - e^{-(s+k) ell_gamma}); Z's zeros: s = 1/2 +- i r_j (with multiplicity) from the Laplace spectrum, plus trivial zeros at s = -k with multiplicity (2g-2)(2k+1) for genus g. Guillemin trace formula (to be USED as a stated hypothesis, not proved): for the geodesic flow with generator X on the unit tangent bundle M = Gamma\PSL(2,R) of a compact hyperbolic surface, the flat trace of the transfer operator e^{-tX} (Koopman: (e^{-tX}f)(m) = f(phi_{-t}(m)) or whichever sign convention you fix and STATE) is, as a distribution on t>0, Tr^flat e^{-tX} = sum_gamma sum_{k>=1} ell_gamma delta(t - k ell_gamma)/|det(1 - P_gamma^k)| where P_gamma is the linearised Poincare map on the transverse (stable+unstable) directions.

TASKS. For each, give the statement as a Proposition with a label, the hypotheses listed explicitly (mark each as "standard, to be cited" or "proved here"), a Lamport-structured proof (steps <1>1, <1>2, ... each with PROOF), and a status line: proved-here | sketched | conditional-on <hypothesis>. If a statement as I have drafted it is false or needs a different constant or hypothesis, say so plainly, correct it, and prove the corrected version. Do not round up.

T1 (Circle comb). For L > 0 let U_t be translation by t on L^2(R/LZ). Prove that, as a tempered distribution in t, Tr U_t = L sum_{k in Z} delta(t - kL) (state precisely in what sense: pairing with a Schwartz test function, or the trace of the smeared operator int f(t) U_t dt being trace class). Deduce that for the direct sum over all primes p of the circles of circumference log p, the trace of the flow restricted to t > 0 is sum_{n>=2} Lambda(n) delta(t - log n) with Lambda the von Mangoldt function. Prove also: for a FINITE set S of primes, the spectrum of the generator of the direct-sum flow is the union over p in S of (2 pi i / log p) Z, which is exactly the set of poles of the partial Euler product prod_{p in S} (1 - p^{-s})^{-1} as a function of s (make the identification of variables explicit), and conclude that no zero of zeta appears for any finite S.

T2 (Lindbladian = sum of squares = Casimir = Laplacian). (a) For a commutative *-algebra of smooth functions and a family of real vector fields X_j acting as derivations, with jump operators L_j = -i X_j (formally self-adjoint on L^2 for a volume-preserving flow), prove that the Heisenberg-picture Lindbladian L(f) = sum_j (L_j f L_j - (1/2){L_j^2, f}) = -(1/2) sum_j [L_j,[L_j,f]] acts on f (a multiplication operator) as the second-order differential operator (1/2) sum_j X_j^2 f. (b) On G = PSL(2,R) with left-invariant vector fields H, E, W for the basis H = diag(1,-1), E = ((0,1),(1,0)), W = ((0,1),(-1,0)) of sl(2,R), the Casimir is Omega = (1/4)(H^2 + E^2 - W^2) (in U(g), acting through the left-invariant fields). Prove: for f right-K-invariant, i.e. f(g) = F(g.i) with g.i = x+iy, one has W f = 0, Omega f = c y^2 (F_xx + F_yy) with c an explicit constant you determine (compute it; do not guess), and (1/2)(H^2 + E^2) f = 2 Omega f. Conclude: the Lindbladian with the two non-compact jump operators L_1 = -iH, L_2 = -iE, restricted to the K-invariant sector (which it preserves), is a constant multiple of the hyperbolic Laplacian; give the constant relative to Delta = -y^2(d_x^2+d_y^2). State clearly that the Casimir has the compact direction with a MINUS sign and is therefore not itself a Lindbladian on all of L^2(Gamma\G), and that the identification holds on the K-invariant sector. (c) For a unitary representation pi of a Lie group on a Hilbert space with anti-self-adjoint generators pi(X_j), prove that the Lindbladian rho -> -(1/2) sum_j [pi(X_j)... careful with i's: with L_j = -i pi(X_j) self-adjoint, L(rho) = sum_j (L_j rho L_j - (1/2){L_j^2, rho}) equals (1/2) sum_j (d pi tensor bar-pi)(X_j)^2 acting on rho viewed as an element of H tensor bar H (Hilbert-Schmidt operators), i.e. the sum-of-squares (Casimir-type) operator of the representation pi tensor bar-pi. State what this means for sl(2,R): the "quantum Lindbladian" of a representation is the Casimir of pi tensor bar-pi up to the W^2 sign issue of (b), and record as a remark (not a theorem) that its spectral gap is governed by the decomposition of pi tensor bar-pi into irreducibles.

T3 (Poincare map). Prove that for a closed geodesic of length ell on a hyperbolic surface, the linearised Poincare map of the geodesic flow on the transverse space has eigenvalues e^{ell}, e^{-ell}, hence |det(1 - P^k)| = 4 sinh^2(k ell/2) = e^{k ell}(1 - e^{-k ell})^2. Use the description of the flow as right multiplication by a_t on Gamma\PSL(2,R) and the adjoint action on sl(2)/RH.

T4 (Continuous Ihara--Bass, formal). Assume the Guillemin trace formula (hypothesis) and T3. Define, for Re(sigma) large, the Laplace transform Lambda(sigma) = int_0^infty e^{-sigma t} Tr^flat e^{-tX} dt. Prove: (i) Lambda(sigma) = sum_gamma sum_{k>=1} ell_gamma e^{-sigma k ell_gamma}/(4 sinh^2(k ell_gamma/2)); (ii) using 1/(4 sinh^2(x/2)) = sum_{m>=0} (m+1) e^{-(m+1)x}, Lambda(sigma) = -(d/d sigma) log D(sigma) with D(sigma) = prod_{j>=1} Z(sigma + j) (absolutely convergent product for Re sigma large; be precise about where); (iii) hence the "resonances" (poles of Lambda, i.e. zeros of D counted with multiplicity, in the region where D continues; state that the meromorphic continuation of Z to C is standard-to-cite) are exactly sigma = -1/2 - k +- i r_j (k >= 0, j >= 0) together with the images of the trivial zeros of Z; (iv) in particular all resonances of the first band lie on Re sigma = -1/2 iff every r_j is real iff Delta has no eigenvalue in (0, 1/4) (Selberg's 1/4 property), the continuous counterpart of "Ramanujan iff RH for the Ihara zeta"; (v) compare with the regular graph: the Hashimoto trace Tr T^m = sum_gamma sum_{k: k ell_gamma = m} ell_gamma has no Jacobian factor (the tree is one-dimensional), which is why the Ihara zeta has no tower prod_{j>=1}; make this comparison a precise remark, including the statement that the graph analogue of D is 1/det(1-uT) and of the tower is absent. Also relate to the Ruelle zeta zeta_R(s) = prod_gamma (1 - e^{-s ell_gamma}) = Z(s)/Z(s+1) (prove this identity of formal products). Be careful and explicit about which variable is which (the flat-trace Laplace variable sigma versus the Selberg variable s) and about signs; this is where errors hide.

T5 (The cusp term of the modular surface is the prime comb). Let phi(s) = sqrt(pi) Gamma(s - 1/2) zeta(2s - 1)/(Gamma(s) zeta(2s)) (the scattering determinant of PSL(2,Z); take this formula as a hypothesis to be cited). Consider the distribution on R in the variable t: C(t) := (1/2pi) int_R e^{i r t} m(r) dr with m(r) = -(1/2)(phi'/phi)(1/2 + i r) (interpret as the Fourier transform of a tempered distribution; justify that m is tempered, e.g. polynomial growth from the digamma terms and boundedness/log growth from zeta'/zeta on Re s = 1 and the functional equation). Prove: C(t) = sum_{n>=2} (Lambda(n)/n) [delta(t - 2 log n) + delta(t + 2 log n)] + A(t) where A is an explicit smooth-or-mildly-singular archimedean term coming from the digamma functions and from the Gamma factors of the functional equation used to move zeta'/zeta(2 i r) to zeta'/zeta(1 - 2 i r). Get the constants exactly (the factor 2 in front of zeta'/zeta, the 1/2 in m, the 2 in 2 log n) and state the convention (positive weights = peaks of C, or dips, depending on sign). Then record as a corollary: the weights Lambda(n)/n at t = 2 log n equal e^{-t/4} times the weights Lambda(n) n^{-1/2} at t = 2 log n of the trace of the compressed semigroup Z(t) in notes/riemann-channel-note.md section 5, i.e. the cusp term of the modular trace formula is the trace of the Riemann channel damped at the uniform rate 1/4; say exactly what identity this is and what it does NOT prove (nothing about RH).

T6 (What is and is not established). A closing section listing, for each proposition, the exact hypotheses that are "standard, to be cited" (Guillemin trace formula; meromorphic continuation and zeros of Z; the formula for phi; the Selberg trace formula itself is NOT needed if you avoid it), and a list of statements the orchestrator drafted that you found false or imprecise, with the correction.

FORMAT: Markdown with LaTeX math ($...$ and $$...$$), one section per task, Lamport steps, a final "RESULT:" line summarising which propositions are proved-here, which conditional, which corrected. Be complete rather than brief; this file will be reviewed adversarially by another model and then distilled into a lab-book section, so every constant must be right and every step justified.
