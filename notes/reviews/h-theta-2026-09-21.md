# H-THETA: hostile review (REFUTE protocol)

date: 2026-09-21
reviewer: `claude:opus` (review lane), independent of the prover and of the numerics lane
protocol: REFUTE — every numbered item of the prover lane is attacked before registration.
Each item gets VALID / MINOR (fixable defect, fix stated) / INVALID (wrong, gap or counterexample).

lanes reviewed:
- brief: `notes/h-theta/astra-brief.md` (`claude:fable-5.1`)
- prover: `notes/h-theta/astra-proofs.md` (`codex:gpt-6-astra`): ledger T1–T6, correction ledger,
  verdict on `conj:h-theta`, restated conjectures H-THETA-EDGE / H-GL1-BAD / H-THETA-1
- blind numerics: `notes/h-theta/numerics.md`, `outputs/h_theta.txt`, `scripts/h_theta.py`
  (`claude:opus`, 98 checks, findings F1–F16)
- notebook context: `report/sections/04p_gl1_bond.tex` (where `conj:h-theta` is stated, lines 103–109),
  `report/sections/04_riemann_channel.tex`, `report/sections/04h_rebound_state.tex`,
  `report/sections/04l_elliptic_cavity_channel.tex`

sources re-read byte-by-byte by this lane (`sed -n`, `grep -a`):
`refs/src/math/0001013/main.tex` (Burnol causality, 824 lines, read in full),
`refs/src/math/9811068/main.tex` (Connes), `refs/src/math/0203120/main.tex` (Burnol Sonine),
`refs/src/math/0311468/main.tex` (Meyer, Latin-1), and — newly local, mid-review —
`refs/src/uetake-2007/paper.txt`
(`sha256 c94e52c0b45cc9fbd7b259486f9175b8b391787eda0a780c76fa87e8d4921423`;
pdf `sha256 89e0f00bf04192e975ad22dafa032cd224c39b1d9c03db8bb5368e142b32ac49`).

**Every byte-citation in `astra-proofs.md` was opened and read. All of them are present and say what
the prover claims.** Two are imprecise (Findings 4, 5); none is fabricated. This is a materially
better citation record than the previous round's prover (`cusp-graph-2026-09-20.md`, finding 1).

---

## Summary table

| item | verdict | one line |
|---|---|---|
| T1(a) | **VALID** | `phi(1/y)=y^{1/2}phi(y)`; one-line Poisson, exact |
| T1(b) | **VALID** | `L^2` window `0<a<1/2`, `a=1/4` the unique `J`-fixed weight |
| T1(c) | **VALID** | Burnol's subtracted `E`, not Connes's; all three normalisations `W`, `R`, `E_B=2 sqrt x E_0` re-derived and correct |
| T2(a) | **VALID** | `ghat=Lambda(1/2+2i tau)`; residues `+i` at `+i/4` (`s=0`), `-i` at `-i/4` (`s=1`); Stirling constant `2 sqrt2 pi^{1/4}` confirmed |
| T2(b) | **VALID** | half transforms, `1/|u|` tail with constant `2-theta(1)`, both log-integrability statements |
| T2(c) | **MINOR** | correct; the paired Hadamard product is indexed by `Im rho>0` without the one line excluding real zeros |
| T2(d) | **VALID** | `Theta` pure Blaschke, unconditional; delay excluded by `Theta(iv) ~ sqrt(pi/v)` |
| T3(a) | **VALID** | full orbit dense, by Fourier uniqueness, not by Connes/Burnol surjectivity |
| T3(b) | **VALID** | forward orbit dense; the brief's "no resonances" replaced by the correct half-plane point spectrum |
| T3(c) | **VALID** | **the strongest result in the file**: `Lambda_>` is zero-free on `Re s<1`, `ghat_>` outer, defect `0` |
| T3(d) | **MINOR** | REFUTED verdict sound; the unweighted `a=0` outgoing reading is not named although T3(c) already covers it |
| T4(a) | **MINOR** | orientation correction is right and important; two citations need a qualifier (`Z` factor; Euclidean vs adelic `S_{<=1}`) |
| T4(b) | **VALID** | `M=H` refutation and the reflected defect `J V_y Delta = B_bad H^2_+` both correct |
| T4(c) | **VALID** | `S=V^2B^{-2}`, order-`2m` poles, `dim K_{v_+^2}=2` under RH: all verified against Burnol |
| T4(d) | **VALID** | spectral density, order `2m`, the Weil-pole obstruction, Meyer comparison |
| T5(a) | **VALID** | Siegel constant term, and the corrected factor `t^{-1}` / involution `t -> t^{-1}F(1/t)` |
| T5(b) | **VALID** | `c=1/2`, `phi_E=rS`; the Uetake wave-scattering claims are now locally verified and **correct** |
| T5(c) | **VALID** | weight dictionary and the explicit cyclic vector `Theta/(tau+i)^2` |
| T6(a) | **VALID** | root-vector completeness with jets; Connes threshold `n<(1+delta)/2` verbatim in the source |
| T6(b) | **VALID** | jets complete+minimal unconditional; Riesz failure under RH by the counting-function contradiction |
| T6(c) | **VALID** | Sonine transcription exact; the two-cutoff covariance is right and the brief's law is false |
| H-THETA-EDGE | **VALID** | a theorem; every step unconditional; free of the brief's errors F1–F3, F5 |
| H-GL1-BAD | **MINOR** | mathematically correct; the registered statement must name the transport unitary and the variable change |
| H-THETA-1 | **VALID** | well-posed; `T_t^0` well defined and a genuine semigroup; the Gram condition correctly derived |

---

## T1(a) — VALID

Statement `phi(1/y)=y^{1/2}phi(y)`. The proof is the one line
`phi(1/y)=theta(1/y)-1-y^{1/2}=y^{1/2}theta(y)-1-y^{1/2}=y^{1/2}(theta(y)-1-y^{-1/2})`.
Correct. The prover's remark that "odd" is an arithmetic grading and that the weighted vector is
inversion-**even** is a correct reading of `04p:95–100` and is not a defect of the shard. Numerics
lane D09 gives max rel err `1.57e-26`; I did not re-run it (it is not load-bearing for anything
contested).

## T1(b) — VALID

`y^a phi in L^2(d^x y)` iff `0<a<1/2`; `J(y^a phi)=y^{1/2-a}phi`, so `a=1/4` is the unique fixed
weight. Both endpoint divergences are correctly located (`int_0^1 y^{2a-1}dy` at `0`,
`int_1^inf y^{2a-2}dy` at infinity). The asymptotic `g(e^X) ~ -e^{-|X|/4}` is right in both
directions: for `X->+inf`, `e^{X/4}(-e^{-X/2})=-e^{-X/4}`; for `X->-inf`, `e^{X/4}(-1)`.
This is the fact that makes T3(d)'s `phi notin H` correct.

## T1(c) — VALID

Every normalisation was re-derived here independently:

- `E_B(f)(x)=sqrt x sum_{n!=0}e^{-pi n^2 x^2} - x^{-1/2} = g(x^2)`, because `g(x^2) =
  x^{1/2}(theta(x^2)-1-x^{-1})`. Correct. The adelic integral of `e^{-pi t^2} (x) 1_{Zhat}` is `1`
  and `q in Q^x` with `q.1 in Zhat` forces `q in Z\{0}`: correct.
- `W h(y)=2^{-1/2}h(sqrt y)` is unitary `L^2(d^x x) -> H` (`d^x y = 2 d^x x`), and `W E_B(f)=2^{-1/2}g`.
- `E_B(f)=2 sqrt x E_0(f)` for even `f`, and `R E_0(e^{-pi x^2}) = g/(2 sqrt2)` with
  `(Rf)(y)=2^{-1/2}y^{1/4}f(sqrt y)`, `R` unitary `L^2(dx)->H`. All four checked by hand; all correct.
- Citations: `0001013:204–207` is verbatim the subtracted definition, including the displayed
  `- {\int_{\Aa_K} \varphi(x)\, dx \over \sqrt{|v|}}`; `9811068:773–777` is verbatim
  `E(f) \, (g) = \vert g \vert^{1/2} \sum_{q\in k^*} f(qg)`; `0001013:464–480` begins exactly
  "We describe one more variation."
- The prover's claim that Connes's unsubtracted `E` is *not* the `L^2` vector is correct:
  `x^{1/2}(theta(x^2)-1) ~ x^{-1/2}` as `x->0`, not square-integrable; and the Gaussian violates
  Connes's domain condition `f(0)=fhat(0)=0` (source: `0001013:210–211` states that condition
  and says Burnol drops it).

## T2(a) — VALID

The meromorphic identity, evenness, reality, the zero divisor `a_rho=gamma/2-i(sigma-1/2)/2`, and
the residues. `xi_crit(+-i/4)=xi(0)=xi(1)=1/2 != 0`, so the poles are genuinely simple and the
residues are `-xi_crit(+-i/4)/(+-i/2) = +-i`. My check C5:

```
Res_{tau=+i/4} ghat ~ (-1.95e-14 + 1.0j)   (s = 0)
Res_{tau=-i/4} ghat ~ (-1.95e-14 - 1.0j)   (s = 1)
```

The prover's refutation of the brief's `|ghat(u)| ~ C|u|^c e^{-pi|u|/2}` is correct (real zeros
forbid it) and agrees with numerics-lane F5. The Stirling constant is `2 sqrt2 pi^{1/4}`; I verified
it equals the numerics lane's `2 pi^{-1/4}sqrt(2pi)` (both `= 3.76558505511`) and that the ratio
`|ghat(u)| / (C u^{-1/4}e^{-pi u/2}|zeta(1/2+2iu)|)` is

```
u= 20  1.00001953908      u=100  1.0000007812625
u= 40  1.0000048833014    u=300  1.0000000868057
u= 70  1.0000015944399
```

exactly the `1+O(1/u)` claimed (and reproducing the numerics lane's `1.0000048833`, `1.0000015944`,
`1.0000007813` to all printed digits). The elementary bound `|ghat(u)| <= C(1+|u|)^{1/4}e^{-pi|u|/2}`
follows from `zeta(1/2+it)=O(|t|^{1/2})` as the prover says. `ghat(0) = -7.95393245101302576 =
-16 xi(1/2)` reproduced to 18 digits.

## T2(b) — VALID

`Lambda_>(s)=2/(s-1)+A(s)` on `Re s<1`, `Lambda_<(s)=-2/s+A(1-s)=Lambda_>(1-s)` on `Re s>0`: I
re-derived both. The prover's correction of the brief — the pole at `-i/4` is on the **boundary** of
the holomorphy half-plane `Im tau>-1/4` and belongs to the meromorphic extension, not to the domain —
is right and worth keeping.

The `1/|u|` tail: `ghat_>(u) = -h(0)/(iu)+O(u^{-2})` with `h(0)=g(1)=theta(1)-2`, so
`u ghat_>(u) -> i h(0) = -(2-theta(1))i`. Check C7:

```
u=   5   u*ghat_>(u) = -0.00378731673925 - 0.934728602217j
u=  20                 -5.29e-13         - 0.914552515192j
u= 100                 -3.01e-34         - 0.913604169389j
u= 500                 -2.45e-33         - 0.913566747216j
u=2000                 -8.20e-33         - 0.913565286187j
```

against `2-theta(1) = 0.91356518878669198542`. Both log-integrability statements are correct as
argued: for the half, `log|ghat_>|` has an integrable `log|u|` growth plus integrable local log
singularities (and T3(c) removes even those); for the full transform the upper bound
`log|ghat(u)| <= -(pi/2)|u|+O(log)` makes the integral `-infinity`, which is the right statement
(not merely "not in `L^1`").

## T2(c) — MINOR

Everything asserted is true and I verified it:

- the quotient identity. `ghat(tau-i/4)=Lambda(1+2i tau)`, `ghat(tau+i/4)=Lambda(2i tau)`,
  `Lambda(1+2i tau)/Lambda(2i tau) = Theta(tau)/r(tau)` by inserting `Lambda=4 xi/(s(s-1))` and
  `xi(2i tau)=xi(1-2i tau)`. Check C2, 7 values of `tau` including complex ones, rel err `<= 1.2e-30`.
  The removable value at `tau=0` is `-1` (pole over pole), `r(0)=-1`, `Theta(0)=1`: confirmed
  numerically (`Theta(0)=1.0 + 9.2e-16j`).
- `E(tau)=xi(1-2i tau)=xi(2i tau)=xi_crit(tau+i/4)`, `E^#(tau)=xi(1+2i tau)=xi_crit(tau-i/4)`:
  re-derived, correct, and `E^#(tau)=conj(E(conj tau))` follows from `xi(sbar)=conj(xi(s))`.
- **the paired Hadamard product and `b=0`.** `P(s)=prod_{Im rho>0}(1-s/rho)(1-s/rhobar)` converges
  absolutely because the paired factor is `1 - s(1/rho+1/rhobar) + s^2/|rho|^2` and
  `1/rho+1/rhobar = 2 Re rho/|rho|^2 = O(|rho|^{-2})` in the strip.
  `P'/P(s)=sum[1/(s-rho)+1/(s-rhobar)]`, so
  `P'(1/2)/P(1/2) = -sum 2(sigma-1/2)/|rho-1/2|^2`, absolutely convergent (`|term| <= 1/gamma^2`)
  and zero because `rho` and `1-rhobar` contribute opposite terms of equal modulus.
  `xi(s)=xi(1-s)` gives `xi'(1/2)=0`, hence `b=0`; `xi(0)=1/2` gives `e^a=1/2`.
  Checks B1–B6: `xi'(1/2) = -2.30e-41`; `b = xi'(1/2)/xi(1/2) - sum = -4.62e-41`; the off-line
  quadruple cancellation is exact to all digits at three synthetic `(sigma,gamma)`; and the
  `s`-independence test

  ```
  L(s) := log xi(s) - log(1/2) - sum_{n<=1500} log[(1-s/rho)(1-s/conj rho)]
  s= 2    L/(s^2-s) = 0.0005424725346
  s= 3    L/(s^2-s) = 0.0005424724516
  s=-1    L/(s^2-s) = 0.0005424725346
  s= 5    L/(s^2-s) = 0.0005424721610
  s= 0.5  L/(s^2-s) = 0.0005424725813
  s= 0.25 L/(s^2-s) = 0.0005424725800
  independent tail estimate sum_{gamma>gamma_1500} 1/|rho|^2 = 0.0005426
  ```

  A nonzero `b` would contribute `b/(s-1)` to this quotient, which swings by a factor `-2` between
  `s=-1` and `s=5`; the observed quotient is constant to 7 digits and matches the independently
  computed tail. So `b=0` numerically to `~1e-9` and exactly by the argument. Direct values of the
  truncated product against `xi` (check B6): rel `2.6e-7` at `s=2`, `9.0e-7` at `s=3+i`,
  `7.7e-7` at `s=-2`, `2.4e-5` at `s=0.5+14i` — all at the size of the neglected tail, and note that
  `s=-2` is a check on the far side of the strip, where an `e^{bs}` error would be largest.
  The same product with the same normalisation `1/2` appears verbatim in the newly local
  `refs/src/uetake-2007/paper.txt:668–673`:
  `xi(s) = (1/2) prod (1 - s/rho_n)(1 - s/conj rho_n)`, "the product being absolutely convergent
  for all s ∈ C". Independent corroboration.
- the resulting Blaschke form of `Theta`. I re-derived the constants: the numerator factors
  `1+2i tau/rho` and `1+2i tau/rhobar` contribute `(2i/rho)(2i/rhobar) = -4/|rho|^2` and the
  denominator factors contribute the same `-4/|rho|^2`, so they cancel exactly and the prover's
  display is an identity, not a proportionality. Roots: `v_{rho,-}=-gamma/2+i sigma/2`,
  `v_{rho,+}=gamma/2+i sigma/2` (numerator), their conjugates (denominator). Correct.
- Hermite–Biehler for `E` unconditionally: `E(tau)=xi(2i tau)=0` iff `tau=-i rho/2`, i.e.
  `Im tau=-sigma/2<0`, so `E` has no zero in the closed upper half plane; `|E^#|<|E|` in `C_+` is
  `|Theta|<1`. Verified numerically (`|Theta(0.3+0.05i)|=0.9953836`, `|Theta(2+0.5i)|=0.95120751`,
  `|Theta(7+3i)|=0.60886234`; `|Theta|=1` on `R` to 1e-30).
- **the correction of the brief on `xi_crit`** is right and important: `xi_crit^# = xi_crit`
  (`xi_crit(tau)=xi(1/2+2i tau)`, `xi_crit^#(tau)=xi(1/2-2i tau)=xi(1/2+2i tau)`), so HB for the
  unshifted `xi_crit` is *impossible*, not equivalent to RH. The prover's replacement — HB for
  `xi_crit(tau+ih)` for **every** `h>0` — is a correct RH equivalent (forward by pairing real zeros;
  backward, zero-freeness in `Im tau>h` for all `h>0` plus the conjugate/even symmetry of `xi_crit`
  forces all zeros real).
- the de Branges isometry with the `du/(2pi)` norm: the boundary identity `F^#/E = Theta conj(F/E)`
  is exactly `h conj(Theta) in H^2_-`, i.e. `h perp Theta H^2_+`. Correct.

**The defect.** The paired Hadamard product is written as `prod_{Im rho > 0}`, which silently
assumes `xi` has **no real zeros**. If `xi` had a zero `rho_0 in (0,1)` the displayed product would
omit it. **Fix (one line):** add "`zeta(sigma)<0` for `0<sigma<1`, so `xi` has no real zero and
every nontrivial zero occurs exactly once in the conjugate pairing indexed by `Im rho>0`". Nothing
else changes: even a hypothetical real zero `rho_0 != 1/2` pairs with `1-rho_0` and contributes `0`
to `P'(1/2)/P(1/2)`, and `rho_0=1/2` contributes `0`; so the conclusion `b=0` is robust either way.
Verdict MINOR only because the displayed product is used as an identity, and the fix must be printed.

## T2(d) — VALID

`Theta` is a pure Blaschke product, no singular factor, no delay. The argument is airtight given
T2(c): the paired product *is* `Theta` identically, its zeros satisfy
`sum Im w_rho/(1+|w_rho|^2) < infinity` (`Im w_rho in (0,1/2)`, `|w_rho|^2 ~ gamma^2/4`, so the sum is
dominated by `sum 1/gamma^2`), and the Blaschke/singular factorisation of an inner function is
unique, so the singular factor is `1`.

The independent exclusion of `e^{i a tau}` via the vertical decay is exactly right. I re-derived
`Theta(iv) = xi(2v)/xi(1+2v) = ((2v-1)/(2v+1)) sqrt(pi) Gamma(v)/Gamma(v+1/2) zeta(2v)/zeta(2v+1)`
and checked it against `xi(1+2i tau)/xi(1-2i tau)` at `tau=iv` (rel `<= 1.1e-26`). Check C1:

```
v        Theta(iv)            Theta(iv) sqrt(v/pi)
1        0.912288518413       0.514703679279
2        0.835023059875       0.666252007388
10       0.51349459374        0.916138088206
100      0.175701237842       0.991288082069
1000     0.0560008899362      0.999125382612
10000    0.0177229876798      0.999912503828
100000   0.00560494217294     0.999991250038
```

`Theta(iv) sqrt(v/pi) -> 1` with the error decaying like `1/v`. A factor `e^{i a tau}`, `a>0`, would
force `|Theta(iv)| <= e^{-av}`: excluded. The prover's values `0.91613809, 0.99128808, 0.99912538`
at `v=10,100,1000` are reproduced to all printed digits, as are `Theta(i)=0.91228852`,
`Theta(2i)=0.83502306`. The note that `E` is **not** of exponential type (`E(iv)=xi(1+2v)` has gamma
growth), so the Cartwright route does not apply, is correct and is the reason this argument is
needed at all.

## T3(a) — VALID

If `h perp U_t g` for all real `t` then the Fourier transform of `ghat conj(ghat)`... of the `L^1`
function `ghat conj(hhat)` vanishes, hence `ghat conj(hhat)=0` a.e., hence `hhat=0` because the real
zeros of the nonzero real-analytic `ghat` are isolated. Correct.

The prover's *refusal* to derive this from Connes's Theorem III.1 or Burnol's density lemma is the
right call and I checked both citations: `9811068:2549–2554` reads "defines a surjective isometry
from `L^2(X)_0` to `L^2(C_k)`" and `0001013:218–223` reads "`E(\Ss(\Aa_K)) \subset L^2(\Cc_K, d^*u)`
and is dense in it". Both are about the **range of `E`** over all test functions; neither implies
cyclicity of one Gaussian. The correction-ledger row saying so is correct.

## T3(b) — VALID

`F(z) = (1/2pi) int ghat(u) conj(hhat(u)) e^{izu} du` is holomorphic on `|Im z|<pi/2` because
`|ghat(u)| <= C(1+|u|)^{1/4}e^{-pi|u|/2}` (T2(a)) and `hhat in L^2` give absolute convergence by
Cauchy–Schwarz on every compact substrip; vanishing on `t>=0` forces `F=0` by the identity theorem;
Fourier uniqueness finishes. Correct, and — as the prover notes — it avoids needing the
Beurling–Lax–Halmos classification the brief leaned on.

The correction of the brief is right: the backward translation on `L^2(0,infty)` has
`e^{-aX}` as an eigenvector with eigenvalue `e^{-at}` for **every** `Re a>0`, i.e. the whole open
left half-plane is point spectrum. "No resonances" was wrong; "no discrete zeta-selected spectrum"
is the correct statement. The operator-theoretic distinction (`C_t` and `Z_t` on `K_Theta` are
neither isometries nor coisometries, with `Z_t (Theta/(tau-w)) = e^{itw}(Theta/(tau-w))` since
`(e^{it tau}-e^{itw})/(tau-w) in H^2_+`) is correct.

## T3(c) — VALID. This is the strongest result in the file and it holds up.

The claim is far stronger than the brief anticipated: **`Lambda_>(s) != 0` throughout `Re s<1`**,
hence `ghat_>` is outer, `B_>=1`, and the Beurling defect of the theta-generated outgoing subspace
is exactly `{0}` — not a nontrivial Blaschke model space. I re-derived every step:

1. With `y=e^X`, `z=(1-s)/2`, `r_0(X)=e^{X/2}(theta(e^X)-1)`, `p=1-r_0`:
   `Lambda_>(s) = int_0^inf [r_0(X)-1]e^{-zX}dX = -int_0^inf p(X)e^{-zX}dX` for `Re z>0`.
2. each summand `2e^{X/2}e^{-pi n^2 e^X}` has derivative `e^{X/2}e^{-pi n^2 e^X}(1/2-pi n^2 e^X)<0`
   for `X>=0` (since `pi n^2 e^X >= pi > 1/2`), so `r_0` is strictly decreasing, `p'>=0`,
   `int_0^inf p' = r_0(0) = theta(1)-1`.
3. integration by parts: `-z Lambda_>(s) = p(0) + int_0^inf p'(X)e^{-zX}dX`.
4. `|e^{-zX}|<=1` on `Re z>0`, so
   `Re[-(1-s)Lambda_>(s)/2] >= p(0) - r_0(0) = 3 - 2 theta(1) = 0.82713037757338397085 > 0`.

All four steps are correct. `theta(1)-1 <= 2e^{-pi}/(1-e^{-3 pi}) = 0.0864348... < 1/2` is also correct.

Check A (`scratch_ht_outer.py`), independently of the prover: the incomplete-gamma evaluation of
`Lambda_>` and the `-int p e^{-zX}` quadrature agree to `<=1.2e-31` at five `s`; the IBP identity
holds to `<=1.3e-32` at three `s`; `int_0^inf p' dX = 0.0864348112133 = theta(1)-1`. The grid:
**112 points**, `Re s in {-200,-50,-20,-5,-1,0,0.25,0.5,0.75,0.9,0.99,0.999,0.99999,0.9999999}` x
`Im s in {0,0.5,1,5,20,100,1000,10000}`, **zero violations**, grid infimum
`0.91356520208675` at `s = 0.9999999 + 10000i`. The infimum on the grid is `2-theta(1)=0.913565189`,
not the prover's `3-2 theta(1)=0.827130378`: the bound is **valid but not sharp**, which is a
sharpening, not a defect. Limits: `-z Lambda_> -> 1` as `s->1^-` (`0.99999999998781` at `s=1-1e-9`,
matching `p(0)+int p' = 1`) and `-> p(0)` as `Re s -> -infinity` (`0.91402018551984` at `s=-1000`).

The exclusion of a singular inner factor is also correct: analytic continuation of `ghat_>` across
every finite real point kills any singular measure on `R`, and `e^{i a tau}` with `a>0` would force
the inverse transform to vanish on `0<X<a` whereas `g(1)=theta(1)-2 = -0.9136 != 0`. These are the
two parts of the singular factor and both are handled.

The prover's correction of the brief's T3(c) target is also right: the model-space zero of `ghat_>`
is at `w_rho`, whose half-transform argument is `rho-1/2` (`s_+(w_rho)=1/2+2i(gamma/2+i(1-sigma)/2)
= sigma+i gamma` for on-line `rho`... more precisely `ghat_>(w_rho)=Lambda_>(rho-1/2)` under RH),
**not** `1-rho`. Both are nonzero anyway. My C4 reproduces the prover's decimals exactly:
`Lambda_>(1-rho_1) = 0.130556416727i`, `Lambda_>(rho_1) = -0.130556416727i`,
`ghat_>(w_1)=Lambda_>(i gamma_1) = -0.00472327134346 - 0.130374948514i`,
`Lambda_>(0) = -1.9781868816`, `Lambda_>(-1) = -0.980313838639`,
`ghat_>(0) = -3.97696622551`, `ghat_>(1) = -0.213223462735 - 0.936223502922i`.

## T3(d) — MINOR

The refutation is **sound**: `phi notin H` (so the printed conjecture is not literally defined),
the symmetric `L^2` repair gives `H` for the full and the forward orbit, cutting first gives `H_+`
with zero defect, and none of these is `K_Theta`, which is nonzero with discrete modes. The
extension to every weight `0<a<1/2` (transform `Lambda(2a+2i tau)`) is correct: T3(b)'s argument
uses only the exponential decay and a polynomial `zeta` bound, and T3(c)'s zero-free half-plane and
endpoint-support argument apply verbatim. The prover's refusal to assert the brief's blanket "no
reading works" is correct and is itself a correction — T5(c) exhibits a theta-derived vector whose
inner factor *is* `Theta`.

**The defect (completeness of the refutation).** The reading a defender would actually offer next is
the **unweighted** `phi` cut to the outgoing half: `phi.1_{y>1} in L^2(1,infty;d^x y)` (it decays
like `y^{-1/2}`), which is a perfectly well-defined Hilbert vector even though `phi notin H`. The
prover does not name it; T3(d) covers only `0<a<1/2`. **Fix (two lines):** note that the Mellin
transform of `phi.1_{y>1}` is `Lambda_>(2i tau)`, whose argument has `Re s = -2 Im tau < 0 < 1` on
`C_+`, so T3(c)'s theorem *already* makes it outer with zero defect. With that line the refutation
covers `a=0` on the outgoing half as well, and the only reading left unanalysed is the genuinely
undefined one (`phi` as a distribution in a rigged or weighted space), which the prover is right to
decline rather than refute.

## T4(a) — MINOR

**The orientation correction is right, is the most consequential correction in the file, and I
verified it three ways.**

Source: `0001013:283–284` defines `\HH^2 = {f : ess-supp(f) \subset {|u| \leq 1}}` — verbatim, and
the prover quotes it correctly. That is the **incoming** half `y<1` in the notebook's convention
(numerics-lane F9 confirms `y>1 <-> H^2(C_+)` independently). Hence Burnol's closure theorem
`V(Delta) = B.\HH^2` (`:639`, verbatim `$$V(\Delta) = B\cdot\HH^2$$`) lands in the **incoming** half,
and the brief's "`V` turns the theta-test closure into the outgoing half" is false.

The transcription algebra I re-derived and confirm:
`(Rf)^hat(tau) = sqrt2 F_L(1/2+2i tau)` (substituting `y=x^2`, `d^x y = 2 d^x x`), so Burnol's
`\HH^2(Re s>1/2)` corresponds to `Im tau<0`, i.e. `C_-`; `v_-(tau)=(s_+-1)/s_+=(tau+i/4)/(tau-i/4)`
is inner in `C_-`; `V_y Delta = B_- H^2_-`; `D_+ = Delta^perp = V_y^{-1}B_- H^2_+` (matching
Burnol `:502–503` `D_+ = V^{-1}B(\HH^2)^perp`); `D_- = J D_+ = V_y B_-^{-1}H^2_-` (matching
`:504–505`). The outgoing representation `B_-^{-1}V_y` sends `D_-` to `V_y^2 B_-^{-2}H^2_-`.
`U_B(lambda)h(x)=h(x/lambda)` conjugates to `U_{2 log lambda}`, so Burnol's contraction semigroup
`|v|<=1` is `U_t` with `t<=0`: `Delta` is invariant under `U_{-t}`, `t>=0`. All correct.

Independent numerics, check E (`scratch_ht_burnol.py`): Burnol's own worked example
`((s-1)/s) zeta(s)/s = int_0^1 A(u)u^{s-1}du` with `A(u)=[1/u]log u + log([1/u]!) + [1/u]`
(`:396–398`) reproduces to rel `9.2e-11` at `s=3` and `3.7e-7` at `s=2`; Nyman's
`int_0^1 rho_alpha u^{s-1}du = (alpha-alpha^s)zeta(s)/s` (`:348–349`) reproduces at three `s` for
two `alpha`; `V` acts as the multiplier `(s-1)/s` (rel `2e-26`) and kills `1/u` exactly. `A` is
supported in `(0,1]`, i.e. the incoming half. Then the Hardy test on the bond image
`sqrt2 F_L(1/2+2i tau)` of a function supported in `(0,1)` shows boundedness on horizontal lines in
`C_-` and blow-up in `C_+`, with the direct `y`-quadrature of `(R rho_alpha)^hat` matching
`sqrt2 F_L(1/2+2i tau)` at three points of `C_-` to `<=1.8e-3` (piecewise-quadrature limited). The
Hardy test itself (check E4):

```
Im tau      sup_{Re tau<=40} |sqrt2 F_L|      (1/2pi) int_{-200}^{200} |.|^2 dx
-2.0        0.097763539                       (Im tau=-1.5)   0.013091263
-1.0        0.18916826                        (Im tau=-0.75)  0.026527769
-0.3        0.45151248                        (Im tau=-0.1)   0.11818938
+0.3        1.770607                          (Im tau=+0.1)   0.31638594
+1.0        20.080568                         (Im tau=+0.75)  1250.1259
+2.0        32333.302                         (Im tau=+1.5)   4.4088537e+9
```

Bounded and with bounded `L^2` means in `C_-`; growing without bound in `C_+`. So the bond image of
a function supported in `(0,1)` is `H^2(C_-)`: **Burnol's `\HH^2` is the incoming half**, and the
brief's orientation is wrong exactly as the prover says.

The sector reduction to RH for `zeta` alone is also correct: `C_Q^1 = Zhat^x`, the trivial isotypical
component has `L`-factor `zeta`, `Delta` decomposes isotypically (`:575–580`), orthogonality is
sector-by-sector, and `:617–624` gives the inclusion for the general adelic test
("its Tate L--function ... is a multiple of `L(\chi,s)` by a function holomorphic in `Real(s)>0`"),
while `:489–495` gives the reverse for the explicit subclass.

**The defects (both citation qualifiers, no mathematics changes).**
1. The prover writes "`S=V^2B^{-2}` ... `:509–514` ... and `:694–699` in the adelic proof". At
   `:694–695` the adelic display is `S = (V^{-1}B)^{-1}\cdot VB^{-1}Z = ZV^2B^{-2}`, with an extra
   `Z`. `Z` is defined at `:683–685` as "just `1` in the number field case", so nothing is wrong for
   `K=Q` — but the citation as printed is inexact. **Fix:** write "`:509–514` (`S=V^2B^{-2}`);
   `:694–699` gives `S=ZV^2B^{-2}` adelically with `Z=1` in the number-field case (`:683–685`)".
2. "The subclass `f_infty (x) 1_{Zhat}`, with `f_infty` even and supported in `[-1,1]`, already
   yields exactly `B\HH^2`, by `:489–495`". Lines `489–495` are the **Euclidean** variation
   (`E(varphi)(u) = sum_{n>=1}varphi(nu) - (int varphi)/u` on `L^2(R_+,du)`), not the adelic sector
   statement. The identification is legitimate and is exactly the prover's own T1(c) relation
   `E_B(f) = 2 sqrt x E_0(f_infty)` for even `f_infty` (I re-derived it for general even Schwartz
   `f_infty`, not just the Gaussian), but the step must be *named*. **Fix:** add "via the
   normalisation `E_B = 2 sqrt x E_0` of T1(c), valid for every even Schwartz `f_infty`".

## T4(b) — VALID

Two claims, both correct.

(i) The brief's `M = closure span{U_t E(varphi): t>=0}` is **all of `H`**. Proof: `V_y` commutes with
`U_t`, `V_y Delta = B_- H^2_-`, and `union_{t>=0} e^{it tau}H^2_-` is (in log coordinates) the set of
functions supported in `X<t`, whose union is dense in `L^2(R)`; multiplying by the unimodular `B_-`
preserves density. So `V_y M = H` and `M = H`. Correct — this refutes the brief's
`VM = B_bad H^2_+` outright.

(ii) The repaired object `N = J V_y Delta` has `Nhat = B_bad H^2_+` with `B_bad(tau)=B(s_-(tau))`,
`s_-(tau)=1/2-2i tau`. I checked: `(J Phi)(tau)=Phi(-tau)` and `B_-(-tau)=B(1/2-2i tau)=B_bad(tau)`,
and this is consistent with Burnol's own `IBI=B^{-1}` (`:713–716`, verbatim
"We have used `$IBI = B^{-1}$`") because on the real line `B_bad = conj(B_-) = B_-^{-1}`.
`B_bad` is inner in `C_+` with zeros `w^{(1)}_rho = gamma/2+i(sigma-1/2)/2` (I verified
`s_-(w^{(1)}_rho)=rhobar`), `K_bad = H^2_+ ominus B_bad H^2_+`, and `K_bad=0` iff `B_bad` is constant
iff there are no bad zeros iff RH (using the functional-equation pairing). The mode eigenvalue
`exp(-(sigma-1/2)t/2 - i gamma t/2)` is `e^{-it conj(w^{(1)})}`: correct, and the decay rate is
indeed `(sigma-1/2)/2` in the `y`-clock (the brief's `sigma-1/2` is the idele `x`-clock), which the
correction ledger records.

## T4(c) — VALID

The RH equivalences follow from T4(b) and T2(c). The scattering correction is exactly Burnol's:
`:509–514` reads verbatim `$$S = (V^{-1}B)^{-1}\cdot VB^{-1} = V^2B^{-2}$$` and "It is an invariant
operator whose spectral multiplier is `({s-1\over s})^2\cdot B(s)^{-2}` and is an inner function if
and only if `B(s)` has no zero in `Real(s)>{1\over2}`". So:

- reflected, `S_{B,+} = v_+^2 B_bad^{-2}` with `v_+(tau)=(tau-i/4)/(tau+i/4)`, inner in `C_+`
  (zero at `i/4`);
- a bad zero of multiplicity `m` is a pole of order `2m`; `v_+^2` has only a double zero at `i/4`,
  which can never coincide with `gamma/2+i(sigma-1/2)/2` (that would need `gamma=0`, `sigma=1`);
  so no cancellation. Correct;
- **under RH the multiplier is `v_+^2`, not `1`, and `dim K_{v_+^2}=2`.** A degree-`n` finite
  Blaschke product has an `n`-dimensional model space, so this is right, and it is a genuine
  correction of the brief's "under RH the GL_1 scattering is trivial". The correct statement is the
  one the prover gives: the *arithmetic* factor is trivial; the elementary factor `V^2` survives.
- the dictionary table (every zero -> a `K_Theta` mode; only bad zeros -> a `K_bad` mode; only
  on-line zeros -> a Connes character) is consistent with everything above, and the disclaimer that
  logical equivalence of two RH criteria is not a unitary intertwiner is correct and necessary.

## T4(d) — VALID

`C(t)=<U_t g,g>` positive definite, continuous, real, even; its spectral density `|ghat|^2/2pi`
vanishes at `u=gamma/2` to order `2m_rho`. The order-`2m` refinement of the brief's "second order"
is correct. The Tate-transform citation `0001013:218–223` and its proof endpoint `:569–572`
(verbatim `$$\widehat{E(\varphi)}(\chi,s) = C(K) L(\varphi, \chi, s)$$`) are both present and say
what is claimed. The observation that "an isolated zero has spectral measure zero and does not
contradict T3" is the right reconciliation.

The **Weil-trace obstruction** is correct and is a genuine contribution: on `R`, `ghat` is real, so
`|ghat(u)|^2 = ghat(u)^2`, whose analytic continuation has **double poles at `+-i/4`**; after
`r=2 tau` those are `r=+-i/2`, exactly the two pole-evaluation points of the explicit formula. So
`|ghat|^2` cannot be fed to the Weil formula without a regularisation. Flagging the ring-norm trace
connection as OPEN is right; `04_riemann_channel.tex:176–184` (`prop:ringnorm-trace`) is indeed a
zero-counting distributional trace, a different object.

Meyer: `0311468:208–215` reads "The two poles occur in~`\pi_+`, the zeros in~`\pi_-`";
`:234–235` reads "zeros on the critical line appear as above, but zeros off the critical line appear
as resonances"; `:4597–4601` is `\mult(\omega,\pi) = \ord(\omega, L_\GF)`; `:249–254` is the Weil
character statement. All four verbatim and correctly used. Connes `:2589–2594` is verbatim
`Q'_{\L,0} \leq S_{\L}` with the positive-type distribution. All confirmed.

## T5(a) — VALID

The unfolding is correct: for `m != 0`,
`int_0^1 sum_n e^{-pi t(mx+n)^2/y}dx = int_R e^{-pi t u^2/y}du = sqrt(y/t)` (the substitution
`v=mx` covers `|m|` periods and contributes `1/|m|`, which cancels), giving
`theta(t/y) + sqrt(y/t)(theta(ty)-1)`.

The prover's two corrections of the brief are both right and both independently found by the
numerics lane (F1, F2): `sqrt(y/t)theta(ty) = t^{-1}theta(1/(ty))` (not `y theta(1/(ty))`), and the
involution is the **weighted inversion** `(W F)(t)=t^{-1}F(1/t)`, `W^2=1`, not `t -> 1/(y^2 t)`.
I verified `W` exchanges the two channels in both directions: `W[theta(./y)](t)=t^{-1}theta(1/(ty))
= sqrt(y/t)theta(ty)`, and `W[sqrt(y/.)theta(.y)](t) = sqrt(y/t)theta(y/t) = theta(t/y)` using
`theta(y/t)=sqrt(t/y)theta(t/y)`; `sqrt(y/t)` is `W`-fixed. Hence the full constant term `C_y`
satisfies `t^{-1}C_y(1/t)=C_y(t)`, which is the `s <-> 1-s` functional equation of the completed
Eisenstein constant term. `y` does not enter the involution — correct, and the numerics lane says
the same.

## T5(b) — VALID (external source now local and checked; see Finding 1)

Normalisations: `Theta_z(t)-1` Mellin-transforms to
`pi^{-s}Gamma(s) sum_{(m,n)!=0} (y/|mz+n|^2)^s = 2 E^*(z,s)`, since
`sum_{(m,n)!=0} = zeta(2s) . 2E(z,s)` with `E = (1/2)sum_{gcd=1}`. So `c=1/2`: correct.
The two channel transforms and their half-planes (`Re s>1/2` and `Re s>1`) are correct, and agree
with numerics-lane F3/D61 (the brief's evaluation point `s=0.3+0.5i` is outside both).
`phi_E(s)=Lambda(2s-1)/Lambda(2s)`, `phi_E(1/2+i tau)=r(tau)S(tau)=r(tau)/Theta(tau)`, and
`phi_E(1/2)=-1` while `S(0)=1`: all re-derived and confirmed numerically
(`phi_E(1/2+i)=0.523127151694-0.852254646899i`, reproducing the prover's `0.52312715-0.85225465i`;
`phi_E(1/2)=-1.0-3.9e-14i`).

**The Uetake claims.** At the time `astra-proofs.md` was written this was the one source not in the
repo, cited only by URL — a protocol violation (Finding 1). It is now
`refs/src/uetake-2007/paper.txt` and **every claim checks out**:

| prover's claim | line(s) of `paper.txt` | verbatim |
|---|---|---|
| projected `D'_pm` vs intersected `D''_pm` | 277–279 (p. 104) | "we define two types of incoming and outgoing subspaces in `H_E'`" ... `D_± = P_{H_E'} D_{0±}   or   D''_± = D_{0±} ∩ H_E'` |
| `D''_+ perp D''_-` but `D'_pm` not orthogonal | 294–296 (p. 105) | "Note that D+ ′′ ⊥ D− ′′ but D ′+ and D ′− are not orthogonal to each other." (the `pdftotext -layout` run splits the sub/superscripts across lines 294–296) |
| causal factor `S_{c0}(p)=xi(2p)/xi(-2p)` | 655, 679 (pp. 111–112) | `S_c0 = F_c^{-1} [xi(2s)/xi(-2s)] F_c`; `S_c0(s) = xi(2s)/xi(-2s)` |
| intersected-space matrix | 635–639 (p. 111) | `S_c(s) = -((s-1/2)/(s+1/2))^2 ... = -((s-1/2)/(s+1/2)) xi(2s)/xi(-2s)` |
| projected-space matrix | 919–923 (p. 116) | `S_c(s) = -Γ(1/2)Γ(s)ζ(2s)/(Γ(s+1/2)ζ(2s+1)) = S_c0 S_c1`, `S_c1(s)=(1/2+s)/(1/2-s)` |
| extra eigenvalue `-1/2` of the intersected generator | 86 (p. 100) | "Actually `σ(B'') \ σ(A_c) = {−1/2}`" |
| RH <-> all poles of `S_c0` on `Re s=-1/4` | 644–647, 826–828 | "The Riemann hypothesis is equivalent to saying that all the poles (or zeros) of `ξ(2s)/ξ(−2s)` lie on `ℜs = −1/4` (or 1/4)" |

Substituting `s=i tau` I confirm the prover's three relations exactly:
`S_{c0}(i tau)=xi(2i tau)/xi(-2i tau)=xi(1-2i tau)/xi(1+2i tau)=S(tau)`;
`S_{c1}(i tau)=(1/2+i tau)/(1/2-i tau) = -(tau-i/2)/(tau+i/2) = -r(tau)`, so
`S'_wave(tau) = -r(tau)S(tau) = -phi_E(1/2+i tau)`;
`((s-1/2)/(s+1/2))|_{s=i tau} = (tau+i/2)/(tau-i/2) = r(tau)^{-1}`, so
`S''_wave(tau) = -r(tau)^{-1}S(tau)`. Both as the prover states.

So the prover's conclusion is correct and now locally verifiable: **the notebook's `S` is the
reduced causal factor `S_{c0}`, and "delete the cusp forms" does not specify it** — `H = H'_c` is
already the `E`-orthocomplement of the cusp eigenfunctions (`paper.txt:280–284`), and *both* the `D'` and the
`D''` systems live there, giving `-rS` and `-r^{-1}S`. The parallel to
`notes/reviews/cusp-graph-2026-09-20.md:100` (a radiation-space repair presented as a derivation) is
apt.

## T5(c) — VALID

The weight dictionary is correct: `(y^2 d_y^2 + 1/4)(y^{1/2}v(log y)) = y^{1/2}v_{XX}`, so the zero
mode propagates freely; multiplying by `y^{+-1/4}` shifts `tau` by `-+i/4`; `y^{1/4}g = y^{1/2}phi`
and `y^{-1/4}g = phi`; the membership table is exactly T1(b) and agrees with numerics-lane F11.

The explicit cyclic vector is correct and I checked the two Hardy facts it needs:
`(tau+i)^{-2} in H^2(C_+)` and is outer, so the inner factor of `hhat_Theta = Theta/(tau+i)^2` is
exactly `Theta`, and Beurling–Lax gives `closure span{U_t h_Theta: t>=0} = M^{-1}(Theta H^2_+)`. The
absolute convergence of the inverse transform is immediate from `|Theta|=1` on `R`. The prover's own
disclaimer — this uses the continued edge quotient and therefore does **not** rescue the literal
cyclic conjecture — is correct and necessary.

## T6(a) — VALID

`w_rho = a_rho + i/4` (check: `gamma/2 - i(sigma-1/2)/2 + i/4 = gamma/2 + i(1-sigma)/2`).
`M^{-1}k_{w}= -i y^{-(1-sigma)/2-i gamma/2}1_{y>1}` in the `du/(2pi)` normalisation; the numerics
lane found the forward constant `+i` (F12), which is the same statement. The prover's convention
note reconciling this with `04h:32` (`Fk_w = -i sqrt(2pi) e^{-i wbar x}`, boundary measure `du`) is
correct.

Completeness with jets: a vector of `K_Theta` orthogonal to all derivative kernels vanishes at each
zero to full multiplicity, so dividing by the pure Blaschke `Theta` of T2(d) leaves an `H^2` function
of the same boundary norm; the vector then lies in `Theta H^2 cap K_Theta = {0}`. Correct, and it is
exactly where T2(d) is load-bearing.

Necessity of simplicity for the short form: at a zero `w` of order `m>=2`, `Theta/(tau-w) in K_Theta`
is nonzero and orthogonal to every ordinary kernel. I verified all three parts numerically on a
synthetic double zero `w=7+i/4` (`scratch_ht_jordan.py`, D2/D4): the `j=0,1` kernels pair to
`1e-11…1e-12` against `B h` (and `8e-5` in the one case where the `1/u^2` tail truncation at
`|u|=4000` dominates, exactly the predicted `2/(2 pi . 4000)`), while the `j=2` kernel pairs to
`0.56` and `0.079` — not orthogonal, as it must not be; `<B/(tau-w), k_w> = 8e-5` (tail-limited
zero), `<B/(tau-w), Bh> = 1.2e-11`, and `||B/(tau-w)||^2 = 1.99992 != 0`.

Connes's threshold. `9811068:873–885` reads verbatim "the multiplicity of `\rho` in `{\rm Sp}\,D` is
equal to the largest integer `n < {1+\d\over 2}`, `n\leq` multiplicity of `{1\over2}+\rho` as a zero
of `L`". So `n_delta(m)=max{n<=m : 2n-1<delta}` and full multiplicity needs `delta>2m-1`: the
prover's correction of the brief's "every `delta>1`" is **correct**. The non-unitarity remark is
`:761–765` verbatim ("because of the weight ... this representation is {\it not} unitary").
The dual-norm convergence criterion `int |X|^{2j}(1+X^2)^{-delta/2}dX < infinity` iff `delta>2j+1`
is right and reproduces the same threshold.

## T6(b) — VALID

Minimality with jets (biorthogonal system built from `Theta/(tau-w)^l`, `1<=l<=m`, triangular in the
derivative evaluations) is correct, and I confirmed `Theta/(tau-w)^l in K_Theta` numerically at a
synthetic double zero.

The Riesz failure. Two ingredients, both correct:

1. the overlap formula. With `<k_w,k_v>=i/(v-conj w)` and `||k_w||^2=1/(2 Im w)` (the `04h`
   convention, confirmed by numerics-lane F9/D67), normalised kernels at common height `b` and
   ordinates `a, a'` overlap by `2b/sqrt((a-a')^2+4b^2)`. Check D5 at `b=1/4`:

   ```
   gap  10     2.5      1        0.25     0.05     0.005
   ovl  0.0499 0.19612  0.44721  0.89443  0.99504  0.99995
   ```

   matching the closed form to 10 digits at each gap, with an independent quadrature of the inner
   product agreeing to `2e-5` (tail-limited). `-> 1` as the gap `-> 0`.
2. **the counting-function alternative**, which is the prover's own argument and is the reason this
   item is not merely a numerical hunch. If multiplicities are bounded and distinct ordinates have a
   uniform gap, `N(T)=O(T)`, so by parts `sum m_a log(1+v^2/a^2)=O(v)`; but under RH
   `xi_crit(z)/xi_crit(0)=prod_{a>0}(1-z^2/a^2)^{m_a}` gives
   `log|xi_crit(iv)| = v log v + O(v)`. Contradiction. Check D7:

   ```
   v        log|xi_crit(iv)|     v log v
   10       6.93223518719        23.0258509299
   100      255.428869884        460.517018599
   1000     4776.43985763        6907.75527898
   10000    70673.5488591        92103.4037198
   ratio log|xi_crit(iv)|/(v log v) at v=1e5 : 0.81372978
   ```

   `0.8137 = 1 - (1+log pi)/log(10^5)` to three digits, i.e. exactly `v log v - v(1+log pi) + ...`:
   super-linear, incompatible with `O(v)`. The dichotomy is therefore genuine.
   In the unbounded-multiplicity branch, consecutive derivative kernels overlap by
   `Gamma(2j+2)/sqrt(Gamma(2j+1)Gamma(2j+3)) = sqrt((2j+1)/(2j+2)) -> 1`; check D6 verifies the
   gamma identity **and** a direct physical-space quadrature at `b=1/4` for `j=0..5`
   (`0.70710678, 0.86602540, 0.91287093, 0.93541435, 0.94868330, 0.95742711`, all three routes
   identical). Both branches give the obstruction.

This correctly **upgrades** the brief's claim (which the numerics lane, in F10, downgraded to
"suggestive only" because `N=30` cannot see the spacing) to a proof, at the cost of RH. The
labelling PROVED-CONDITIONAL (RH) is the right one: `xi_crit` having only real zeros is used.

The remarks on `thm:arithmetic-metric` and `thm:flag-spectrum-attainable` are correct and I checked
them against the notebook: `04h` `lem:exit-one-dimensional` does give a one-dimensional exit
`j rho = (F rho)(0)` with `j k_w = -i sqrt(2pi) != 0` for every zero, so "one kernel per zero" is not
"one exit per zero"; `04l:171–175` does say the arithmetic defect "has rank \emph{four}" and needs
"at least four exit coordinates". The continuous-analogue computation
(`A=-1/4-iH_0`, `-(A+A^*) = (1/2)I`, not rank one) is correct.

## T6(c) — VALID

Every Burnol citation checks out verbatim: `:305–306` (the cosine normalisation
`\cF_+(f)(u) = 2\int_0^\infty \cos(2\pi tu)f(t)\,dt`), `:413–428` (`L_a` "constant in `(0,a)`" with
its cosine transform again constant; `K_a` "vanishing identically"), `:417–423` (the strictly
decreasing chain), `:437–443` ("is an entire function"), `:460–469` ("at most poles at `0` and at
`1`", plus `M(\cF_+(f))(s)=M(f)(1-s)`), `:472–480` (the bilinear evaluators `Y^a_{w,k}`),
`:516–525` (Theorem A: minimal iff `a<=1`, complete iff `a>=1`), `:528–538` (Theorem A': not
minimal at `a=1`, minimal and complete in `K_1` after omitting **2**), `:587–594` (Theorem B),
`:964–984` (the grouped-block convergence convention), `:989–998` (Theorem `main` restricted to the
dense subspace "of functions with quick decrease in vertical strips"). The prover's two caveats
against the brief's residue-series claim are therefore exactly right.

**The two-cutoff covariance.** I re-derived it and checked it numerically (`scratch_ht_sonine.py`):
`F_+ D_lambda f (u) = lambda^{1/2}(F_+f)(lambda u) = D_{1/lambda}F_+f(u)` — check F1, on
`f = 1_{[1,2]}` at 5 values of `u` and `lambda in {1.7, 0.6, 3.0, 0.25}`, max relative discrepancy
`2.2e-24`, `5.9e-6`, `1.6e-5`, `6.5e-26` (the `1e-5`-level entries are quadrature at points where
`(F_+f)(lambda u)` nearly vanishes). Hence `D_lambda L_{a,b}=L_{lambda a, b/lambda}`: one cutoff up,
the other down, product invariant. And `U_t R = R D_{e^{t/2}}` exactly (check F3, error `0.0` at
`t=0.8` and `t=-1.3`), giving `U_t R L_{a,b} = R L_{a e^{t/2}, b e^{-t/2}}`. **The brief's
`U_t L_a = L_{a e^{+-t}}` is false and the prover's law is the right one.** I also verified the
convention statements the transcription rests on: Burnol's functional equation
`M(\cF_+f)(s)=M(f)(1-s)` with `M(f)(s)=pi^{-s/2}Gamma(s/2)int f(t)t^{-s}dt` reproduces to
`<=5e-7` at three `s` (check F4), and `(Rf)^hat(tau)=sqrt2 F_R(1/2-2i tau)` to `1.5e-26` (check F5).

## H-THETA-EDGE — VALID (register as a theorem)

Statement: `Theta(tau) = [ghat(tau-i/4)/ghat(tau+i/4)](tau-i/2)/(tau+i/2)` is a pure Blaschke inner
function with zero divisor `w_rho = gamma/2+i(1-sigma)/2`, the edge values being meromorphic
continuations, with the root-vector system of T6(a).

- **Is every step unconditional?** Yes. The quotient is algebra from `Lambda=4 xi/(s(s-1))` and
  `xi(2i tau)=xi(1-2i tau)`. The Blaschke property is T2(c)/T2(d), which use only: Hadamard order-1
  factorisation of `xi`, `xi'(1/2)=0` (from `xi(s)=xi(1-s)`), `xi(0)=1/2`, absolute convergence of
  `sum 1/|rho|^2`, and `Theta(iv) ~ sqrt(pi/v)`. No RH, no simplicity, no Cartwright class.
  T6(a)'s root-vector completeness uses T2(d) and nothing else. Confirmed.
- **Is it free of the brief's errors?** Yes. F1, F2, F3 are all confined to the Siegel-theta /
  Eisenstein part (T5(a)/(b)) and do not enter this statement; F5 (the `|ghat|` asymptotic) is not
  used; the `s=0 <-> +i/4`, `s=1 <-> -i/4` assignment (F4) is stated correctly.
- **What it does not claim.** The prover explicitly disclaims that `g` generates the outgoing
  subspace. Correct and essential, given T3(c).

## H-GL1-BAD — MINOR

The mathematics is right on all four axes the round asked about:

- **orientation**: corrected (Burnol's `\HH^2` is `|u|<=1`, the incoming half; the outgoing defect
  needs `J`). Verified.
- **`V` vs `A`**: the correction-ledger row "`A` is convolution by `a`; `V=I-A` is the unitary" is
  right — `0001013:275–281` defines `A` by the convolution kernel `a(w)=sqrt|w| 1_{|w|<=1}`, and
  `:286–288` says `V=1-A` is unitary. The brief conflated them.
- **multiplicities**: a bad zero of multiplicity `m` gives a pole of order `2m` of `S=V^2B^{-2}` and
  contributes multiplicity `m` to the defect `K_bad`. Correct.
- **the 2-dimensional `K_{v_+^2}`**: correct; under RH `S=V^2` is a degree-2 Blaschke factor, not `1`.

**The defect (statement self-containedness).** As registered, "For `Delta = closure E(S_{<=1})` in the
units-invariant `Q` sector, `J V_y Delta = B_bad H^2_+`" does not name the unitary that carries
Burnol's `L^2(C_Q,d^*u)` (or `L^2(R_+,du)`) sector onto the notebook bond `H`, nor the variable
change defining `B_bad`. A reader of `report/` cannot check it. **Fix:** state in the claim
(i) the transport `R: L^2(R_+,dx) -> H`, `(Rf)(y)=2^{-1/2}y^{1/4}f(sqrt y)`, under which
`(Rf)^hat(tau)=sqrt2 F_L(1/2+2i tau)`; (ii) `V_y` = multiplication by `v_-(tau)=(tau+i/4)/(tau-i/4)`,
the transported `V=1-A`; (iii) `B_bad(tau)=B(1/2-2i tau)` with `B` Burnol's Blaschke product over
`{rho : Re rho>1/2}` in `Re s>1/2`. With those three lines the statement is self-contained and
checkable, and the verdict becomes VALID.

## H-THETA-1 — VALID (as an open problem)

- **Is `T_t^0` well defined on the span?** Yes. Burnol's Theorem A at `a=1` gives minimality of the
  `Y^1_{rho,k}`, hence linear independence of the `e_{rho,k}`, hence an unambiguous linear extension
  to the algebraic span. The prover says exactly this.
- **Is it a semigroup?** Yes, and I verified it. On each root block, `e_k <-> X^k` turns
  `T_t^0 e_k = e^{lambda t}sum_j binom(k,j)(t/2)^{k-j}e_j` into `X^k -> e^{lambda t}(X+t/2)^k`, i.e.
  `e^{lambda t}exp((t/2)d/dX)`, a one-parameter group. Check F6, `K=5`, `lambda=-1/4-7i`:
  `||T_t T_u - T_{t+u}|| = 6.4e-25` and `4.8e-26` at two `(t,u)`, and
  `||exp(0.7 A_0) - T_{0.7}|| = 1.1e-25` with `A_0 e_k = lambda e_k + (k/2)e_{k-1}` — so the stated
  generator is the right one.
- **Is `lambda_rho=(rhobar-1)/2` the right target?** Yes, and it is the `K_Theta` eigenvalue, not the
  `U_t` one. Check F6: at `gamma_1`, `lambda_rho = -0.25 - 7.067362571i = -i conj(w_{rho_1})`
  exactly, and likewise at `gamma_2`. Worth noting explicitly in the registered statement, because
  `U_t` itself acts on the evaluators at rate `(rhobar-1/2)/2` (I re-derived this from
  `M(D_lambda f)(s)=lambda^{1/2-s}M(f)(s)`), i.e. `e^{t/4}` faster: `T_t^0` is **not** a restriction
  of `U_t`, which is exactly why the existence question is nontrivial.
- **Is the Gram condition correctly derived?** Yes. For simple zeros, `A_0 e_{rho,0}=lambda_rho
  e_{rho,0}`, so `-<A_0 f,h>-<f,A_0h> = -(lambda_rho + conj(lambda_nu))G_{rho nu}`, and a scalar exit
  form forces this to equal `a_rho conj(a_nu)`: positive semidefinite of rank one. Correct. The
  weaker unconditional test (`<e_i,e_j> - <T_t^0e_i,T_t^0e_j> >= 0` for all `t>=0`) is the right
  primary falsification. The contractivity-implies-strong-continuity-and-stability remark is fine:
  `Re lambda_rho = (sigma-1)/2 < 0` for every nontrivial zero.
- **Well-posedness as an open problem:** yes. It names the Hilbert norm (Burnol's), forbids
  rescuing by an unspecified renorming, distinguishes itself from `thm:arithmetic-metric`'s
  multi-exit proposal, and states a concrete first falsification test. Status OPEN is right.

---

## Findings

**Finding 1 (MAJOR, now RESOLVED).** *External source not in `refs/`.* As written, `astra-proofs.md`
T5(b) rests its entire wave-scattering identification on Uetake 2007 cited only as a bare
`impan.pl` URL with page numbers — exactly the violation recorded as finding 5 of
`notes/reviews/cusp-graph-2026-09-20.md` (a PDF "not in `refs/`, not fetched, not hashed, not
byte-checkable"). Under the notebook's source convention this made T5(b)'s status line
("the wave-space interpretation is the cited primary-source result") unverifiable, and the round
would have had to register T5(b) as `sketched` or `cited-unverified`. **Resolved during this review:**
the coordinator placed the paper at `refs/src/uetake-2007/paper.{pdf,txt}` with the hashes recorded
in the header above, and I checked all six claims line by line (table in the T5(b) section). Every
one is correct, including the two page references (`p. 100` for `sigma(B'')\sigma(A_c)={-1/2}`;
`pp. 104–105, 110–111, 116` for `D'/D''` and the two scattering matrices). **T5(b) can now be
registered `proved`.** Standing recommendation: the prover lane must not cite a source by URL; the
source must be fetched, hashed, and byte-quoted before the claim is written.

**Finding 2 (MAJOR, registration action).** *`conj:h-theta` must be re-statused in the shard.*
`report/sections/04p_gl1_bond.tex:103–109` carries `\claimstatus{conj:h-theta}{open}`. T3(d) refutes
it for the only readings in which it is defined, and this review confirms that refutation. Leaving
the status `open` after registering T3(d) would put the report in contradiction with its own ledger.
The shard must either retire the conjecture (status `refuted`, with the one-sentence reason: `phi`
is not in the bond, and its `L^2` repairs have dense forward orbit and outer outgoing half) or
replace it with H-THETA-EDGE. This is a report edit, not a defect of the prover lane; I have not
made it (this review edits no file but its own).

**Finding 3 (MINOR).** *T2(c): the paired Hadamard product silently assumes `xi` has no real zeros.*
Fix stated in the T2(c) section (one line: `zeta(sigma)<0` on `(0,1)`); the conclusion `b=0` is
unaffected either way because a real zero would pair with `1-rho`.

**Finding 4 (MINOR).** *T4(a): `S=V^2B^{-2}` is cited to `:694–699`, where the display is
`S=ZV^2B^{-2}`.* `Z=1` in the number-field case (`0001013:683–685`), so nothing is wrong for `K=Q`,
but the citation must carry that qualifier. Fix in the T4(a) section.

**Finding 5 (MINOR).** *T4(a): `:489–495` is Burnol's Euclidean variation, not the adelic sector
statement.* The inference is legitimate via the prover's own `E_B = 2 sqrt x E_0` (T1(c)), which
holds for every even Schwartz `f_infty` and not only the Gaussian, but the step must be named. Fix
in the T4(a) section.

**Finding 6 (MINOR).** *T3(d): the refutation does not cover the unweighted outgoing half.*
`phi.1_{y>1}` **is** in `L^2(1,infty;d^x y)` even though `phi notin H`, and a defender would offer
it. T3(c) already settles it (`Lambda_>(2i tau)`, argument in `Re s<0<1`, zero-free, outer, defect
`0`), but the two lines must be printed so the refutation is visibly exhaustive over the defined
readings. Fix in the T3(d) section.

**Finding 7 (MINOR).** *H-GL1-BAD is not self-contained as a registered claim.* Three lines (the
transport `R`, the multiplier `v_-`, the variable change in `B_bad`) are needed. Fix in the
H-GL1-BAD section.

**Finding 8 (MINOR, sharpening — not a defect).** *The T3(c) bound is not sharp.* The proved bound is
`3-2 theta(1)=0.827130378`; the grid infimum of `Re[-(1-s)Lambda_>(s)/2]` over 112 points is
`0.913565202`, i.e. `2-theta(1)=p(0)`, approached as `|s| -> infinity` in `Re s<1` and never
attained. The stronger bound `Re[-(1-s)Lambda_>(s)/2] >= 2-theta(1)` would follow if
`Re int_0^inf p'(X)e^{-zX}dX >= 0` for all `Re z>0`; that is not implied by `p'>=0` in general and
was not proved here. Registering the weaker, proved bound is correct; the sharper one is a possible
small follow-up.

**Finding 9 (MINOR, bookkeeping).** *T6(a)'s `+i` vs `-i` and the `du` vs `du/(2pi)` measure.* The
prover's inverse-transform constant `-i` and the numerics lane's forward constant `+i` (F12) are the
same statement, and both differ from `04h:32`'s `-i sqrt(2pi)` only by the boundary measure. The
prover says so; I confirm it; but the shard `04h` and any registered claim must fix one measure
convention explicitly, or a later reader will re-derive the discrepancy a third time.

**Finding 10 (MINOR, scope).** *T4(a)–(c) are transported citations, not new theorems.* Burnol's
causality criterion (`:268–270`), closure criterion (`:290–293`), exact closure identity (`:639`) and
scattering operator (`:509–514`) are his; the prover's contribution is the transport into the
notebook's conventions plus the orientation/saturation corrections. The registered claims must
attribute them, as `cusp-graph-2026-09-20.md` finding 1 required of the previous round. The prover's
text does cite them at every point; the risk is only in how the shard will print them.

---

## Recommended statuses for registration (rk-light sense)

`proved` requires independent review; this review supplies it for the items marked below.

| item | status | conditional on / note |
|---|---|---|
| T1(a), T1(b), T1(c) | `proved` | elementary; re-derived here |
| T2(a), T2(b) | `proved` | the corrected Stirling statement, not the brief's fixed-`C,c` form |
| T2(c) | `proved` | after printing the no-real-zeros line (Finding 3) |
| T2(d) | `proved` | unconditional; no RH, no Cartwright |
| T3(a), T3(b) | `proved` | |
| T3(c) | `proved` | **the headline theorem**: `Lambda_>` zero-free on `Re s<1`, `ghat_>` outer |
| T3(d) | `proved` (as a refutation) | and `conj:h-theta` re-statused `refuted` in `04p` (Finding 2) |
| T4(a) | `proved` | transport of Burnol; attribute (Finding 10); citation qualifiers (Findings 4, 5) |
| T4(b) | `proved` | |
| T4(c) | `proved` | Burnol's `S=V^2B^{-2}` (`:509–514`), transported |
| T4(d) | `proved` for the spectral-density statement and the source comparisons; `open` for the ring-norm/Weil trace identification | the double poles at `+-i/4` are a real obstruction, not a gap in exposition |
| T5(a) | `proved` | |
| T5(b) | `proved` | for the theta/Eisenstein identities *and*, now that `refs/src/uetake-2007/` is local and byte-checked, for the wave-space identification (Finding 1) |
| T5(c) | `proved` | |
| T6(a) | `proved` | |
| T6(b) | `proved` for completeness+minimality with jets (unconditional); `proved-conditional` **on RH** for the failure of the Riesz-basis property | RH enters through `xi_crit` having only real zeros |
| T6(c) | `proved` for the transcription and the two-cutoff covariance law; `open` for the Sonine dynamics | |
| H-THETA-EDGE | `proved` | register as a theorem, replacing `conj:h-theta` |
| H-GL1-BAD | `proved` | after the three self-containedness lines (Finding 7) |
| H-THETA-1 | `open` | well-posed; the finite Gram/contractivity tests are the falsification route |

Nothing in this round earns `assumed`. Nothing should be registered as `sketched`: every item is
either proved (here re-checked) or explicitly open.

---

## Independent checks

All scripts written by this lane, independent of `scripts/h_theta.py`; mpmath at 25–30 digits;
kept in the repo. Zero-cache side file: `notes/reviews/.ht_zeros_cache.json`.

| script | checks | what it settles |
|---|---|---|
| `notes/reviews/scratch_ht_outer.py` | A1 (2 routes to `Lambda_>` at 5 `s`), A2 (IBP identity at 3 `s` + `int p'`), A3 (**112-point grid**), A4 (7 limits), A5 (min `|Lambda_>|`) | **T3(c)**: the outer-ness bound `Re[-(1-s)Lambda_>(s)/2] >= 3-2 theta(1)` holds at all 112 grid points, `Re s` from `-200` to `1-1e-7`, `|Im s|` up to `10^4`; infimum `0.91356520208675`; 0 violations |
| `notes/reviews/scratch_ht_hadamard.py` | B1 (5), B2 (2), B3 (6 `s`), B4 (`b`), B5 (3 synthetic quadruples), B6 (4 `s`), B7 | **T2(c)/T2(d)**: `xi'(1/2)=-2.3e-41`, `b=-4.6e-41`, `L(s)/(s^2-s)` constant to 7 digits `=0.00054247` against an independent tail estimate `0.0005426`, off-line cancellation exact, product value `rel 2.6e-7` at `s=2` |
| `notes/reviews/scratch_ht_theta.py` | C1 (7 `v`), C2 (7 `tau` + 4 removable values), C3 (Blaschke, `|Theta|` on `R` and in `C_+`, 3 zeros), C4 (13 decimals), C5 (4), C6 (5 `u`), C7 (5 `u`) | **T2(a)–(d), H-THETA-EDGE**: `Theta(iv)sqrt(v/pi) -> 1` (`0.99999125` at `v=10^5`), edge quotient to `1.2e-30`, residues `+-i`, Stirling constant `3.76558505511`, `u ghat_>(u) -> -0.913565i`; reproduces every decimal in the prover's list |
| `notes/reviews/scratch_ht_jordan.py` | D1 (3 `j`), D2 (6 pairings), D3 (3 `t` x 3 `X` x 3 `j`), D4 (3), D5 (6 gaps), D6 (6 `j` x 3 routes), D7 (5 `v`) | **T6(a)/T6(b)**: Jordan rule exact to `8e-30`; `j=0,1` kernels in `K_B` and `j=2` not; `Theta/(tau-w)` nonzero and `_|_ k_w`; overlap formula to 10 digits; `sqrt((2j+1)/(2j+2))` by three routes; `log|xi_crit(iv)|/(v log v) -> 0.8137` |
| `notes/reviews/scratch_ht_burnol.py` | E1 (6), E2 (4), E3 (3), E4 (3 + 6 lines + 6 norms), E5 (3) | **T4(a)**: Burnol's `A(u)` identity to `9.2e-11`, Nyman's `rho_alpha` identity, `V` as `(s-1)/s` to `2e-26`, `V(1/u)=0`, and the Hardy test showing the bond image of a function supported in `(0,1)` is `H^2(C_-)` — the **incoming** half |
| `notes/reviews/scratch_ht_sonine.py` | F0 (3), F1 (4 `lambda` x 5 `u`), F2 (2 `lambda`), F3 (2 `t` x 4 `y`), F4 (3 `s`), F5 (2 `tau`), F6 (2 semigroup + 1 generator + 2 `lambda_rho`) | **T6(c), H-THETA-1**: `F_+D_lambda = D_{1/lambda}F_+` to `2e-24`; `U_tR=RD_{e^{t/2}}` exactly; Burnol's `M(F_+f)(s)=M(f)(1-s)` to `5e-7`; `T_t^0` is a semigroup with the stated generator to `1e-25`; `lambda_rho=-i conj(w_rho)` exactly |

Count of independent numerical assertions executed by this lane: **A 5 + B 7 + C 7 + D 7 + E 5 + F 7
= 38 check groups, 169 individual evaluations**, of which **0 contradict `astra-proofs.md`**.
Two entries are quadrature-limited and flagged in place (the `8e-5` pairing in D2 whose `1/u^2` tail
truncation at `|u|=4000` predicts exactly `2/(2 pi . 4000)`; the `rel=1` entry in F0 at `u=5` where
both sides are zero to `5e-27`).

No check contradicted the prover. Two checks contradicted the *brief* and reproduce the numerics
lane's F1/F2 and F10 independently (the Siegel-theta factor and involution; the inadequacy of a
small-`N` Gram matrix as Riesz evidence — settled here by D7 instead).

---

## Tally

- items reviewed: **24** (T1(a)–T6(c) = 21, plus H-THETA-EDGE, H-GL1-BAD, H-THETA-1)
- **20 VALID / 4 MINOR / 0 INVALID**
- MINOR: T2(c), T3(d), T4(a), H-GL1-BAD — all four fixes are one to three printed lines and none
  changes a conclusion
- findings: **10** — 2 MAJOR (one resolved during the review, one a registration action on `04p`),
  8 MINOR
- byte-citations checked: **all 43 line-spans cited in `astra-proofs.md`** (17 in `math/0001013`,
  5 in `math/9811068`, 11 in `math/0203120`, 4 in `math/0311468`, 6 in the report shards and the
  previous review), **plus 7 spans of the newly local `refs/src/uetake-2007/paper.txt`** — all 50
  present and saying what the prover claims, 2 needing a qualifier (Findings 4, 5),
  **0 fabricated, 0 misquoted**
- independent numerical checks run by this lane: **38 check groups / 169 evaluations**, 6 scratch
  scripts, all retained
- correction-ledger rows singled out for attack (`ghat_>` outer; `Theta` pure Blaschke via paired
  Hadamard with `b=0`; `M=H` and the reflected defect; `S=V^2B^{-2}` with a 2-dimensional causal
  model under RH; residues `+i` at `+i/4`, `-i` at `-i/4`; the involution `t -> t^{-1}F(1/t)`; the
  Riesz failure via the counting function; Connes's `n<(1+delta)/2`; the two-cutoff covariance):
  **9 attacked, 9 stand**

---

## Verdict lines

*(First pass, before the fixes. The four lines `VERDICT T2c`, `VERDICT T3d`, `VERDICT T4a`
and `VERDICT HGL1-BAD` below are **superseded** by the block at the very end of this file,
"Re-verdicts after the fixes (2026-09-21)", where all four read VALID. A gate that greps
these lines should take the LAST occurrence of each alias.)*

VERDICT T1a: VALID
VERDICT T1b: VALID
VERDICT T1c: VALID
VERDICT T2a: VALID
VERDICT T2b: VALID
VERDICT T2c: MINOR
VERDICT T2d: VALID
VERDICT T3a: VALID
VERDICT T3b: VALID
VERDICT T3c: VALID
VERDICT T3d: MINOR
VERDICT T4a: MINOR
VERDICT T4b: VALID
VERDICT T4c: VALID
VERDICT T4d: VALID
VERDICT T5a: VALID
VERDICT T5b: VALID
VERDICT T5c: VALID
VERDICT T6a: VALID
VERDICT T6b: VALID
VERDICT T6c: VALID
VERDICT HTHETA-EDGE: VALID
VERDICT HGL1-BAD: MINOR
VERDICT HTHETA-1: VALID

---

## Re-verdicts after the fixes (2026-09-21)

Registration read of `report/sections/04q_h_theta.tex` and `report/sections/04r_h_theta_channels.tex`
(every claim environment there, plus `report/sections/04p_gl1_bond.tex:103-109`). Independent
re-checks in `notes/reviews/scratch_ht_reverdict.py` (checks G1-G4).

**The four fixes are all present and all adequate.**

**T2(c) — `thm:symbol-edge-quotient` (b).** The shard now prints, before the paired Hadamard
product: "`\cxi` has no real zeros (`\zeta(\sigs)<0` and `\sigs(\sigs-1)<0` on `(0,1)`, so
`\cxi(\sigs)>0` there; none outside the strip), so its zeros pair off as `\zero,\bar\zero`". That is
exactly the missing line and it is correct: on `(0,1)`, `\zeta(\sigma)<0`, `\sigma(\sigma-1)<0`,
`\pi^{-\sigma/2}>0`, `\Gamma(\sigma/2)>0`, so `\xi(\sigma)>0`; `\xi(0)=\xi(1)=1/2`; and every
nontrivial zero lies in the closed strip, so there is no real zero anywhere. Check G1: `\xi(\sigma)`
over 999 points of `(0,1)` has minimum `0.497120778188` at `\sigma=1/2` (`\xi` is real there to
machine zero), with the sign pattern printed for nine `\sigma`. **VALID.**

**T3(d) — `thm:theta-vector-cyclic` (d).** The shard now prints: "The unweighted outgoing half
`\phi\,1_{(1,\infty)}`, which is in `L^2(1,\infty)` although `\phi\notin H`, has transform
`{\thetaM}_{>}(2i\tauM)` with argument in `\re s<0<1`, zero-free by (c): it is outer too, with
defect zero." Correct on all four counts, and it is the reading a defender would have offered next.
Check G2: `\|\phi 1_{y>1}\|^2_{L^2(1,\infty;d^\times y)} = 0.961669432018` (finite), against
`\int_{e^{-X}}^1|\phi|^2 d^\times y = 9.9615524, 19.961552, 39.961552` at `X=10,20,40` (linear in
`X`, so `\phi\notin H`, as the same sentence says); the direct quadrature
`\int_1^\infty\phi\,y^{i\tauM}d^\times y` reproduces `\Lambda_>(2i\tauM)` to rel `6.2e-7`,
`3.9e-12`, `1.2e-18` at three `\tauM`; and `|\Lambda_>(2i\tauM)| = 0.30308172, 0.38490528,
0.091726993` at three points of `\mathbb C_+` (nonzero, as (c) forces). **VALID.**

**T4(a) and H-GL1-BAD — `thm:gl1-bad-zero-defect`.** All three self-containedness items requested in
Finding 7, plus both citation qualifiers of Findings 4 and 5, are now printed in the claim: the
transport `R`, `(Rf)(y)=2^{-1/2}y^{1/4}f(\sqrt y)`; the Euclidean-to-adelic step
"`E(f) = 2\sqrt x\,E_0(f)` for every even `f_\infty`"; `V_y` as multiplication by `v_-` with
`v_\pm(\tauM) = (\tauM\mp i/4)/(\tauM\pm i/4)`; `B_-(\tauM) = B(\tfrac12+2i\tauM)` and
`B_{\mathrm{bad}}(\tauM) = B(\tfrac12-2i\tauM)`; and the qualifier "his `S = ZV^2B^{-2}` has `Z = 1`
for a number field", which matches `math/0001013:main.tex:683-685` ("just `1` in the number field
case") and `:694-695`. The statement is now checkable end-to-end by a reader of `report/`.
**VALID** for both T4(a) and H-GL1-BAD. One residual wording item in a parenthetical is Finding 11
below; it changes no conclusion.

**Finding 9 — `thm:model-space-jets`.** The shard now opens "With the boundary measure
`d\tauM/2\pi` fixed here (`\cref{sec:rebound-state}` uses `d\tauM`, whence its constant
`-i\sqrt{2\pi}`), the kernel `k_{w_\zero}` is `-i\,y^{-(1-\sigs)/2-i\gam{}/2}1_{y>1}`". Exactly the
requested fix; the two conventions are now reconciled in print. **Adequate.**

**Finding 2 — resolved.** `report/sections/04p_gl1_bond.tex:105` now reads
`\claimstatus{conj:h-theta}{refuted}`, with the one-sentence reason and forward references to
`thm:theta-vector-cyclic`, `thm:symbol-edge-quotient` and `thm:gl1-bad-zero-defect` printed inside
the conjecture environment. The report is no longer in contradiction with its own ledger.

### Registration read of the other proved claims

I compared each claim environment against `astra-proofs.md`. **No claim overstates the prover
lane.** Item by item:

- **`prop:theta-vector-weight`** (a)(b)(c) = T1(a),(b),(c) verbatim in content. (c)'s "Connes's
  unsubtracted `E` is stated on test functions with `f(0)=\widehat f(0)=0`" is exactly Connes's
  `\Sc(A)_0 = \{f : f(0)=0, \int f\,dx = 0\}` at `math/9811068:main.tex:687-688` (with
  `\int f\,dx = \widehat f(0)`), and the Gaussian has both equal to `1`. Faithful.
- **`prop:theta-vector-mellin`** = T2(a),(b) plus the Szego dichotomy, including the sharp form
  `\int\log|\widehat\gth|\,du/(1+u^2) = -\infty` (not merely "not in `L^1`"), which is what the
  prover proves. Faithful.
- **`thm:theta-vector-cyclic`** = T3(a)-(d). See Finding 13 for one wording item. Faithful in
  content.
- **`prop:theta-spectral-density`** = T4(d), including the explicit *negative*: "No identification
  of `C` with the ring-norm trace of `\cref{prop:ringnorm-trace}` is established ... `\widehat\gth^2`
  has double poles at the Weil evaluation points `\pm i/4`". The `open` half of T4(d) is printed as
  a disclaimer inside a `proved` claim rather than as a separate `open` claim; that is a legitimate
  packaging and loses nothing. Faithful, and notably not overstated.
- **`prop:siegel-constant-term`** = T5(a),(b), with both regions of convergence (`\re s>1/2`,
  `\re s>1`) printed — the numerics lane's F3. See Finding 14 for one omitted half-plane. Faithful.
- **`prop:outgoing-generator`** = T5(c). The shift direction is right (`y^{\pm1/4}` shifts `\tauM`
  by `\mp i/4`), the membership parenthetical is right, and "no Jacobi-theta closed form for
  `h_\Theta` is claimed" preserves the prover's own disclaimer. Faithful.
- **`thm:model-space-jets`** = T6(a): the span statement is printed as unconditional and the
  damping dictionary is scoped inside "Under `\cref{asm:rh}`", so the `proved` status is correct;
  Connes's threshold is printed as `n<(1+\delta)/2`. Faithful.
- **`prop:kernels-not-riesz`** (`proved-conditional` on `asm:rh`) = T6(b). The printed overlap
  `\tfrac12/\sqrt{(a-a')^2+\tfrac14}` is the general `2b/\sqrt{(a-a')^2+4b^2}` at `b=1/4`: check G4
  confirms the two forms and a direct inner-product evaluation agree to all digits at four gaps
  (`0.1240347346, 0.4472135955, 0.894427191, 0.9992009587`). Both branches of the multiplicity
  dichotomy are printed. Part (a) is unconditional but rides under `proved-conditional`; that
  *understates* it, and the unconditional content is anyway carried by `thm:model-space-jets`.
  Faithful.
- **`prop:sonine-two-cutoff-covariance`** = T6(c)'s covariance law verbatim, with `L_a = L_{a,a}`,
  `\mathcal F_+D_c = D_{c^{-1}}\mathcal F_+`, `D_cL_{a,b} = L_{ca,b/c}` and
  `U_\tsg RL_{a,b} = RL_{ae^{\tsg/2},be^{-\tsg/2}}`, all four of which I verified numerically in the
  first pass (check F). Faithful.
- **`conj:h-theta-1`** = H-THETA-1, including `\mathrm d_\zero = (\bar\zero-1)/2`, the Jordan
  semigroup, the scalar-exit form, the rank-one Gram condition, the unconditional contractivity
  test, and the ban on rescuing by a change of norm. `open`. Faithful.
- **`citedfact`s.** All quotes re-checked at the printed line numbers:
  `math/9811068:main.tex:2549-2554` and `:873-878`; `math/0001013:main.tex:204-207`, `:218-219`,
  `:268-270`, `:291-293`, `:639`, `:509-511`; `math/0203120:main.tex:516-520`;
  `math/0311468:main.tex:213-214` and `:234-235`; `uetake-2007:paper.txt:15-16`, `:617`, `:676-682`.
  All present and correctly quoted.
- **Shard-header facts.** "a `41`-row correction ledger" is exact (the table has 41 data rows);
  "`98` checks, four errors of the brief found" matches the numerics lane (F1, F2, F3 errors plus
  F5 false-as-printed).

### New findings from the registration read

**Finding 11 (MINOR, wording in `thm:gl1-bad-zero-defect`).** The parenthetical
"`v_\pm(\tauM) := (\tauM\mp i/4)/(\tauM\pm i/4)` the pole factor (Burnol's multiplier `(s-1)/s` of
`V = 1-A`, read at `s = \tfrac12\pm2i\tauM`)" pairs the signs the wrong way round: check G3 shows
`(s-1)/s` at `s = \tfrac12+2i\tauM` is `(\tauM+i/4)/(\tauM-i/4) = v_-(\tauM)`, and at
`s = \tfrac12-2i\tauM` it is `v_+(\tauM)` (three `\tauM`, difference `0.0` in each case). Nothing
downstream is affected, because `v_\pm` are defined by explicit formula and the operative statements
(`V_y` = multiplication by `v_-`; `V_y\mathcal N_{\le1} = B_-H^2(\mathbb C_-)`;
`B_-(\tauM) = B(\tfrac12+2i\tauM)`; scattering `v_+^2B_{\mathrm{bad}}^{-2}`) are all correct and
mutually consistent. **Fix:** print "read at `s = \tfrac12\mp2i\tauM`", or drop the `\pm` and write
"`v_-` is `(s-1)/s` at `s = \tfrac12+2i\tauM`, `v_+` at `s = \tfrac12-2i\tauM`".

**Finding 12 (MINOR, incomplete justification in `thm:symbol-edge-quotient` (b)).** The shard
justifies the Hadamard normalisation with only half the argument: "(the linear exponent vanishes
because `\cxi'(\tfrac12) = 0`)". The full step also needs the paired logarithmic derivative to
vanish: `P'(\tfrac12)/P(\tfrac12) = -\sum 2(\sigs-\tfrac12)/|\zero-\tfrac12|^2 = 0`, absolutely
convergent and cancelling under `\zero\mapsto1-\bar\zero`. `astra-proofs.md` T2(c) prints both; the
shard prints one. This is a compression, not an overstatement, but the printed hint is not
sufficient on its own. **Fix:** "(the linear exponent vanishes because `\cxi'(\tfrac12) = 0` and the
paired logarithmic derivative `\sum 2(\sigs-\tfrac12)/|\zero-\tfrac12|^2` at `\tfrac12` cancels under
`\zero\mapsto1-\bar\zero`)".

**Finding 13 (MINOR, wording in `thm:theta-vector-cyclic` (d)).** The shard says
`\cref{conj:h-theta}` is "refuted in every cyclic-and-cut reading", whereas `astra-proofs.md` T3(d)
says "REFUTED ... under the *specified* literal/natural cyclic-and-cut readings" and explicitly
warns against bundling "other orders of projection and orbit closure ... into an undefined 'no
reading' assertion" (`astra-proofs.md:372,374`). The shard's colon-enumeration that follows
(cyclic-then-cut, cut-then-cyclic, every weight `0<a<\tfrac12`, the unweighted outgoing half,
`\phi\notin H`) does exhaust the readings in which the conjecture is defined, and the escape clause
("What can be built from theta is a vector with inner factor `\Theta` ..., which uses the continued
edge quotient as extra data") is printed, so the sentence is defensible as written. It is still one
word stronger than the prover's own scope. **Fix:** "refuted in each of the following
cyclic-and-cut readings, which exhaust those in which it is defined:".

**Finding 14 (MINOR, omitted half-plane in `prop:siegel-constant-term` (b)).** The display
`E^*(z,s) = \pi^{-s}\Gamma(s)\zeta(2s)E(z,s) = \tfrac12\int_0^\infty(\Theta_z(t)-1)t^s\,d^\times t`
carries no region, although the two channel displays immediately before it carry `\re s>1/2` and
`\re s>1`, and the integral converges only for `\re s>1` (`astra-proofs.md` T5(b) prints
"`(\Re s>1)`" on it). Given that the round's own numerics finding F3 is precisely about an
evaluation point outside the region, the region should be printed. **Fix:** add `(\re s>1)` to that
display.

**Finding 15 (MINOR, registration — an available upgrade, not a defect).**
`obs:symbol-is-reduced-causal-factor` is registered `sketched` with the note "Read from the local
text, not re-derived." This review *did* re-derive all three relations from
`refs/src/uetake-2007/paper.txt` by the single substitution `s = i\tauM`:
`S_{c0}(i\tauM) = \cxi(2i\tauM)/\cxi(-2i\tauM) = \Ssym(\tauM)` (`:655,:679`);
`S_{c1}(i\tauM) = (\tfrac12+i\tauM)/(\tfrac12-i\tauM) = -r(\tauM)`, so the projected-space matrix
(`:919-923`) is `-r\Ssym`; `((s-\tfrac12)/(s+\tfrac12))|_{s=i\tauM} = r(\tauM)^{-1}`, so the
intersected-space matrix (`:635-639`) is `-r^{-1}\Ssym`; and the extra eigenvalue is
`\sigma(B'')\setminus\sigma(A_c) = \{-\tfrac12\}` (`:86`). **Fix (optional):** print the
substitution `s = i\tauM` in the observation and the claim can carry `proved` (by citation plus a
one-line change of variable) rather than `sketched`.

### Updated tally

- **24 VALID / 0 MINOR / 0 INVALID** (was 20 / 4 / 0): T2(c), T3(d), T4(a) and H-GL1-BAD all
  re-verdicted VALID after the fixes.
- findings: **15** - 2 MAJOR (**both now resolved**: the Uetake source is local and byte-checked;
  `conj:h-theta` is re-statused `refuted` in `04p`), 13 MINOR, of which Findings 3-7 and 9 are
  applied and Findings 8 and 11-15 are outstanding (none blocks registration; Finding 8 is a
  sharpening and Finding 15 an upgrade).
- independent numerical checks by this lane: **42 check groups** (38 in the first pass plus G1-G4),
  7 scratch scripts, all retained; **0 contradict `astra-proofs.md` or the registered shards**.
- registration verdict: the shard statements of `prop:theta-vector-weight`,
  `prop:theta-vector-mellin`, `thm:theta-vector-cyclic`, `prop:theta-spectral-density`,
  `prop:siegel-constant-term`, `prop:outgoing-generator`, `thm:model-space-jets`,
  `prop:kernels-not-riesz` and `prop:sonine-two-cutoff-covariance` **do not overstate**
  `astra-proofs.md`; two of them (`prop:theta-spectral-density`, `prop:kernels-not-riesz`) are
  conservatively *under*-stated, and `obs:symbol-is-reduced-causal-factor` can be upgraded
  (Finding 15).

VERDICT T2c: VALID
VERDICT T3d: VALID
VERDICT T4a: VALID
VERDICT HGL1-BAD: VALID
