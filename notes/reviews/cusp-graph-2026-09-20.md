# REFUTE review: a finite graph with a cusp, its scattering, and the rebound channel

Reviewer: `claude:opus` (adversarial lane, two-model-family protocol).
Date: 2026-09-20.

## Inputs read (in full)

| file | what it is |
|---|---|
| `notes/cusp-graph/astra-brief.md` | the orchestrator's brief (196 lines, C1–C3, T1–T8, deliberately slightly too strong) |
| `notes/cusp-graph/astra-proofs.md` | prover output, `codex:gpt-6-astra` (810 lines; C1–C3, T1(a)–(d), T2(a)–(c), T3(a)–(e), T4, T5(a)–(c), T6(a)–(b), T7(a)–(d), T8; 34-row ledger; numerics; "what a refuter should attack first") |
| `notes/cusp-graph/numerics.md`, `scripts/cusp_graph.py`, `outputs/cusp_graph.txt` | the blind Opus numerics lane (ledger D1–D19, 569 checks) |
| `notes/cusp-graph/sources.md`, `notes/cusp-graph/childs-lead.md` | the two literature lanes, byte-verified |
| `refs/src/1103.5077/main.tex` | Childs–Strouse, *Levinson's theorem for graphs* (read directly, §§ scattering / bound / Levinson / degree) |
| `refs/src/1203.6557/levinson2.tex` | Childs–Gosset, *Levinson II* (the `n`-tail S-matrix and count) |
| `refs/src/2603.26443/final_draft.tex` | Arends–Peterson–Weich, *Resonances on geometrically finite graphs* (read directly, lines 62, 136–138, 856–870, 1896–2110) |
| `report/sections/04h_rebound_state.tex`, `04_riemann_channel.tex:1–140`, `08_quantum_ihara_general.tex`, `09c_selberg_tower_cusp.tex`, `02h_definitions_graded_ramanujan.tex` | the notebook rows the proofs cite |

## Scripts written by this lane (independent; nothing imported from `scripts/cusp_graph.py`)

| script | checks | covers |
|---|---|---|
| `notes/reviews/scratch_cg_scatter.py` | **1186** | C1 (ansatz solve, `R = -p/ptilde`, Childs's `-Q(1/z)/Q(z)`, FE, `|R|=1`) on 21 cores; C2 (Krylov/cusp split, gcd, threshold simplicity, bound states against a length-400 ray, and the count in **all three** forms); C3 (`q = 2,3,5,7`, `phi = 1/R`, the `pi i/log q` period, `xi` vs `Lambda` to 25 digits); **APW's `H(mu)` against `p(z)`** and their four examples; T2 (2.1)–(2.5), Catalan ray walks; T7 (both examples exactly, the product identity, self-reciprocity, `n=1` sweeps) |
| `notes/reviews/scratch_cg_model.py` | **1197** | T1(a) (`W^T E W = E`, forward/backward invariance, (1.1), `D_+^0 perp_E D_-^0`, the `B_1 = 0` obstruction); T1(c) (**the 3/32 number and its `q`-family**, isometry of the projected radiation spaces, `Theta = eta R B_bd`, `R H^2 cap H^2 = Theta H^2`, the model space, (1.3), (1.4)); **(1.2a) re-derived in closed form** and its Abel limit; T3–T6 on four graph-derived models |
| `notes/reviews/scratch_cg_probe.py` | **497** | T3(e) criterion (3.3) in both directions on 7 simple product eigenvalues; T5(b) with **arbitrary (full-rank) defect**; T7(c) (7.3)/(7.4)/companion criteria; APW's **multi-cusp** determinant; the `c = 1` delay modes; the discrete-time outgoing/incoming orientation |

**2880 checks, 0 failures.** Every number below is from these scripts unless labelled as the prover's or the numerics lane's.

## Citations byte-checked

**Notebook (15 ids, 15 verified).** `obs:model-modes-sign` (`04h:36`), `def:rebound-renewal-generator` (`04h:62`), `thm:rebound-renewal` (`04h:78`), `prop:infinite-mean-rebound` (`04h:101`), `prop:odd-modes-protected` (`04h:118`), `prop:graded-stationary-segment` (`04h:129`), `prop:rebound-persistence-secular` (`04h:143`), `prop:mode-diagonal-rebound-rh` (`04h:160`), the Gram-spectrum line `04h:171`, `thm:rebound-admissibility` (`04h:180`), `thm:flag-spectrum-attainable` (`04h:195`), `prop:functional-model-modes` (`04_riemann_channel.tex:68`), `cor:qihara-unitary` (`08:100`), `prop:cusp-prime-comb` (`09c:78`), `def:graded-rh-fe-ramanujan` (`02h:60`). All resolve to the named environment on the named line. **One off-by-two:** C3 cites `04_riemann_channel.tex:27` for the entire `xi`; line 27 is the sentence "With the completed zeta", the display `\cxi(s) = \tfrac12 s(s-1)\pi^{-s/2}\Gamma(s/2)\zeta(s)` is at line 29. The algebra is right: I verified `xi(2s-1)/xi(2s) = ((s-1)/s) Lambda(2s-1)/Lambda(2s)` to 25 digits.

**External.** The proofs file contains **exactly one** external citation, a bare web URL (Kurasov, *Scattering from an Impurity*, `staff.math.su.se/.../LAXFINAL.pdf`). It is not in `refs/`, not fetched, not hashed, not byte-checkable. `grep -n "2603\|1103\|1203\|Arends\|Childs\|Peterson\|Weich"` over `astra-proofs.md` returns **nothing**. See finding 1.

**Prior-art quotes I re-verified myself with `grep -n -F`.** `1103.5077/main.tex`: `:181` (`R(e^{ik}) = -Q(e^{-ik})/Q(e^{ik})`), `:185` (`Q = 1 - e^{ik}(a+C)`), `:186` (`C = b^dag(2cos k - D)^{-1}b`), `:232` (half-bound states), `:242` (confined bound states + the degenerate count), `:277` (Levinson), `:303` (no non-real root in the closed disc), `:365` (degree `2m - 2n_c`). `1203.6557/levinson2.tex`: `:365` (`S(z) = -Q(z)^{-1}Q(1/z)`), `:511` (matrix Levinson). `2603.26443/final_draft.tex`: `:1907` (the resonance matrix), `:1919` (`<= 2|L|` resonances), `:1977` (modular curve), `:2008` and `:2056` (the two elliptic determinants), `:2014`, `:2058` (which roots are `l^2`), `:2092`, `:2093` (the `q^{-1/4}` circle and the `+-1` topological resonances), `:138` (Lax–Phillips is **follow-up work**). `cd refs && sha256sum -c --quiet manifest.sha256` → **MANIFEST OK**.

---

## Verdicts

**VERDICT C1: VALID.** The tree normalisation is right: detailed balance forces `m_0(q+1) = m_1 q` and `m_n = q m_{n+1}`, so `m_0 = 1`, `m_n = (q+1)q^{-n}`, the junction coefficient is `sqrt(q(q+1))`, and after dividing by `sqrt q`, `c^2 = q+1`. The reflection calculation, the **minus sign** and the **zero** power of `z` are exact: I solved `T g = lambda g` directly for `(g_X, R)` on 21 cores at two circle points and matched the `G`-form to `1e-8`, then matched `-p/ptilde`, then matched Childs's `-Q(1/z)/Q(z)` with `a = 0`, `b = c e_0`, `D = T_X`. `ptilde(z) = z^{2n}p(1/z)` is a coefficient reversal (exact), `p` monic with `p(0) = 1-c^2`, `ptilde(0) = 1`, hence `R(z)R(1/z) = 1`, `R(zbar) = conj R(z)`, `|R| = 1` on the circle. The reduced degree `2d - tau` is right: `deg p_b = 2 dim Krylov(T_X, e_0)` on every core. The height/orientation correction (ledger 2, `h = -n`) is a genuine repair of the brief. **The formula is Childs–Strouse's theorem, not new (finding 1).**

**VERDICT C2: VALID.** Every clause checks. `X_c = X_b^perp` is `T_X`-invariant and equals the span of core eigenvectors vanishing at `e_0`, so `sum_t m_c(t) = n - d` (verified on all cores, including two engineered cusp-form cores); `p = D_c p_b`, `ptilde = D_c ptilde_b` by block-diagonality; `B` and `D` are coprime by cyclicity. (i)–(ii): poles of the reduced `R` in the disc are real (verified), simple, nonzero (`ptilde_b(0) = 1`), and are exactly the `l^2` eigenvalues outside `[-2,2]` **with nonzero ray component** — I diagonalised `T` on a 400-site ray and counted: `#l^2 outside band = b + (cusp forms outside)`, so outside-band cusp forms are `l^2` but invisible, exactly as the prover says and exactly the numerics lane's D4. (iii) `gcd(p, ptilde) = D_c H` verified: on every core the common roots are cusp roots or `z = +-1` and nothing else. The threshold-simplicity argument is correct and I re-derived it: `p_b(z) = z^d[D(lambda) - c^2 z^{-1}B(lambda)]` and `dlambda/dz = 0` at `z = +-1` give `p_b'(+-1) = +- c^2 B(+-2)`, nonzero because `B(+-2) = 0` would force `D(+-2) = 0` against coprimality; verified numerically on every core with `tau > 0`. (iv) The count **`N = 2d - b - tau`** holds on all 21 cores, and is *identical* to the numerics lane's `2(n - mu) - t_nc - beta` and to Childs's Levinson count `2n - 2n_c - n_b - n_h`; the exterior roots of `p_b` are exactly `1/beta`. The `z = 0` bookkeeping (subtract `ord_0 p`, zero under H-INVERT) is right. **C2(i)–(iii) and the count are Childs–Strouse (finding 1).**

**VERDICT C3: VALID.** `p = z^2 - q`, `ptilde = 1 - qz^2`, poles `+-q^{-1/2}`, zeros `+-q^{1/2}`, `N = 0`, verified for `q = 2,3,5,7`; `1/R = q zeta_K(2s-1)/zeta_K(2s)` to `1e-10` at complex `s`, and `R` itself differs by `O(1)`, so **`phi = 1/R`** — agreeing with the numerics lane's D7 and with `childs-lead.md` §3. The period correction (ledger 9) is right: `1/R` is a function of `z^2 = q^{2s-1}`, period `pi i/log q` (verified). The `xi` vs `Lambda` caveat (ledger 11) is correct and is a real catch on the notebook's own text. The discrete-time orientation is also right: with `u_m = z^m g`, `z^{-(k-m)}` is a function of `k-m` and moves up the ray, so in `g_k = z^{-k} + R z^k` the coefficient `1` is outgoing and `R` incoming — the opposite labelling to Childs's continuous-time convention, which is *why* `phi = 1/R` here. **APW's modular-curve example (`:1977`) is this calculation, with `mu = 1/z` for the bound states (finding 1).**

**VERDICT T1a: VALID.** `W^T E W = E` exactly. `W(B, S^*B) = (SB, B)` and `W^{-1}(F,SF) = (SF, S^2F)`: forward-only / backward-only invariance, the right correction of the brief (ledger 12). The condition `B_1 = 0` is *forced*, and the prover's reason (otherwise `TB` acquires a core component `cB_1` at `v_0` and the datum leaves `D_+^0`) is better than the numerics lane's; I verified both, including `<b_1, E f_1> = (1-c^2)/2` (numerics D8). The energy formulas (1.1) are exact — I re-derived them by hand and checked 120 random profiles per core to `1e-9` — and `D_+^0 perp_E D_-^0` holds identically (I verified the cancellation term by term). Multiplicity one is right: both difference maps are `I - S^2` on `l^2(N_0)`, whose adjoint `I - S^{*2}` has no `l^2` kernel.

**VERDICT T1b: MINOR.** The substance is correct — on the spectral fibre the energy matrix `[[1, -lambda/2],[-lambda/2, 1]]` has eigenvalues `1 +- lambda/2 > 0` for `|lambda| < 2`, degenerate only at the two band edges (a set of zero a.c. measure), so `E > 0` on `H_ac (+) H_ac`; `W` extends to an `E`-unitary; the two arcs `e^{+-i theta}` give a scalar bilateral shift. The item is nevertheless labelled **PROVED** while its two load-bearing spectral inputs are one-sentence sketches: "There is no singular continuous spectrum here. For example, finite Schur elimination ..." and the implicit completeness of scattering + bound + confined states. Both are *theorems in the prior art sitting in `refs/`*: completeness is `1103.5077:234` ("form a complete basis for the state space \cite{Gol08}") and is re-proved for `n` tails in `1203.6557`. Fix: cite them and downgrade the sketch to a remark.

**VERDICT T1c: MINOR.** This is the round's hardest item and most of it survives a from-scratch rebuild.
*What I confirmed.* (a) The obstruction is real and the number is exact: for the pure `q = 2` cusp, `a = (r_2, r_1) in D_+^0` and `b = (r_2, r_3) in D_-^0` are `E`-orthogonal (`< 1e-12`), and after deleting both normalised bound eigenfunctions in each component `<Qa, Qb>_E = 0.093750000000` — **`3/32` exactly** — with `|f_1|^2 = 3/8`, exactly the prover's numbers. I also verified the `q`-family: the value is `2 * (1/2) f_1^2 (1-beta^2)^2` for `q = 2,3,5`, and that it vanishes identically when there are no bound states (H-LOCAL). (b) The isometry lemma is correct and I supplied the reason the prover omits: for `B` supported on `k >= 2`, `(Sf)_k = f_{k-1} = f_k/beta` on the support, so the bound-block coefficients of an outgoing datum are forced to be `(a, a/beta)` — and of an incoming datum `(a, beta a)` — whose block energy is `0` because `lambda = beta + 1/beta`. I verified deletion preserves the whole inner product *within* each radiation space (64 + 81 pairs, `1e-9`). (c) The factorisation `Theta = eta R B_bd` holds with `eta` a constant of modulus one (verified at 60 disc points on 6 models), and `R H^2 cap H^2 = Theta H^2`: `R*1` has negative Fourier mass `> 1e-6` while `R*B_bd` has `< 1e-8`. (d) The model is exactly as stated: `dim K_Theta = N`, `spec C = {z_i}` (not `1/conj z_i`), `C^m -> 0`, `I - C^*C = jj^*`, `G - D^*GD = 11^*`, `G_ij = 1/(1-conj(z_i)z_j)` cross-checked by circle quadrature, `<j,k_i> = 1`, `j = VG^{-1}1`. (e) **(1.2a) is correct**, and I derived it in closed form rather than trusting the Abel manipulation: for a `W`-eigendatum `X = (g, g/w)` one has `<X,(u,v)>_E = ((1-w^2)/2)[<g,u> - w^{-1}<g,v>]`, which gives outgoing amplitude `prop (w^2-1)/(1-t w^2)` and incoming amplitude `prop conj(R)(w^2-1)w^{-3}/(1-t w^{-2})`; both match the truncated numerics to `1e-6`, and the ratio tends to `-w R` as `t -> 1`. No unrecorded power of `w`.
*Why MINOR.* Two steps are asserted, not established. (i) The wandering vectors are not in the space: I verified that on a finite cut `E(gamma_+) = 2`, not `1` — the cut contributes exactly one extra seam difference — and that `||gamma_+||_{l^2}` grows without bound. Only the *ratio* of amplitudes is computed; `U gamma_- = w^{-1}` is a normalisation decree, and the energy completion carrying `gamma_+-` is never constructed. The numerics lane reached the same place independently (D10 and its §4: "I did not construct the completion ... the *operator* unitary equivalence is asserted, not verified"). (ii) The repair is a **choice**. `D_+ = O_+ cap O_-^perp` is asymmetric; the mirror `D_- = O_- cap O_+^perp` is not examined, and neither is the route Lax–Phillips actually take on the modular surface — keep the indefinite energy on the bound-state block, where the residual spectrum (the pole at `s = 1`) lives. That is not a side issue here: it is precisely the even sector T6(b) then declares missing. "The use of (1.2b), rather than just projecting both local spaces, is essential" is argued; that (1.2b) is *the* correct discretisation is not.

**VERDICT T1d: VALID.** The Sz.-Nagy–Foias/discrete Lax–Phillips statement is correctly quoted as standard, and the identification of the characteristic function with the **pole-removed** `eta R B_bd` — not `R`, not `1/R` — is right; this is independently the numerics lane's D13, and both lanes refute the brief's "identify it with `R` or `1/R`". The prover is the sharper of the two here: the lane says the scattering function is fixed only up to a power of `z`, and the prover *pins* it by fixing the wandering profiles, which I verified. The minimality hypothesis of the model theorem is the one thing not established, and I have booked that against T1c.

**VERDICT T2a: VALID.** (2.1) `log ptilde = -sum tr(zA - z^2 D)^l/l` with `D = I - c^2 P_0` verified to `1e-9` on all 21 cores (`det((1+z^2)I - zA - c^2z^2P_0) = det(I - (zA - z^2 D))` is an identity). (2.2) verified where `c^2 != 1`; (2.3) `p = det H_0 (1 - c^2 h)`, `ptilde = det H_0(1 - c^2 z^2 h)` verified to `1e-10`. (2.4) is the valuable one and it is exact: I extracted `[w^m] log ptilde` by contour average and confirmed `sum_i z_i^m = -m[w^m]log ptilde - sum_t m_c(t)(a_t^m + a_t^{-m}) - sum_eps eps^m - sum_beta beta^{-m}` for `m = 1,2,3` on every core, including cusp-form and threshold cores. Ledger 18 (zero-centred `log p` gives **inverse** root sums) is a real correction; ledger 17 (the Ihara–Bass edge factor `(1-u^2)^{|E|-|V|}` must not be silently dropped) is correct and honest. The "signed weighted walks, ordered insertions" qualification (ledger 19) is right.

**VERDICT T2b: VALID.** (2.5) verified against numerical differentiation of `log R`, and the partial-fraction form `-R'/R = -sum_i (1/(z-z_i) + conj(z_i)/(1-conj(z_i)z)) + sum_beta (...)` verified to `1e-4` at two complex points on every core — it follows from `R = eta^{-1} Theta/B_bd` and `b_a'/b_a = 1/(z-a) + conj(a)/(1-conj(a)z)`. The refusal to call this a prime comb, with the pointer to `prop:cusp-prime-comb`, is correct and important.

**VERDICT T2c: VALID.** `m_ray(lambda) = z` verified on a 4000-site ray to `1e-6`; the Catalan series equals `z` to `1e-9`; and `Cat_r` really is the number of length-`2r` ray walks `r_1 -> r_1` (verified for `r <= 6` by powers of the adjacency matrix). `c^2/z = c^2 lambda - c^2 z` is an identity, so the prover's point — only the physical branch has a convergent positive walk sum, the other is its continuation (ledger 20) — is right. The honest closing ("nothing in these Catalan excursions labels monic irreducibles") is the correct answer to the brief's question.

**VERDICT T3a: VALID.** Kraus set `{C} ∪ {sqrt(p_i)|omega_i><j|}`, `C^*C + jj^* = I`, Choi positivity: verified on four graph-derived models × 4 random rebounds.

**VERDICT T3b: VALID.** `m_Omega(m) = s_{m-1} - s_m >= 0`, total mass `1` (remainder below `1e-12` at `m = 4000`), `mu = sum_m s_m = sum_m m·m(m)` and `mu = Tr (I - E_0)^{-1}Omega`: all verified. The observation that the infinite-mean phenomenon of `prop:infinite-mean-rebound` cannot occur in finite dimension is correct and agrees with the numerics lane.

**VERDICT T3c: VALID.** This is the best item in the file. Uniqueness is unconditional (`rho = ell(rho) S_Omega`, trace one), verified even for the periodic example: `dim ker(I - E_Omega) = 1`. Attraction iff aperiodic: the peripheral spectrum is exactly the `d`-th roots of unity, each simple (`mhat'(u) = mu/u` at a peripheral zero), verified; for `d = 2` attraction demonstrably fails. The `+-ir` counterexample is correct in every detail (`I - C^*C = jj^*`, eigenvalues `+-ir`, `m(2k) = (1-r^4)r^{4(k-1)}`, `rho_inf = I/2`), and I add that **it is realised by a genuine graph core**: `T_X = [0]`, `c^2 = 1 - r^2` has exactly the resonances `+-ir`. The distinction the prover insists on — "support on a resonance" meaning a modal mixture (always aperiodic, `m(1) > 0`) versus an arbitrary `Omega` (can be periodic) — is exactly right, and I verified both halves on the same contraction.

**VERDICT T3d: VALID.** `ran S_Omega = span_m C^m ran Omega`, so `rho_inf` is pure iff `Omega = |v><v|` with `Cv = av`, `|a| < 1`, `a = 0` allowed. Verified: eigenvector rebounds give purity `1.000000` and `rho_inf = Omega`; generic pure rebounds give purity `< 1`. This is the numerics lane's D15 with the `a = 0` case correctly included.

**VERDICT T3e: VALID.** (3.2) verified at three complex `u` on four models to `1e-9`, with `mhat(u) = sum_m m(m)u^m` confirmed independently. (3.3) verified in **both** directions: on 7 simple product eigenvalues `nu = z_a conj(z_b)` across three models, and 12 rebounds each (6 engineered to have `L_ab(Omega) = 0`, 6 generic), `nu` persists iff `L_ab(Omega) = 0`, every time. `ell(X_ab) = <j,k_a><k_b,j> = 1` is never zero, so the eigen-*operator* never survives — the correct discrete analogue of the 04h statement. The refusal to state a coordinate rule at Jordan collisions, with the pointer to the same limitation in `prop:rebound-persistence-secular`, is right.

**VERDICT T4: VALID.** (4.1) verified: `mu_Omega = sum_i p_i/(1-|z_i|^2)` to `1e-5` and the modal `rho_inf` to `1e-9`; `rho_inf = Omega` for every weight vector **iff** all `|z_i|` coincide, checked as an iff on four models (the RH models give `3e-16`, the non-RH ones fail); under uniform modulus `mu = 1/(1-r^2)` independently of the mixture. T4(c) is exact: the nonzero spectrum of `rho_inf` equals `spec(diag(sqrt w) O diag(sqrt w))` and is **never** the weight list (verified, deviation from the weight list `> 1e-6` on every model). The time-convention remark is the right answer to the brief's invitation to "compare with `mu = 2` of 04h": these are different clocks, not equal means.

**VERDICT T5: VALID.** All three parts. (a) `Q_sigma >= 0` iff stationary, `Tr Q_sigma = <j,sigma j>` verified to `1e-10` on 200 random densities, and `Tr Q_sigma > 0` by strong stability. (b) The flag theorem holds exactly as stated: `P - CPC^*` is `(I_V - C_V C_V^*) (+) 0 >= 0` (the prover's one-liner is terse but the step `PC^* = PC^*P` for a `C`-invariant `V` makes it airtight), and Schur triangularisation supplies a full flag for **any** finite contraction. I rebuilt it: the flag `sigma` has *exactly* the prescribed spectrum (`1e-9`), is admissible, and is stationary for its own `Omega` (`1e-9`). His extra claim that the rank-one defect is unnecessary is also true — verified on 60 random strict contractions with **full-rank** defect. "No graph or arithmetic is used" is correct and is the right transcription of the previous round's finding. (c) (5.2) verified: `Q_sigma` has coefficient matrix `A_ij(1 - z_i conj z_j)`, `Tr sigma = Tr(AG)`, `Tr Q_sigma = 1^* A 1` (because `G_ji(1-z_i conj z_j) = 1`), and the warning that these are expansion coefficients, not matrix elements `(GAG)_ij`, is essential.

**VERDICT T6a: VALID.** With `Ctilde = 1 (+) C`, `jtilde = 0 (+) j`, the exit functional annihilates odd blocks and `Etilde(|k_i><vac|) = z_i |k_i><vac|` — verified to `1e-9` on four models with random graded rebounds. Eigenvalue `z_i`, **not** `conj z_i`, which is independently what the numerics lane found. The parity remark and the Jordan-chain remark are right.

**VERDICT T6b: VALID.** The correction is exactly right and I verified every piece on the pure `q = 2` cusp: an eigenfunction `f` with `Tf = lambda f` gives wave data `(f, f/t)` with `W`-eigenvalue `t in {beta, 1/beta}` — **not** `1` — each individually `E`-null, and the `2x2` block over the two branches has negative determinant, i.e. it is `E`-**hyperbolic**. (The numerics lane found the same hyperbolicity independently in D9; the prover says "indefinite", the lane is a shade sharper.) `|t| != 1` rules out any positive-definite `W`-invariant form on that block, so the constant function of the pure quotient cannot be a wave vacuum without changing the dynamics. The graded conclusions (segment for a rebound inside `K`; unique vacuum when the rebound has positive vacuum weight) are the correct transcription of `prop:graded-stationary-segment`. The honest verdict — "the answer may be no; a unique mixed graded stationary state still needs an even exit" — is the right one, and it is the same obstruction the 04h round hit.

**VERDICT T7a: VALID.** Both examples are exact. `a = 29/20`, `c^2 = 1/2`: two real resonances `(29 +- sqrt 41)/40 = 0.5649218941, 0.8850781059`, no bound states, no cusp forms — RAM holds, RH fails. The two-vertex core: `p(z) = (z^2 + 1/2)(z-2)(z+3/2)` **exactly** (symbolic match to `1e-8`), resonances `+-i/sqrt 2` so RH holds non-vacuously, visible bound parameters `1/2` and `-2/3` giving eigenvalues `5/2` and `-13/6`, the core is not bipartite, so RAM fails. Independence is proved. The numerics lane is sharper in coverage (it realises all four cells, shows `n = 1` can never realise `¬RAM & ¬RH`, and observes that non-vacuous RAM essentially forces a bipartite core) but there is no conflict.

**VERDICT T7b: VALID.** `prod(all roots of p) = 1 - c^2` exactly (verified on 21 cores; the numerics lane's D17 agrees that there is no sign ambiguity). The retained product `prod_i z_i = (1-c^2) prod_beta beta / prod_eps eps` is verified including the threshold denominator, hence (7.2). `a = 0` gives `r = sqrt(1-c^2)`, any value in `(0,1)`, so nothing generic forces `q^{-1/4}` — correct, and this is the round's honest negative result. The `c^2 = 1` caveat is right. See finding 7: the prover stops one line short of the sharp arithmetic consequence the numerics lane draws.

**VERDICT T7c: VALID.** (7.3) is necessary (verified on every RH root set) and **not sufficient** — and I strengthened his counterexample from a polynomial curiosity to a fact about graphs: `{0.8, 0.2}` is scaled-self-reciprocal at `r = |Q(0)|^{1/N} = 0.4` with `|F(0)| = 1`, and it is *realised* by the genuine core `T_X = [1]`, `c^2 = 0.84` (two resonances `0.2, 0.8`, no bound states, no thresholds). (7.4) is a valid necessary-and-sufficient test — the Cayley map is a bijection from the extended real line to the circle and the degree drop counts `w = 1` — and I verified it decides RH correctly on 7 root sets. The companion-matrix criterion `M_Q^* H M_Q = r^2 H` with `H > 0` is also verified, with the H-SIMPLE caveat correctly stated. See finding 9 for the adjudication against the numerics lane's "iff".

**VERDICT T7d: VALID.** One vertex suffices: `a = 0`, `c^2 = 1/2` gives `+-i/sqrt 2`, a non-real pair with RH. His caveat that the pure-cusp constraint `c^2 = q+1 > 1` would disallow it is correct — 400 random one-vertex cores with `c^2 > 1` produced **no** non-real resonance — matching the numerics lane's D18 (which additionally identifies `n = 2` as the minimum in that regime).

**VERDICT T8: MINOR.** The scope declaration is accurate about what is generic and the four H-ARITH targets are correctly stated as hypotheses. Three deductions are wrong or stale as *scope*. (i) The `h`-cusp scattering matrix is not something the arithmetic round must invent: `S(z) = -Q(z)^{-1}Q(1/z)` with `Q(z) = I - z(A + B^dag[(z+z^{-1})I - D]^{-1}B)` is `1203.6557:365`, and the `h`-tail Levinson count is `:511`. (ii) In the *curve* direction (as opposed to the *level* direction), the resonance divisor, the `q^{-1/4}` circle and the four-type classification are already computed in APW (`:2008`, `:2056`, `:2092`); what is genuinely open is the `Gamma_0(N)` / Dirichlet-`L` direction, and T8 does not distinguish the two. (iii) "determinant cancellation alone does not specify all matrix pole/zero directions" is true of the *model*, but not of the divisor: APW's scalar resonance determinant covers arbitrarily many cusps, and I verified the generalisation `det H(mu) = (-1)^n (2 mu)^{-n} det((1+mu^2)I - mu T_X - diag(c_v))` on 120 random multi-cusp cores. Also unremarked: `tau <= 2` and the threshold-simplicity lemma are **one-cusp** facts — APW's second elliptic example has `(mu-1)^2(mu+1)^2`, threshold roots of multiplicity two.

**Tally: 21 VALID, 3 MINOR (T1b, T1c, T8), 0 INVALID.**

---

## Findings

| # | sev | statement | what is wrong | fix |
|---|---|---|---|---|
| 1 | **MAJOR** | C1, C2, C3, (0.5), and the framing of T8 | The proofs file cites **no** prior art. `R(z) = -Q(1/z)/Q(z)` with `Q(z) = 1 - z(a + b^dag(lambda-D)^{-1}b)` is Childs–Strouse `1103.5077:181,185,186` — I checked it reduces to C1 *identically* (numerically, on 21 cores, at 4 complex `z` each) with `a = 0`, `b = c e_0`, `D = T_X`, including the minus sign the prover corrects the brief on. C2(i) is their evanescent bound states, C2(ii) is their Lemma `:303`, C2(iii) is their confined bound states `:242` with the same degenerate-eigenvalue count, `z = +-1` is their half-bound states `:232`, `deg = 2d` is `:365`, and `N = 2n - 2n_c - n_b - n_h` is Levinson `:277`. Separately, `p(z)` **is** APW's resonance determinant: `det H(mu) = (-1)^n (2mu)^{-n} p(mu)`, which I verified exactly on 21 single-cusp cores and in the multi-cusp generalisation on 120 random cores. Both papers are already in `refs/` with byte-verified quotes in `childs-lead.md` and `sources.md`. | Section 0 must open with both citations. C1, C2(i)–(iv) and (0.5) are **re-derivations**; they can be registered as `proved` but must not be registered as new. Only the discrete-time reading (`phi = 1/R` because `z^{-(k-m)}` is outgoing) is the prover's own. |
| 2 | **MAJOR** | T1(c), the repair `D_+ = O_+ cap O_-^perp` | The repair is a choice presented as a derivation. The mirror choice `D_- = O_- cap O_+^perp` is not examined; neither is the route Lax–Phillips themselves take on the modular surface, namely keeping the indefinite energy form on the bound-state block. That is not a side issue: the residual spectrum living in that block is the pole at `s = 1`, i.e. exactly the even sector T6(b) then declares to be missing. Additionally the final geometric identification (compression of `W` on `H_E (-) (D_+ (+) D_-)` is unitarily equivalent to `Z` on `K_Theta`) and the minimality of the Lax–Phillips system are asserted, not proved; the numerics lane says the same independently (D10, §4). | Either prove minimality, or state T1(c)/(d) as **PROVED-CONDITIONAL** on a named hypothesis H-MINIMAL and record the un-examined alternative. The Hardy-side facts (`Theta = eta R B_bd`, `dim K_Theta = N`, (1.3), (1.4)) are theorems and can be registered now. |
| 3 | **MAJOR** | T8 | Declares as "to be supplied by the arithmetic" three things that are already published and in `refs/`: the `h x h` S-matrix and its determinant/Levinson count (`1203.6557:365`, `:511`); the resonance divisor and the `q^{-1/4}` circle for *curve* quotients (`2603.26443:2008,2056,2092`); and a scalar multi-cusp resonance determinant. And it does not flag that `tau <= 2` and threshold simplicity are one-cusp facts, contradicted by APW's `(mu+-1)^2`. | Rewrite T8 to separate **(a)** what exists (matrix `S`, matrix Levinson, curve-direction resonances) from **(b)** what is genuinely open (the `Gamma_0(N)` level direction: the constant term as a ratio of Dirichlet `L`-functions over `F_q[T]`, and the `L`-zeros-are-resonances statement, which `sources.md` Target 7 records as NOT FOUND). |
| 4 | MINOR | T1(b), labelled PROVED | Absence of singular continuous spectrum and completeness of scattering + bound + confined states are asserted in one sentence each, and they are the load-bearing inputs. Both are citable (`1103.5077:234`; `1203.6557`). | Cite; relabel the no-`sc` remark as a citation, not a proof. |
| 5 | MINOR | the Kurasov reference | A bare web URL to a PDF not in `refs/`, not fetched, not hashed, not byte-checkable. It is the file's only external citation. This violates the notebook's source convention (`refs/README.md`, arXiv TeX, manifest). | Either fetch it into `refs/src/` with a hash and a byte-verified quote, or drop it — the counterexample is the prover's own and stands without it. |
| 6 | MINOR | T1(c), the wandering vectors | `gamma_+-` are not in `l^2` and not in the space before completion. I verified that on a finite ray cut `E(gamma_+) = 2`, not `1` (the cut adds exactly one seam difference of the same size), and that `||gamma_+||_{l^2}` grows with the cut. Only the **ratio** `(U gamma_+)/(U gamma_-) = -wR` is computed; `U gamma_- = w^{-1}` is a normalisation decree. | State it that way. The mathematics is fine — I re-derived both amplitudes in closed form — but the sentence "Here is also a direct way to fix the phase and delay" should say that it fixes the *relative* phase and delay. |
| 7 | MINOR | T7(b), one line short | (7.2) is stated but its sharpest consequence is not drawn. With `c^2 = q+1` and the Perron pair `+-q^{-1/2}` as the only bound states, (7.2) gives `prod_i |z_i| = q * q^{-1} = 1`, impossible for moduli `< 1`, so **`N = 0`**: any arithmetic quotient with resonances must have `l^2` spectrum outside the band beyond the Perron pair. This is the numerics lane's D16/§3.2, and it is the single most useful checkable demand the toy makes on the next round. | Add it to T7(b) and to T8's list of things to check first. Credit the numerics lane. |
| 8 | NOTE | adjudication, D14 vs T3(c) | **The prover is right.** The numerics lane's "the periodic counterexample needs `z = 0`, which happens iff `c = 1`" is over-general: the `+-ir` example is periodic with non-zero, non-real resonances, and I verified it is realised by the genuine core `T_X = [0]`, `c^2 = 1-r^2`. The lane's statement is correct **only for modal `Omega`**, which is precisely the distinction the prover insists on and which I verified on the same contraction (a modal rebound there has `m(m) > 0` for all `m`). | Register the prover's T3(c). Amend the numerics row to scope D14 to modal rebounds. |
| 9 | NOTE | adjudication, numerics §2.4 vs T7(c) | **Both are right, at different radii, and neither says which.** At `r = |Q(0)|^{1/N}` (the geometric mean, the prover's) scaled self-reciprocity is only **necessary**: `{0.8, 0.2}` passes and is realised by `T_X = [1]`, `c^2 = 0.84`. At `r = ` the **arithmetic mean** of the moduli (what `scripts/cusp_graph.py:1416` actually computes) it is a genuine **iff**, by an argument the lane does not give: self-reciprocity at `r` makes the modulus multiset invariant under `x -> r^2/x`, so its sum is `(#fixed) r + sum_pairs (x + r^2/x) >= r * (count)` with equality iff every modulus is `r`. I verified the AM step on 2000 random inversion-symmetric multisets and the iff on every core with `>= 2` resonances. | Register **both**, each with its `r` stated explicitly. The lane's version is the better practical test; the prover's (7.4) Cayley test is the better exact/symbolic one. |
| 10 | NOTE | adjudication, D10 vs T1(c) | No contradiction: both lanes agree `dim K = #res` is **false in `l^2`** and is a statement about the energy completion. The lane calls the completion statement unverified; the prover asserts it. On the model side (`dim K_Theta = N`) it is a theorem and I verified it; on the geometric side neither lane establishes it. | Record as an open step, not as proved. Folded into finding 2. |
| 11 | NOTE | adjudication, D13 vs T1(d) | **The prover is sharper.** The lane observes that the scattering function is defined only up to a power of `z` (the free half line has `R = -z^2`); the prover removes that freedom by fixing the wandering profiles and shows "no unrecorded power of `w` is present". I verified the computation. Both lanes refute the brief's "identify with `R` or `1/R`"; the answer is `eta R B_bd`. | Register the prover's T1(d) statement. |
| 12 | NOTE | the numerics lane's untested gap, now closed | The lane records that it never exercised a core with a band-edge root **and** resonances, so "the threshold Jordan-block phenomenon is not exercised". My core `T_X = [3/2]`, `c^2 = 1/2` has `tau = 1`, `N = 1`, `b = 0`, and (0.5) holds. More: the phenomenon **cannot** occur for one cusp — I re-derived the prover's lemma, `p_b'(+-1) = +- c^2 B(+-2) != 0` by coprimality of `B` and `D`, and verified it on every core with `tau > 0`. | Register threshold simplicity as a proved lemma (one cusp only — see finding 3). |
| 13 | NOTE | C3's line citation | `report/sections/04_riemann_channel.tex:27` is the sentence; the `\cxi` display is at `:29`. The mathematics is right (verified to 25 digits). | Reword to `:29`. |
| 14 | NOTE | `eta` in (1.2) | The file says `Theta` "can and will have real coefficients" but never records the value of `eta`. I verified `eta` is a constant of modulus one on every model; with real, conjugation-closed root sets it is `+-1`. | Print `eta = -1` (or the sign convention actually used) so downstream phases are determinate. |

**No BLOCKER. No INVALID statement found.** I attacked, in order, all four items the prover nominated: the `3/32` obstruction and the phase/completeness/intersection arguments repairing it (the number is exact, the isometry lemma is correct and I supplied its missing reason, the intersection identity is correct, the phase computation is correct — but the completion is not built); the threshold and zero-delay bookkeeping in `N = 2d - b - tau` (correct, in three independently derived forms, on 21 cores including threshold, cusp-form and `c = 1` cores); the time/height orientation and characteristic-function convention (correct, and the `phi = 1/R` answer is forced by the discrete time factor `z^m`); and H-ARITH (correctly scoped as a hypothesis, but over-stated as to what remains to be supplied).

---

## Prior art versus what is new in this round

**Prior art — Childs–Strouse `1103.5077` and Childs–Gosset `1203.6557`** (both in `refs/`, hashes verified):
the object (finite weighted graph plus semi-infinite unit-weight path, energy `2 cos k = z + 1/z`); the reflection coefficient `R(z) = -Q(1/z)/Q(z)`, `Q(z) = 1 - z(a + b^dag((z+z^{-1})-D)^{-1}b)`, **identical to C1 including the sign**; rationality, `|R| = 1` on the circle, `R(z)R(1/z) = 1`, continuation off the circle; evanescent bound states (C2(i)); reality of the roots in the closed disc (C2(ii), Lemma `:303`); confined bound states with the degenerate-eigenvalue count (C2(iii), `:242`); half-bound states at `z = +-1` (`:232`) — the brief had no such case, the prover rediscovers them as `E_th`; the numerator degree `2m - 2n_c = 2d` (`:365`); Levinson's theorem (`:277`, `:511`) which is the count (0.5); completeness of scattering + bound + confined states (`:234`), the input T1(b) needs; and the `n`-tail S-matrix `S(z) = -Q(z)^{-1}Q(1/z)` (`1203.6557:365`) that T8 asks the arithmetic round to supply.

**Prior art — Arends–Peterson–Weich `2603.26443`** (in `refs/`): the resonance matrix `H(mu) = (1/(2 sqrt q))(A_L + B(mu)) - z(mu) I` (`:1907`) — I verified `det H(mu) = (-1)^n (2 mu)^{-n} p(mu)` exactly, and its multi-cusp form with `c^2 P_0` replaced by `diag(c_v)`; the `mu^k f(mu)` normal form (`:1919`), which is the prover's `ord_0 p` / delay modes; the pure-cusp example (`:1977`, "a single vertex attached `q+1` times to a cusp"), which is C3 with `mu = 1/z`; the two elliptic-curve determinants (`:2008`, `:2056`) whose factors are *exactly* the prover's four classes — `(mu^2 - q)` the reciprocals `1/beta` of his bound-state poles, `(mu^2+1)^2` cusp forms on the unit circle (`:2014`, `l^2`), `(mu+-1)^2` his threshold/`tau` roots (`:2058`, explicitly **not** `l^2`), and the Hasse–Weil factors `2mu^4-2mu^2+1`, `3mu^4+1` his resonances, all of modulus `q^{-1/4}` (I verified all four classifications); and the programme statement (`:2092`, `:2093`) that resonances on the `q^{-1/4}` circle are Hasse–Weil zeros by Weil's theorem and that `+-1` resonances are "topological". APW also state (`:138`) that connecting this with the **Lax–Phillips** wave equation is *follow-up work* — which is why T1 is genuinely not in the literature.

**New in this round:** the discrete wave group `W` with the conserved form `E`, the d'Alembert local radiation spaces with the forced offset `B_1 = 0` and their one-sided invariance (T1(a)); the `3/32` obstruction and the orthogonalised outgoing space `D_+ = O_+ cap O_-^perp`, hence the identification of the characteristic function as the pole-removed inner function `eta R B_bd` and `dim K = #resonances` (T1(c),(d)) — the piece APW flag as not yet done; the discrete renewal channel and everything downstream of it (T3–T6), which is the transcription of shard 04h and which the prover correctly certifies uses **nothing** about the graph; the walk/insertion expansions (2.1)–(2.4) and the Catalan identification of the ray self-energy (T2); and the RAM-vs-RH independence, the retained-product identity (7.2) and the checkable criteria (7.3)/(7.4) (T7), whose content is negative — nothing in the toy forces `q^{-1/4}`.

---

## Adjudication: prover versus the numerics lane

| item | numerics lane | prover | who is right |
|---|---|---|---|
| sign in `R` (D1) | `R = -p/ptilde`, power of `z` is `0` | same (ledger 3) | **agree**; both refute the brief; it is also Childs's `-Q(1/z)/Q(z)` |
| tree measure (D2) | `m_0 = 1`, `m_n = (q+1)q^{-n}`, `c^2 = q+1` | same (ledger 1) | **agree**, verified |
| `c = 1` degree drop (D3) | `deg ptilde` drops, `roots(ptilde) = 1/roots(p)` fails | the identity `ptilde = z^{2n}p(1/z)` always holds; H-INVERT scopes the expansions | **both right**, different statements (polynomial identity vs root correspondence) |
| C2(i) (D4) | poles = outside-band `l^2` eigenvalues **not vanishing at `v_0`** | same (ledger 5) | **agree**; I verified against a 400-site diagonalisation |
| C2(iii) (D5) | cusp forms **plus** band-edge roots at `z = +-1` | same (ledger 6), with the exact statement `gcd = D_c H` | **agree**; the prover's form is sharper (it also proves simplicity) |
| the count (D6) | `#res = 2(n-mu) - t_nc - beta` | `N = 2d - b - tau` | **identical**; and identical to Childs's `2n - 2n_c - n_b - n_h`. Verified on 21 cores |
| `phi` (D7) | `phi = 1/R` | `phi = 1/R` | **agree**; `childs-lead.md` §3 agrees |
| `D_+` offset (D8) | `B` on `k >= 2`, by `E`-orthogonality | `B_1 = 0`, by forward invariance | **agree**; the prover's reason is the better one (I verified both) |
| bound-state energy block (D9) | `E`-**hyperbolic**, eigen-data `E`-null | "individual energy zero, joint form indefinite" | **agree**; the lane's word is a shade sharper. Verified |
| `dim K` (D10) | false in `l^2`; unverified in the completion | `dim K_Theta = N`, asserted geometrically | **both right, different objects.** The model statement is a theorem (verified); the geometric one is established by neither. Finding 2 |
| `spec Z` (D11) | the resonances themselves | same | **agree**, verified |
| Gram / dark modes (D12) | `<k_n,k_m>(1-conj z_n z_m) = conj(a_n)a_m`; `a_n != 0` is forced | (1.4) with `a_n = 1`; same impossibility argument | **agree**, algebraically the same |
| characteristic function (D13) | neither `R` nor `1/R`; defined only up to `z^k` | `eta R B_bd`, with the power of `w` **pinned** by (1.2a) | **prover sharper**; I verified the pinning in closed form |
| periodicity (D14) | needs `z = 0`, i.e. `c = 1` | `+-ir` example is periodic with non-real resonances; aperiodicity needs `ran Omega` to contain an eigenvector | **prover right**; the lane's claim holds only for modal `Omega`. I realised the prover's example on the core `T_X = [0]`, `c^2 = 1-r^2` |
| purity (D15) | pure iff `Omega` pure on an eigenvector | same, with `a = 0` allowed | **agree**; the prover's is marginally more general |
| what fixes `r` (D16) | nothing does; **and** `c^2 = q+1` with only the Perron pair forces `N = 0` | nothing does; (7.2) | **agree on the negative; lane sharper** — it draws the `N = 0` corollary the prover omits. Finding 7 |
| root product (D17) | exactly `1 - c^2`, no sign ambiguity | exactly `1 - c^2` | **agree**, verified on 21 cores |
| smallest non-real core (D18) | `n = 1` needs `c^2 < 1`; with `c^2 = q+1` need `n >= 2` | `n = 1` with `0 < c^2 < 1`, same caveat | **agree**; I verified the `c^2 > 1` obstruction on 400 draws |
| RAM/RH cells (D19) | all four cells; `n = 1` cannot do `¬RAM & ¬RH` | two cells, both exact | **agree; lane sharper in coverage**, prover's examples are exact and symbolic |
| self-reciprocity (§2.4 vs T7(c)) | an **iff** on 15 cores | **necessary only**, plus a Cayley real-root test | **both right, different `r`** — geometric mean (prover) vs arithmetic mean (lane's code). I settle it with a realised counterexample and a proof of the lane's version. Finding 9 |
| Krylov factorisation (lane §3.1) | `p = p_red * prod_cusp(z^2 - lambda z + 1)` | `p = D_c p_b` via `X_b = Krylov(T_X, e_0)` | **identical**, found independently by both |
| infinite mean (lane §4) | impossible in finite dimension | same (T3(b)) | **agree** |

---

## Recommendation for registration

**Can be registered as proved now.** C1, C2, C3 (as *re-derivations*, with Childs–Strouse and APW cited — finding 1); T1(a); T2(a),(b),(c); T3(a)–(e); T4; T5(a),(b),(c) — T5(b) restated in the generality it has (any finite stable contraction, any positive defect), continuing the previous round's finding M1; T6(a),(b); T7(a),(b) (with the `N = 0` corollary of finding 7 added), (c) (with the radius made explicit, finding 9), (d).

**Stays sketched.** T1(b) until the two spectral inputs are cited (finding 4). T1(c) and T1(d) as a pair: the Hardy-side statements (`Theta = eta R B_bd`, `R H^2 cap H^2 = Theta H^2`, `dim K_Theta = N`, (1.3), (1.4), (1.2a)) are proved and can be registered; the geometric identification stays `proved-conditional` on a named minimality hypothesis (finding 2). T8 until it is rewritten to separate the published from the open (finding 3).

**Stays open.** Everything under H-ARITH in the `Gamma_0(N)` level direction — `sources.md` Target 7 records that no source states "`L`-zeros are resonances" for `Gamma_0(N)` on the tree, and this review found none either. Whether any core with `c^2 = q+1` can have `r = q^{-1/4}` at all: by finding 7 it needs `l^2` spectrum beyond the Perron pair, and neither lane searched for one.
