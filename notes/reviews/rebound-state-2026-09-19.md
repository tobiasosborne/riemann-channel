# REFUTE review: the rebound state inside the model space

Reviewer: `claude:opus` (adversarial lane, two-model-family protocol).
Date: 2026-09-19.

## Inputs read (in full)

| file | what it is |
|---|---|
| `notes/rebound-state/astra-brief.md` | TJO's brief to the prover (177 lines) |
| `notes/rebound-state/astra-proofs.md` | prover output, `codex:gpt-6-astra` (665 lines, C1–C4, T1–T7, 32-row correction ledger) |
| `notes/rebound-state/numerics.md` | independent Opus numerics lane (331 lines) |
| `scripts/rebound_state.py`, `outputs/rebound_state.txt` | numerics lane evidence, 150 checks |
| `refs/src/1707.02266/TORUN.tex` | Siemon–Holevo–Werner, 688 lines (source for every SHW citation) |
| `report/sections/02b_definitions_arithmetic.tex`, `02c_definitions_phantasm.tex`, `04_riemann_channel.tex`, `04c_phantasm_channels.tex`, `04h_rebound_state.tex`, `db/claims.tsv` | the notebook's registered definitions and rows |

## Scripts written by this lane (independent; nothing imported from `scripts/rebound_state.py`)

| script | checks | covers |
|---|---|---|
| `notes/reviews/scratch_rebound_conventions.py` | **75** | C1–C3 by direct quadrature in the prover's plain-Lebesgue convention: `<k_w,k_v> = 2 pi i/(w - conj v)`, `\|k_w\|^2 = pi/Im w`, the reproducing vector `i k_w/(2 pi)`, Paley–Wiener inversion of `F k_w = -i sqrt(2 pi) e^{-i conj(w) x}`, refutation of the brief's `e^{i w x}`, Plancherel, the eigen-relation and its modulus, the old-vs-corrected sign moduli, and the full reconciliation of the two lanes' constants |
| `notes/reviews/scratch_rebound_finite.py` | **218** | T2(b) eq (3); T4(a) eq (8) **including engineered eigenvalue collisions** (off-diag/off-diag and off-diag/diagonal); T4(b) eq (9); T4(c) eq (10); T5(a),(b),(c) eqs (11)–(14); T6(c) eqs (18)–(19); plus refuter probes on uniqueness/attraction for small-support rebounds |
| `notes/reviews/scratch_rebound_flag.py` | **200** | T2(c) formula (4) by numerical differentiation + full-line quadrature, and the strip bound; T3(a),(b),(c) on the graded space for four rebound types; **T6(d) rebuilt from scratch for N = 3, 4 with off-line zeros**; T6(e) truncated Gibbs flags; cross-check of the numerics lane's `\|H_ab\|` bound and its "commutes with H" corollary |

**493 checks, 0 failures.** Every number below was produced by these scripts unless it is labelled as coming from the prover or the numerics lane.

## Citations byte-checked

**13 SHW citations, 13 verified, 0 failures.** Every `1707.02266:TORUN.tex:<lines>` in the proofs was opened against the file and the cited range contains what the prover says it contains:

`147` (the first-order-vs-finite-time warning), `172–173` (`\begin{defi}\label{def:no-event}` + "It is necessarily of the form ... $C_t=\exp(tK)$"), `189–198` (the unbounded standard form, `\eqref{standUB}`, "The natural domain ... is $\domsq K$"), `203–208` (the exit-space definition and `\eqref{exitspace}`, ending "There is always a unique minimal exit space"), `213–215` (`\eqref{Jbounded}`, the $\norm{J\phi}^2$ computation whose equality case C3 supplies), `228–234` (`\eqref{Jntau}` and "it is the same minimal semigroup as constructed in the next section" — the event expansion), `246` (Prop. `stdExit`(a), reinsertion maps), `249–262` (item (d) through the $\norm{\jsq\rho}\leq\norm{\gen^0\rho}$ continuity estimate), `253–254` ("via $L_\alpha=M_\alpha j$"), `311–313` (`\eqref{resmin}`), `317–321` (`\begin{prop}\label{prop:minsol}` and its hypothesis `\eqref{subnPP}`), `346–347` (`\begin{lem}\label{lem:finrank}`: finite rank ⟹ `\dom\gen=\dom\gen^0`), `442–450` (the half-sided shift, `j\psi=\psi(0)`, and "reset to the ``rebound'' state $\Omega$").

I additionally checked the **SHW hypotheses**, not just the line numbers: `P_Omega rho = ell(rho) Omega` is completely positive with `Tr P_Omega(rho) = -Tr(A rho)`, i.e. `\eqref{subnPP}` with **equality**, so `prop:minsol` applies; and `P_Omega` has rank one on the trace class whatever the Hilbert-space rank of `Omega`, so `lem:finrank` applies. The prover's decision to declare **no** `H-SHW` is therefore justified, and is a real strengthening of the brief.

**Notebook references.** All five ids cited in the proofs (`conj:phantasm-both-halves`, `def:bond-entanglement` ×2, `obs:complementary-halves`, `prop:normal-kms-gibbs`, `prop:vacuum-renewal-trivial-entanglement`) exist and the substance matches. In particular `def:bond-entanglement` (`02c_definitions_phantasm.tex:69–76`) does say "the *entanglement Hamiltonian* is $-\log\dm$, its spectrum the *entanglement spectrum*", so ledger item 23 is correct and the numerics lane's looser usage of "entanglement spectrum" for the density spectrum is the sloppy one.

**One stale line citation (NOTE, not a failure).** The proofs say `report/sections/04_riemann_channel.tex:71–75` "has an adjoint/sign error". Those exact lines were corrected in the repository on 2026-09-19 *after* the proofs were written (they now print `\Zt k_\lambda = e^{i\tsg\bar\lambda}k_\lambda` with a pointer to `obs:model-modes-sign`). The correction the prover demands is the correction that was applied, verbatim; but the citation as printed no longer matches the file and must be reworded before it is shardified.

---

## Findings

| # | sev | statement | what is wrong | fix |
|---|---|---|---|---|
| M1 | **MAJOR** | T6(d), T6(e), and the shard summary of `04h` | The flag construction uses **nothing about zeta**. It needs only: (i) a strongly stable contraction semigroup with a one-dimensional exit form, (ii) `N` (or countably many) eigenvectors of its generator spanning a nested invariant flag. The prover *scopes it correctly* inside T6(d) ("PROVED-CONDITIONAL on the availability of `N` distinct modes"), but T7 and the `04h` SHARD-SUMMARY ("invariant flags of modes realise every finite density spectrum ... without RH") read as a fact about the Riemann channel. I verified it works identically for off-line and for entirely arbitrary `w_n in C_+` (`scratch_rebound_flag.py`, sets `N=3 off-line`, `N=4 off-line`, `N=4 on-line`; `scratch_rebound_finite.py`, set `mixed`). | Register `thm:flag-spectrum-attainable` as a statement about **any** one-exit no-event semigroup with an invariant flag of eigenvectors, and state its specialisation to `K_Theta` as a corollary. Otherwise the row over-claims arithmetic content. |
| M2 | **MAJOR** | T7, and the amendment already made to `obs:complementary-halves` | "`no construction in this book has both` is false" is cheap in the sense the prover uses it: the **direct sum** of the vacuum-decay Lindbladian on `C vac (+) K` with the prime-chain Gibbs Lindbladian on `l^2(N)` already has the zeros as odd modes and a stationary density with entanglement spectrum `beta log n + log zeta(beta)`, and needs none of T1–T6. Nothing in T7 distinguishes the new construction from that triviality. The genuinely new content is that the mixed stationary density lives **on `K` itself** — the same space that carries the zero modes — and that its rebound is the cusp flux `Q_sigma/Tr Q_sigma`, not a free second summand. | T7 (and the amended `obs:complementary-halves` text at `04c_phantasm_channels.tex:219–222`) must say that. As printed, the amendment ("the renewal channel with a rebound inside `K_S` has both as a spectral statement") is *adequate* but does not say why a direct sum would not have done. |
| m1 | MINOR | C4 | Labelled **STATUS: PROVED**. It is a scope declaration, not a proposition. Its two actual mathematical assertions are correct (`H-UES` ⟹ `mu_Omega <= M^2/(2 omega)` for every density; finite modal spaces are exponentially stable but not uniformly in the dimension). | Relabel `SCOPE`. Fold in the numerics lane's positive finding that `\|C_t\|_2 < 1` strictly for every `t > 0` in every finite Gram model, so the unreviewed `\|C_t\| = 1` claim is a statement about the genuinely infinite-dimensional `K_Theta` only. |
| m2 | MINOR | (20) in T6(d) | Writes `P_k B^*`. `B^*` is not everywhere defined; the object meant is `(B P_k)^*`, which is bounded, finite rank, and self-adjointly paired with `B P_k`. The proof is unaffected (`B P_k` is bounded because `V_k` is finite-dimensional and `B`-invariant). | Print `Q_k = -(B P_k + (B P_k)^*)`. |
| m3 | MINOR | T6(e), "the condition on `U`" | The necessary condition the prover supplies (inequality (19) with `s_nm` built from `U`) is correct but weak. The numerics lane derived a strictly sharper *basis-level* condition, which I re-derived independently and confirmed: writing `Q_sigma = i[H,sigma] + (1/2){|j><j|,sigma}` with `H = (i/2)(B - B^*)`, the `2x2` minors in the eigenbasis of `sigma` give `\|H_ab\| >= (1/2)\|j_a\|\|j_b\| \|sqrt(l_a) - sqrt(l_b)\|/(sqrt(l_a)+sqrt(l_b))`, hence: **`sigma` commuting with `H` with non-degenerate spectrum is never admissible** (all `j_a != 0` by C3). | Register both. The two are not in conflict with T6(d): I checked that the flag states have `\|[H,sigma]\|` in `[0.074, 0.218]` and satisfy the bound with margin, while `H`-diagonal Gibbs states give `lambda_min(Omega_cand) = -0.076` (`beta=1.5`), `-0.115` (`beta=2`). |
| m4 | MINOR | T2(a), the Blackwell paragraph | Correct, but the step-function / uniform-interval-mass route is laborious and the "approximate on a compact interval" step is only sketched. A shorter and fully rigorous route exists: `\|A_Omega(t)\|_1 = S_Omega(t)` is decreasing and integrable and `A_Omega` is trace-norm continuous, so `A_Omega` is directly Riemann integrable; the scalar key renewal theorem applied to `<phi, A_Omega(.) psi>` gives weak convergence, applied to `S_Omega` gives `Tr (U * A_Omega)(t) -> 1`, and weak convergence plus trace convergence of positive trace-class operators is trace-norm convergence. | Replace the paragraph. |
| n1 | NOTE | correction ledger item 1 / C-SIGN | Confirmed, and **already applied** in the repository (`prop:functional-model-modes` now prints `Z_t k_lambda = e^{i t conj(lambda)} k_lambda`; `obs:model-modes-sign` registered in `04h`). The prover's citation of the pre-correction text is now stale. I reproduced both moduli: old `1.2840254167`, corrected `0.7788007831` at width `1/4`, `t = 1`. | reword the ledger row as "corrected 2026-09-19, see `obs:model-modes-sign`". |
| n2 | NOTE | the prover's own numerical snippet | He builds the synthesis map with an **upper Cholesky** factor of the Gram matrix, not `G^{1/2}`. That choice is exactly what makes `diag(0.7,0.3)` the invariant-flag state of (21) — his "flag target" row therefore does test (21), but only because of an unstated convention. With any other square root, `diag(p)` is a different state and is generically **inadmissible**: I found `4000/4000` random failures. | State the convention wherever (21) is illustrated numerically. |
| n3 | NOTE | cross-lane: the vacuum | The numerics lane (`numerics.md`, check 126) calls `prop:vacuum-renewal-trivial-entanglement` "exactly the `Im w = 0` end of T2's criterion". Taken literally this **contradicts** T2(a), since the vacuum has `mu = infinity` *and* a stationary density. **The prover is right** (ledger item 10): the vacuum's holding law has mass zero on finite times and an atom at infinity, the no-event semigroup is not strongly stable there, and it is an absorbing boundary case, not a limit of the criterion on `K`. | Adopt the prover's reading in any row that cites `prop:vacuum-renewal-trivial-entanglement` alongside T2. |
| n4 | NOTE | cross-lane: T4(a) | The numerics lane writes "an eigenvalue of `L_0` persists **iff** `Omega_k[n,m] = 0`". That is only correct at a **simple** `lambda_nm`, and degeneracy is generic here (under RH every `lambda_nn = -1/2`, multiplicity `N`). **The prover is right**: the criterion is `r_lambda = ell(P_lambda Omega) = 0`, and when `r_lambda != 0` the old eigenvalue survives with algebraic multiplicity exactly `d_lambda - 1`. I confirmed this at engineered collisions: `d=4 -> 3`, `d=3 -> 2`, `d=2 -> 1`, and also at an off-diagonal/diagonal collision (`w = (0.1i, 0.4i, 3+0.25i)`, `lambda_12 = lambda_33 = -1/2`), where (13) is still exact to `5e-15`. | Adopt the prover's (8). |
| n5 | NOTE | cross-lane: the constants | **No disagreement.** `c = 2 pi i` (prover, plain `d tau`) and `c = i` (numerics lane, `d tau/2 pi`) differ by exactly the measure normalisation. On normalised modes the two coincide identically: `G_nm = 2i sqrt(y_n y_m)/(w_n - conj w_m)` and `j e_n = -i sqrt(2 y_n)` in **both** (verified, deviation `5.6e-17`; the `sqrt(2 pi)` cancels). It is the **brief** that is internally inconsistent, pairing an exit value `-i sqrt(2 pi)` with a Gram constant `i`; the prover's pairing is the consistent plain-Lebesgue one. | Record `c = 2 pi i` with `j k_w = -i sqrt(2 pi)` as the shard convention, and note the normalised-mode form as the convention-free one. |
| n6 | NOTE | cross-lane: T5(b) | Both lanes independently contradict the brief, and their corrected formulas are **algebraically identical**: `spec(D_r^{1/2} G_norm D_r^{1/2})` with `r_n = p_n/(2 y_n mu)` equals `spec(diag(p_n/mu) G_unnorm)` because `G_norm = 2 sqrt(y_n y_m) G_unnorm`. I reproduced the prover's worked example exactly (`\|G_12\| = 1/sqrt 5`, spectrum `0.7236067977, 0.2763932023`) and the numerics lane's `N=2` closed form. | No action; register the prover's (12). |
| n7 | NOTE | cross-lane: the "load-bearing failure" | The numerics lane's headline worry ("the programme must *invert* `p -> spec(G^{1/2} diag(.) G^{1/2})`, whose input data are the zero ordinates") is **answered, not by inversion, but by leaving the mode-diagonal family**: T6(d) prescribes the spectrum exactly using the Gram–Schmidt flag basis. Likewise the numerics lane's `400/400` Haar failures and my `4000/4000` are about bases chosen *without* reference to the modes; the flag basis is chosen *from* them. The two lanes are consistent, and the prover's T6(d) supersedes the numerics lane's "refuter should attack next" item 1. | Register T6(d) as the resolution of that item. |
| n8 | NOTE | T5(a) | The prover's own verdict — "a renewal-channel reformulation of the already known equal-rate criterion in C1, not an additional mechanism or evidence for RH" — is correct and should be preserved verbatim in the shard. The `H-SYM` quantifier is doing all the work that pins the common width at `1/4`; the numerics lane's sharpened quantifier ("all `Im w_n` **with `p_n > 0`**") is the same statement. | Register as an **observation**, not a theorem about RH. |
| n9 | NOTE | T2(a), uniqueness with small-support rebounds | Worth registering explicitly because it is stronger than it looks and I checked it: even a **rank-one** rebound (modal or non-modal) on a multi-mode space gives `dim ker L_Omega = 1` and global attraction from arbitrary initial densities (spectral gaps `0.347`–`0.380`, convergence to `1e-13`). For a *modal* rank-one rebound `rho_inf` is pure; for a non-modal one it is full rank — the support statement "closed span of `C_t supp Omega`" is sharp. | — |

**No BLOCKER. No INVALID statement found.** I attacked, in order, the four things the prover himself nominated (`D(L_Omega)=D(A)` via the rank-one resolvent; the trace-norm renewal limit; the projection identity (20) and the closed-generator limit in (23); the sign and index conventions in (3), (7), (10)) and all four hold.

---

## Verdicts

**VERDICT C1: VALID.** I recomputed `<k_w,k_v> = 2 pi i/(w - conj v)`, `\|k_w\|^2 = pi/Im w`, the reproducing constant, and the Paley–Wiener image `F k_w = -i sqrt(2 pi) e^{-i conj(w) x}` by full-line quadrature (deviations `1e-13` to `1e-10`), and confirmed that the brief's `e^{i w x}` is **not** the image of `k_w` under the stated linear convention; the eigen-relation `C_t k_w = e^{-i t conj(w)} k_w` and its modulus `e^{-t Im w}` follow from the adjointed evaluation identity and I verified both.

**VERDICT C2: VALID.** `D(B) = M cap H^1(0,infinity)` with no boundary condition is right (the backward-shift generator on the half line has domain `H^1`), and the "difference quotients stay in the closed invariant subspace, so `g' in M` automatically" argument is correct; the form identity `-(<B psi,phi> + <psi,B phi>) = conj(psi(0)) phi(0)` follows by parts, `\|g(0)\|^2 <= 2\|g\|\|g'\|` gives graph continuity, and `j k_w = -i sqrt(2 pi) != 0` makes the minimal exit space exactly `C`. The explicit refusal to read `-(B+B^*) = j^*j` as an operator identity is the right correction of the brief (ledger 3).

**VERDICT C3: VALID.** `\|C_t g\|^2 = int_t^infty \|g\|^2 -> 0` is immediate, `(Jg)(t) = g(t)` makes `J` the isometric inclusion into `L^2(R_+)`, which is precisely the equality case of SHW `\eqref{Jbounded}` at `213–215`; and the distinction between the time-domain value `(F k_w)(0)` and evaluation of `k_w` at spectral parameter `0` (ledger 4) is a genuine and necessary correction.

**VERDICT C4: MINOR.** Both mathematical claims are correct (`H-UES` ⟹ `mu_Omega <= M^2/(2 omega)` for every density; finite modal spaces are exponentially but not uniformly stable), and refusing to use `\|C_t\| = 1` is the right call — but the item is labelled **PROVED** when it is a scope declaration, and it should absorb the numerics lane's finding that `\|C_t\|_2 < 1` strictly for `t > 0` in every finite model. See finding m1.

**VERDICT T1: VALID.** Conservativity by nonexplosion of a renewal process with a proper holding law is correct (iid holding times with `Pr(H >= eps) > 0` have divergent partial sums a.s.), the SHW event expansion specialises correctly to a scalar renewal because the exit is one-dimensional, and the resolvent computation is right: `f_z(rho) = Tr rho - z Tr R_0(z) rho = mhat_rho(z)`, `(P_Omega R_0)^n = q_z^{n-1} P_Omega R_0` with `q_z < 1`, hence `R_Omega = R_0 (I - P_Omega R_0)^{-1}` with a bounded bijective second factor and `D(L_Omega) = D(A)` exactly. I verified the SHW hypotheses `\eqref{subnPP}` (with equality) and `lem:finrank` independently.

**VERDICT T2(a): VALID.** Existence (`R = int A_Omega` is in `D(A)` with `AR = -Omega`, `ell(R) = 1`), uniqueness (`A sigma = -r Omega`, `Tr T_t^0 sigma = 1 - r int_0^t S_Omega -> 0` gives `1 = r mu_Omega`), non-existence at `mu = infinity`, and the support statement all check out line by line; the trace-norm convergence via Blackwell is correct though laboriously argued (finding m4). I confirmed numerically that the stationary state is unique and globally attracting even for rank-one rebounds.

**VERDICT T2(b): VALID.** Eq (3) verified against an independent Lyapunov solve in four mode sets (on-line, off-line, collision, generic), deviations `4e-16` to `8e-15`, together with `Tr(WG) = 1` and `ell(rho_inf) = 1/mu`. The warning that `omega_nm` are expansion coefficients and **not** `<e_n, Omega e_m>` is essential and correct.

**VERDICT T2(c): VALID.** I re-derived formula (4) analytically (`-i conj(B) B' = sum 2 Im z_k/\|tau - z_k\|^2`, then the two-Lorentzian convolution `pi(p+q)/(pq((a-b)^2+(p+q)^2))`) and verified it numerically for `N = 2, 3, 4` to `2e-11`; the `H-SYM` reflection `(x,y) -> (x, 1/2 - y)` does put at least half of each unit-`x` window into `1/4 <= y < 1/2`, the `H-ZCOUNT` pigeonhole is correct (a uniform per-interval bound would force `N_+(T) = O(T)`), each summand is then `>= 1/2` (worst value found `0.824`), and `Omega_infinity = sum 2^{-r}\|f_r><f_r\|` is a genuine density (positive, trace class, trace exactly `1`) with `mu >= sum 2^{-r} 4^r = infinity` by Tonelli. It uses exactly `H-MODEL`, `H-SYM`, `H-ZCOUNT` and nothing else, and the observation that it survives under RH is right — under RH no *mode-diagonal* rebound can have infinite mean (`mu = 2` always), so leaving that family is forced.

**VERDICT T3(a): VALID.** The orientation `L(\|e_n><vac\|) = -i conj(w_n) \|e_n><vac\|` is correct and I confirmed it for four rebound types including one with vacuum–`K` coherences (deviation `1.8e-15`); it agrees with the numerics lane's independent finding, so the brief's assignment is transposed. The odd subspace is invariant for **every** rebound because `ell` annihilates it, and the Jordan-chain remark for multiple zeros (derivative kernels of orders `0..m-1`) is the right correction of the brief (ledger 14).

**VERDICT T3(b): VALID.** Parity covariance `iff P Omega~ P = Omega~` verified exactly: covariant for the two even rebounds, not covariant for the two non-even ones. The proof (no-event part commutes with `P`, `ell(P rho P) = ell(rho)`, and some `rho` has nonzero flux) is complete.

**VERDICT T3(c): VALID.** The vacuum is stationary for every rebound (`B~ vac = 0` and `ell(vac) = 0`); with `Omega~ = 0 (+) Omega` the vacuum mass and the `K` mass are separately conserved, so the stationary set is exactly the segment (6) — `dim ker = 2` confirmed, and `e^{200 L} rho_0` converges to the right point of the segment to `4e-14`. The `q > 0` half is also right and I verified it (`dim ker = 1`, convergence to `\|vac><vac\|` to `3e-15`); the prover's excursion argument is sound and can be tightened in one line: the vacuum mass obeys `d/dt <vac\|rho\|vac> = q ell(rho_t)`, so `int_0^infty ell(rho_t) dt <= 1/q`, i.e. the expected number of events is finite and the last excursion decays by strong stability.

**VERDICT T4(a): VALID.** The separation of "the eigen*operator* survives" (`ell(X) = 0`) from "the eigen*value* survives" (`r_lambda = ell(P_lambda Omega) = 0`) is the correct repair of the brief (ledger 16–17), and the determinant identity (8) with grouped multiplicities is exact — I checked it at `z = 1.3 - 0.4i` in four mode sets (deviations `2e-13` to `2e-5` relative) and, crucially, **at genuine collisions**, which occur even for simple zeros: `d = 4 -> 3`, `d = 3 -> 2`, `d = 2 -> 1` surviving copies, and an engineered off-diagonal/diagonal collision behaves as (8) predicts.

**VERDICT T4(b): VALID.** Off `spec A` the secular equation `1 = mhat_Omega(z)` is exactly right in both directions (`ell(X) = 0` would force `X = 0`), the determinant identity `det(z - L_Omega) = det(z - L_0)(1 - mhat(z))` holds at every off-spectrum point I tested (max relative deviation `2e-9`), and the two refusals — no meromorphic continuation claimed at `spec A`, and `mhat(0) = 1` alone does **not** give a stationary density (the `mu = infinity` case) — are correct and important.

**VERDICT T4(c): VALID.** `-lambda_nm G_mn = q_nm = 2 sqrt(y_n y_m)` verified exactly, hence (10); I checked (10) against a resolvent computation at three complex `z` in four mode sets (deviations `<= 1e-11`). `c = 2 pi i` is the right unnormalised constant. The disclaimer that the "one equals a sum of fractions" shape is the universal rank-one determinant identity and proves **no** Connes–Consani–Moscovici identification is correct, and the numerics lane reached the same conclusion independently.

**VERDICT T5(a): VALID.** Necessity via linear independence of `\|e_n><e_n\|` for distinct modes is correct, and the quantifier structure is exact: equality for every finitely supported mixture on a family forces all widths in that family equal; `H-SYM` (`y = 1/2 - y`) then pins `1/4`. I verified the "iff" in both on-line and off-line models over random weight vectors, including the off-line sub-family supported on the two modes that happen to sit at `1/4`. The honest conclusion — that this adds **nothing** to `prop:functional-model-modes` — is correct and must be preserved.

**VERDICT T5(b): VALID** (of the corrected statement; the brief's version is false). `spec_{!=0}(D_r^{1/2} G D_r^{1/2})` verified to `8e-15` against direct diagonalisation in both models, and I reproduced the prover's worked example to ten digits (`\|G_12\| = 1/sqrt 5`, `0.7236067977`, `0.2763932023`). It is algebraically identical to the numerics lane's independently derived correction. The observation that under RH the *operators* `rho_inf` and `Omega` coincide so their spectra agree — but neither is `{p_n}` — is exactly right.

**VERDICT T5(c): VALID.** The triangular block structure in coefficient coordinates is correct, and the characteristic polynomial (13) is exact — verified as a multiset against the `N^2 x N^2` superoperator spectrum in on-line and off-line models to `2e-14`, and, as an extra probe, at an engineered off-diagonal/diagonal collision to `5e-15`. The root-counting on `(-h^{(j)}, infinity)` and between successive poles is correct, `mhat = (1/2)/(z+1/2)` under RH holds for **every** weight vector (deviation `1e-16`), and (14) is exact.

**VERDICT T6(a): VALID.** The criterion `sigma in D(A)`, `Q_sigma = -A sigma >= 0` is proved in both directions, the flux is strictly positive (else `A sigma = 0` contradicts strong stability), uniqueness of `Omega = Q_sigma/r` is immediate, `mu_Omega = 1/r` follows from T2, and the semigroup form `T_t^0 sigma <= sigma` (with `sigma in D(A)` retained) is correct. The consequence that the support of an admissible density is `C_t`-invariant is right — it is Douglas's range inclusion `ran(C_t sigma^{1/2}) subset ran(sigma^{1/2})`, which the prover asserts without naming; it should be named.

**VERDICT T6(b): VALID.** The orbit-cone description (17) with the finite-flux side conditions `0 < sum c_alpha < infinity` and `sum c_alpha Tr Phi(psi_alpha) = 1` is correct in both directions (`A Phi(psi) = -\|psi><psi\|` plus closedness of `A`), and the insistence that an unqualified "closed cone generated by orbit states" omits the flux condition (ledger 26) is a genuine repair.

**VERDICT T6(c): VALID.** Criterion (18) is exactly `Q_sigma >= 0` — I verified the equivalence on `300` random `sigma` in each of four mode sets (`1200/1200`) — the conjugation correction `a_n = i conj(w_n)` is right and reconciles with the numerics lane's `conj(a_n) + a_m` with `a_n = -i w_n` (identical matrices), the `2x2` determinant `-(y_n-y_m)^2 - (x_n-x_m)^2 < 0` is exact, and (19) held on every admissible sample. The sharp rigidity — an admissible **pure** state must be a mode — is correct and I confirmed both halves (random non-modal superpositions fail positivity by `-0.57` to `-5.75`; modal pure states pass).

**VERDICT T6(d): VALID.** This is the prover's main constructive claim and it survives a from-scratch rebuild. For `N = 3` and `N = 4` with **off-line** zeros (`Im w = 0.31, 0.19, 0.42, 0.11`) and for `N = 4` on-line: each `Q_k = -(B P_k + (B P_k)^*)` is positive semidefinite of **rank exactly one** with `Tr Q_k = 2 sum_{n<=k} y_n` (deviations `<= 9e-16`); `sigma = sum p_i \|u_i><u_i\|` has **exactly** the prescribed spectrum; `Q_sigma = sum (p_k - p_{k+1}) Q_k >= 0` with `r = 2 sum p_n y_n`; `Omega = Q_sigma/r` is a genuine density (positive, trace exactly one); `sigma` is stationary for `L_Omega` to `3e-16`; `mu_Omega = 1/r`; and the rank of `Omega` equals the number of positive differences `p_k - p_{k+1}` in every case tested, so a **flat** target forces a **pure** rebound (ledger 31) and a non-flat one a mixed rebound. No RH is used anywhere. See finding M1 about how this should be worded when registered.

**VERDICT T6(e): VALID.** The infinite-flag construction is correct: `sum k Delta_k = sum d_n = 1` (using `n d_n -> 0` for decreasing summable sequences), `sum Delta_k Tr Q_k = 2 sum d_n y_n in (0,1)`, and closedness of `A` on the partial sums gives `sigma in D(A)` with `-A sigma = Q >= 0` without ever summing the frequencies. I verified the truncations (`K = 10` modes, `beta = 1.5, 2, 3`, on-line and off-line): admissible with `lambda_min(Omega_cand) > 0`, spectrum exactly the renormalised Gibbs list, `r = 2 sum d_n y_n`, and under RH `r = 1/2` and `mu_Omega = 2` **exactly for every `beta > 1`** — confirming the prover's point that the critical obstruction does **not** appear as a diverging holding time. The `beta = 1` answer (the candidate is not trace class at all, which is `prop:normal-kms-gibbs`'s trace-normalisation obstruction) is correct, and is a *different* question from the numerics lane's `beta > 1` positivity obstruction for a prescribed eigenbasis; both should be registered (finding m3). The arithmetic identification is honestly left OPEN.

**VERDICT T7: MINOR.** Every clause is individually accurate — the unique attracting mixed state on `K`, the non-uniqueness on the graded space, the prescribed spectrum by inverse construction, the possibly-pure rebound for a flat target, the absence of any arithmetic selection of `Omega`, and the refusal to claim the infinite even spectrum — but the headline that `obs:complementary-halves` is "false as literally stated" is obtainable by a trivial direct sum and the paragraph does not say what the construction adds over that (finding M2). Fix: say that the mixed stationary density lives on `K` itself, with the cusp flux as its rebound, and that this is what a direct sum does not give.

**Tally: 21 VALID, 2 MINOR (C4, T7), 0 INVALID.**

---

## Recommendation for registration

### Can be registered as **proved**

The following are verified line by line here and, where finite, checked numerically by this lane independently of the prover and of the numerics lane:

* **C1, C2, C3** → `obs:model-modes-sign` (already registered, `sketched`) and `lem:exit-one-dimensional` (already registered, `sketched`). Both can be promoted to **proved**. `lem:exit-one-dimensional` should print `D(B^*) = K cap H^1` *with no boundary condition* and should state the exit form as a form identity, not an operator identity.
* **T1, T2(a), T2(b)** → `thm:rebound-renewal`: promote to **proved**. Its three parts (conservativity + `D(L_Omega) = D(L_0)`; existence/uniqueness/attraction iff finite mean; the finite modal formula) are all established, with **no** `H-SHW`: the SHW hypotheses are verified, not assumed.
* **T2(c)** → `prop:infinite-mean-rebound`: promote to **proved-conditional** on `asm:zero-reflection-count` (which is exactly `H-SYM` + `H-ZCOUNT`, already registered as `assumed`). Formula (4) is exact; I re-derived and re-verified it.
* **T3(a), T3(b), T3(c)** → `prop:odd-modes-protected` and `prop:graded-stationary-segment`: promote to **proved**. `prop:odd-modes-protected` must print the *corrected orientation* (`\|k_w><vac\|` carries `-i conj(w)`), which two independent lanes agree on. `prop:graded-stationary-segment` must carry both halves: the segment for a rebound inside `K`, and the unique pure-vacuum attractor when the rebound has positive vacuum weight.
* **T4(a), T4(b), T4(c)** → `prop:rebound-persistence-secular`: promote to **proved**, with the determinant form (8) as the primary statement (not the simple-eigenvalue "iff"), because collisions are generic.
* **T5(a), T5(b), T5(c)** → `prop:mode-diagonal-rebound-rh`: promote to **proved**, but as an **observation-grade** statement about RH (finding n8), and it must carry the corrected T5(b): the density spectrum is the Gram spectrum `spec(D_r^{1/2} G D_r^{1/2})`, **never** the weight list `{p_n}` for two or more modes, and `def:bond-entanglement`'s entanglement spectrum is its negative logarithm.
* **T6(a), T6(b), T6(c)** → `thm:rebound-admissibility`: promote to **proved**. Cite Douglas for the range-inclusion step.
* **T6(d)** → `thm:flag-spectrum-attainable`: promote to **proved**, *restated in the generality it actually has* (finding M1): any strongly stable one-exit no-event semigroup with an invariant flag of eigenvectors. I rebuilt it for `N = 3, 4` with off-line zeros and the spectrum is exactly prescribed, the rebound is a genuine density, and stationarity is exact.
* **T6(e)** → `prop:gibbs-flag-isometry`: promote to **proved** for the construction, **open** for the arithmetic identification, as the prover already scopes it. Add the numerics lane's `\|H_ab\|` necessary condition and its "commutes with `H` and non-degenerate ⟹ never admissible" corollary as a separate registered row: I re-derived it independently and it is correct.

### Stays **sketched**

* **T2(a)'s Blackwell paragraph**, until the shorter direct-Riemann-integrability argument (finding m4) replaces the step-function sketch. This does not hold up `thm:rebound-renewal` as a whole, whose existence/uniqueness half is complete; only the "every initial state converges" clause is sketch-grade in infinite dimension.
* **T7** → `obs:rebound-both-halves-scope`: stays **sketched** until it is reworded per finding M2.

### Stays **open**

* Any identification of the flag isometry `U` with the Bost–Connes representation, its observables, its dynamics, or a critical (`beta = 1`) KMS state. Both lanes agree; `conj:phantasm-both-halves` is untouched in its substantive sense.
* Whether `\|C_t\| = 1` on `K_Theta`. Neither lane proves it, the finite Gram models **refute** its finite shadow, and nothing in T1–T6 needs it.
* Whether the secular shape is more than the universal rank-one determinant identity. Both lanes say there is no evidence; that should be recorded as a **dead end**, not an open lead.

### Ledger corrections of the brief: which I confirm

I confirm **all 32** ledger rows, with the following qualifications:

* **Row 1 (C-SIGN):** confirmed *and already applied* to `prop:functional-model-modes`; the ledger's line citation is now stale (finding n1).
* **Row 5 (C4-norm):** confirmed, but the row should absorb the numerics lane's stronger finding that `\|C_t\|_2 < 1` for every `t > 0` in every finite model.
* **Row 10 (T2-vacuum):** confirmed, and it **overrules** the numerics lane's contrary reading (finding n3).
* **Row 23 (T5-entanglement):** confirmed by byte-check against `02c_definitions_phantasm.tex:69–76`.
* **Row 28 (T6-spectra):** confirmed, and this is the single most valuable correction in the file: it converts the numerics lane's "load-bearing failure" into a solved problem.
* **Row 30 (T6-critical):** confirmed as far as it goes, but incomplete — it answers the `beta = 1` question and leaves the `beta > 1` prescribed-eigenbasis positivity obstruction to the numerics lane (finding m3).
* **Row 32 (T7-both-halves):** confirmed, but under-stated; see finding M2.

### Existing notebook rows that must be amended

1. **`prop:functional-model-modes`** — the sign amendment has already been made (`db/claims.tsv:20`, `04_riemann_channel.tex:71–76`) and is **correct**: `Z_t k_lambda = e^{i t conj(lambda)} k_lambda`, modulus `e^{-(1-sigma)t/2}`. It should now be promoted from `sketched` on the strength of C1 + `obs:model-modes-sign`, and its `note` field should record that the kernels are eigenvectors of the *adjoint* compression, which is the source of the original error.
2. **`obs:complementary-halves`** — the amendment has already been made (`04c_phantasm_channels.tex:219–222`) and is **adequate but incomplete**. Literally, the observation is now false, and the amendment correctly dates the old claim ("As of 2026-09-14"). It must additionally say what the direct sum of the vacuum-decay and prime-chain generators would *not* have given, namely a mixed stationary density on `K` itself whose rebound is the cusp flux (finding M2). Its status should stay at `sketched` until then, and its `deps` should gain `thm:flag-spectrum-attainable` and `prop:odd-modes-protected`.
3. **`prop:vacuum-renewal-trivial-entanglement`** — no change to the statement, but its `note` should record that it is an **absorbing** boundary case with a defective holding law, *not* the `mu = infinity` case of `thm:rebound-renewal` (finding n3), so that future rows do not repeat the numerics lane's reading.
4. **`num:rebound-state`** (the numerics row) — should be amended to flag the two places where the numerics lane's prose is wrong even though its numbers are right: the `Im w = 0` reading of the vacuum (check 126, finding n3) and the "persists iff `Omega_k[n,m] = 0`" phrasing of T4(a) (checks 90–94, finding n4).
5. **`thm:vacuum-decay-finite`, `prop:absorbing-vacuum-channel`** — no amendment required; T3(a) reproduces their odd-sector content with the corrected orientation, and I confirmed the orientation independently.
