REVIEWER: claude:opus

# Adversarial review of `notes/selberg/astra-proofs.md`

Date: 2026-09-12. Stance: REFUTE — the reviewer's job was to find a reason each
proposition fails, and to default to INVALID under uncertainty. Target:
`notes/selberg/astra-proofs.md` (805 lines, author `codex:gpt-6-astra`), brief
`notes/selberg/astra-brief.md`. Cross-check material: `notes/extract/selberg-sources.md`
(byte-cited arXiv TeX), `scripts/selberg_lindblad.py` + `outputs/selberg_lindblad.txt`
(independent worker), `notes/riemann-channel-note.md` §5 (the audited notebook passage).

**Outcome of the refutation attempt: it failed.** Every one of the thirteen
propositions re-derives correctly, every disputed constant and sign checks out
against my own algebra and numerics, and every one of the author's twelve listed
"corrections" to the orchestrator's drafted statements is right and is neither an
over- nor an under-correction. What remains are provenance gaps and cosmetic
imprecisions, listed below.

---

## 1. Issues

No BLOCKER issues. No MAJOR issues. The MINOR issues below are recorded in
descending order of consequence; none of them changes a verdict.

### M1 — H-SZ's zero divisor is not byte-checkable from the current extract
*Location:* `astra-proofs.md` T4.2, hypothesis **H-SZ**; cross-check
`notes/extract/selberg-sources.md` §F2b, §F3a.
*Severity:* MINOR (provenance; blocks promotion of T4.2 from `conditional-on` to `proved`).
*What is wrong:* the extract contains the Selberg zeta **Euler product** verbatim
(`math/0407288:selberg07.tex:2245-2246` and `1606.04560:zazi.tex:117-119`), and the
author's `Z_Sel` matches both character for character. It contains **no quote at all**
for the substantive half of H-SZ: entireness on `C`, the spectral divisor
`sum_j ([1/2+ir_j]+[1/2-ir_j])`, the trivial divisor `(2g-2) sum_n (2n+1)[-n]`, order
`2g-1` at `s=0` and simplicity at `s=1`. The extract's closing claim "Nothing marked
[NOT FOUND]" refers to its own five target facts, which do not include this divisor.
*Fix:* add a citation (Hejhal, LNM 548 vol. I, or Iwaniec GSM 53 Ch. 10 — neither on
arXiv, so record them as named-not-quoted as the extract already does for Hejhal), or
mark H-SZ `[NOT FOUND]` in the provenance table.
*Mitigating check I ran:* the author's divisor is self-consistent with the one zeta
fact that **is** quoted. From `ord_{s=0} Z = 2g-1` and `ord_{s=1} Z = 1` and the quoted
identity `zeta_R(s) = Z(s)/Z(s+1)` (`1606.04560:zazi.tex:117-119`) one gets
`ord_{s=0} zeta_R = (2g-1) - 1 = 2g-2 = |chi(Sigma)|`, which is exactly the main theorem
of that paper ("vanishes at zero to the order given by the absolute value of the Euler
characteristic"). This simultaneously confirms the trivial-zero normalisation
`(2g-2)(2n+1)` **and** the author's orientation convention (see M2). H-SZ is right; it is
merely unsourced here.

### M2 — H-GUI: two convention differences against the quoted source, both already reconciled
*Location:* T4.1, hypothesis **H-GUI**; source `1306.4203:zeta.tex:260-264`, `:506-508`.
*Severity:* MINOR (no defect; recorded for the reconciliation table).
(a) The source's linearised Poincaré map is `P_gamma := d phi_{-T_gamma}|_{E_s + E_u}` —
**backward** time, so `|mu| < 1` on `E_u`. The author computes the **forward** map
(`T3.1 <1>1`, `Ad(a_t^{-1})`). The author flags this in `T3.1 <1>3` ("Reversing the flow
interchanges the two multipliers, so this absolute determinant is unchanged") and it is
correct: `|det(1-P^k)| = 4 sinh^2(k l/2)` either way.
(b) The source sums over **all** periodic orbits with the primitive period `T_gamma^#` in
the numerator; the author writes the same sum with the repetition index `k` explicit.
Identical.
(c) *Genuinely absorbed:* Guillemin's theorem needs nondegenerate periodic trajectories
(Anosov) and the wave-front condition for `tr^flat` to exist (`1306.4203:zeta.tex:270-274`).
H-GUI as phrased ("for this flow and this orbit convention holds") swallows both rather
than stating them. Fix: add "the geodesic flow on a compact hyperbolic surface is Anosov
with nondegenerate closed orbits" as an explicit clause of H-GUI.
*Direction check:* the source's propagator `e^{-itP}` with `P = -iV` equals `phi_{-t}^*`;
the author's `(e^{-tX}f)(m) = f(phi_{-t}m)` with `X f = d/dt|_0 f circ phi_t = (H/2)f` is
the same operator. **The Laplace transform direction is right.**

### M3 — H-LAP: opposite square-root branch to the only quotable source; no quote either
*Location:* T4.2, hypothesis **H-LAP**; nearest source `math/0407288:selberg07.tex:1667,1674`.
*Severity:* MINOR (harmless; record in table).
Marklof fixes `rho_j = sqrt(lambda_j - 1/4)` with `-pi/2 <= arg rho_j < pi/2`, which for
`lambda_j < 1/4` forces `rho_j = -i nu_j`. The author takes `r_j = +i nu_j` and
`r_0 = +i/2`. Immaterial, because every divisor in the file is symmetric under
`r -> -r`, but it is a difference from the source. There is also no byte-quote for the
completeness/discreteness half of H-LAP.
*Favourable note:* the author correctly **tightens** the brief, which wrote the
exceptional range as `i r_j in (0,1/2]`; the half-open endpoint `nu = 1/2` is
`lambda = 0`, the constant mode, not an eigenvalue in `(0,1/4)`. The author uses the open
interval `0 < nu_j < 1/2` and handles `r_0 = i/2` separately. Correct.

### M4 — H-PHI: exact match, no issue
*Location:* T5.1, hypothesis **H-PHI**; source `1607.08053:main.tex:592-594`.
Verbatim agreement:
`phi(s) = sqrt(pi) Gamma(s-1/2)/Gamma(s) * zeta(2s-1)/zeta(2s)`. The author's derived
`phi(1/2) = -1` (T5.1 `<1>2`) matches the same source at `:598`. I reproduced
`phi(1/2 + 1e-12) = -1.0` at 40 digits. **No issue.**

### M5 — undeclared (trivial) premise: unique factorisation and the Dirichlet series for zeta
*Location:* T5.1 `<1>4` ("the absolutely convergent Euler product follows by expanding the
finite prime products"); T1.3 `<1>2` ("`p^a = q^b`, impossible for distinct primes").
*Severity:* MINOR (cosmetic).
*What is wrong:* H-BASE lists Fourier, trace-class, measure-theory and holomorphic
background but **no arithmetic**; H-ZETA lists continuation, `zeta(0)`, the Laurent
expansion, the functional equation, the zero-free line and the boundary estimate but
**not** `zeta(z) = sum_{n>=1} n^{-z}` on `Re z > 1` nor the Euler product. Both are used.
Unique factorisation is declared only inside T1.2's hypothesis block, and is silently
reused in T1.3, T5.1 and T5.2.
*Fix:* add "the fundamental theorem of arithmetic and the Dirichlet-series definition
`zeta(z) = sum n^{-z}` for `Re z > 1`" to H-BASE (or to H-ZETA).

### M6 — "the full cusp distribution C" overstates what C is
*Location:* T5.2 Statement and `<1>2`; §T5 heading; T6.1 ledger row T5.2.
*Severity:* MINOR (terminology).
*What is wrong:* `C = F^{-1} m` is the **scattering-determinant** (`phi'/phi`) part of the
continuous term only. The quoted trace formula
(`1108.5659:main.tex:2128-2129`) has
`C = -(1/4pi) int (varphi'/varphi)(1/2+ir) h(r^2+1/4) dr + (K_0/4) h(1/4)`
with `K_0 = tr Phi(1/2) = phi(1/2) = -1` for `PSL(2,Z)`, and the modular parabolic
contribution carries further terms besides. Calling `C` "the full cusp distribution" is
loose. T6.1's own §-title ("The modular scattering term") is the accurate wording.
*Fix:* replace "full cusp distribution" by "scattering-determinant part of the continuous
term" in T5.2.
*Favourable check the author did not make:* the stipulated factor `-1/2` in
`m = -(1/2)(phi'/phi)(1/2+ir)` is **exactly** the trace-formula normalisation:
`-(1/4pi)(phi'/phi) = (1/2pi) m`, so `C_{MV} = (1/2pi) int m(r) h~(r) dr = <F^{-1}m, g>`
with `g` the Marklof/Momeni-Venkov transform `g(u) = (1/2pi) int h~(r) e^{-iru} dr`
(`math/0407288:selberg07.tex:896`). So the author's `C` is precisely the kernel of the
continuous term against `g` — an independent confirmation of the `-1/2` that the file does
not claim.

### M7 — the Laplace pairing in T4.1 needs H-GUI read as an identity of measures
*Location:* T4.1 Statement and `<1>3`.
*Severity:* MINOR.
*What is wrong:* `e^{-sigma t}` is not in `D((0,infty))`, so
`Lambda_fl(sigma) = int_0^infty e^{-sigma t} Tr^flat e^{-tX} dt` is not literally a
pairing of the distribution H-GUI supplies. The author bridges this correctly ("The
integral means the absolutely convergent Laplace integral of the positive atomic measure
in H-GUI" plus the cutoff argument in `<1>3`), but that reading is a strengthening of
H-GUI as stated ("in `D'((0,infty))`").
*Fix:* state in H-GUI that the identity holds as an identity of locally finite positive
measures on `(0,infty)`; the extension to `e^{-sigma t}` then follows from the author's
own absolute convergence bound.

### M8 — T1.2 `<1>2`: wrong reason given for a correct vanishing
*Location:* T1.2 `<1>2`, "For `log p > b`, the scalar trace of that component is
nevertheless zero by T1.1, owing to cancellation between Fourier modes."
*Severity:* MINOR (cosmetic).
By T1.1 the component trace is `log p * sum_{k in Z} h(k log p)`, which is zero because
**no lattice point `k log p` meets `supp h`** (`h(0)=0` and `k log p > b` for `k>=1`), not
because of a cancellation. The conclusion is right; the gloss is not.

### M9 — T6.1 bookkeeping slips
*Location:* T6.1 `<1>1`; final RESULT line.
*Severity:* MINOR (cosmetic).
(a) "T4.1 invokes H-GUI exactly once" — it is used twice: in `<1>3` to produce the orbit
series, and again in `<1>5` to call `D` a flat dynamical determinant.
(b) RESULT: "T1.1–T1.3's **finite-product** claims" — T1.1 and T1.2 make no finite-product
claim; only T1.3 does.
(c) T4.3's display groups `Tr T^m = sum_gamma sum_k ell_gamma` under "For sufficiently
small `|u|`"; that first identity has no `u` in it.

### M10 — sanity checks are not reproducible from the repo
*Location:* T6.1 "Sanity checks (not proofs)"; `notes/selberg/scratch/` is empty.
*Severity:* MINOR (process).
The brief asked for scratch scripts under `notes/selberg/scratch/`; the author used
"inline, non-writing Python". I re-derived all three quoted numbers independently and
they are **exact** (see §2, computation C5), so this is not a correctness problem — but
the file's numerical evidence cannot be re-run from the repository.

---

## 2. Independent computations run (reviewer's own, from scratch)

Scripts written for this review (they import nothing from the repo):
`notes/reviews/scratch_selberg_t5.py`, `notes/reviews/scratch_selberg_t1_t4.py`.

**C1. T5.1 `<1>1`, the chain rule.** Compared the author's closed form
`phi'/phi(s) = psi(s-1/2) - psi(s) + 2 q(2s-1) - 2 q(2s)` against a 40-digit numerical
`d/ds log phi(s)` at `s = 1/2 + ir`, `r = 0.31, 0.9, 2.7, 11.3`. Residuals
`6.5e-41, 1.6e-41, 2.0e-41, 5.3e-51`. **The factor 2 in front of both `zeta'/zeta` terms
is right, and the derivative is in `s` not `r`.**

**C2. `phi'/phi(1/2+ir)` is real.** `|Im m_auth| <= 2.3e-41` at the same points; and
`m_script(r)/2 - m_auth(r) = 0` to 1e-42. **Reconciles the two normalisations exactly:
the script's `m = -Re(phi'/phi)` is 2x the author's `m = -(1/2)(phi'/phi)`.**

**C3. T5.1 `<1>2`, the functional-equation transfer.** Re-derived by hand:
substituting `q(2ir) = chi'/chi(2ir) - q(1-2ir)` with
`chi'/chi(z) = log pi - (1/2)psi((1-z)/2) - (1/2)psi(z/2)` cancels `psi(ir)` exactly and
leaves `m = a + b`, `a = -log pi + (1/2)[psi(1/2+ir)+psi(1/2-ir)]`, `b = q(1+2ir)+q(1-2ir)`.
Numerically `|a+b-m| <= 3.2e-41`. Also `|a - m_arch_script/2| <= 2.3e-41` at all four
points — i.e. **the author's `a` is exactly half the script's independently-derived
archimedean multiplier**, even though the two were split differently (the script keeps
`psi(ir)` and uses `chi'/chi(s) = log 2pi + (pi/2)cot(pi s/2) - psi(1-s)`).

**C4. T5.1 `<1>3`, the value at the origin.** `m(0) = gamma_E - log pi - 2 log 2 =
-1.95380858207`; numerically `m(1e-6) = -1.95380858206`. **Correct.**

**C5. T5.1 pairing, `h(t) = exp(-t^2/2)`.** Independently:
`<P,h> = 2 sum_n (Lambda(n)/n) e^{-2 log^2 n} = 0.342066654304729763`;
`<A,h> = sqrt(2pi)/2 - (gamma_E+log pi) + int_0^inf [e^{-u} - e^{-u/2}e^{-u^2/2}]/(1-e^{-u}) du
= -0.487137717291990449`; so `-<P,h>+<A,h> = -0.829204371596720212`. Quadrature of
`int m(r) e^{-r^2/2}/sqrt(2pi) dr` gives `-0.829204370038` (residual `1.6e-9`, my
quadrature error). **All three of the author's quoted numbers reproduce to every printed
digit, and `C = -P + A` holds.** In particular the `+1/2` and the **negative** prime sign
are both confirmed.

**C6. Reconciliation of the `+1/2` with the script's `+1`.** Script measures the additive
constant `1.00000000 +- 1.6e-8` and dip amplitude ratio `K = -2G/sqrt(2pi) = -31.9154`
(i.e. `-2 Lambda(n)/n` per atom) for `m_script = 2 m_auth`. Halving: constant `+1/2`,
atoms `-Lambda(n)/n`. **Exactly the author's `C = -P + A` with `A = 1/2 - 1/(4 sinh(|t|/2))`.**
Further: the script's `max|C_arch| = 1.984` on `[0.5,7]`; `2 x 1/(4 sinh(0.25)) = 1.979`.
**The archimedean kernel `-1/(4 sinh(|t|/2))` is confirmed numerically as well.**

**C7. T4.1, the disputed sign.** Toy length list `{1.3, 2.1, 2.1, 3.7}` (the identity is
formal in the lengths). At `sigma = 0.35, 0.9, 2.0`:
`Lambda_fl(sigma) = sum ell e^{-sigma k ell}/(4 sinh^2(k ell/2))` vs `d/dsigma log
prod_{j>=1} Z(sigma+j)`: `0.841110357369793` vs `0.841110357369793` (diff `0`),
`0.325379014351077` (diff `2.5e-31`), `0.0604456460860735` (diff `0`).
**`Lambda_fl = +D'/D`. The brief's `-(d/dsigma) log D` is wrong by a sign; the author's
correction is right and is not an over-correction.** Also verified in the same run:
`D = prod_gamma prod_{m>=0} (1-e^{-(sigma+m+1)ell})^{m+1}` agrees with
`prod_{j>=1} Z(sigma+j)` (`7.4e-32`), and both agree with
`exp(-int_0^inf (e^{-sigma t}/t) Tr^flat dt)` (`6.7e-30`), i.e. T4.1 `<1>5` is right.

**C8. T4.1 transverse expansion.** `1/(4 sinh^2(x/2)) = e^{-x}/(1-e^{-x})^2 =
sum_{m>=0}(m+1)e^{-(m+1)x}`: all three equal `1.3710499549605300409` at `x=0.83`.

**C9. T3.1.** Symbolically, `det(1-P^k) = 2 - 2 cosh(k ell) < 0`, and
`|det| - 4 sinh^2(k ell/2) = 0`, `|det| - e^{k ell}(1-e^{-k ell})^2 = 0`,
`|det| - (e^{k ell}-1)(1-e^{-k ell}) = 0`. **All three forms in the statement agree.**
Matches `outputs/selberg_lindblad.txt` (D).

**C10. T2.2 recomputed from the author's own section.** Independently in sympy, using the
author's `g_{x,y} = [[sqrt y, x/sqrt y],[0, 1/sqrt y]]` (det 1, `g.i = x+iy`):
`g exp(uH).i = x + i y e^{2u}`; `g exp(uE).i = x + y tanh 2u + i y sech 2u`;
`H^2 f = 4y^2 F_yy + 4y F_y`; `E^2 f = 4y^2 F_xx - 4y F_y` (residuals 0);
`W^2 f = 0`; `Omega f = y^2(F_xx+F_yy)`, **`c = 1`**; `(1/2)(H^2+E^2)f - 2 Omega f = 0`;
`(1/2)(H^2+E^2)f = 2y^2(F_xx+F_yy) = -2 Delta F` (residual 0). **The first-derivative
terms do cancel, and the author's warning that projecting tangent vectors would miss the
`E`-curve drift is correct.** Independently matches `outputs/selberg_lindblad.txt` (B),
which uses a completely different route (Iwasawa coordinate pushforward).

**C11. T2.2 brackets.** By hand: `[H,E]=2W`, `[H,W]=2E`, `[E,W]=-2H`; hence
`[H,E^2]=[H,W^2]=2(WE+EW)`, `[E,H^2]=[E,W^2]=-2(WH+HW)`, `[W,H^2+E^2]=0`. **Centrality of
`Omega` and `K`-invariance of `H^2+E^2` follow.** I also verified the author's terse
"rotating the pair `H,E` by `Ad K`" step explicitly: `X_A R_k = R_k X_{Ad(k^{-1})A}` and
`(cH+sE)^2 + (-sH+cE)^2 = H^2+E^2`, so `(H^2+E^2)R_k = R_k(H^2+E^2)`. **Correct.**
Against `1511.04265:AutomorphicBook.tex:2851`: with `H=h`, `E=e+f`, `W=e-f`,
`(1/4)(H^2+E^2-W^2) = (1/4)h^2 + (1/2)(ef+fe)` — **identical to FGKP's Casimir.**

**C12. T2.1 / T2.3 algebra.** `[L,[L,M]] = L^2M - 2LML + ML^2`, so
`-(1/2)[L,[L,M]] = LML - (1/2){L^2,M}`; with `L=-iX` on a commutative algebra
`[L,[L,M_f]] = -M_{X^2 f}`, giving `+M_{(1/2) sum X_j^2 f}`. For T2.3, `L_j = -i A_j` with
`A_j` anti-self-adjoint gives `(-i)^2 = -1`, hence `+(1/2) sum [A_j,[A_j,rho]] =
+(1/2) sum B_j^2 rho`. I verified the HS-skewness `<rho,[A,sigma]> = -<[A,rho],sigma>`
used for `<rho, L rho> <= 0`. **The "two factors of `i`" and the `+1/2` are right.**
Independently confirmed by `outputs/selberg_lindblad.txt` (A) and (C).

**C13. T1.1 Poisson normalisation.** `sum_n hhat(-2 pi n/L) = L sum_k h(kL)` for
`h = e^{-t^2/2}`, `L = 1.7`: `2.5120459397776796335` both sides, diff `0`.
**The `L` and the `2 pi n/L` are in the right places.**

**C14. T4.2 divisor bookkeeping.** For `N = 1..4`, `sum_{n=0}^{N-1} (2g-2)(2n+1) =
(2g-2)N^2` (symbolically true), plus constant-mode contributions `s_*=0, j=N` and
`s_*=1, j=N+1` = 2, total `(2g-2)N^2+2`. At `sigma = 0` only `s_*=1, j=1` contributes:
simple. **Matches the statement.** Strip check: `k=0` nonconstant points have
`Re = -1/2` (real `r_j`) or `-1/2 -+ nu_j in (-1,0)` (exceptional, `nu_j < 1/2` strictly);
`k>=1` gives `Re <= -3/2` (real) or `< -1` (exceptional, strictly); constant-mode and
topological points sit at integers. **`B_0` is exactly the open-strip pole multiset, and
the three-way equivalence holds.** I found no eigenvalue case the author missed:
`lambda_j = 1/4` gives `r_j = 0 in R` and a doubled point at `sigma = -1/2`, consistent
with H-SZ's order `2d` at `s=1/2`.

**C15. Cross-check of T4.2 against the quoted band structure.** `1403.0256:181-191`:
`lambda_{j,m} = -m-1+s_j` with `Spec(Delta) = {s_j(1-s_j)}`. With `s_j = 1/2+ir_j` this is
`-1/2 - m + i r_j`. **Identical to the author's `sigma = -1/2 - k +- i r_j`.** For `j=0`,
`s_0 in {0,1}` gives `lambda_{0,0} in {0,-1}` — exactly the two boundary points the author
excises from the band. **DFG's theorem excludes `C \ (-1 - (1/2)N_0)`, so it says nothing
about the author's integer points; the author correctly declines to identify his "trace
resonances" with Pollicott–Ruelle resonances.**

**C16. T4.3.** By hand: `log det(1-uT) = -sum_m (u^m/m) Tr T^m = -sum_{gamma,k}
u^{k ell}/k = sum_gamma log(1-u^{ell_gamma})`, and with `u = e^{-sigma}`,
`d/dsigma log det(1-e^{-sigma}T) = sum_m Tr T^m e^{-sigma m}` — **the same sign as
`d/dsigma log D`, so the graph counterpart of `D` is `det(1-uT)` and of `D^{-1}` is the
Ihara zeta. The brief's "graph analogue of `D` is `1/det(1-uT)`" is wrong; the correction
is right.** Telescoping: `prod_{q=0}^{Q}(1-e^{-(s+q)l}) / prod_{q=0}^{Q}(1-e^{-(s+1+q)l})
= (1-e^{-sl})/(1-e^{-(s+Q+1)l}) -> 1-e^{-sl}`, giving `zeta_R(s) = Z(s)/Z(s+1)` —
**verbatim the quoted `1606.04560:zazi.tex:117-119` and `:54`.** Also
`D(sigma)/D(sigma+1) = Z(sigma+1)` is immediate.

**C17. Orientation consistency (the factor-2 trap).** The author's global convention
("Opposite directions are not identified") makes `Z_Sel` run over both orientations. I
checked this is forced: if it ran over unoriented geodesics the trivial-zero multiplicity
would have to be `(g-1)(2n+1)` and `ord_0 zeta_R` would be `g-1`, contradicting the
quoted `|chi| = 2g-2`. **The author's convention, H-SZ, `H_*` in `math/0407288:1626-1627`
and `G` in `1606.04560:45-47` are all mutually consistent.**

**C18. T5.2's audit of `notes/riemann-channel-note.md` §5.** I read §5. Its displayed
explicit formula carries `-2 sum_n (Lambda(n)/sqrt n) g(log n)`, its mode expression is
`Tr Z(t) = e^{-t/4} sum_gamma e^{i gamma t/2}`, and `u = t/2`. Propagating:
`-2 Lambda(n) n^{-1/2} delta(u - log n)`, pull back with `delta(t/2 - log n) =
2 delta(t - 2 log n)`, damp with `e^{-t/4} = n^{-1/2}` at `t = 2 log n`, giving
`-2 Lambda(n)/n`. §5 nevertheless asserts weight `+Lambda(n) n^{-1/2}` and then applies
`e^{-t/4}` a second time to reach `Lambda(n)/n`. **The author's audit in T5.2 `<1>3` is
accurate: §5 is out by a factor `-2` and double-counts the damping.** T5.2's refusal to
promote the damping identity to "the cusp term is the trace of the Riemann channel" is
therefore not over-cautious; it is required.

---

## 3. Convention reconciliation table

| Quantity | `astra-proofs.md` (author) | `scripts/selberg_lindblad.py` | Byte-cited source | Verdict |
|---|---|---|---|---|
| Laplacian sign | `Delta = -y^2(d_x^2+d_y^2)`, positive, `lambda = 1/4 + r^2` | works with `y^2(F_xx+F_yy)` unnamed; prints `Omega = +1 x` it | DFG `1403.0256:181-191`: `Delta` positive, `Spec = {s(1-s)}` — **same**. Marklof `math/0407288:1667,1674`: `(Delta+lambda)phi=0`, `lambda>=0` — same `lambda`, `Delta` negative-named. FGKP `1511.04265:409-414`: `Delta = y^2(d^2)` with `Delta f_s = s(s-1) f_s` — **opposite sign name** | consistent; FGKP mismatch flagged in the extract |
| Casimir | `Omega = (1/4)(H^2+E^2-W^2)`, `H=diag(1,-1)`, `E=[[0,1],[1,0]]`, `W=[[0,1],[-1,0]]` | same basis; also `H^2/4 + (X_+X_- + X_-X_+)/2` | FGKP `:2851`: `Omega = (1/4)h^2 + (1/2)(ef+fe)` — **identical** after `H=h, E=e+f, W=e-f` | agree |
| `c` in `Omega f = c y^2(F_xx+F_yy)` | `c = 1` | `c = +1` (exact) | FGKP `:3110` + `:409` (assembled by the extract, not one display) | agree |
| Two-jump Lindbladian | `(1/2)(H^2+E^2) = 2 Omega = -2 Delta` on `Wf=0` | `(1/2)(H^2+E^2) f = 2 y^2(F_xx+F_yy)` | — | agree |
| Compact-direction caveat | `(1/2)(H^2+E^2) = 2 Omega + (1/2)W^2` on all of `M`; `Omega` symbol indefinite | `W f = W^2 f = 0` only on `K`-invariant `f` | — | agree |
| Exceptional branch | `r_j = +i nu_j`, `0 < nu_j < 1/2`; `r_0 = +i/2` | — | Marklof `:1674`: `arg rho_j in [-pi/2, pi/2)` forces `rho_j = -i nu_j` — **opposite branch** | harmless (divisor symmetric) |
| Poincaré map | forward, `Ad(a_t^{-1})`; multipliers `e^{+-l}` | `Ad(a_l)`; multipliers `e^{+-l}` | DZ `1306.4203:506-508`: `P_gamma := d phi_{-T_gamma}` — **backward** | all give `4 sinh^2(kl/2)`; author proves the reversal invariance |
| Flat-trace direction | `(e^{-tX}f)(m) = f(phi_{-t}m)`, `X = H/2` | (determinant only) | DZ `:260-264` + `:490-491`: `e^{-itP}`, `P = -iV`, `= phi_{-t}^*` — **same direction** | agree |
| Laplace variable | `sigma`, `Re sigma > 0`; Selberg variable `s = sigma + j` | — | DFG `:209-211` uses the Laplace transform, poles in the left half-plane, `lambda = s_j - 1 - m` | agree (`sigma <-> lambda`) |
| Orbit index | primitive, **oriented**, both directions kept, same index in every product | — | DZ `1606.04560:45-47` primitive closed geodesics with multiplicity; Marklof `:1626-27` primitive hyperbolic classes | agree (confirmed by `ord_0 zeta_R = 2g-2`) |
| Ruelle zeta | `zeta_R(s) = prod(1-e^{-s l}) = Z(s)/Z(s+1)` | — | DZ `1606.04560:54`, `:117-119` — **verbatim** | agree |
| Selberg product | `prod_gamma prod_{q>=0}(1-e^{-(s+q)l})` | — | Marklof `:2245-2246`, DZ `:117-119` — **verbatim** | agree |
| Selberg **divisor** | entire; `sum_j([1/2+ir_j]+[1/2-ir_j]) + (2g-2) sum_n (2n+1)[-n]` | — | **no quote anywhere in the extract** | M1 |
| `phi(s)` | `sqrt(pi) Gamma(s-1/2) zeta(2s-1)/(Gamma(s) zeta(2s))` | same | `1607.08053:592-594` — **verbatim**; `phi(1/2) = -1` at `:598` | agree |
| Scattering multiplier | `m = -(1/2)(phi'/phi)(1/2+ir)` | `m = -Re(phi'/phi)(1/2+ir)` = **2x** the author's (the log-derivative is real on the line) | MV `1108.5659:2128-29`: `C = -(1/4pi) int (phi'/phi) h~ dr = (1/2pi) int m_author h~ dr` — the author's `-1/2` **is** the trace-formula normalisation | reconciled, factor 2 |
| Fourier pair | `hhat(r) = int e^{-irt}h dt`, `F^{-1}b = (1/2pi) int e^{irt} b dr`, `F^{-1}delta_0 = 1/(2pi)` | windowed cosine `(1/2pi) int e^{-r^2/2G^2} cos(rt) m dr` | Marklof `:896`: `g(t) = (1/2pi) int h(rho) e^{-i rho t} d rho` — same on even `m` | agree |
| Boundary constant | `+1/2` (from the pole of `zeta` at 1) | `+1` measured to `1.6e-8` | — (derived, not quoted) | **`1 = 2 x 1/2`; reconciled** |
| Prime atoms | `-Lambda(n)/n` at `t = +-2 log n`: **DIPS** | `-2 Lambda(n)/n`: **DIPS** (`K = -2G/sqrt(2pi)`, `0.999998` of prediction) | — | **reconciled; sign agreed** |
| Archimedean kernel | `A(t) = 1/2 - 1/(4 sinh(|t|/2))`, `t != 0` | `C_arch -> -1/(2 sinh(|t|/2))`; `max|C_arch| = 1.984` vs `2/(4 sinh(0.25)) = 1.979` | — | reconciled, factor 2 |
| Continuous term completeness | `C` alone | `C` alone | MV `:2128-29` also has `+(K_0/4) h(1/4)`, `K_0 = phi(1/2) = -1` | M6 |

---

## 4. Per-proposition premises and verdicts

PREMISES T1.1: H-BASE (circle Fourier basis and inversion; trace-class criterion for an absolutely summable diagonal operator; integration by parts). NEW: none.
VERDICT T1.1: VALID

PREMISES T1.2: H-BASE; T1.1; unique prime factorisation (declared in this proposition's own hypothesis block). NEW: none. (The positive-time "orbital trace" is a *definition* given in the hypotheses — the statement is then a regrouping, not a Hilbert-space trace theorem; this is exactly what the author claims, and the resulting comb `sum_{n>=2} Lambda(n) delta(t - log n)` IS exact in `D'((0,infty))`, since each component's distributional trace is exact by T1.1 and only finitely many components meet any compact subinterval of `(0,infty)`. The non-trace-class argument — the constant modes give one eigenvalue `int h > 0` of infinite multiplicity, hence non-compactness — is correct, as is the non-temperedness argument via `sum_p log p /(1+log p)^N >= sum_{p large} 1/p = infinity` for every `N`.)
VERDICT T1.2: VALID

PREMISES T1.3: H-BASE; T1.1's Fourier basis; H-ZETA and H-GAMMA (for the disjointness assertion only). NEW: unique factorisation (used in `<1>2` to rule out `p^a = q^b`), declared in T1.2 but not here — cosmetic (M5).
VERDICT T1.3: VALID

PREMISES T2.1: H-BASE (change of variables, integration by parts, invariant domain and `div_mu X_j = 0` as stated in the hypotheses). NEW: none.
VERDICT T2.1: VALID

PREMISES T2.2: H-BASE (unimodularity criterion, Lie-conjugation differential); T2.1; the specified geometric model `G/K = H` with the Möbius action; H-AN (closed heat-semigroup realisation only). NEW: none.
VERDICT T2.2: VALID

PREMISES T2.3: H-BASE (Hilbert–Schmidt completion); T2.1's algebraic identity; the stated common smooth invariant domain. NEW: none. (The spectral-gap paragraph is explicitly a Remark and claims nothing.)
VERDICT T2.3: VALID

PREMISES T3.1: H-BASE; the homogeneous description of the geodesic flow stated in the hypotheses. NEW: none.
VERDICT T3.1: VALID

PREMISES T4.1: H-BASE; H-GEO; H-GUI; T3.1. NEW: H-GUI must be read as an identity of locally finite **positive measures** on `(0,infty)`, not merely as a distributional identity, for the Laplace pairing against `e^{-sigma t}` (M7). The author supplies the absolute convergence that makes this legitimate, but the strengthening is not written into H-GUI.
VERDICT T4.1: VALID

PREMISES T4.2: H-BASE; H-GEO; H-GUI (through T4.1, for the flat-trace reading of `Lambda_fl`); H-LAP; H-SZ; T4.1. NEW: none.
VERDICT T4.2: VALID

PREMISES T4.3: H-BASE; finite matrix algebra and the stated graph conventions (graph part); H-GEO (convergence of the hyperbolic products); H-SZ (meromorphic continuation of the quotient); H-GUI through T4.1 (flat-trace interpretation); the regular-graph Ihara–Bass factorisation, declared as background and used only in the remark `<1>4`. NEW: none.
VERDICT T4.3: VALID

PREMISES T5.1: H-BASE; H-PHI; H-ZETA (continuation, `zeta(0) = -1/2`, Laurent at 1, functional equation, zero-free line, the explicit `log^2` boundary estimate uniform in `0 <= eps <= eps_0`); H-GAMMA (meromorphy, poles, conjugation symmetry, `Gamma(1/2) = sqrt pi`, the digamma integral, digamma vertical growth). NEW: the Dirichlet-series definition `zeta(z) = sum_{n>=1} n^{-z}` on `Re z > 1` together with unique factorisation, used in `<1>4` to produce the Euler product; neither is in H-BASE or H-ZETA (M5) — cosmetic.
VERDICT T5.1: VALID

PREMISES T5.2: none beyond local finiteness for the damping half; T5.1 and hence H-PHI, H-ZETA, H-GAMMA for the scattering identification. NEW: inherits T5.1's M5; `<1>3` additionally quotes `notes/riemann-channel-note.md` §5 as *data being audited*, which the author explicitly declares is not a hypothesis of the corollary. What is identified: the equality of three explicitly defined atomic measures on `(0,infty)`, `e^{-t/4} P_{ch,prime} = P_+ = -(C-A)|_{(0,infty)}`. What is **not**: any identity between the full continuous/cusp distribution and a semigroup trace, any operator or spectral identity, and anything bearing on RH. The author says exactly this.
VERDICT T5.2: VALID

PREMISES T6.1: T1.1–T5.2 only (an audit of the preceding propositions). NEW: none. The twelve corrections in `<1>2` were each re-checked; all twelve are correct, and none over-corrects. The two the brief singled out for scrutiny — item 6 (`Lambda_fl = +D'/D`) and item 10 (negative prime atoms) plus item 11 (`+1/2`) — are confirmed by independent computation (C7, C5, C6). Bookkeeping slips in `<1>1` and the RESULT line are cosmetic (M9).
VERDICT T6.1: VALID

---

## 5. Notes on the specific points the review was asked to scrutinise

- **T1.2, "local orbital, not Hilbert-space".** Correct, and necessary. The obstruction
  is real (a nonzero eigenvalue of infinite multiplicity), and the comb remains exact on
  `(0,infty)` because "orbital trace" is defined as the local sum of the component
  distributional traces, each of which T1.1 supplies exactly. The extra observation that
  the extended-by-zero positive comb is **not tempered** is correct and is a genuine
  addition to the brief.
- **T4.1–T4.2 sign.** `+D'/D`, confirmed to 1e-31 (C7). The brief's minus applies to
  `D^{-1}`.
- **`lambda_0 = 0` and the exceptional eigenvalues.** Handled correctly: `r_0 = i/2`
  gives `sigma = 0, -1`, both on the strip boundary; exceptional `r_j = i nu_j` give the
  two real points `-1/2 -+ nu_j` strictly inside `(-1,0)` and strictly off the midpoint.
  The `j >= 1` qualification in the band is essential and the author says so.
- **Multiplicity at `lambda = 1/4`.** `r_j = 0`, the two divisor points coincide, order
  `2d` at `s = 1/2` and hence multiplicity `2d` at `sigma = -1/2` in `B_0`. Forced by
  H-SZ as stated; consistent.
- **"First band on `Re sigma = -1/2` iff no eigenvalue in `(0,1/4)`".** The equivalence
  holds exactly as stated (C14), and `B_0` really is the whole open-strip pole multiset.
- **T5.1 constants.** Factor 2 from `zeta(2s-1)`/`zeta(2s)`: confirmed (C1). Functional
  -equation transfer of `zeta'/zeta(2ir)` with exact cancellation of `psi(ir)`: confirmed
  (C3). The `+1/2`: confirmed analytically (`F^{-1}(pi delta_0) = 1/2`), numerically
  (C5), and against the script's `+1` at twice the normalisation (C6). Sign: **dips**,
  confirmed both ways.
- **T5.2, what is and is not identified.** See PREMISES T5.2 above. The author's audit
  of the notebook's §5 is independently correct (C18).

---

RESULT: ACCEPT — thirteen of thirteen propositions VALID; no BLOCKER or MAJOR issue found; every disputed constant and sign (`c=1`, `-2Delta`, `4 sinh^2(kl/2)`, `+D'/D`, the negative prime comb, the `+1/2`) independently reproduced, with the only substantive defect being that H-SZ's zero divisor and H-LAP have no byte-cited source in `notes/extract/selberg-sources.md` (M1, M3) and must be cited before T4.2 is promoted past `conditional-on`.

Files written by this review:
- `notes/reviews/selberg-2026-09-12.md` (this file)
- `notes/reviews/scratch_selberg_t5.py` (T5.1: chain rule, reality of `phi'/phi`, functional-equation split, `phi(1/2)`, `m(0)`, the `<A,h>/<P,h>/<C,h>` pairing)
- `notes/reviews/scratch_selberg_t1_t4.py` (T4.1 sign on a toy length list, tower/Laplace forms, `4 sinh^2`, T3.1 determinant, T2.2 from the author's own section, T1.1 Poisson, T4.2 divisor bookkeeping)
