# REFUTE review: two arithmetic cavities with cusps (elliptic curves over F_2 and F_3), their scattering matrices, the zeta identification, and the rebound channel

Reviewer: `claude:opus` (adversarial lane, two-model-family protocol).
Date: 2026-09-20. Prover: `codex:gpt-6-astra`.

## Inputs read (in full)

| file | what it is |
|---|---|
| `notes/elliptic-cavity/astra-brief.md` | the orchestrator's brief (266 lines; C1–C3, T1–T6, deliberately slightly too strong) |
| `notes/elliptic-cavity/astra-proofs.md` | prover output (934 lines; C1–C3, two consistency checks, T1(a)–(e), T2(a)–(d), T3(a)–(c), T4(a)–(d), T5(a)–(b), T6; a 44-row correction ledger; numerics; "what a refuter should attack first") |
| `notes/elliptic-cavity/numerics.md`, `scripts/elliptic_cavity.py`, `outputs/elliptic_cavity.txt` | the blind Opus numerics lane (ledger D1–D35, 192 checks) |
| `notes/elliptic-cavity/sources.md` | the literature lane (942 lines; Lorscheid ×3, Sørensen 2001, function-field Bost–Connes ×3, Bianchi QE, APW, Childs–Gosset; every quote byte-verified) |
| `report/sections/04i_cusp_graph_scattering.tex`, `04j_cusp_graph_renewal.tex`, `04h_rebound_state.tex` | the notebook rows the proofs cite |
| `notes/reviews/cusp-graph-2026-09-20.md`, `notes/cusp-graph/astra-proofs.md` | the previous round (format, finding 7, T3(c)) |
| `refs/src/2603.26443/final_draft.tex` | Arends–Peterson–Weich, read directly (lines 427–458, 852–892, 1892–1935, 1977–2096) |
| `refs/src/2603.26443/serre_example.png`, `takahashi_example.png` | **read directly as images** (see transcription verdict) |
| `refs/src/1203.6557/levinson2.tex` | Childs–Gosset (lines 320–380, 460–515) |
| `refs/src/sorensen-2001/paper.txt`, `refs/src/1012.4825/elliptic.tex`, `1012.3513/hecke.tex`, `1012.3223/main.tex` | the new sources, read at the quoted lines |

## Scripts written by this lane (independent; nothing imported from `scripts/elliptic_cavity.py`)

| script | checks | covers |
|---|---|---|
| `notes/reviews/scratch_ec_diagram.py` | **417** | diagrams rebuilt from the PNGs and the brief; degrees; the **Euler–Poincaré mass formula against `zeta_K(-1)`** (new); C1 in all three normalisations; `p`, `ptilde`, `R` exactly; the matching expansion (0.8); `det H(mu)` and the Laurent constants; `ord_0 p`, (0.6); `gcd(p,ptilde)`; cusp forms; `l^2` spectrum; the matrix `S` from `Gamma`, from a **direct generalised-eigenfunction solve**, from Childs–Gosset's `-Q(z)^{-1}Q(1/z)`, and from the Schur form; unitarity/FE/reality/symmetry; the Klein blocks and (2.4); the height congruence (2.6) and the **uniqueness of `a-b = -1`**; the **exact Sørensen group-matrix test**; thresholds and (0.7); the Levinson winding; the product identity and its genus obstruction; both zeta identities exactly; the funnel determinant (5.1) on APW's two examples for `q = 2,3,5`; Sørensen's `h[2]` formulas |
| `notes/reviews/scratch_ec_model.py` | **137** | `Theta_2 = -R_2 B_bd` exactly and inner; the Hardy model built from scratch (companion matrix + Gram from the Taylor series of `1/qq`); `dim K`, `spec Z`, `Z^m -> 0`, `I - Z^*Z = jj^*`, telescoping, `\|\|j\|\|^2 = 1 - \|Theta(0)\|^2`, the size-2 Jordan block; the prover's explicit `Z` of (4.4), its defect vectors, char poly, and the transfer identity (4.5); the Gram entries `3/5, 2/5, 1/10`; `Omega_J` (law, purity, uniqueness, attraction, `P_HW j = 0`); the APW-convention `K_HW` version (`3/4`, law, mean `7/3`, spectrum `{1/7,1/7,1/7,4/7}`); (4.6), (4.7), the equal-weight density spectrum, the three angles; mode-diagonal means `2+sqrt2` and `1/(1-3^{-1/2})`, exit rate `1-q^{-1/2}`; the cone (4.8), parity+reality, the rank-4 obstruction; CPTP, (3.8), both T3(b) counterexamples, the C3 counterexample, flags, vacuum protection; the D3 even/odd model blocks |
| `notes/reviews/scratch_ec_convention.py` | **43** | **moving the core/cusp cut** along the ray (0,1,2,3 steps) and its effect on `c^2`, `ord_0 p`, `N`, `R`, the nonzero divisor, `[z^m]p`; and the identification of the T2(d) height shift with the **equal-depth cut** of the four D3 cusps |

**597 checks, 0 failures.** Every number below is from these scripts unless labelled as the prover's or the numerics lane's.

## Transcription verdict (I read both PNGs myself)

`serre_example.png`: top row `6 --2-- 2 --2(dashed)-- 4 --4-- 8 ...`; hanging from the second vertex, `2 --2-- 2 --1-- 1` and then `--1-- 3`, `--1-- 3`. That is H-DIAG-2 exactly.
`takahashi_example.png`: spine `12 --6-- 6 --2-- 2 --2-- 4`, `48` above the left `12` by an edge `12`, `8` above the central `2` by an edge `2`, `2 --2-- 6 --6-- 12` downward with `48` at its left by an edge `12`; dashed cusp edges `12 --12-- 36 --36-- 108` at each `12`, and **twice** `4 --4-- 12 --12-- 36` at the `4`. That is H-DIAG-3 exactly.
**Both diagrams are transcribed correctly, vertex for vertex and edge for edge**, confirming the numerics lane's §0 independently.

## New independent check on H-DIAG: the Euler–Poincaré mass formula

Neither lane tested the **absolute** stabiliser sizes (`sources.md` §1b explicitly records that Lorscheid's figure only confirms the ratios `S(v)/S(e)`). I did. For a graph of groups,
`chi = sum_v 1/S(v) - sum_e 1/S(e)` (both sums over the full infinite diagram), and:

| | `sum_v 1/S(v)` | `sum_e 1/S(e)` | `chi` | `-zeta_K(-1) = -P(q)/((1-q)(1-q^2))` |
|---|---|---|---|---|
| D2 | `10/3` | `5` | `-5/3` | `-5/3` |
| D3 | `7/4` | `7/2` | `-7/4` | `-7/4` |
| Nagao, `GL_2(F_q[T])`, any `q` | — | — | `-1/((q-1)^2(q+1))` | `-1/((1-q)(1-q^2))` |

Exact in all three cases (the Nagao one symbolically in `q`, from `S(v_0) = (q^2-1)(q^2-q)`, `S(v_n) = (q-1)^2q^{n+1}`, `S(e_0) = (q-1)^2q`, which also reproduces the junction `c^2 = q+1` of 04i C1). Genus 0 and genus 1, two different curves, two different `q`: the transcribed stabiliser sizes satisfy Serre's mass formula on the nose. **This is a real arithmetic test of H-DIAG-3 that no source in `sources.md` supplies**, and it passes.

## Citations byte-checked

**External (the prover cites exactly two papers; all 16 line addresses resolve).**
`2603.26443:final_draft.tex` — `:442` (the adjacency `Af(v) = sum S(v)/S(e) f(o(e))`) ✔; `:445` (`prop_self_adjoint`: bounded degree ⟹ bounded self-adjoint) ✔; `:452` (the **measure kernel** `K(v,w) = S(v)S(w)/S({v,w})`, which is exactly the distinction ledger 2 turns on) ✔; `:1899` (`B(mu)` with `c_v sqrt q/mu + f_v/(sqrt q mu)`) ✔; `:1907` (`H(mu)`) ✔; `:1911` ("The resonances are exactly those `mu_0 in C^x` such that `det(H(mu_0)) = 0`") ✔; `:1915` — **contains both outgoing conditions verbatim**, `f(w) = sqrt q/mu_0 f(v)` on a cusp and `f(u) = 1/(sqrt q mu_0) f(v)` on a funnel ✔ (the previous round's sources note was right); `:1928` (the funnel tree `H = -(mu - q^{-1}mu^{-1})/2`) ✔; `:1983` (Takahashi Theorem 5) ✔; `:2008`, `:2056` (the two displayed polynomials) ✔; `:2030` (`P(mu^2)` and the `q^{-1/4}` circle) ✔; `:2058` (the `mu = ±1` resonances of multiplicity 2, not `l^2`) ✔; `:2088` (the paragraph introducing the four-type expectation) ✔. **One slip:** `:2085` is cited for "APW's expectation"; line 2085 is the symmetric-space sentence `(H^2)^r x (H^3)^c`, and the four-type list is at `:2090`–`:2093`. Finding 8.
`1203.6557:levinson2.tex` — `:365` (`S(z) = -Q(z)^{-1}Q(z^{-1})`) ✔; `:369` (`Q(z) := 1 - z(A + B^dag (1/z+z-D)^{-1}B)`) ✔; `:511` (`w(det S) = 2(m - n_b - n_c - n_h/2)`) ✔.
`cd refs && sha256sum -c --quiet manifest.sha256` → **MANIFEST OK** (258 lines).

**Notebook (12 claim ids + 15 line addresses, all resolve).** `thm:cusp-wave-contraction` (`04i:197`), `prop:pure-cusp-scattering` (`04i:100`), `def:cusp-diagram` (`04i:25`), `04i:46` (the Nagao normalisation sentence), `thm:cusp-renewal-discrete` (`04j:38`), `prop:cusp-modal-rh` (`04j:48`, `:55`), `thm:cusp-admissible-flags` (`04j:61`), `prop:cusp-odd-protected` (`04j:73`), `obs:cusp-no-graph-vacuum` (`04j:82`), `prop:cusp-radius-product` (`04j:110`), `obs:cusp-arith-next` (`04j:161`), `prop:graded-stationary-segment` (`04h:130`, the `\label` line), `prop:mode-diagonal-rebound-rh` (`04h:160`), the Gram-spectrum line `04h:171`, `notes/cusp-graph/astra-proofs.md:449`. Eleven ids are in `db/claims.tsv`; `def:cusp-diagram` is in `db/definitions.tsv:88` (correct — it is a definition, not a claim). `04h:130` is the `\label`, the environment opens at `:129`; the previous review cited `:129`. Cosmetic.

**The prover cites none of the seven new sources of `sources.md`.** See finding 1.

---

## Verdicts

**VERDICT C1: VALID.** The correction is right and it is a real catch. `U f(v) = f(v)/sqrt(S(v))` gives `UAU^{-1} = D^{-1/2} A D^{1/2}` for the **coordinate** adjacency `A_{vw} = S(v)/S(e)`, whose `(v,w)` entry is `sqrt(S(v)S(w))/S(e)`; the brief's `D^{-1/2}AD^{-1/2}` is correct only for the **measure kernel** `S(v)S(w)/S(e)` of `2603.26443:452`. I verified all three forms exactly on both diagrams: `D^{-1/2}A_X D^{1/2}/sqrt q = T_X` ✔, `D^{-1/2}(measure kernel)D^{-1/2}/sqrt q = T_X` ✔, and the brief's form is **not** `T_X` (it is not even symmetric, since `A_X` is not). `c^2 = S(v)S(c_1)/(q S(e)^2) = S(v)/S(e)` because the degree condition at `c_1` forces `S(c_1) = q S(e)` — verified for all five junctions (D2: `4 = 2·2`; D3: `36 = 3·12` twice, `12 = 3·4` twice), so every junction has `c^2 = 1` and `C_QQ = 2`. The displayed `T_2` (with `a = sqrt(3/2)`, `b = 1/sqrt2`) and `T_3` (with `d = 2/sqrt3`, `e = sqrt(2/3)`) match my independently built matrices **entry for entry**, as do `C_2 = diag(0,1,0,0,0,0)` and `C_3 = diag(0,1,0,0,0,0,1,0,2)`. The Nagao `c^2 = q+1` I re-derived from the actual `GL_2(F_q[T])` stabilisers. Note this refutes the *prose* of `numerics.md` §1 and the header of `scripts/elliptic_cavity.py:22` (see adjudication row 1) — though not their computations.

**VERDICT C2: VALID.** Every clause. `S(z) = (z Gamma - I)^{-1}(I - z^{-1}Gamma)` is algebraically identical to `-Q(z)^{-1}Q(1/z)` with `Q = I - z Gamma`, and I checked it against a **direct generalised-eigenfunction solve** (core + `h` rays, the ansatz imposed at the junction and the first ray equation) at 8 points per diagram: max deviation `< 1e-9`. Symmetry `S = S^T`, `S(z)S(1/z) = I`, `S(conj z) = conj S(z)`, unitarity on `|z| = 1` (5 circle points each): all `< 1e-9`. `det S = (-1)^h p/ptilde` at 8 points each: `< 1e-8`. The Schur form `S = -I + (z^{-1}-z)W^*(lambda - T_X - zC)^{-1}W` agrees: `< 1e-8`. (0.3) `det H(mu) = (-1)^n(2mu)^{-n}p(mu)` is an **exact symbolic identity** for both diagrams, and so are the two Laurent constants `F_2/(128 mu^4)` and `-F_3/(1536 mu^5)`. On the Childs–Gosset reconciliation the prover is **right and the numerics lane is wrong** (adjudication row 2): CG's line 332 says `|1,j>` is "the vertex where the `j`th line connects to the graph", and the displayed matrix equation puts amplitude `z^{-1}delta + zS` there and `z^{-2}+z^2S` at `x = 2` — i.e. CG's `n` boundary vertices are the notebook's `r_1^{(a)}`, with `A = 0`, `B = W`, `D = T_X`, `m = n_core`. With that reading the two `S`-matrices are *equal*, not off by `z^2`, and the lane's caveat "CG attach at most one tail per vertex, so D3 is outside their setup" also dissolves (two tails at `Q` give two distinct boundary vertices). I verified `-Q(z)^{-1}Q(1/z) = S` numerically on **both** diagrams including D3.

**VERDICT C3: MINOR.** Everything computed is correct; one load-bearing framing defect.
*Correct.* `X_c = X_b^perp` is the sum of core eigenspaces killed by `W^*`, and "an eigenvector orthogonal to the attachments is already a cusp form, with no extra class for `h > 1`" is right — D3's third kernel vector fails to vanish at `Q`, consistent with T2(c) and with numerics D11. The reality argument for `beta`, the unit-circle argument (`Im(bar z)·||W^*x||^2 = 0`), and the Schur-elimination resolvent identity all check out. **The multichannel counterexample is exactly right**: `T_X = 0_{2x2}`, `C = diag(5/4,5)` gives `S = diag(-(z^2-1/4)/(1-z^2/4), -(z^2-4)/(1-4z^2))`, `p = ptilde = z^4 - (17/4)z^2 + 1`, `det S = 1`, a **zero** at `±1/2` in the `c^2 = 5/4` channel and a **pole** at `±1/2` in the `c^2 = 5` channel — I re-derived both entries from `Gamma` (the displayed pair is listed in the order opposite to `C = diag(5/4,5)`: and I re-derived both entries from `Gamma`). `p(0) = prod(1 - C_vv) = 0`; the Schur formula (0.6) gives `U = span(B)`, `rank(I-C) = 5`, bracket `-1` for D2 and `U = span(L1,Z)`, `rank 7`, `det A_V = -1`, bracket `-I_2` for D3, hence `ord_0 p = 2` and `4`, and in both cases `[z^m]p = det A_V · det(bracket) = -1` exactly. `T_UU = 0` (no loops, attachments pairwise non-adjacent) verified — the prover's `(0.6)` and numerics D7 are the same statement, his sharper. `gcd(p,ptilde) = (z^2+1)^2` (D2) and `(z^2+1)^2(z^2-1)^2` (D3). (0.7) verified at `z = ±(1-1e-5)` and `±(1-1e-7)`: `S(eps) = (-1) (+) (1) (+) (-eps sigma_x)`, eigenvalues `{+1,+1,-1,-1}`, `tr S = 0`. `N_2 = 12-4-2 = 6`, `N_3 = 18-4-2-4 = 8` match a direct root count in the disc after removing the gcd, and the windings `4` and `6` match a 400001-point contour integral of `det S` and Childs–Gosset `:511`.
*Why MINOR.* C3 presents `ord_0 p` (hence `N`, hence `dim K`) as a property **of the diagram**. It is not: it is a property of where the core/cusp cut is placed. Because `S(c_k) = S(c_k c_{k+1})` on every ray, `c^2 = 1` is preserved when you move the cut outward, and I verified on D2 that absorbing `k` ray vertices into the core gives
`c^2 = 1`, `ord_0 p = 2(1+k)`, `N = 4 + 2(1+k)`, `R_new = z^{2k} R_old`, with the nonzero divisor (`±sqrt2`, `(z^2+1)^2`, `2z^4-2z^2+1`) and `[z^m]p = -1` **invariant** (`k = 0,1,2,3`). APW's restriction to `mu in C^x` is exactly the quotient by that choice, and they say so: `2603.26443:1922`, "`det H(mu)` ... can be written in the form `mu^k f(mu)` for some `k in Z`". Fix: state `N` and `dim K` relative to the declared cut, and make the cut-invariant count (`4` for both diagrams) the headline. Finding 2.

**VERDICT CK1: VALID.** Every core vertex of D2 has degree `3` and of D3 degree `4` (I recomputed all 15, including the junction contributions); at `c_1` the condition is `S(c_1)/S(e) + 1 = q+1`, i.e. `S(c_1) = qS(e)`, satisfied at all five junctions; at `c_k`, `k >= 2`, it is `q + 1` identically. Finite volume: `10/3` and `7/4`. The prover's degree tables are correct entry by entry. I add the mass formula above as a second, arithmetic consistency check the prover did not have.

**VERDICT CK2: VALID.** The forest matching expansion (0.8) is right (in a permutation expansion a forest admits only fixed points and disjoint edge transpositions; the transposition `(ij)` contributes `-(zT_{ij})^2`), and substituting it reproduces `p_2` and `p_3` **exactly** — I implemented the sum over all matchings independently and got zero polynomial difference in both cases. The elimination certificate for D2 (edge squares `3/2, 1/2, 1, 3/2, 3/2`, unmatched diagonal `1+z^2` except `z^2` at `B`) is correct. The Laurent relation to APW's displays is exact, and the numerics lane's equivalent form `p = z^m F/q` with `m = ord_0 p` also holds, with `lc(F) = q` in both cases.

**VERDICT T1a: VALID.** `p_2 = (z^2/2)(z^2-2)(z^2+1)^2(2z^4-2z^2+1)`, `ptilde_2 = -(1/2)(z^2+1)^2(2z^2-1)(z^4-2z^2+2)`, `R_2 = z^2(z^2-2)(2z^4-2z^2+1)/((2z^2-1)(z^4-2z^2+2))`: all three **exact** (zero polynomial difference), computed in the integer variable `t` with `z = sqrt q t` and independently from (0.8). `p` monic of degree `12`, `ptilde(0) = 1`, `ptilde = z^{2n}p(1/z)`.

**VERDICT T1b: MINOR.** The mathematics is exact and the hypothesis discipline is right; one omission.
*Correct.* `N_1 = 1` (no affine `F_2` points — I enumerated), `a = 2`, `P(T) = 1-2T+2T^2`. The general identity (1.2) `zeta_K(2s-1)/zeta_K(2s) = [P(t)/P(t/q)]·(1-t/q)/(1-qt)` with `t = z^{-2}` is an exact rational identity (verified symbolically in the variable `T = q^{-s}` for both `q = 2` and `q = 3`), and (1.3) `1/R_2 = z^{-2} zeta_K(2s-1)/zeta_K(2s)` is an **exact identity of rational functions**, not a sample agreement. The prover's three displayed table values are correct to `1e-12`.
*Why MINOR.* "The pattern `q^{1-2gs}` fits these two genus values" is displayed as *the* pattern. So does `q^{1-2s}`, which fits `g = 1` and differs from `q^{1-2gs}` only at `g != 1` — the numerics lane's D16 makes exactly this point, and `sources.md` Target 2b adds a third constraint from `1012.3223:573`/`:187` (the adelic factor is `chi^2(fc)|fc|^{2s}` with `deg fc = 2g-2`, a monomial whose exponent is *affine in `g`*, in a different normalisation). Marking it H-EIS is right; displaying one of at least two equally-fitting one-parameter families without saying so is a fixable defect. Fix: one sentence, plus the Lorscheid citation. Finding 4.

**VERDICT T1c: VALID.** Bound parameters `±1/sqrt2`, `lambda = ±3/sqrt2 = ±2.1213203`, the Perron vector `1/sqrt(S(v))` (I verified `Tv = ((q+1)/sqrt q)v` including the cusp coupling term). `dim ker T_X = 2`; the displayed basis `(1/sqrt2, 0, -sqrt(3/2), 0, 1, 0)` and `(1/sqrt2, 0, -sqrt(3/2), 0, 0, 1)` are in the kernel and **both vanish at `B`**, so both are cusp forms and the factor is `(z^2+1)^2`. The correction of APW's phrasing is right and important (ledger 11): `±i` each carry multiplicity two of the *same* two-dimensional `lambda = 0` eigenspace, not four eigenfunctions. The four resonances satisfy `z^2 = (1±i)/2 = 1/alpha`. `sigma T sigma = -T`; in the model `sigma Z sigma = -Z` (verified: `sigma = diag((-1)^k)` is an isometry because `p` and the Blaschke denominator are even). `spec(Z^2|K_HW) = {(1-i)^{-1}, (1+i)^{-1}}` each twice, and `Z ~ [[0, F^{-1}],[I,0]]` gives `Z^2 ~ F^{-1} (x) I_2`. The hedging ("a spectral similarity, not a canonical identification with étale cohomology, an isometry in the energy metric, or an arithmetically constructed square-root functor") is exactly the right amount, and "bipartite pairing is exactly the two square roots; it is not a further doubling" is correct.

**VERDICT T1d: VALID.** `|z| = 2^{-1/4} = 0.8408964152537145` to `1e-14` on all four roots; `arg z = ±22.5°, ±157.5°`; the displayed root `0.776886987015019 + 0.321797126452791i` matches to `1e-12`. The disclaimer that this is RH **only on the four-dimensional Hasse–Weil sector**, not on the six-dimensional model, is correct and is the honest reading of `2603.26443:2030`.

**VERDICT T1e: VALID.** The refutation of the previous round's finding 7 is correct: that finding's premise was `c^2 = q+1`; D2 has `c^2 = 1`, so `p(0) = 1 - c^2 = 0` and `prop:cusp-radius-product` is vacuous there — and the report already anticipates this (`04j:110` says "or more than one cusp, or a smaller junction weight"). (1.4) `prod_{z_i != 0} z_i = (-1)^m a_m` is right for monic `p` of even degree, and (1.5) is right: with the junction first, `p = det M_0 · [z^2 - az - z^2 b^* M_0^{-1} b]`, so `a = 0` and `||b||^2 != 1` give `m = 2` and `[z^2]p = 1 - ||b||^2`. For D2 `||b||^2 = 3/2 + 1/2 = 2`, product `-1`, and the factor breakdown `(-2)(1)(1/2) = -1` is exact. I additionally verified `[z^m]p = -1` for **D3** as well. Two things are one line short (finding 3, MINOR severity): (i) the sharp consequence — with the Perron pair `±q^{-1/2}` as the only `l^2` spectrum outside the band, `|prod_{nonzero}| = 1` forces `q · r^{4g} = 1`, hence `r = q^{-1/4}` at `g = 1`: the Weil radius is **pinned by the Perron pair alone**, which is numerics D17 and answers 04j's own flag; (ii) `[z^m]p = -1` is *not* structural — I built a synthetic `c = 1` three-path core with `||b||^2 = 3` and got `[z^2]p = -2` — so the pinning is a genus-one fact and its naive extrapolation `r = q^{-1/(4g)}` contradicts Weil for `g >= 2`.

**VERDICT T2a: VALID.** `p_3 = (z^4/3)(z-1)^2(z+1)^2(z^2-3)(z^2+1)^2(3z^4+1)` and `ptilde_3 = -(1/3)(z-1)^2(z+1)^2(z^2+1)^2(3z^2-1)(z^4+3)`: **exact**, by two independent routes (the `9x9` determinant in `t`, and the matching expansion (0.8)), plus a third from the prover's own `p_3 = p_o p_e` factorisation which I also verified by building the reduced odd and even cores from the involution.

**VERDICT T2b: VALID.** This is the strongest item in the file. `S = (-1) (+) z^2 (+) S_e(z)` in the constant Klein basis: verified at 9 points, off-block entries `< 1e-9`. `Gamma q_- = 0` (indeed `rank Gamma = 3 < h = 4`, the numerics lane's cleaner reason, D18) gives `S q_- = -q_-`. The odd channel: the reduced core is the 3-path `{L1p,L1,L2}` with weights `2/sqrt3`, `sqrt(2/3)` and `c = 1` in the middle; `p_o = z^2(z^2+1)(z^2-1)`, `ptilde_o = (z^2+1)(1-z^2)`, `-p_o/ptilde_o = z^2` — verified by building the 3-vertex core from the diagram. The "leaf rotation to a dark vertex plus a two-vertex core with coupling `sqrt2`" is correct and is the *same* statement as the numerics lane's criterion: I proved the general 3-path identity `p + z^2 ptilde = -z^2(z^2+1)^2(a_1^2 + a_2^2 - 2)` symbolically, and here `a_1^2 + a_2^2 = 4/3 + 2/3 = 2`. So the lane's "there is no hidden transparency" and the prover's "a leaf rotation gives coupling `sqrt2`" are the same fact; the prover's phrasing is the mechanism. (2.4) is **exact**, and so are the three displayed `Gamma` entries (I verified `(zGamma - I)S_e = I - Gamma/z` symbolically). `p_e` verified from the reduced even core. (2.5) `det S_e = z^2 zeta_K(2s)/zeta_K(2s-1)` is an **exact rational identity**. The curve arithmetic is right: affine points `(0,1),(0,2),(1,0)`, `N_1 = 4`, `a = 0`, `2·(0,1) = (1,0)` (slope `1/2 = 2` in `F_3`, `x_3 = 4 = 1`, `y_3 = 0`), only one 2-torsion point, so `Pic = Z/4`.

**VERDICT T2c: VALID.** `3z^4+1` roots at `3^{-1/4} = 0.759835686`, angles `±45°, ±135°`. The Smith–McMillan split at zero is right: `(S_e)_{22}(0) = -3/-3 = 1` is invertible, `(S_e)_{11} = z^2 + O(z^4)`, `det S_e = z^2 + O(z^4)`, so the even block carries one size-two zero block and the odd channel `z^2` carries the other — two and two, total `ord_0 p_3 = 4`. I verified the even model has `rank Z = 5` on `dim 6`, i.e. one Jordan block. Thresholds: `(2.3)` and `p_e` each supply one half-bound state per sign, `tau = 4`, and `(0.7)` is regular with the nontrivial limit — verified. `l^2`: bound parameters `±1/sqrt3`, `lambda = ±4/sqrt3 = ±2.3094011`. The kernel bookkeeping is exactly right and I re-derived every equation: the leaf rows force `L1 = Z = X = 0`; the remaining rows give `L2 = -sqrt2 L1p`, `Y = -sqrt2 Zp`, `-sqrt2(L1p+Zp) + (2/sqrt3)Xp + sqrt(2/3)Q = 0`; so `dim ker T_X = 3`, and imposing `Q = 0` leaves `2`. The claim "the third core zero eigenvector is not an eigenfunction after zero extension" is the numerics lane's D11, correctly stated.

**VERDICT T2d: MINOR.** Correct, and correctly cautious — but it decides in the negative a question that is provable in three lines, and the positive answer is the round's biggest arithmetic result.
*Correct.* The raw even block has no constant eigenbasis: `(a-d)/b = -(z^2-1)^2(3z^4-2z^2+3)/(4z^3(z^2+1))` is nonconstant, and the discriminant numerator `9z^16 - 48z^14 + 124z^12 - 144z^10 + 374z^8 - ...` is squarefree (`gcd` with its derivative is `1`), so the eigenvalues are irrational — I reproduced both. (2.6) `H^* D S_e D H = diag(R_3, z^2)` with `D = diag(1,z)`, `H` the `2x2` Hadamard, is **exact**. The uniqueness of `a - b = -1` is right: I scanned every half-integer `a-b` in `[-4,4]` and the diagonal-difference/off-diagonal ratio is constant for exactly one value. `diag(z^{-1},1)` gives `diag(R_3/z^2, 1)`. The delay bookkeeping (`det(DS_eD) = z^2 det S_e`, four extra zero modes on all four exits) is right.
*Why MINOR.* The file concludes "This is consistent with the genus-one character prediction, although it **does not identify these vectors with specific `Z/4` characters**" and "the visible graph group is Klein with two cusp orbits; they are not a transitive class-group action". Both sentences are true but weaker than the truth, and I proved the stronger statement symbolically. In the exit basis `(L1, Z, Q_1, Q_2)`, with `D = diag(1,1,z,z)`,
```
  D S D  =  (a-d) P_inv  +  d J ,      a - d = z^2 ,   a + 3d = R_3 = z^2 zeta_K(2s)/zeta_K(2s-1)
```
**exactly**, where `J` is all-ones and `P_inv` is the involution fixing `L1` and `Z` and swapping `Q_1, Q_2`. That is literally Sørensen's Theorem 7.2 form `Phi = (group matrix) x (inversion permutation)`, and it *does* identify the labelling: inversion on `Z/4` fixes exactly two elements (identity and the 2-torsion) and swaps the other two, matching `S(L1) = S(Z) = 12` against `S(Q) = 4` twice; inversion on `(Z/2)^2` is the identity, so **the graph's own `S`-matrix distinguishes `Z/4` from `(Z/2)^2`**. The Klein group of the diagram is then not "not the class group": it is `<translation by the 2-torsion, inversion> ⊂ Hol(Z/4)`, precisely the symmetry a group-matrix-times-inversion has. The character decomposition is `fhat(chi_0) = a+3d = R_3` (the zeta channel) and `fhat(chi) = a-d = z^2` for all three nontrivial characters — i.e. `L(s,chi) = 1` at genus one, exactly as H-CLASS predicts, with the monomial displayed. And more: `1/det(DSD) = -z^{-8} zeta_K(2s-1)/zeta_K(2s)`, whose sign `-1` is Sørensen's `sign(Cl) = (-1)^{(h-h[2])/2} = (-1)^{(4-2)/2}` (`:1487`), while D2's `1/R_2 = +z^{-2} zeta_K(2s-1)/zeta_K(2s)` has `sign(Cl) = +1` for `h = h[2] = 1`. Finally the height shift is not an algebraic trick: I verified that **`D = diag(1,1,z,z)` is exactly the geometric operation "cut all four cusps at the same ray depth"** — rebuilding D3 with the two `S = 12` first vertices of the `Q`-rays absorbed into the core (so all four rays begin at `S(c_1) = 36`) gives precisely `D S D`, at four test points. Fix: replace the negative sentences with the display above and cite Sørensen. Finding 5.

**VERDICT T3a: VALID.** The directional pole removal is correct: with `P` the orthogonal projection onto `ran R_beta^*`, `R_beta(I-P) = 0` kills the pole, and `b_beta` has a simple zero at `beta` so the retained direction stays regular; recomputing residues and repeating, in an **ordered** minimal product, is the right prescription and the insistence on order and on updated pole spaces (ledger 21) is a genuine repair. For D2, `Theta_2 = -R_2 B_bd` with `B_bd = (2z^2-1)/(2-z^2)` gives `Theta_2 = z^2(2z^4-2z^2+1)/(z^4-2z^2+2)`, which I verified symbolically, verified is inner (`z^6 N(1/z) = D`, `|Theta| = 1` on the circle at four points), and whose zeros are exactly the six model roots. I then built the Hardy model from scratch (companion matrix in the basis `w^k/prod(1-conj z_j w)`, Gram from the Taylor series): `dim K = 6 = deg det Theta`, `spec Z` = the resonances, `||Z|| <= 1`, `Z^200 ~ 1e-12`, `I - Z^*Z` rank one with `||j||^2 = 1 - |Theta(0)|^2 = 1`, `sum_m Z^{*m}J^*J Z^m = I` to `1e-9`, `rank Z = 5` so **one** size-two Jordan block at `0`. The Gram identity (3.4) and the scalar `k_a = Theta/(w-a)`, `Jk_a = 1`, `||k_a||^2 = (1-|a|^2)^{-1}` all check. H-LP is declared, inherited and not re-litigated, which is the right scope. (The cut-dependence of `dim K` is booked under C3, not counted twice.)

**VERDICT T3b: VALID.** The Kraus set `{Z} ∪ {V_l J}` with `Z^*Z + J^*J = I` gives CPTP with no limiting argument ✔ (verified on five random `Phi`). The instrument is correctly promoted from an `h x h` matrix to a CP superoperator on exit matrices (ledger 24) — a real correction of the brief. The finite mean, the renewal representation `rho_inf = U tau / Tr U tau`, and the bijection between stationary exit densities and stationary densities are correct. **"Uniqueness iff the exit renewal channel has one stationary density, not iff irreducibility"** (ledger 25) is right, and T4(a) is its own counterexample. (3.8) `det(I-uE) = det(I-uE_0)det(I - Mhat(u))` is operator-Sylvester and I verified it numerically at `u = 0.3, -0.45, 0.6i` to `1e-8`. (3.9) and the common-modulus criterion are correct for a fixed reset, and the two counterexamples are both exact: `Phi = id`, `Z = diag(r_1,r_2)`, `J = diag(sqrt(1-r_1^2), sqrt(1-r_2^2))` makes **every** diagonal density stationary with `r_1 != r_2` (verified to machine precision at three mixtures) — so the brief's general modal criterion is indeed false. The `Q = rho - Z rho Z^* >= 0` admissibility criterion and the invariant-flag positivity `P_j - Z P_j Z^* >= 0` I verified on Schur flags of the D2 model (all five dimensions). The vacuum protection `E(|k_n><vac|) = z_n|k_n><vac|` verified for all six modes. **Its correction of the notebook is a genuine catch on a registered `proved` claim**: `thm:cusp-renewal-discrete` (`04j:38`) says "Aperiodicity holds iff `0` is not a resonance, i.e. `c != 1`, whenever the range of `reb` contains an eigenvector of `C`". `Omega_J = |j><j|` has `Zj = 0` (so `j` *is* an eigenvector, and `0` *is* a resonance, and `c = 1`), yet `m(1) = 1` and `d = 1`: aperiodic. The report's "iff" is false; `notes/cusp-graph/astra-proofs.md:449` states the correct version ("every modal mixture, and every pure resonance eigenstate, is aperiodic"), so this is a report-text defect that survived the previous round. Finding 6.

**VERDICT T3c: VALID.** `dim K_e = 6`, `dim K_o = 2`, `0` from the Dirichlet `-1` channel: verified by computing `det Theta_even = R_3 B_bd = -z^2(3z^4+1)/(z^4+3)` (degree 6, inner) and `Theta_odd = z^2` (degree 2). The refutation of the brief's block claim is correct: a covariant **channel** preserves isotypic components of the *conjugation* representation on operator space, and two inequivalent one-dimensional Hilbert types both give trivially-transforming densities, so populations can move between them; the reset-to-an-invariant-mixture example is a valid witness. "Within `K_e`, the Hasse–Weil and delay spectral subspaces are generally a nonorthogonal direct sum" — verified (the zero eigenvector has overlap `0.53` with the Hasse–Weil span). "For genus one the extra character channels add delay/constant factors and no additional nonzero Hasse–Weil roots, but they need not be dynamically irrelevant" is the right conclusion and is sharper than the numerics lane's D31.

**VERDICT T4a: MINOR.** Every computation is exact; the headline conclusion is about a convention.
*Correct.* `j = J^*1 = Theta_2(w)/w` with `||j|| = 1` and `Zj = P_K Theta_2 = 0`; `g = Theta_2/w^2 in K`, `||g|| = 1`, `g ⊥ j`, `Zg = j`. I re-derived `Jf = <Theta, wf>` from `(I-P_K)(wf) = Theta Jf` and confirmed `Theta/w ⊥ Theta H^2`, `<j,k_a> = 1`. `Omega_J = |j><j|` is stationary and **pure**, `spec rho_inf = (1,0,0,0,0,0)`, `m(m) = delta_{m1}`, `mhat(u) = u`, `mu = 1`, aperiodic, unique and attracting (six random initial densities converge to `1e-9`), and `P_HW j = 0` to `1e-7` while the Hilbert overlaps with the Hasse–Weil eigenvectors are `> 0.3`. The explicit `(4.4)` is correct in every entry: `I - Z^*Z = e_6 e_6^*` exactly, `Zj = 0`, char poly `z^6 - z^4 + z^2/2`, `I - ZZ^* = kk^*` with the stated `k` of norm one, same spectrum as the Hardy model. The Gram entries `3/5`, `2/5`, `1/10`, `0` for the basis `w^k/(w^4-2w^2+2)` are exact (I summed the two-step recurrence: `a_{k+4} = -a_k/4` gives `(9/16)/(15/16) = 3/5`, `(3/8)(16/15) = 2/5`, `(3/32)(16/15) = 1/10`). The **opposite-defect** correction (4.5) is right and is the same catch as numerics D26: `u<k,(I-uZ^*)^{-1}j> = Theta_2(u)` (verified at three `u`), while the same-defect amplitude `<j,(I-uZ)^{-1}j> = 1` identically. So the brief's suggested derivation of `mhat` from `Theta` is refuted by both lanes.
*Why MINOR.* "Flux returns where it left" collapsing to a deterministic one-step channel is entirely an effect of the `z = 0` sector, and the `z = 0` sector is a cut, not a diagram (C3 finding 2). Under APW's convention — or equivalently after moving the cut one step the other way — the same rebound is genuinely non-degenerate: `||j||^2 = 1 - q^{-2} = 3/4`, law `(3/4, 0, 1/12, 0, 1/48, ...)` supported on odd times only, `gcd = 1`, mean exactly `7/3`, `spec rho_inf = {1/7,1/7,1/7,4/7}` (all verified here, independently reproducing numerics D25). The file states the negative result without saying that the opposite convention gives the opposite answer. Fix: state T4(a) as "for the diagram cut at `c_1`", and display the `K_HW` version beside it. Finding 2.

**VERDICT T4b: VALID.** `m(m) = delta·2^{-(m-1)/2}`, `mu = 1/delta = 2 + sqrt2 = 3.4142136` independent of the weights (verified on three weight vectors, deviation `< 1e-8`), and the general genus-one value `1/(1-q^{-1/2})` — I checked `q = 3` too: `2.3660254`. The clock caveat against the modular surface's `2` is correct. `(4.6)` is exact: with `a = sqrt((1+i)/2)` in the first quadrant and the order `(a,-a,bar a,-bar a)`, `d = delta/(1+|a|^2) = 3-2sqrt2`, `u = delta/(1-bar a^2) = delta(1-i)`, `v = delta/(1+bar a^2) = delta(3+i)/5`, and the full matrix matches `1/(1-conj(z_i)z_j)·delta` to `1e-12`. `(4.7)` `Sigma^* O Sigma = O` and `Sigma diag(z) Sigma = -diag(z)` hold exactly. The equal-weight density spectrum `0.11448581, 0.16190739, 0.29972775, 0.42387905` is right to `1e-7`, sums to `1`, and is far from `(1/4,1/4,1/4,1/4)`. I add (numerics D27, not in the proofs) that the exit rate is `<j|Omega|j> = 1 - q^{-1/2} = 0.2928932` for **every** mode-diagonal `Omega`, forced by `|<j,k_i>|^2/||k_i||^2 = 1-|z_i|^2`; and D28's negative (a rebound charging a `z=0` mode is not stationary with the Hasse–Weil modes, mean `2.2071068 = 1/2 + (1/2)/(1-q^{-1/2})`) is also correct.

**VERDICT T4c: VALID.** `(4.8)` is right and I re-derived it: `(Z^*HZ)_{ij} = bar z_i H_{ij} z_j`, and on a common circle `bar z_i z_j = r^2` iff `z_i = z_j`, so with distinct eigenvalues `H` must be diagonal; the cone has real dimension `4`, `3` modulo scale (the prover states both readings; the brief's "dimension 4 ... modulo scale" was ambiguous and the numerics lane flagged the same). Verified: three random positive diagonal `H` satisfy `Z^*HZ = r^2H` to `1e-9`, a non-diagonal one fails by `> 1e-3`. Parity forces `h_1 = h_2`, `h_3 = h_4`; the real structure forces `h_1 = h_3`, `h_2 = h_4`; together **one ray** — and I verified that `<parity, reality>` acts transitively on the four modes, which is the reason. The energy metric is outside: `||U^*U - I|| = 0.51` with `U = q^{1/4}Z`. The three absolute overlaps `3-2sqrt2`, `sqrt2-1`, `(sqrt2-1)/sqrt5` and the angles `80.120718°, 65.530199°, 79.324762°` are exact. The rank obstruction is correct and is the best new idea in T4: in an arithmetic metric `I - Z^{*'}Z = (1-r^2)I` has rank **four** on `K_HW`, so it cannot factor through a one-dimensional cusp exit, while the energy metric gives rank one on the same invariant subspace — I verified both. The warning that one cannot merely "read" a density in a new metric (ledger 35) is right; I confirmed the complementary fact, that after rebuilding the metric the modal `rho_inf` does have the weights as spectrum (numerics D30).

**VERDICT T4d: VALID.** (It is declared OPEN under H-HECKE, and the declaration is accurate.) The scope statement is accurate: stabiliser sizes specify the adjacency but not the vertex/edge groups, embeddings or double cosets, so no Hecke correspondence can be built from H-DIAG alone; and "commuting with adjacency and preserving cusp asymptotics does not by itself prove modal selection" is correct, with the right reasons (`Z^2` already has doubled eigenvalues; covariance of a CP map does not force diagonal outputs). The list of what a selection theorem would need (adjoint-compatible action, separated joint characters, a dephasing condition) is the right list. One omission: `sources.md` records that Lorscheid `1012.3513` **does** construct the Hecke operators on exactly these quotient graphs (`:146`, `:992`), so H-HECKE has a named source for the missing data rather than being unlocated. Finding 1.

**VERDICT T5a: VALID.** The correction is right and necessary: `1 in l^2(V,1/S)` needs only finite volume, and bounded degree gives boundedness (`2603.26443:445`), but `A1(v) = deg(v)`, so **regularity** is what makes `1` an eigenfunction (ledger 37) — the brief conflated the three. For a `(q+1)`-regular diagram the normalised Perron vector is `1/sqrt(S(v))` with `lambda = (q+1)/sqrt q > 2`; I verified `Tv = lambda v` exactly on both diagrams, including the cusp coupling term, and the tail `g(c_k) ∝ q^{-k/2}`. It is a **visible bound state** (a pole of `det S` at `z = q^{-1/2}`, hence removed by the Blaschke factor and absent from `K`), not an invisible cusp form: correct, and it corrects the brief's "invisible to every cusp". The wave-group computation is right: `W(f, t^{-1}f) = t(f,t^{-1}f)` needs `Tf = (t+t^{-1})f`, so `t in {sqrt q, q^{-1/2}}`, neither `1`, the block is energy-indefinite, and the mode is *not* stationary — so this is `obs:cusp-no-graph-vacuum` re-confirmed, not an exception to it. The final scoping ("finite volume alone does not prohibit designing a different dynamics or a different defect") is honest.

**VERDICT T5b: MINOR.** The derivation is exact; the forward-looking sentence omits the one fact in `sources.md` that bears on it.
*Correct.* From APW `:1899`/`:1901`/`:1915` (all byte-verified, and `:1915` does contain both outgoing conditions verbatim), `-2 mu H(mu) = (mu^2+1)I - mu T_X - C_cusp - q^{-1}C_funnel`, which is (5.1) — I verified this symbolically with independent `c_v`, `f_v`. The one-vertex `(q+1)`-funnel tree gives `p = z^2 - q^{-1}` with roots `±q^{-1/2}`, agreeing with `:1928`, and the one-vertex `(q+1)`-cusp modular curve gives `p = z^2 - q`: verified for `q = 2,3,5`. The mixed-outgoing observation is correct and sharp (`z = sqrt q` for the cusp condition, `z = 1/sqrt q` for the funnel one, so no globally constant resonance), and "a research target, not a proved decomposition" is the right verdict.
*Why MINOR.* "A core with a cusp and a funnel is a small candidate" is offered without recording `2603.26443:854`: "any lattice `Gamma` in such a setting is necessarily geometrically finite, and the quotient has finitely many cusps (possibly none) and **no funnels**. See Theorem 6.1 of Lubotzky." So the proposed candidate is *not* an arithmetic quotient of the tree by a lattice in `PGL(2,F)`; it must be built by hand, and whatever zeta factor it carries is not supplied by the same mechanism. `sources.md` flags this as item 4 of "things the round should not assume", and it is exactly the constraint on the one construction T5(b) proposes. Fix: one sentence plus the citation. Finding 7.

**VERDICT T6: VALID.** (It is declared OPEN under H-CLASS and H-ARITH, and the declaration is accurate.) The arithmetic is right: degree `2g` numerator gives `4g` square-root resonances (`4g = 4` on both diagrams ✔), an unramified nontrivial character has `L` of degree `2g-2` hence `4g-4` resonances (`0` at `g = 1`, so `L(s,chi) = 1` — confirmed exactly by my `fhat(chi) = z^2` computation above), and at `g = 2` the count is `8 + 4(h-1) = 4h+4`. "Square root and bipartiteness are the same doubling, not two factors of two" (ledger 41) is correct. The refusal to promise the number-field case and the correct scoping of Efrat are right. Two gaps (finding 3): `h = |Pic(R)|` needs Lorscheid's refinement `h · deg x` for a place of degree `> 1` (`1012.3513:992`, `1012.4825:305`) — harmless for D2/D3 but not for the genus-2 target; and the genus-2 prediction can be made much sharper. From the same bookkeeping, assuming only the Perron pair outside the band and the H-CLASS channel structure,
```
  |[z^m] p|  =  q · q^{-g} · q^{-(h-1)(g-1)}  =  q^{1 - g - (h-1)(g-1)} ,
```
which is `1` for both D2 and D3 (`g=1`), and `q^{-h}` for any genus-2 diagram. That is a single integer to compute on the first genus-2 diagram and it decides at once between "Weil holds and there is no extra bound spectrum" and "there is extra `l^2` spectrum outside the band" — the cleanest testable consequence the round produces.

**Tally: 19 VALID, 5 MINOR, 0 INVALID.**

---

## Findings

| # | sev | statement | what is wrong | fix |
|---|---|---|---|---|
| 1 | **MAJOR** | the whole file: T1(b)/H-EIS, T2(d)/H-CLASS, T4(d)/H-HECKE, T5(b), T6 | The proofs cite **exactly two** papers (APW and Childs–Gosset) and **none** of the seven sources the literature lane fetched, hashed and byte-verified for this round. The cost is concrete, not bibliographic. (i) Sørensen 2001 (`refs/src/sorensen-2001/paper.txt`) is a *free, complete, primary* proof of the number-field statement T2(d) and T6 declare missing: Thm 7.2 `Phi = xihat(2s)^{-1}xihat(2s-1)P`, Prop 8.1 the Dedekind determinant, Cor 8.2/8.3, `:1487` `sign(Cl) = (-1)^{(h-h[2])/2}`, `:1618` `tr Phi(1/2) = h[2]-2`, `:1631` `m_± = (h ± (h[2]-2))/2`. I verified **all four** of these against D2 and D3 (see finding 5 and tension (i)). (ii) `1012.3223:573`/`:187` constrains H-EIS (monomial with exponent affine in `g`, `deg fc = 2g-2`). (iii) `1012.3513:146`,`:992` constructs the Hecke operators on exactly these quotient graphs — the data H-HECKE says is unavailable. (iv) `1012.4825:227`,`:309` is the warning about H-DIAG-3. (v) `2603.26443:854` (Lubotzky) kills T5(b)'s candidate as arithmetic. | Add a Section 0 prior-art paragraph. Restate H-CLASS against Sørensen (it becomes a *verified* structure, not an open hypothesis, for D3); restate H-EIS against Lorscheid; point H-HECKE at `1012.3513`; put Lubotzky into T5(b). |
| 2 | **MAJOR** | C3, T1(c), T2(c), T3(a),(c), T4(a) | The `z = 0` "delay" sector is presented as a property of the diagram (`N(D2) = 6`, `N(D3) = 8`, `dim K = 6/8`, "a size-two Jordan block", `\|\|j\|\| = 1`, "the canonical rebound never charges the arithmetic"). It is a property of **where the core/cusp cut is placed**. Because `S(c_k) = S(c_k c_{k+1})` on every ray, `c^2 = 1` survives moving the cut outward, and on D2 I get `ord_0 p = 2(1+k)`, `N = 4+2(1+k)`, `dim K = N`, `R_new = z^{2k}R_old` for `k = 0,1,2,3` extra ray vertices, while the nonzero divisor and `[z^m]p = -1` are **invariant**. APW say as much: `det H(mu) = mu^k f(mu)`, `2603.26443:1922`, which is why they restrict to `mu in C^x`. Consequence: T4(a)'s whole conclusion flips under the other convention (see the `K_HW` numbers, verified here and in numerics D25). | State every count relative to the declared cut. Make the invariant count (`4` Hasse–Weil modes on both diagrams) the headline, and present T4(a) as a pair: `Omega_J` on the cut model (trivial) versus on `K_HW` (mean `7/3`, spectrum `{1/7,1/7,1/7,4/7}`, aperiodic, odd-time support). |
| 3 | **MAJOR** | T1(e), T6 | Two consequences are one line short and they are the round's testable content. (a) With the Perron pair as the only outside-band `l^2` spectrum, `\|prod_{nonzero roots}\| = 1` gives `q·r^{4g} = 1`, so at `g = 1` the Weil radius `r = q^{-1/4}` is **pinned by the Perron pair alone** (numerics D17; answers 04j's flag that nothing pins `r`). (b) `[z^m]p = -1` is *not* structural — my synthetic `c = 1` core with `\|\|b\|\|^2 = 3` gives `[z^2]p = -2` — so the naive extrapolation `r = q^{-1/(4g)}` contradicts Weil for `g >= 2`. The correct genus-`g` bookkeeping is `\|[z^m]p\| = q^{1-g-(h-1)(g-1)}`, i.e. `1` at `g = 1` and `q^{-h}` at `g = 2`. | Add (a) to T1(e), (b) and the closed form to T6 as the genus-2 test. Credit the numerics lane for (a). |
| 4 | MINOR | T1(b) | `q^{1-2gs}` is displayed as "the pattern" fitting the two data points. So does `q^{1-2s}`; the two agree at `g = 0,1` and differ elsewhere, so the data cannot select either (numerics D16). Lorscheid `1012.3223` adds a third, differently-normalised constraint. | One sentence stating the under-determination; cite `1012.3223:573`/`:187`. |
| 5 | MINOR | T2(d) | Decides in the negative ("does not identify these vectors with specific `Z/4` characters"; "not a transitive class-group action") what is provable in three lines and is the round's best arithmetic result: `D S D = (a-d)P_inv + dJ` **exactly**, with `a-d = z^2` (all three nontrivial characters, `L = 1`) and `a+3d = R_3` (the zeta channel); `P_inv` fixes exactly the two `S = 12` cusps and swaps the two `S = 4` ones, which is `Z/4` inversion and **not** `(Z/2)^2` inversion; `sign(1/det) = -1 = (-1)^{(h-h[2])/2}`. And `D = diag(1,1,z,z)` is not a trick: it is the **equal-depth cut** of the four cusps (verified by rebuilding D3 with the two `S = 12` `Q`-ray heads in the core). | Replace the two negative sentences with the display; cite Sørensen Thm 7.2, Prop 8.1, Cor 8.2, `:1487`. Then T2(d) can be registered as a *verification of the Sørensen structure* for D3, not merely "a concrete diagonalisation". |
| 6 | MINOR | the notebook, `thm:cusp-renewal-discrete` (`04j:38`), status `proved` | The prover is right and the *report* is wrong: "Aperiodicity holds iff `0` is not a resonance, i.e. `c != 1`, whenever the range of `reb` contains an eigenvector of `C`" is falsified by `Omega_J` on D2 (`c = 1`, `0` a resonance, `j` an eigenvector, `m(1) = 1`, `d = 1`, aperiodic). `notes/cusp-graph/astra-proofs.md:449` already states the correct version. This defect survived the previous round's review. | Amend the `04j` sentence to the proof file's version: a modal mixture (including a pure `z = 0` eigenstate) always has `m(1) = 1-\|z_i\|^2 > 0` and is aperiodic; periodicity needs a non-modal `Omega`. |
| 7 | MINOR | T5(b) | Proposes "one cusp and one funnel" as the smallest candidate without recording `2603.26443:854` (Lubotzky Thm 6.1): arithmetic lattices in `PGL(2,F)` give quotients with **no funnels**. | Add the citation and say explicitly that such a diagram must be built by hand and is not an arithmetic quotient of this type. |
| 8 | NOTE | citation `:2085` | Cited for "APW's expectation"; `:2085` is the symmetric-space sentence, the four-type list is `:2090`–`:2093` (and `:2088` is right). | Reword to `:2088`–`:2093`. |
| 9 | NOTE | (3.9) and the ledger | Two `\quad` macros are written `quad` in the display after (3.8), so the line renders as `\|e_i\rangle\langle e_i\|,quad Ze_i=z_ie_i,quad\|e_i\|=1:`. Purely cosmetic; I record it because the display is quoted downstream. (For the record, the C3 multichannel counterexample is *exactly* right in every detail, including which channel carries the `±1/2` zero and which the `±1/2` pole — I re-derived both entries from `Gamma`.) | Fix the two macros. |
| 10 | NOTE | `numerics.md` §1 and `scripts/elliptic_cavity.py:22` | Both state `T_X = D^{-1/2} A_X D^{-1/2}/sqrt q` with `(A_X)_{vw} = S(v)/S(e)`, which is not symmetric and is not `T_X`. Line 24 of the same docstring gives the correct similarity, and the code builds `sqrt(S(u)S(v))/(S(e) sqrt q)` directly, so **no computation is affected**. The prover's ledger 2 is the correct statement. | Fix the prose in both places; register the prover's C1 wording. |
| 11 | NOTE | `04h:130` | The prover cites `04h:130` for `prop:graded-stationary-segment`; `:130` is the `\label`, the environment opens at `:129` (which is what the previous review cited). | Harmless; pick one convention. |

**No BLOCKER. No INVALID statement found.** I attacked, in order, the five items the prover nominated. (1) *The directional pole removal and H-LP*: the removal is correct and minimal, `Theta_2` is exactly `-R_2 B_bd`, the model is exactly what is claimed; H-LP is declared and inherited, and I did not re-litigate it. (2) *Height congruence versus class characters versus delay count*: this is where the file is weakest and where it is most wrong-by-understatement — the congruence **is** a geometric cut, and it **does** produce the class-character structure exactly (finding 5). (3) *Same-defect versus opposite-defect transfer*: the prover is right, both amplitudes verified, and the brief's derivation is refuted by both lanes. (4) *H-CLASS and H-HECKE data*: H-CLASS is now much better supported than the file says; H-HECKE genuinely needs `1012.3513`'s double-coset data. (5) *The funnel*: (5.1) is exact, the "open" verdict is right, but Lubotzky's no-funnel theorem must be recorded.

---

## The three literature tensions

**(i) Sørensen Cor 9.2 (`tr Phi(1/2) = h[2] - 2`) versus APW's `±1` resonances of multiplicity 2 versus the numerics lane's `tr S(±1) = 0` and `(h + tr S)/2 = 2`.**
**No tension: it is the same statement, and the lane's unasserted observation now has a proof and a name.** Sørensen `:1628`–`:1631` reads, for `A = Phi(1/2,0,chi)` with `A^2 = I_h` and multiplicities `m_±` of its `±1` eigenvalues:
```
  m+ + m- = h ,  m+ - m- = h[2] - 2 ,  hence  m+ = (h + h[2] - 2)/2 ,  m- = (h - h[2] + 2)/2.
```
`Pic(R) = Z/4` gives `h = 4`, `h[2] = 2`, so `tr = 0` and `m+ = 2`. I verified `tr S(±1) = 0` at `z = ±(1-1e-9)`, with eigenvalues `{+1,+1,-1,-1}` (from `(-1) ⊕ (1) ⊕ (-eps·sigma_x)`), hence `m+ = 2` — **exactly APW's threshold multiplicity 2 at each of `mu = ±1`** (`2603.26443:2058`). The same formula works on D2: `Pic` trivial, `h = h[2] = 1`, `tr = -1` and `m+ = 0` — and indeed `R_2(±1) = -1` and D2 has **no** threshold roots. So `(h + tr S(±1))/2 = m+` is Sørensen's `:1631`, the graph cavity satisfies it on both examples, and APW's own item 4 (`:2093`, "multiplicity ... related to the number of cusps and the functional equation of the zeta function/trace of the scattering matrix") is this identity. **Effect on the claims:** C3's `(0.7)` and `tau = 4` are confirmed arithmetically rather than only computationally; the numerics lane's "2 data points, not asserted" can be asserted with a source; and the `±1` multiplicity becomes a *prediction* for any future diagram (`m+ = (h + h[2] - 2)/2`), not a computed accident. (Orientation check: at the centre `z = 1`, `Phi(1/2) = S(1)^{-1} = S(1)` since `S(1)^2 = I`, so the trace is the same in either orientation, and the height shift `D(1) = I` does not change it.)

**(ii) Lorscheid's graph for the same `F_3` curve is disconnected and 7-vertex; APW's is connected and 9-vertex. Does this cast doubt on H-DIAG-3?**
**No — they are different objects, and I supply a positive test that H-DIAG-3 passes.** Lorscheid's `G_x` is the graph of the Hecke operator `Phi_x` on `Bun_2`, and he states himself (`1012.4825:227`) that "if the class number is even, the correspondence between the local and the global situation breaks down", and (`:309`) that "the connected components of `G_x` stay in bijection with the 2-torsion elements of `Pic X`", so for `Z/4` his graph has exactly 2 components. The quotient `Gamma \ T_x` of a *tree* by a group is necessarily **connected**, so APW's connected 9-vertex core cannot be Lorscheid's `G_x`, and the disagreement is predicted by Lorscheid's own theorem rather than being evidence against either. The cusp count `4` agrees, and is independently supported (`2603.26443:892`; `1012.3513:992`; `1012.4825:305`). What *was* missing is any check on the **absolute** stabiliser sizes (`sources.md` §1b: Lorscheid's figure fixes only the ratios). I supply one: `sum_v 1/S(v) - sum_e 1/S(e) = -zeta_K(-1)` holds **exactly** for D3 (`7/4 - 7/2 = -7/4 = -28/16`), for D2 (`10/3 - 5 = -5/3 = -5/3`), and symbolically in `q` for the Nagao quotient of `GL_2(F_q[T])`. Three cases, two genera, two curves, with the correct group (`S(L1p) = 48 = |GL_2(F_3)|`, `S(A) = 6 = |GL_2(F_2)|`). **Effect on the claims:** H-DIAG-3 stays a hypothesis (Takahashi is still NOT FOUND), but it is now a hypothesis that has passed a nontrivial arithmetic test on precisely the data no other source confirms. Finding 5 adds a second: the `S`-matrix built from those stabilisers reproduces Sørensen's `Z/4` structure, including which cusp is the 2-torsion class — a coincidence that a mis-transcribed diagram would be very unlikely to produce.

**(iii) APW (citing Lubotzky) say arithmetic lattice quotients have no funnels. What does that do to T5(b)?**
It removes the programme's implied arithmetic guarantee, but not the formula. `2603.26443:854`: "any lattice `Gamma` in such a setting is necessarily geometrically finite, and the quotient has finitely many cusps (possibly none) and **no funnels**". So "one cusp and one funnel" cannot arise as `Gamma \ T` for a lattice in `PGL(2,F)`; it must be hand-built, and then nothing supplies the Hasse–Weil factor of `p`. This is the exact opposite of the reassurance T5(b)'s closing paragraph offers ("a small candidate for studying simultaneous arithmetic resonances and leakage"). What **survives** is (5.1) itself, which is a theorem about APW's constant-type resonance matrix for *any* diagram with both end types (I verified it symbolically, and on their two worked examples for `q = 2,3,5`), and the sharp negative the prover already draws: the cusp outgoing condition wants `z = sqrt q` and the funnel one `z = 1/sqrt q`, so no globally constant function is simultaneously outgoing — the vacuum does not simply "leak out the funnel". Combined, the honest statement is: **a cusp-plus-funnel diagram is a non-arithmetic deformation, and the burden is to show that any zeta factor survives the deformation, not that the leak exists.** T5(b) should say so.

---

## Adjudication: prover versus the numerics lane

| # | item | numerics lane says | prover says | who is right | my evidence |
|---|---|---|---|---|---|
| 1 | C1 normalisation | `T_X = D^{-1/2}A_X D^{-1/2}/sqrt q` with `A_X` the *coordinate* adjacency (D3, §1) | that is wrong; use `D^{-1/2}A_X D^{1/2}/sqrt q`, or `D^{-1/2}(measure kernel)D^{-1/2}` | **prover** | `D^{-1/2}A_XD^{-1/2}` is not symmetric; I checked all three forms exactly on both diagrams. The lane's *code* (`scripts/elliptic_cavity.py:157`) builds `sqrt(S(u)S(v))/(S(e)sqrt q)` directly, so nothing it computed is affected; only its prose and docstring are wrong. Finding 10 |
| 2 | Childs–Gosset (D9) | `S_notebook = z^2 S_ChildsGosset`, an origin shift; and D3 is "outside their setup" (two tails at one vertex) | (0.1) **is** Childs–Gosset with `A = 0`, `B = W`, `D = T_X`, the first ray vertices as their boundary vertices | **prover** | CG `:332` says `\|1,j>` is the vertex where the tail meets the graph and the displayed equation puts `z^{-1}delta+zS` there with `z^{-2}+z^2S` at `x=2`: their boundary vertices are the notebook's `r_1^{(a)}`. I verified `-Q(z)^{-1}Q(1/z) = S` at 9 points on **both** diagrams, D3 included. The lane's `z^2` is the shift to a *different* cut; it is finding 2 in disguise (see row 3) |
| 3 | the `z = 0` sector (lane's refuter item 1) | "Is `z = 0` a resonance *of the diagram* at all, or an artefact of putting the attachment vertex outside the ray? If the right convention kills them, D24 evaporates and D25 is the truth" | a genuine eigenvalue of the finite model, a delay mode; `ord_0 p` from (0.6) | **the lane's worry is correct; the prover's algebra is correct** | Settled here: moving the cut `k` steps gives `ord_0 p = 2(1+k)`, `N = 4+2(1+k)`, `R -> z^{2k}R`, nonzero divisor invariant. So D24 **is** an artefact and D25 **is** the invariant statement. APW's `mu in C^x` and `:1922` are the same quotient. Finding 2 |
| 4 | `ord_0 p` (D7) | `ord_0 p = 2 dim ker(I-C)`, with the two geometric conditions (no loop at an attachment, attachments pairwise non-adjacent) | (0.6) `ord_0 p = 2 dim U` when `T_UU = 0` and the bracket is invertible, with both caveats | **identical; the prover's is sharper** | Same statement: `T_UU = 0` is exactly the lane's two conditions. I verified `U`, `rank(I-C)`, `det A_V`, the bracket and `[z^m]p = det A_V det(bracket)` on both |
| 5 | the odd `z^2` channel (D19) | "There is **no** hidden transparency"; the reason is `a_1^2 + a_2^2 = 2` | "a leaf rotation gives a dark zero vertex and a two-vertex core with coupling `sqrt2`" | **both; same fact, prover's is the mechanism** | The bright coupling is `sqrt(a_1^2+a_2^2) = sqrt2`, which is the lane's condition. I proved the general 3-path identity `p + z^2 ptilde = -z^2(z^2+1)^2(a_1^2+a_2^2-2)` symbolically |
| 6 | H-CLASS / T2(d) (D21) | "**H-CLASS is CONFIRMED, not refuted**"; `D = diag(1,z)` works, eigenvectors `(1,±1)/sqrt2`, spectrum `z^2{1,1,-1,zeta ratio}` | the congruence works, but "it does not identify these vectors with specific `Z/4` characters"; H-CLASS stays OPEN | **both understate; the truth is stronger than either** | The height-shifted `S` is *exactly* `(a-d)P_inv + dJ`, i.e. Sørensen's group matrix times class inversion, with `P_inv` forced to be `Z/4` inversion (two fixed cusps, one swapped pair) and `fhat` equal to `R_3` on `chi_0` and `z^2` on all three nontrivial characters; `sign(Cl) = -1` matches `:1487`. The lane's "confirmed" was right for the wrong reason (it only matched multiplicities); the prover's caution was right about the *general* theorem but wrong about D3. Finding 5 |
| 7 | the height shift's meaning | "the two `Q`-cusps start one step deeper ... `a-b = -1` is one ray step" (physical guess) | a diagonal **congruence**, changing the delay counts | **the lane's guess is exactly right, and it is a theorem** | I rebuilt D3 with the two `S = 12` `Q`-ray heads inside the core (all four rays then starting at `S(c_1) = 36`) and got `S_new = D S_old D` with `D = diag(1,1,z,z)` at four points. The congruence *is* the equal-depth cut |
| 8 | the canonical rebound (D24/D25) | D24 "A NEW NEGATIVE": deterministic holding time 1, `rho_inf` pure in the delay sector, `P_HW j = 0`; D25 gives the `K_HW` version | T4(a) CORRECTED: identical numbers, "a modal rebound at zero, not a nonmodal Hasse–Weil rebound" | **agree exactly; both computed the same thing** | All of D24 and all of D25 re-verified independently (law `(3/4,0,1/12,0,1/48,...)`, odd support, mean `7/3`, spectrum `{1/7,1/7,1/7,4/7}`). But both are cut-dependent: row 3 |
| 9 | the `Theta` transfer function (D26) | "the suggested derivation does not work"; `theta_{m+1} = <j'\|Z^{*m}\|j>` with `j'` the **second** defect vector | (4.5): the characteristic formula "uses **opposite** defect spaces"; `<k,u(I-uZ^*)^{-1}j> = Theta_2(u)` | **agree; the prover gives the closed form** | Verified at three `u` to `1e-10`, and the same-defect amplitude `<j,(I-uZ)^{-1}j> = 1` identically |
| 10 | the arithmetic inner-product cone (D30) | 4-parameter, 3-dimensional modulo scale; the symmetric sub-cone is a **single ray** because `<z->-z, z->conj z>` acts transitively | real dimension 4, or 3 modulo scale; parity + reality leave one ray | **agree**; the lane supplies the transitivity reason, the prover the rank-4 obstruction | Both verified. The lane's own caution ("unique for D2, not in general") is right and the prover's statement is scoped to D2 |
| 11 | `r = q^{-1/4}` pinning (D17) | pinned by the Perron pair; `prod\|nonzero\| = prod\|l^2\| = 1/q` | (1.4), (1.5), `[z^m]p = -1` for D2; the consequence not drawn | **lane sharper**, and both miss the genus obstruction | I verified the pinning for D2 *and* D3, and then broke its extrapolation: `[z^m]p = -1` is not structural (synthetic counterexample `-2`), so `r = q^{-1/(4g)}` would contradict Weil. Correct genus law in finding 3 |
| 12 | `dim ker T_X` vs cusp forms (D11) | D3: `dim ker T_X = 3` but only 2 vanish at all exits; the third is not `l^2` | T2(c): identical, with the explicit equations | **agree**; the prover's is constructive | I re-derived every leaf and interior equation |
| 13 | thresholds (D22) | regular, `S_even(±1) = -(±1)sigma_x`, `tr S = 0`, and `(h + tr S)/2 = 2` "not asserted" | (0.7), regular, two `+1` and two `-1` eigenvalues | **agree**; and the lane's unasserted observation is a theorem — Sørensen `:1631` | Tension (i) above |
| 14 | the funnel determinant (D33) | confirmed on APW's tree and modular-curve examples, `q = 2,3,5`; "still H-FUNNEL" | (5.1) derived from `:1915`, the tree example at `:1928` | **agree** | Verified both examples for `q = 2,3,5` and (5.1) symbolically |
| 15 | model space / `Z` (D23, D31) | `dim K(D2) = 6`, `Z` a contraction, rank-one defect, one Jordan block, `sigma Z sigma = -Z`; D3 `6+2+0 = 8` | identical | **agree** | Rebuilt the model from the Taylor series of `1/qq` and confirmed every item |
| 16 | H-EIS (D16) | cannot separate `q^{1-2gs}` from `q^{1-2s}`; "H-EIS stays unasserted" | "the pattern `q^{1-2gs}` fits these two genus values"; OPEN | **lane right** | Trivially: the two families agree at `g = 0,1`, which is all the data there is. Finding 4 |
| 17 | `h`-exit renewal machinery (D35) | NOT TESTED (every lane computation has a scalar exit) | T3(b) proves the `h`-exit statements | **prover; the lane leaves it to him** | I tested the `h`-exit CPTP/Kraus, (3.8) and both counterexamples independently |

---

## What to register

**Theorem-grade, can be registered as proved now.** C1 (the normalisation, with the measure-kernel/coordinate distinction — this supersedes the brief and the numerics prose); C2 in full, **with Childs–Gosset `1203.6557:365`,`:369` cited as the source of the `h`-tail formula and the reconciliation stated as an identity, not a `z^2` shift**; consistency checks 1 and 2, plus the Euler–Poincaré mass identity `sum_v 1/S(v) - sum_e 1/S(e) = -zeta_K(-1)` (new, verified on D2, D3 and the Nagao family); T1(a), T1(b)'s exact identity (1.2)/(1.3) — not the prefactor pattern; T1(c), T1(d); T1(e)'s (1.4)/(1.5) with the pinning corollary added; T2(a), T2(b) in full (this is the strongest block in the file); T2(c); **T2(d) rewritten as the Sørensen structure** (`DSD = (a-d)P_inv + dJ`, `a-d = z^2`, `a+3d = R_3`, `P_inv` = `Z/4` inversion, `sign(Cl)`, and the equal-depth-cut interpretation of `D`); T3(a) on the model side (H-LP stays conditional, inherited); T3(b) in full **including its refutation of `thm:cusp-renewal-discrete`'s aperiodicity clause** — the report line must be amended; T3(c); T4(b), T4(c); T5(a); (5.1) of T5(b).

**Numerical / example-level.** The two determinants and their factorisations; the resonance moduli and angles; the Gram matrix (4.6) and the equal-weight density spectrum; the three angles; the mean `2+sqrt2` and the exit rate `1-q^{-1/2}`; `Omega_J`'s two behaviours (cut model vs `K_HW`); the `K_HW` law `(3/4,0,1/12,0,1/48,...)`, mean `7/3`, spectrum `{1/7,1/7,1/7,4/7}`; the thresholds `m_+ = 2` / `m_+ = 0`.

**Stays open.** H-DIAG-2/3 (Takahashi NOT FOUND; but now with two independent positive tests: Lorscheid's weights for D2, and the mass formula for both). H-EIS (under-determined by the data; constrained by `1012.3223`). H-CLASS **as a general theorem** — for D3 it is now verified, not hypothesised; what remains open is that the tree-quotient `S`-matrix of `GL_2(R)` is a `Pic(R)`-group matrix times inversion *for every* elliptic `R`, and a fortiori for higher genus. H-HECKE (needs `1012.3513`'s double cosets). H-LP (inherited from 04i, never built for these diagrams by either lane). T5(b)'s vacuum leak (and it is now known not to be arithmetic). T6's genus-2 prediction.

**Do not register.** `N(D2) = 6`, `N(D3) = 8`, `dim K = 6/8`, and "the canonical cusp rebound is the one that never charges the arithmetic" as diagram invariants — they are cut-dependent (finding 2).

## What the next round should do first

1. **Build a genus-2 diagram and compute one integer.** The prediction is `|[z^m]p| = q^{1-g-(h-1)(g-1)}`, i.e. `q^{-h}` at `g = 2`, versus `1` at `g = 1`. If it comes out `1`, Weil is violated or there is extra `l^2` spectrum outside the band; if it comes out `q^{-h}`, H-CLASS's channel bookkeeping is confirmed at the first genus where the nontrivial `L`-functions are not `1`. This single number decides more than anything else in the round.
2. **Promote the Sørensen structure from D3 to a statement.** Prove or refute: for `R` the coordinate ring of an affine curve over `F_q`, the equal-depth-cut `h x h` scattering matrix of `GL_2(R) \ T` is `M · P_inv` with `M_{ab} = f(b-a)` a `Pic(R)`-group matrix. D3 verifies it (and pins the cusp-to-class labelling by the stabiliser sizes); D2 is the trivial case. Sørensen's Prop. 8.1/Cor. 8.2, `1012.3223`'s constant term and `1012.3513`'s Hecke graphs are the ingredients, all now in `refs/`.
3. **Fix the two report defects this review found**: the aperiodicity clause of `thm:cusp-renewal-discrete` (`04j:38`, currently `proved` and false as stated), and the cut-dependence of every resonance count (state `N` relative to the declared core/cusp cut, and make the Hasse–Weil count the invariant headline).
4. **Build the funnel example by hand and measure what breaks.** (5.1) is proved; Lubotzky says the result is not arithmetic. Take D2, attach one funnel at a chosen core vertex, and compute whether `2z^4-2z^2+1` survives in `p_funnel` and where the constant mode goes. The honest question is "how much zeta survives a non-arithmetic deformation", not "does the vacuum leak".
5. **Either build the Lax–Phillips energy completion for D2, or stop quoting `dim K` geometrically.** H-LP has now been inherited unexamined through three shards (04i T1c, this round's T3(a), T4). D2 is six-dimensional — or four, after finding 2 — and is the smallest arithmetic case in which the completion could actually be constructed.

---

## Re-verdicts after fixes

Requested by the coordinator after the fixes were applied to the registered shards. I re-read
`report/sections/04k_elliptic_cavity_scattering.tex`, `04l_elliptic_cavity_channel.tex`,
`04m_elliptic_cavity_sources.tex`, the amended `04j_cusp_graph_renewal.tex`, the `db/claims.tsv` row for
`thm:cusp-renewal-discrete`, the erratum appended to `notes/elliptic-cavity/numerics.md` and the corrected
docstring of `scripts/elliptic_cavity.py`. The four lines below **supersede** the corresponding MINOR
verdicts in the section above; the tally line above is kept as the pre-fix record.

**VERDICT C3: VALID.** `prop:multi-exit-spectrum`(iii) now states in italics that "these delay counts, `N`
and `dim K` are properties of where the core/cusp cut is placed, not of the diagram", with my exact
`k`-step statement (`c^2 = 1` preserved, `ord_0 p = 2(1+k)`, `R -> z^{2k}R`, nonzero divisor and `[z^m]p`
invariant) and with `2603.26443:final_draft.tex:1922` cited as APW's own version of the same remark — I
re-checked the four numbers against `notes/reviews/scratch_ec_convention.py` (`k = 0,1,2,3`: `ord_0 p = 2,4,6,8`,
`N = 6,8,10,12`) and the `:1922` line text; everything else in the proposition (the Schur bracket, `ord_0 p = 2/4`,
the multichannel counterexample, `N = 2(n-m_c)-b-n_h`, the windings `4` and `6`, the threshold limit) was already
verified and is unchanged.

**VERDICT T2d: VALID.** The substance I said was one three-line argument short is now registered:
`prop:d3-group-matrix` (04m, `sketched-conditional`, author `claude:opus`, proof pointing at my review) carries
`D S D = (a-d)P_inv + d J` exactly with `a-d = z^2`, `a+3d = R_3`, the `Z/4`-versus-`(Z/2)^2` discrimination by the
number of `P_inv` fixed points, `sign(1/det) = -1 = (-1)^{(h-h[2])/2}`, and the equal-depth-cut reading of `D`;
`obs:h-class` states general H-CLASS in the `M · P_inv` form with Lorscheid's `h·deg x` and even-class-number
cautions; and `prop:euler-poincare-mass` carries the mass check. I re-read all three against my scripts and they
match term for term. **One residual (NOTE, not blocking):** `thm:d3-height-diagonalisation` still closes with "It
does *not* identify the channels with specific characters of `Z/4`: the graph automorphisms are the Klein group ...
not a transitive class-group action (ledger 20)". The *reason* given is the one `prop:d3-group-matrix` refutes — the
Klein group is exactly `<translation by the 2-torsion, inversion> ⊂ Hol(Z/4)`, i.e. the stabiliser of Sørensen's
structure, and the identification goes through without a transitive action. Suggested replacement: "the `2x2`
congruence alone does not label the three monomial channels by individual characters; the full four-exit form
`prop:d3-group-matrix` does pin the cusp-to-class dictionary (`L1, Z` self-inverse, `Q_1, Q_2` an inverse pair)."

**VERDICT T4a: VALID.** `prop:d2-canonical-rebound` now says the naive "arithmetic picks the rebound" candidate
fails "*for this cut*: the degeneracy is a property of where the core/cusp cut is placed
(`prop:multi-exit-spectrum`(iii), review finding 2), not of the diagram", and retains the APW-convention numbers
in the same proposition (law `(3/4,0,1/12,0,1/48,...)` on odd times, mean `7/3`, spectrum `{1/7,1/7,1/7,4/7}`) — I
re-verified all of those, plus the unchanged exact content (`Zj = 0`, `m(m) = delta_{m1}`, `rho_inf` pure, the
explicit `Z`, `chi_Z = z^2(z^4-z^2+1/2)`, `k`, and the opposite-defect transfer `= Theta_2(u)`). The inline
correction to `thm:cusp-renewal-discrete`(c) is there, the `04j` text itself is amended ("every modal `reb` ... is
aperiodic, since each normalised eigenvector has one-step exit probability `1-|z_i|^2 > 0` ... the registered
'iff `c != 1`' was false"), and the `db/claims.tsv` row carries the note. Finding 6 is closed.

**VERDICT T5b: VALID.** `prop:funnel-self-energy` now ends "by `cit:apw-cusps-pic-no-funnels` a cusp-plus-funnel
diagram is *not* the quotient of the tree by a lattice in `PGL(2,F)`: it must be built by hand", which is exactly
`2603.26443:final_draft.tex:854` (Lubotzky Thm 6.1), and it keeps the two correct positives: (5.1) itself, and the
incompatibility of the two outgoing conditions for a constant (`z = sqrt q` on a cusp, `z = 1/sqrt q` on a funnel).
I re-checked (5.1) symbolically and both APW worked examples for `q = 2,3,5`; the added sentence "on a funnel the
constant becomes outgoing at `z = q^{-1/2}`, the same number with its role flipped from pole to zero" is also
correct (the funnel condition `f(u) = f(v)/(sqrt q · mu)` is satisfied by a constant exactly at `mu = q^{-1/2}`).

**VERDICT T1b: VALID.** Not in the re-verdict request, but its fix was listed as applied and I verified it:
`thm:d2-zeta` now reads "pattern `q^{1-2gs}` fits the two genera, but so does `q^{1-2s}` (they agree at `g = 0,1`,
all the data there is)", which is the under-determination sentence finding 4 asked for. (Finding 3's two items are
also in place: `prop:nonzero-root-product` carries the Perron-pair pinning, the explicit statement that the
coefficient `-1` is *not* structural, and the corrected genus law `|[z^m]p| = q^{1-g-(h-1)(g-1)}` — `1` at `g = 1`,
`q^{-h}` at `g = 2`. Finding 10's erratum is appended to `numerics.md` and the script docstring is corrected.)

**Tally after fixes: 24 VALID, 0 MINOR, 0 INVALID.**

Still open and unchanged by the fixes: H-DIAG-2/3 (Takahashi NOT FOUND, now with two positive tests), H-EIS,
H-CLASS as a general theorem, H-HECKE, H-LP (inherited unexamined through three shards), the genus-two test, and
the hand-built funnel diagram. Findings 8 and 11 (the `:2085` address, the `04h:130` off-by-one) are cosmetic and
I have not re-checked whether they were applied.
