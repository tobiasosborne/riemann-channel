You are the prover and formulator in a mathematical research notebook (git repo, current directory). Author line for everything you write: codex:gpt-6-astra. Write ONLY the file notes/complex-zeta/astra-proofs.md (create it; overwrite if present). Do not edit anything else. No web access is available; do not assume any. You may run python3 (numpy, scipy, sympy, mpmath) with `timeout` for sanity checks, writing scratch scripts only under notes/complex-zeta/scratch/. Another worker is independently running numerics in scripts/a2_complex_zeta.py on the PGL_3(F_3) Ramanujan complex; do not edit that file, but if outputs/a2_complex_zeta.txt exists you may read it.

THE JOB. Formulate the Ihara zeta function of a finite simplicial complex that is NATURAL AND WANTS TO EXIST: the object that reduces to the Ihara--Hashimoto zeta for graphs, to the Kang--Li / Kang--Yu zeta on quotients of Bruhat--Tits buildings, carries a Bass-type determinant identity with a cochain-graded (Lefschetz, supertrace) structure, admits a Kraus / representation twist (the "quantum" version), and has a Ramanujan reading. Then prove what can be proved about it and say precisely what cannot. TJO (the PI) frames it thus: "these zeros may be associated naturally with surface-like qualities"; "is there a natural Ihara-type result for simplicial complexes that naturally generalises the finite graph result? Then what is the natural quantum generalisation?" We want the correct definition, not a survey.

CONTEXT: READ THESE FIRST (the literature has been fetched and byte-cited for you; treat every quoted statement there as a usable external fact and declare each one you rely on as an explicit hypothesis H-* citing its `<arxiv-id>:<file>:<line>` address):
- notes/extract/complex-zeta-sources.md: C1 Kang--Li PGL(3) identity Z = (1-u^3)^chi / [det(I - A_1 u + q A_2 u^2 - q^3 u^3) det(I + L_B u)] = 1/[det(I - L_E u) det(I - L_E^t u^2)] and its four-way RH <=> Ramanujan theorem (cubic zeros on |u| = q^{-1}; det(I + L_B u) zeros on |u| in {1, q^{-1/2}, q^{-1/4}}; det(I - L_E u) zeros on {q^{-1}, q^{-1/2}}); C2 Kang--Yu PGL_n: (1-u^n)^chi L(Gamma, q^{(n-1)/2} u) = prod_{k=1}^{n-1} Z_k^eps(u)^{(-1)^{k+1}} with Z_k^eps = det(I - (-1)^{k+1} T_k)^{-1} on pointed k-facets, and the torsion derivation prod_i det(Phi_i | C_i)^{(-1)^i} = (1-u^n)^chi; C5 Lubetzky--Lubotzky--Parzanchevski geodesic flow: states (v_0; v_1..v_j) with {v_0..v_j} a cell, step to (v_1; v_2..v_j, w) allowed iff {v_1..v_j,w} is a cell and {v_0,...,v_j,w} is NOT a cell; Ramanujan complex => poles of the flow zeta at u = k^{-s} with |s| = 1 or Re s <= 1/2, and poles with 0 < Re s < 1/2 do occur; C7 Kamber: zeta = 1/prod_i det(1 - h_{beta_i} u^{l(beta_i)}) over Bernstein--Lusztig operators, L_p-expander iff one-sided bound |lambda| <= q^{(p-1)/p} or |lambda| = q; C8 Benard--Chaubet--Dang--Schick: for a triangulated closed n-manifold the signed zeta prod(1 - eps_gamma z^{|gamma|}) over primitive geodesics in the (n-1)-skeleton is a polynomial vanishing to order b_1 at z = (n+2)^{-1}; C6 Storm: the hypergraph zeta is the Ihara zeta of the incidence bipartite graph at sqrt(u); C10 Lubotzky--Samuels--Vishne: Ramanujan complexes of type A~_{d-1} are Cayley (clique) complexes of PGL_d(F_{q^e}) with generators b_u = 1 - r z^{-1}, Hecke operators A_k = colour-shift adjacency, Ramanujan := joint spectrum of (A_1..A_{d-1}) on nonconstant eigenvectors inside the tempered set; the "Not found" section: NO Ihara-type zeta exists for a general (non-building) complex, and the authors say so (Deitmar--Hoffman 2004, Lubotzky ICM 2018, Hong--Kwon 2024, Kang--Yu 2026).
- notes/extract/cohomological-zeta-sources.md: A1 Deitmar: Ruelle zeta of a locally symmetric space = prod_l Z_{sigma_l}(s + l|alpha|)^{(-1)^l} over exterior powers of n, vanishing order = -sum_p p (-1)^p dim H^p (degree-WEIGHTED supertrace; the plain Euler characteristic vanishes there); A2 Dyatlov--Zworski: zeta_R = zeta_1/(zeta_0 zeta_2) on surfaces, order at 0 = -chi; A4 the placement flips with dimension; B3 Hashimoto h'(1) = -2 chi kappa (spanning trees sit in the vertex determinant, not in (1-u^2)^chi); B4 Hoffman 2003: Bass's proof is a torsion-of-complexes computation; B7 Knill: analytic torsion of a complex = SDet(D) = prod_k Det(D_k)^{(-1)^k}, "fermionic" versus "bosonic", with a matrix-tree theorem even-dimensional over odd-dimensional rooted spanning trees; D1 Matsuura--Ohta: Bass's identity as a Berezin integral, Dirac operator block off-diagonal between the vertex fermions (n_V) and the edge fermions (2 n_E) with gamma_5-hermiticity, (1-t^2)^{n_E - n_V} = the difference of block dimensions (index-like), det(Dslash + M) = (1-t^2)^{n_E-n_V} det Delta = zeta^{-1} = det(1 - qB_u); D2 Foata--Zeilberger: (1-u^2)^{c_1-c_0} comes from det(1 - uJ) with J the edge reversal; D4 Aizenman--Warzel: "the loop ensemble has a fermionic nature"; F2: no quantum (channel / Kraus / unitary-weighted) zeta of a complex exists in the literature.
- notes/quantum-ihara-general.md, Theorem 1 and Corollaries 2-4 (the notebook's Ihara--Bass formula for the non-backtracking superoperator of an ARBITRARY family of superoperators E_1..E_D on a finite-dimensional V with a fixed-point-free reversal; Lamport proof style; det_W(1 - uT) = prod_{pairs} det_V(1 - u^2 E_ibar E_i) det_V(1 + D(u) - A(u)); Kraus case Tr T^l = sum_w |Tr A_w|^2 >= 0; unitary case: RH <=> Hastings' bound).
- notes/weil-positivity.md (one-sided bound = Weil positivity; a duality of the spectrum makes it two-sided; Kraus dichotomy) and HANDOFF.md "CENTRAL PRIORITY" (the notebook's fermionic-zeros picture: the pole is an even sector, the zeros an odd sector, the zeta a supertrace; the no-go: no bosonic (positive) ring norm can produce a zeta with a numerator; a zeta with a numerator forces a grading).

CONVENTIONS. X is a finite abstract simplicial complex of dimension d (all cells are simplices; for Cayley complexes X is the clique complex of the Cayley graph). An ORDERED k-cell is a (k+1)-tuple (v_0, ..., v_k) of distinct vertices whose underlying set is a k-cell; Omega_k is the set of ordered k-cells, |Omega_k| = (k+1)! f_k where f_k is the number of k-cells. chi(X) = sum_k (-1)^k f_k. For a building quotient of type A~_{n-1} over a residue field of order q, A_1..A_{n-1} are the colour-shift Hecke operators on vertices (Lubotzky--Samuels--Vishne convention, C10). "Kraus weights" on X means: a finite-dimensional V and, for every ordered 1-cell (x,y), a linear map E_{(x,y)} in End(V) (a "connection" on the 1-skeleton; adjoint-paired means E_{(y,x)} = E_{(x,y)}^dagger in the Kraus sense E = Ad(B), inverse-paired means E_{(y,x)} = E_{(x,y)}^{-1}; flat means E_{(y,z)} E_{(x,y)} = E_{(x,z)} around every 2-cell). Determinants are over the named space; Sylvester det(1 - XY) = det(1 - YX) and Schur complements are used freely. "The graded (super) form" of an identity means: a Z_2-graded space W = W_+ (+) W_- and an even operator, with sdet(1 - u Tau) = det(1 - u Tau|W_+) / det(1 - u Tau|W_-) and log sdet = -sum_m str(Tau^m) u^m/m.

TASKS. For each: statement as Definition / Theorem / Proposition with a label, hypotheses explicit, Lamport-structured proof (<1>1, <1>2, ... with PROOF; sub-steps <2>k where needed), and a status line: proved-here | sketched | conditional-on <H-*> | FALSE-as-drafted (then corrected). I have drafted statements that may be slightly too strong on purpose; if one is false, say so plainly, give the smallest counterexample you can (a concrete complex you can check in python3), correct it, and prove the corrected version. Do not round up. Keep a CORRECTION LEDGER at the end.

T0 (The definition). Define, for 1 <= k <= d, the k-th GEODESIC FLOW operator T_k on C^{Omega_k}: (v_0, ..., v_k) -> sum over w of (v_1, ..., v_k, w), the sum over vertices w not in {v_0..v_k} such that {v_1..v_k, w} is a k-cell and {v_0, ..., v_k, w} is NOT a (k+1)-cell. (For k = 1 and d = 1 this is the Hashimoto non-backtracking operator; for k = 1 and d = 2 it also forbids closing a triangle; for k = d it is unrestricted non-backtracking on top cells.) Define Z_k(X, u) := det(1 - u T_k)^{-1} and the SIGNED version Z_k^eps with the sign (-1)^{k+1} inside as in Kang--Yu, and the TOTAL zeta Z(X, u) := prod_{k=1}^{d} Z_k^eps(X, u)^{(-1)^{k+1}}. State exactly which of these reduce to what: (a) d = 1: Z = Z_1 = Ihara--Hashimoto; (b) on a quotient of the A~_2 building, identify T_1 and T_2 with Kang--Li's L_E (and L_E^t: explain where the u^2 factor of det(I - L_E^t u^2) comes from in this ordered-cell convention: are the colour-2 edges the reversed colour-1 edges, and does T_1 in my convention contain both L_E and L_E^t as blocks?) and L_B (including the 3! ordering of a chamber and the sign +L_B u), and check that Z(X,u) as I defined it equals Kang--Li's Z(X_Gamma, u) or say what the discrepancy is; (c) state whether my T_k equals the LLP j-flow of C5 and Kang--Yu's T_k of C2 (ordered versus pointed cells: give the exact relation, e.g. T_k^{mine} is conjugate to a direct sum of k! copies of Kang--Yu's, or not). If the definition needs a modification to make T0(b) exact (e.g. pointed cells with a cyclic ordering, or a type/colour grading), make it and say why: THE POINT OF T0 IS TO FIX THE DEFINITION THAT MAKES THE REST TRUE.

T1 (The trivial factor is a torsion, for every complex). Drafted: for every finite simplicial complex X there are operators Phi_i(u) on the ordered-cochain spaces C^i := C^{Omega_i} (i = 0..d), built from u and the face maps, such that prod_{i=0}^{d} det(1 - Phi_i(u))^{(-1)^i} = (1 - u^{d+1})^{chi(X)} (Kang--Yu prove this for building quotients; C2 gives the line numbers). Prove it in general or give the counterexample and the correct general statement (perhaps the exponent is a weighted Euler characteristic, or the identity needs pure complexes, or ordered cells must be replaced by oriented cells with signs). Explain in one paragraph what this has to do with Hoffman's/Knill's torsion (B4, B7) and whether Knill's SDet(D) = prod_k Det(D_k)^{(-1)^k} is literally the u -> 1 limit or a different object.

T2 (The Bass identity for a general complex: does the vertex determinant exist?). For graphs Bass's identity holds for EVERY finite graph, regular or not: det(1 - uT_1) = (1-u^2)^{f_1 - f_0} det(1 - uA + u^2 (Deg - 1)). Drafted, too strong: for every finite 2-complex there is a matrix polynomial P(u) = 1 - u A + u^2 Q_1 - u^3 Q_2 on C^{f_0} (A the adjacency matrix, Q_1, Q_2 built from local counts: degrees, triangle counts per edge, link data) such that det(1 - uT_1) det(1 + uT_2)^{-1} = (1-u^3)^{chi(X)} det P(u)^{-1} ... (fix the exponents and placements from T0(b)). Decide: (i) for which class of complexes does a vertex-level determinant identity of this "degree d+1 in u" shape hold (I expect: those whose vertex links are all "generalised polygons / incidence graphs of the same parameters", i.e. locally building-like; prove it for that class by the Schur-complement method of Theorem 1 of notes/quantum-ihara-general.md applied to the Dirac operator of T4, and give the explicit Q_1, Q_2 in terms of q for A~_2, recovering Kang--Li); (ii) exhibit the smallest 2-complex (e.g. a single triangle, the boundary of a tetrahedron, a triangulated torus, or a cone) for which NO such identity holds with Q_j depending only on local data, and say what replaces it (a determinant over C^{f_0} (+) C^{f_1}? over vertices and edges, i.e. the honest Schur complement, which always exists: WRITE IT DOWN, it is the general-complex Bass identity). The deliverable of T2 is the general identity that always holds, plus the characterisation of when it collapses to the vertex level.

T3 (The graded / supertrace form and the parity dictionary). Drafted: Z(X, u) = sdet(1 - u Tau)^{-1} for the Z_2-graded space W = (+)_k C^{Omega_k} with parity of C^{Omega_k} equal to k+1 mod 2 (edges even, chambers odd, ...), Tau = (+)_k (+-) T_k with the Kang--Yu signs; equivalently log Z = sum_m str(Tau^m) u^m / m, so that the zeros of the total zeta are exactly the eigenvalues of the odd sector and the poles those of the even sector, up to the trivial factor. Then, relative to the VERTEX L-function (the Hecke side, the natural "Hilbert--Polya" transfer matrix on vertices): L_vertex(u) = det(1 + uT_2) / [(1-u^3)^chi det(1 - uT_1)] for A~_2, so the chamber flow supplies ZEROS of the vertex L-function and the edge flow supplies POLES. State the dictionary with the Grothendieck--Lefschetz formula zeta = prod_i det(1 - uF | H^i)^{(-1)^{i+1}} for a curve/variety: which cell dimension k plays the role of which H^i (my guess: k-cells <-> H^{k-1}, so that a graph is "H^0 only", which is why its zeta has no numerator, and a 2-complex's chamber flow is the H^1, fermionic, sector), and check the guess against the WEIGHTS: Kang--Li's RH puts the chamber zeros on |u| in {1, q^{-1/2}, q^{-1/4}}, three circles, not one; explain from the representation theory in C1/C2 (which irreducible constituents of L^2(Gamma\G) contribute which circle: unramified principal series, Steinberg-type, the "one-dimensional" ones) which part of det(1 + L_B u) is the honest weight-1 (curve-like, |u| = q^{-1/2}) sector and which parts are the analogue of H^0/H^2 or of the Euler-characteristic factor. Also relate to Deitmar's degree-WEIGHTED supertrace (A1): is the vanishing order of Z at u^{d+1} = 1 the plain chi or a weighted one? Status per sub-claim.

T4 (The Dirac operator: a fermionic construction for complexes). Generalise Matsuura--Ohta (D1) from graphs to complexes: construct Grassmann fields on ordered cells of all dimensions 0..d, a Dirac-type operator D(u) coupling dimension k to k +- 1 through face/coface maps with u-dependent weights, and prove that det D(u) equals (up to a stated trivial factor) prod_k det(1 - (-1)^{k+1} u T_k)^{(-1)^{k+1}} times the vertex-level determinant when it exists, by integrating out the even-dimensional or the odd-dimensional cells (Schur complement). The point is that the SAME Dirac operator yields (a) the geodesic-flow form by integrating out one set of cells and (b) the Hecke/vertex form by integrating out the other, exactly as Matsuura--Ohta's gamma_5-graded Dirac operator does for graphs; and that the Z_2 grading of the Dirac operator (chirality: cells of even versus odd dimension) is what becomes the supertrace of T3. If a clean construction exists only for locally building-like complexes, say so and give it there; if it exists for all complexes with the general Schur complement of T2 as its vertex form, even better. Draft it as a theorem with the explicit block matrix.

T5 (The Kraus / quantum twist). For Kraus weights on the 1-skeleton (conventions above) define the twisted flow operators T_k^E on V (x) C^{Omega_k} (each step (v_0..v_k) -> (v_1..v_k,w) carries the weight E_{(v_k, w)}, or the weight of the edge the walk traverses: choose and justify the choice that makes the Cayley-complex case an Artin L-function), and prove: (a) for a Cayley complex X = Cay(G, S) with a unitary representation pi of the FINITE group G and E_{(g, gs)} = pi(s) (x) conj(pi(s)) (the channel weights), det(1 - u T_k^{E}) is the pi (x) conj(pi)-isotypic block of det(1 - u T_k) of the regular representation, so every determinant identity proved for X restricts blockwise (make the multiplicity bookkeeping exact, including how the trivial factor (1-u^{d+1})^chi splits between blocks: is the exponent chi * dim(pi)^2 / |G| ... or does the trivial factor only live in the trivial block? decide and prove); (b) the general-complex identity of T2/T4 holds for ARBITRARY Kraus weights (no unitarity, no flatness) with the twisted operators, via the same Schur complement (this is the complex analogue of Theorem 1 of notes/quantum-ihara-general.md; state exactly which of adjoint pairing / inverse pairing / flatness is needed for which part: my expectation is that the Schur-complement identity needs nothing, the torsion form of the trivial factor (T1) needs flatness, and the positivity of ring traces needs adjoint pairing); (c) the trace formula: Tr (T_k^E)^m = sum over closed k-geodesics gamma of length m of Tr_V(E_gamma) and in the Kraus adjoint-paired case = sum |Tr B_gamma|^2 >= 0, i.e. side A is a positive ring norm for EVERY k, so by the notebook's no-go argument the zeros-as-odd-sector structure of T3 must come from the alternating signs between different k, not from any single T_k (state this as a proposition).

T6 (Ramanujan quantum expanders of type A~_{d-1}, and the RH for the twisted zeta). Definition: a family of commuting channels Phi_1..Phi_{d-1} on M_N (Phi_k(rho) = (1/|S_k|) sum_{s in S_k} pi(s) rho pi(s)^dagger for a Ramanujan Cayley complex with colour classes S_k and a unitary rep pi of G) is a RAMANUJAN QUANTUM EXPANDER OF TYPE A~_{d-1} if the joint spectrum of (|S_1| Phi_1, ..., |S_{d-1}| Phi_{d-1}) on the orthogonal complement of the fixed points lies in the tempered set Aspec_d of LSV (C10, math/0406208 lines 553-572). Theorem (Harrow transfer): if X = Cay(G, S) is a Ramanujan complex (LSV main theorem, declare as H-LSV) then for EVERY unitary representation pi of G the channels are a Ramanujan quantum expander of type A~_{d-1}, with fixed-point space = the commutant of pi(G) (so one-dimensional iff pi irreducible), and the pi (x) conj(pi)-twisted total zeta satisfies the Kang--Li / Kang--Yu RH (each factor's nontrivial zeros on its prescribed circles) with the trivial/fixed-point block removed. Prove it from the representation-theoretic content: the pi (x) conj(pi)-block of L^2(X) is a sub-representation of the regular representation of G, decompose into irreducibles of G, each nontrivial one is tempered by H-LSV, and the Kang--Li RH is a statement irreducible-by-irreducible (C1: the four-way theorem is proved via the decomposition of L^2(Gamma\G); declare the fact you need as H-KL). Then the converse question: state precisely whether "Ramanujan quantum expander of type A~_2 for ONE faithful pi" implies X is Ramanujan (I expect yes iff every nontrivial irreducible of G occurs in pi (x) conj(pi); give the criterion). Finally state the ONE-SIDED nature: by C5 (LLP) and C7 (Kamber) the higher-dimensional RH is a band Re s <= 1/2 with interior poles present, so in the notebook's Weil-positivity language (notes/weil-positivity.md, T1/T2) it is a one-sided bound without a duality; identify which factors in Kang--Li's four-way theorem have a genuine duality (a functional equation u -> c/u of their zero set, i.e. a Hilbert--Polya reading) and which are only one-sided.

T7 (The zeta that wants to exist: verdict). One page, plain prose plus one displayed definition: the definition you recommend (state it fully, with the grading and the sign conventions fixed), the theorem list it satisfies with statuses, what it does NOT satisfy for general complexes (be blunt: if a vertex-level Bass identity is a building phenomenon, say so and say what the general complex has instead), and the three smallest concrete test complexes (with vertex counts) on which the Opus numerics lane should verify each identity, including one NON-building 2-complex (e.g. the boundary of the 4-simplex, or a 7-vertex triangulated torus) and what the identity predicts there numerically (give the predicted polynomial or at least its degree and trivial factor, so a script can check it).

T8 (Correction ledger and result line). List every drafted statement you corrected, every hypothesis H-* with its citation address, and a final RESULT: line summarising which propositions are proved-here, which conditional, which sketched, which FALSE-as-drafted.

FORMAT: Markdown with LaTeX math ($...$ and $$...$$), one section per task, Lamport steps, explicit hypotheses. Be complete rather than brief; this file will be reviewed adversarially by another model and then distilled into a lab-book section, so every constant, sign and multiplicity must be right and every step justified. Prefer a smaller true statement to a larger unproved one, but do not omit the formulation questions: T0, T2(ii), T3, T4 and T7 are the point.
