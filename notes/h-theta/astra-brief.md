# Brief for the prover: H-THETA. The odd theta vector on the GL_1 bond, what it does and does not generate under dilation, the GL_1 Riemann channel (Burnol causality), and the GL_2 symbol as a ratio of two GL_1 theta channels

You are the prover in a mathematical research notebook (git repo, current directory). Author line for
everything you write: `codex:gpt-6-astra`. Write ONLY the file `notes/h-theta/astra-proofs.md` (create it;
overwrite if present). Do not edit anything else. Do not run git. You may run python3 (numpy, mpmath, sympy,
scipy are installed) for checks, and you may read any file in the repo, especially
`report/sections/04p_gl1_bond.tex` (the round that stated `conj:h-theta`), `report/sections/04_riemann_channel.tex`
(the Riemann channel, the symbol `S`, `prop:functional-model-modes`), `report/sections/04h_rebound_state.tex`
(the upper-half-plane conventions: `Theta(tau) = S(-tau)` inner in `C_+`, the Paley--Wiener map, the exit
functional `j`), `notes/gl1-bond/proofs.md` (B4: the Mellin identity), and the sources under `refs/src/`
listed in Section 6. Be elementary and explicit; a blind numerics lane will run checks against every displayed
formula and a hostile reviewer will try to refute every claim. Do not pad. Where a claim is a theorem in a
source, cite file and line; where you prove it, prove it.

## 0. Why this round, and the verdict you must reach

The previous round (shard 04p) read Riemann's 1859 argument as the `Q` case of "the adelic symplectic space
is the bond": bond `L^2(R_+^x, d^x y)`, bond state Jacobi's `theta`, automorphism `y -> 1/y` (Poisson), even
sector the two constant terms (the poles), odd sector `phi(y) := theta(y) - 1 - y^{-1/2}` whose Mellin
transform is `Lambda(s) := 4 xi(s)/(s(s-1)) = 2 pi^{-s/2} Gamma(s/2) zeta(s)` in the strip. It then wrote,
as an open conjecture (`conj:h-theta`, status `open`, "stated, not claimed"):

> **H-THETA.** The compression of the dilation semigroup to the dilation-cyclic subspace of the odd part
> `theta - 1 - y^{-1/2}` of the bond state, with the outgoing half `y > 1` removed, is unitarily equivalent to
> the Riemann channel of `prop:functional-model-modes` on `K_S = H^2 (-) S H^2`.

TJO's instruction for this round: go hard on H-THETA. The author of this brief (`claude:fable-5.1`) has
worked the mathematics out far enough to believe the following, which you must prove, refute, or sharpen:

1. **As literally stated, H-THETA is degenerate, hence false.** The odd theta vector (with the weight that
   makes it an `L^2` vector) is *too analytic* to generate an outgoing subspace: its Mellin transform decays
   like `e^{-pi|tau|/2}`, so `log|ghat|` is not integrable against `d tau/(1+tau^2)`, so no simply invariant
   (Beurling--Lax) subspace contains it, so its forward dilation orbit is already dense in the whole bond
   (Section 2). Its full orbit is dense by Wiener (Connes, Theorem III.1 at `delta = 0`: `E` is a surjective
   isometry). Compressing the dilation to a dense subspace gives the bond's own unitary dilation, which has
   no resonances; cutting the outgoing half first gives a Beurling subspace whose inner factor is *not* the
   Riemann channel's symbol (Section 2, T3).
2. **The correct GL_1 statement is Burnol's causality theorem** (`refs/src/math/0001013/`): on the GL_1 bond
   `L^2(C_Q, d^*u)`, the Lax--Phillips system whose outgoing subspace is the orthocomplement of the theta
   images of test functions supported in the unit ball satisfies every Lax--Phillips axiom except causality,
   and causality (`D_- perp D_+`) is *equivalent to RH*. Under RH the GL_1 scattering is trivial; the zeros on
   the line are invisible to it (they are Connes's absorption lines), and each off-line zero is a pole of the
   GL_1 scattering operator. In the notebook's language: **the GL_1 Riemann channel generated by theta has as
   its model space `K_B`, `B` the Blaschke product over the zeros with `Re rho > 1/2`; it is zero iff RH**
   (Section 3).
3. **The GL_2 symbol `Theta(tau) = xi(1+2i tau)/xi(1-2i tau)` is a ratio of two GL_1 theta channels**: with
   `ghat` the Mellin transform of the weighted odd theta vector, `Theta(tau) = [ghat(tau - i/4)/ghat(tau + i/4)]
   (tau - i/2)/(tau + i/2) = [Lambda(1+2i tau)/Lambda(2i tau)] (tau - i/2)/(tau + i/2)`: the two edge lines of
   the critical strip, where the constant terms make the Mellin integral diverge. And these two edge values
   are exactly the two coefficients of the constant term of the *completed Eisenstein series*, which is the
   Mellin transform of the Siegel theta of the lattice `Z z + Z`, whose constant term in `x` is the sum of the
   GL_1 theta in two channels exchanged by the automorphism: `theta(t/y) + sqrt(y/t)(theta(ty) - 1)`
   (Section 4). So the modular surface's cavity sees the zeros as resonances at height `(1-sigma)/2` because
   its exit data are the GL_1 theta evaluated `i/4` above and below the critical line; the GL_1 bond alone
   sees them as absorption lines (Connes, `delta > 1`) or as nothing (Burnol, under RH).
4. **The i/4-shift dictionary** between the Riemann channel's modes and Connes's absorption characters, and
   Burnol's level-one Sonine space as the candidate for "one exit per zero mode" (Section 5).

Your deliverable is a proofs file with a numbered ledger (T1--T6 below, with sub-items), each item ending
with a status from {PROVED, PROVED-CONDITIONAL (name the hypothesis), REFUTED, SKETCHED, OPEN}, a
correction ledger against this brief (every place the brief is wrong, with the correction), and a final
one-paragraph verdict on `conj:h-theta` with the restated conjecture(s) you recommend registering.

## 1. Conventions (fixed; do not change them, but correct them if they are inconsistent)

- Bond: `H = L^2(R_+^x, d^x y)`, `d^x y = dy/y`. `y = |x|^2` with `x` the idele variable at the real place, so
  `d^x y = 2 d^x x`. All Mellin transforms below are in `y`; the notebook's Mellin variable `tau` (shard 04:
  "dilation by `e^t` acts on `L^2(R_+^x)` as the multiplier `e^{i t tau}`") is the `y`-variable of the modular
  surface. Mellin transform: `fhat(tau) = int_0^infty f(y) y^{i tau} d^x y`, unitary `H -> L^2(R, d tau/2pi)`.
  Dilation `(U_t f)(y) = f(e^{-t} y)`, so `(U_t f)^hat = e^{i t tau} fhat`, and `U_t` for `t >= 0` maps
  `L^2(1, infty)` into itself: **the outgoing half is `y > 1`**, it corresponds to `H^2(C_+)` (functions
  supported on `y > 1` have Mellin transforms analytic and of Hardy class in `Im tau > 0`), the incoming half
  `y < 1` to `H^2(C_-)`. Check this sign convention first; if it is wrong, fix it and say so.
- Inversion `(J f)(y) = f(1/y)`: unitary, `J U_t J = U_{-t}`, `(Jf)^hat(tau) = fhat(-tau)`.
- Completed zeta `xi(s) = (1/2) s(s-1) pi^{-s/2} Gamma(s/2) zeta(s)` (the notebook's `\cxi`), `xi(s) = xi(1-s)`,
  real on the real axis and on the critical line; `xi(0) = xi(1) = 1/2`. `Lambda(s) := 4 xi(s)/(s(s-1))
  = 2 pi^{-s/2} Gamma(s/2) zeta(s)`, poles at `0, 1` with residues `-2, +2`.
- Theta: `theta(y) = sum_{n in Z} e^{-pi n^2 y}`, `theta(1/y) = y^{1/2} theta(y)`. Odd part
  `phi(y) = theta(y) - 1 - y^{-1/2}`. Then (B4 of shard 04p, proved) `int_0^infty phi(y) y^{s/2} d^x y = Lambda(s)`
  for `0 < Re s < 1`, divergent outside.
- The Riemann channel (shards 04, 04h): symbol `S(tau) = xi(1-2i tau)/xi(1+2i tau)`, inner in `C_-`;
  upper-half-plane form `Theta(tau) := S(-tau) = xi(1+2i tau)/xi(1-2i tau)`, inner in `C_+`, zeros
  `w = gamma/2 + i(1-sigma)/2` for each nontrivial zero `rho = sigma + i gamma`, all on `Im w = 1/4` iff RH.
  Model space `K_Theta = H^2(C_+) (-) Theta H^2(C_+)`, compressed semigroup `Z_t = P_K e^{i t tau}|_K`,
  no-event semigroup `C_t = Z_t^*`, modes the Cauchy kernels `k_w(tau) = 1/(tau - wbar)`, `C_t k_w = e^{-i t wbar} k_w`
  (`obs:model-modes-sign`). Paley--Wiener: `H^2(C_+) <-> L^2(0, infty)` in `x = log y`, `k_w <-> const . e^{-i wbar x}`.
- Nontrivial zeros `rho = sigma + i gamma`; write `xi_crit(tau) := xi(1/2 + 2i tau)`, an entire function of
  `tau`, even, real on `R`, with `xi_crit(tau) = 0` iff `tau = gamma/2 - i(sigma - 1/2)/2`, real iff `sigma = 1/2`.

## 2. The theta vector and what it generates (T1--T3)

**T1 (weight, symmetry, membership).** Define `g(y) := y^{1/4} phi(y)`.
(a) `phi(1/y) = y^{1/2} phi(y)` exactly (no correction terms: the two constant terms transform into each other).
(b) `g in L^2(R_+^x, d^x y)`; `J g = g`; and among the weights `y^a phi`, `a in R`, the value `a = 1/4` is the
    only one with `J(y^a phi) = y^a phi`, and `L^2` membership holds exactly for `0 < a < 1/2`. (Near `y -> 0`,
    `phi -> -1` super-exponentially fast, `phi(y) = -1 + O(y^{-1/2} e^{-pi/y})`; near `y -> infty`,
    `phi(y) = -y^{-1/2} + O(e^{-pi y})`.)
(c) `g = E(f)` for `f = e^{-pi t^2} (x) 1_{Zhat}` in Connes's normalisation `E(f)(x) = |x|^{1/2} sum_{q in Q^*} f(qx)`
    (`refs/src/math/9811068/main.tex` line 776: `E(f)(g) = |g|^{1/2} sum_{q in k^*} f(qg)`) after subtracting the
    `fhat(0)` term as in Burnol's definition (`refs/src/math/0001013/`, Definition of `E`, around line 200:
    `f(vbar) = sqrt|v| sum_{q in K^x} phi(qv) - (int phi)/sqrt|v|`), in the idele variable `x = y^{1/2}`; state
    exactly which of the two normalisations gives `g` and how the `|x|^{1/2}` becomes `y^{1/4}`.
Status expected: PROVED (elementary).

**T2 (Mellin transform of `g`, its halves, and the symbol).**
(a) `ghat(tau) = Lambda(1/2 + 2i tau) = - xi_crit(tau)/(tau^2 + 1/16) = - xi_crit(tau)/((tau - i/4)(tau + i/4))`:
    meromorphic, even, real on `R`, simple poles at `tau = +-i/4` (from the two constant terms: `s = 1` is
    `tau = +i/4`... check: `1/2 + 2i tau = 1` iff `tau = -i/4`; `= 0` iff `tau = +i/4`), residues `+-i` (compute
    them; `xi(0) = xi(1) = 1/2`), zeros exactly at `tau = gamma/2 - i(sigma-1/2)/2`, real iff RH.
    `|ghat(u)| ~ C |u|^{c} e^{-pi|u|/2}` as `u -> +-infty` on the real line (give `c`, from Stirling and any
    polynomial bound on `zeta(1/2 + it)`). Numerically `ghat(0) = Lambda(1/2) = -16 xi(1/2)`.
(b) Halves. `g_> := 1_{(1,infty)} g`, `g_< := 1_{(0,1)} g = J g_>`. `Lambda_>(s) := int_1^infty phi y^{s/2} d^x y`
    converges and is analytic for `Re s < 1`, `Lambda_>(s) = 2/(s-1) + (entire)`; `Lambda_<(s) := int_0^1` converges
    for `Re s > 0`, `Lambda_<(s) = -2/s + (entire)`; `Lambda_<(s) = Lambda_>(1-s)` (Riemann's splitting, from (T1a)).
    Hence `ghat_>(tau) = Lambda_>(1/2 + 2i tau)` is analytic in `Im tau > -1/4` with a simple pole at `tau = -i/4`,
    lies in `H^2(C_+)`, and `ghat_< (tau) = ghat_>(-tau) in H^2(C_-)` with its pole at `+i/4`; `ghat = ghat_> + ghat_<`.
    On the real line `|ghat_>(u)|` decays only like `1/|u|` (the incomplete Gamma functions
    `Gamma(1/4 + iu, pi n^2)` are `O(1/|u|)`, not exponentially small): so `log|ghat_>| in L^1(du/(1+u^2))`
    while `log|ghat|` is not. Prove both integrability statements.
(c) **The symbol as a ratio of edge values.** For real `tau`,
    `Theta(tau) = xi(1+2i tau)/xi(1-2i tau) = [Lambda(1+2i tau)/Lambda(2i tau)] . (tau - i/2)/(tau + i/2)
    = [ghat(tau - i/4)/ghat(tau + i/4)] . (tau - i/2)/(tau + i/2)`,
    and `xi(1+2i tau) = xi_crit(tau - i/4)`, `xi(1-2i tau) = xi_crit(tau + i/4)`: the numerator and denominator of
    the Riemann channel's symbol are the critical-line `xi` shifted by `-i/4` and `+i/4`, i.e. the Mellin
    transforms of `y^{1/2} phi` and of `phi` (the lines `Re s = 1` and `Re s = 0`, the edges of the strip, where the
    Mellin integral of `phi` diverges and only the continuation exists). In de Branges form `Theta = E^#/E` with
    `E(tau) := xi(1 - 2i tau) = xi(2i tau)`, `E^#(tau) := conj(E(conj tau)) = xi(1 + 2i tau) = E(-tau)`; `E` is
    Hermite--Biehler (`|E(tau)| > |E(conj tau)|` for `Im tau > 0`) iff `Theta` is contractive in `C_+`, which is
    the Lax--Phillips innerness (`prop:scattering-inner`, sketched in the notebook: either prove it here from
    `|xi(s)| <= |xi(s+1)|` for `Re s > 0`, or cite it precisely; note that Hermite--Biehler for the *unshifted*
    `xi_crit` would be RH, while for `E` it is unconditional), and `K_Theta` is isometric to the de Branges space
    `H(E)` via `F -> F/E`.
(d) Is `Theta` a Blaschke product times a unimodular constant (no singular inner factor, no `e^{i a tau}`)? The
    notebook needs this (completeness of the Cauchy kernels in `K_Theta`, Section 5) and only sketches it. Prove it
    or give the precise citation (Lax--Phillips 1976 for the modular surface; or directly: `E` has no zeros in
    `C_+`, `Theta = E^#/E`, and the growth of `E` in `C_+`; note `E` is *not* of exponential type, `|E(iv)|`
    grows like `Gamma(v)`, so the standard Cartwright-class argument does not apply as is).
Status expected: PROVED for (a)--(c); (d) PROVED or PROVED-CONDITIONAL with the hypothesis named.

**T3 (the literal H-THETA is degenerate).**
(a) Full orbit: `closure span{U_t g : t in R} = H` (Wiener: `ghat != 0` a.e.). This is the units-invariant,
    trivial-character, Gaussian sector of Connes's Theorem III.1 at `delta = 0` (`refs/src/math/9811068/main.tex`
    lines 2551--2560: "`E` defines a surjective isometry from `L^2(X)_0` to `L^2(C_k)`") and of Burnol's Lemma
    (`math/0001013`, "`E(S(A_K)) subset L^2(C_K, d^*u)` and is dense in it"). State it in the notebook's terms.
(b) Forward orbit: `closure span{U_t g : t >= 0} = H` as well. Proof shape: a closed subspace of `L^2(R)`
    invariant under `e^{i t tau}`, `t >= 0`, is either doubly invariant (`L^2(E)`, `E` measurable) or simply
    invariant (`q H^2(C_+)`, `|q| = 1`) (Beurling--Lax--Halmos; Burnol `math/0001013` lines 334--346 recalls the
    half-plane form); if `ghat in q H^2` then `log|ghat| in L^1(d tau/(1+tau^2))`, contradicting T2(a); so the
    orbit closure is doubly invariant and, since `|ghat| > 0` a.e., equals `H`. Consequence: **the compression
    of the dilation to the dilation-cyclic subspace of `g` is the dilation itself, unitary, no resonances**, and
    "with the outgoing half removed" then means the compression of `U_t` to `L^2(0,1)`, the backward shift,
    again with no resonances (or to `L^2(1,infty)`, an isometric shift). Either way not `K_Theta`.
(c) Half orbit: `closure span{U_t g_> : t >= 0} = B_> . H^2(C_+)` (in the Mellin picture) with `B_>` the Blaschke
    product over the zeros of `ghat_>` in `C_+`, i.e. of `Lambda_>(s)` in `Re s < 1`, times possibly a singular
    factor which you must exclude (analytic continuation of `ghat_>` across the real line to `Im tau > -1/4`
    excludes a singular measure; exclude `e^{i a tau}` by the support of `g_>`). Show `B_> != Theta`: e.g. show
    that `Lambda_>(1 - rho) != 0` for the first zero (numerically, `mpmath`, and say what an analytic argument
    would need), and more structurally that on the critical line `Lambda(1/2 + it) = 2 Re Lambda_<(1/2 + it)`, so
    the zeros of `zeta` are where `Lambda_<` (equivalently `Lambda_>`) is purely imaginary on the line, not where it
    vanishes. Hence the compression of `U_t` to `L^2(1,infty) (-) [g_>]_+ = K_{B_>}` has resonances at the zeros of
    the *incomplete* Mellin transform, not at the zeros of `zeta`.
(d) Conclude: `conj:h-theta` as printed is REFUTED (state precisely which reading you refuted and that no
    reading of "dilation-cyclic subspace of the odd part with the outgoing half removed" inside the unweighted
    bond `L^2(R_+^x)` gives `K_Theta`, because `Theta` has zeros at height `(1-sigma)/2 > 0` at *every* zero while
    the theta-generated Beurling subspaces on the GL_1 bond have inner factors governed by `Lambda_>`, or, for
    compactly supported test functions, by the *bad* zeros only (Section 3)).
Status expected: PROVED for (a), (b), (d); (c) PROVED with the inequality `B_> != Theta` at least numerical.

## 3. The GL_1 Riemann channel: Burnol's causality (T4)

Read `refs/src/math/0001013/` (Burnol, "An adelic causality problem related to abelian L-functions", 2000)
in full; it is 64K. Its objects, restricted to `K = Q` and to the units-invariant sector:
- `E(phi)(v) = sqrt|v| sum_{q in Q^x} phi(qv) - (int phi)/sqrt|v|` on `L^2(C_Q, d^*u)`; for `phi = f_infty (x) 1_{Zhat}`
  with `f_infty` even Schwartz, `E(phi)(x) = x^{1/2} sum_{n != 0} f_infty(nx) - x^{-1/2} fhat_infty(0)` on `R_+^x`
  (his last section, "one more variation": `E(varphi)(u) = sum_{n >= 1} varphi(nu) - (int_0^infty varphi)/u`, in
  `L^2(R_+, du)`; reconcile the two normalisations, `du` versus `d^x u` and the `sqrt u`).
- `S_{<= 1}`: test functions supported in the unit parallelepiped (`|f_infty| supported in `[-1,1]`, times `1_{Zhat}`);
  `D_+ = E(S_{<=1})^perp`, `D_- = I(D_+)`, `I` the inversion.
- Lemma: all Lax--Phillips axioms hold for `D_+` and `D_-` (with `C_K` in place of `R`) except possibly causality.
- **Theorem (causality criterion):** `D_- perp D_+` iff RH for all abelian `L`-functions of `K`.
- **Theorem (closure criterion):** with `V = 1 - A` the unitary convolution by `a(w) = sqrt|w| 1_{|w| <= 1}`
  (the multiplier `(s-1)/s`, killing the pole), `V(closure E(S_{<=1})) subset HH^2 := L^2(|u| <= 1)`, with
  equality iff RH. Nyman's theorem is the `zeta` case; the inner factor of `((s-1)/s) zeta(s)/s` is the Blaschke
  product `B(s)` over the zeros with `Re rho > 1/2` (his display around line 350), with no singular factor.

Tasks:
(a) Transcribe Burnol's system into the notebook's conventions (Section 1): which of his `D_+`, `D_-` is the
    outgoing half `y > 1` after the unitary `V`, what `U(lambda)` is in terms of `U_t`, and what the sector
    restriction (trivial character, even `f_infty`, units-invariant) does to "all abelian L-functions" (it should
    reduce the causality criterion to RH for `zeta` alone; prove or refute this reduction).
(b) **The GL_1 model space.** Let `M := closure span{U_t E(phi) : t >= 0, phi in S_{<=1}}` (the theta-cyclic
    outgoing subspace) and `V M = B_bad H^2(C_+)` in the Mellin picture (after `V`), `B_bad` the Blaschke product over
    `{rho : Re rho > 1/2}` in the `tau`-variable. Then `K_1 := L^2(1,infty) (-) V M = K_{B_bad}` is the model space of
    the **GL_1 Riemann channel**, the compressed dilation `Z^{(1)}_t = P_{K_1} U_t|_{K_1}` has one mode per bad
    zero (Cauchy kernel at the image of `rho`, decay rate `sigma - 1/2 > 0` in the appropriate variable; compute it),
    and **`K_1 = 0` iff RH**. Prove this from Burnol's closure criterion and Beurling--Lax; make the variable change
    between his `HH^2(Re s > 1/2)` and the notebook's `H^2(C_+)` explicit.
(c) Compare with the GL_2 channel: `K_Theta` has a mode at *every* zero, at height `(1-sigma)/2` (so `1/4` under RH);
    `K_1` has a mode only at the bad zeros. Show that `K_1 = 0 <=> RH <=> all K_Theta modes at height 1/4`: the
    notebook's `prop:functional-model-modes` and Burnol's causality are the same statement seen from the two
    cavities, and write the dictionary (zero `rho` -> `K_Theta` mode `w = gamma/2 + i(1-sigma)/2`; `rho` bad ->
    `K_1` mode at ... ; `rho` on the line -> `K_1` sees nothing, Connes's `delta > 1` cokernel sees the character).
(d) Where do the on-line zeros go in the GL_1 channel? They are where `E(phi)^hat` vanishes on the real
    `tau`-line (Burnol's Lemma: the Fourier--Mellin transform of `E(phi)` is the Tate `L`-function of `phi` on the
    critical line), i.e. absorption lines of the *unitary* dilation on the bond: the spectral measure of `g` under
    `U_t` is `|ghat(tau)|^2 d tau/2 pi`, vanishing to second order at each critical zero. Prove: the
    autocorrelation `C(t) = <U_t g, g> = (1/2pi) int |ghat(tau)|^2 e^{i t tau} d tau` is a positive-definite function
    whose spectral density vanishes exactly at the on-line zeros, and connect `C(t)` to the notebook's ring-norm
    trace (`prop:ringnorm-trace`) if there is a clean statement (the Weil explicit formula for the test function
    `|ghat|^2`); if there is not, say so.
Status expected: (a), (b), (c) PROVED (they are transcriptions of Burnol plus Beurling--Lax); (d) PROVED for the
spectral-measure statement, OPEN or SKETCHED for the trace connection.

## 4. The GL_2 symbol from two GL_1 theta channels (T5)

(a) Siegel theta of the lattice `Z z + Z`, `z = x + i y`: `Theta_z(t) := sum_{(m,n) in Z^2} e^{-pi t |mz + n|^2/y}`.
    Its zeroth Fourier coefficient in `x` is
    `int_0^1 Theta_z(t) dx = theta(t/y) + sqrt(y/t) (theta(ty) - 1)`
    (the `m = 0` terms give `theta(t/y)`; for `m != 0` the sum over `n` and the integral over one period unfold to
    a Gaussian integral `sqrt(y/t)` times `e^{-pi t m^2 y}`). Prove it. Note `sqrt(y/t) theta(ty) = y theta(1/(ty))`
    by the theta functional equation: the constant term is `theta(t/y) + y theta(1/(ty)) - sqrt(y/t)`, the GL_1
    theta in two channels exchanged by the automorphism `t -> 1/(y^2 t)`... state the exact involution.
(b) Mellin transform in `t`: `int_0^infty (theta(t/y) - 1) t^s d^x t = y^s Lambda(2s)` and
    `int_0^infty sqrt(y/t)(theta(ty) - 1) t^s d^x t = y^{1-s} Lambda(2s-1)` (each in its own half plane of
    convergence), so the constant term of the completed Eisenstein series `E^*(z,s) = c int_0^infty (Theta_z(t) - 1)
    t^s d^x t` is `c [Lambda(2s) y^s + Lambda(2s-1) y^{1-s}]`, and the Lax--Phillips scattering coefficient is
    `phi(s) = Lambda(2s-1)/Lambda(2s)`, which at `s = 1/2 + i tau` is `Lambda(2i tau)/Lambda(1+2i tau) = 1/Theta(tau)
    . (tau - i/2)/(tau + i/2)` by T2(c); fix `c` and the notebook's `S(tau) = xi(1-2i tau)/xi(1+2i tau)` against this
    (the notebook's `S` is `phi(1/2 + i tau)` up to the pole Blaschke factor; determine the exact relation and whether
    the notebook's `S` or `phi` is the Lax--Phillips scattering matrix of the wave equation `u_tt = y^2 Delta u + u/4`
    with the cusp form sector removed; cite Lax--Phillips 1976 / Faddeev--Pavlov by statement if no local source).
(c) State the theorem: **the exit data of the modular cavity are the GL_1 theta vector read on the two edge lines
    of the strip**, `Theta(tau) = [ghat(tau - i/4)/ghat(tau + i/4)] (tau-i/2)/(tau+i/2)`, and interpret the `i/4`
    as the `y^{1/2}` weight of the cusp wave equation (`u = y^{1/2} v(log y)` makes the zero mode propagate freely)
    against the `y^{1/4}` of the bond vector `g`: the modular cavity multiplies the bond vector by `y^{1/4}` (to
    `y^{1/2} phi`, which is *not* in `L^2(1, infty)` but is in `L^2(0,1)`) on one side and by `y^{-1/4}` (to `phi`,
    in `L^2(1,infty)` but not `L^2(0,1)`) on the other. Make this precise: which weighted half-vectors are in which
    halves, and whether `Theta H^2(C_+)`, the outgoing subspace of the GL_2 channel, is the closed forward orbit of
    an explicit theta-derived vector in `L^2(1,infty)` (Beurling: it is iff that vector's Mellin transform has inner
    factor exactly `Theta`; a candidate is the Mellin inverse of `Theta(tau) . O(tau)` for a simple outer `O`, e.g.
    `1/(tau+i)^2`; decide whether any such vector is a theta function of `y` in closed form, and if not, say so).
Status expected: (a), (b) PROVED; (c) PROVED for the identity, the rest as far as you can take it.

## 5. The i/4 dictionary and one exit per zero mode (T6)

(a) In the `y`-picture the mode `k_w` of `K_Theta` (`w = gamma/2 + i(1-sigma)/2`) is `const . y^{-(1-sigma)/2}
    y^{-i gamma/2} 1_{y > 1}`, which under RH is `y^{-1/4} . chi_gamma(y) . 1_{y>1}` with `chi_gamma(y) = y^{-i gamma/2}`
    the unitary character of the bond at the zero. Connes's Theorem 1 (`math/9811068` lines 873--886; `delta > 1`)
    says the cokernel of `E` in `L^2_delta(C_k)` carries exactly the characters at the critical zeros with the stated
    multiplicity. Prove: `K_Theta = closure span{ y^{-(1-sigma)/2 - i gamma/2} 1_{y>1} }` (completeness of Cauchy
    kernels at the zeros of a Blaschke product; uses T2(d)), and state the dictionary "cut the absorption character
    to the outgoing half and damp it by `y^{-1/4}`" as a theorem, with what fails at an off-line zero (the damping
    rate becomes `(1-sigma)/2 != 1/4` and Connes's `delta > 1` cokernel does not contain that character).
(b) Are the Cauchy kernels at the zeros a Riesz basis of `K_Theta`? Under RH the zeros `w_n = gamma_n/2 + i/4` are
    not uniformly separated in the hyperbolic metric (spacing `~ pi/log gamma_n -> 0` at height `1/4`), so the
    Carleson condition fails: prove that the kernels are complete and minimal but not a Riesz basis (or refute). This
    matters for `thm:arithmetic-metric` (one exit per zero mode) and for `thm:flag-spectrum-attainable`.
(c) Burnol's level-one Sonine space (`refs/src/math/0203120/`, Theorem A, lines 516--526: the evaluators
    `Y^a_{rho,k}` at the zeros are minimal in `L_a` iff `a <= 1`, complete iff `a >= 1`, so complete *and* minimal
    exactly at `a = 1`; for `a < 1` the complement is the co-Poisson subspace `P_a`; Theorem B, line 587: the
    functions `zeta(s)/(s-rho)^l` are minimal and complete in `Lhat_1`): transcribe `L_a` and `K_a` (functions on
    `(0, infty)` vanishing on `(0,a)` together with their cosine transform, with the constant terms allowed in `L_a`)
    into the notebook's bond language. Is `L_1` (level one, complete and minimal, one evaluator per zero *with
    multiplicity, on or off the line*) the natural arithmetic model space with "one exit per zero mode"? State what
    is proved (Burnol) and what would be needed (a dilation semigroup on `L_1` whose modes are the evaluators; note
    `L_a` is *not* invariant under dilation, `U_t L_a = L_{a e^{+-t}}`; so the family `{L_a}` is a filtration and the
    dilation moves along it, which is exactly Lax--Phillips's outgoing/incoming filtration `D_+ (t) = U_t D_+`).
    Formulate the sharpest conjecture you can defend (H-THETA-1): a Lax--Phillips system on the GL_1 bond built
    from the Sonine filtration whose model space at level one has the zeros (all of them, with multiplicity) as modes.
Status expected: (a) PROVED; (b) PROVED or REFUTED; (c) SKETCHED with a precise conjecture.

## 6. Sources (all under `refs/src/`; cite `<id>:<file>:<line>` with a verbatim snippet for anything quoted)

- `math/9811068/main.tex`: Connes 1998, "Trace formula in noncommutative geometry and the zeros of the Riemann
  zeta function". `E` at line 776 and 2551; Theorem III.1 (surjective isometry at `delta = 0`) invoked at
  2551--2560; Theorem 1 (cokernel, `delta > 1`, critical zeros) at 873--886; the cutoff `Q_Lambda`, `S_Lambda`
  and the positive operator `S_Lambda - Q'_Lambda` at 2523--2600 (Theorem 5, positive characteristic; the
  number-field analogue is stated to be equivalent to RH at 2251); `E(f)(x) = E(fhat)(1/x)` at 2580.
- `math/0001013/*.tex`: Burnol 2000, causality. Definition of `E` (~line 200), `S_{<=1}`, `D_+`, `D_-`, the
  Lax--Phillips lemma (~247), Theorem "causality criterion" (~268), Theorem "closure criterion" (~290), Nyman
  (~308), Beurling--Lax and the Blaschke product `B(s)` (~334--360), the operator `V`, the `T(phi)` and `E(varphi)`
  variations (~420--470).
- `math/0203120/*.tex`: Burnol 2002, "Two complete and minimal systems associated with the zeros of the Riemann
  zeta function". de Branges's theorem on `K_a` (437), Proposition on `L_a` (~455), Theorems A, A', B (516--600),
  Theorem `main` (989: the residue expansion `sum_rho G(rho)/zeta'(rho) . zeta(Z)/(Z - rho)` converging in `L^2` on
  the critical line for `G in Lhat_1`).
- `math/0112254/*.tex`: Burnol, "On Fourier and Zeta(s)" (the co-Poisson formula, `L_a`, `K_a` in detail).
- `math/0311468/main.tex` (Latin-1; use `grep -a`): Meyer 2005, "On a representation of the idele class group
  related to primes and zeros of L-functions". A *virtual* representation `pi_+ (-) pi_-` of `C_K` with the two poles
  in `pi_+`, the zeros in `pi_-`, multiplicity the pole order, character the Weil distribution (Introduction,
  178--260; Section "The global difference representation", 3538--4632). This is the notebook's "pole even, zeros
  odd, supertrace the explicit formula" (shard 04b) on the idele class group; record the correspondence in one
  paragraph and note his sentence "in Connes' situation zeros on the critical line appear as above, but zeros off
  the critical line appear as resonances".
- `math/0407443`, `math/0602425`, `math/9911175`, `math/9809119`, `math/0208121`: further Burnol (co-Poisson
  intertwining; scattering and `Gamma(1-s)/Gamma(s)`; scattering for time series and the zeta function of a curve;
  the explicit formula and a propagator; Sonine spaces). Use if helpful; not required.
- The notebook: `report/sections/04p_gl1_bond.tex`, `04_riemann_channel.tex`, `04h_rebound_state.tex`,
  `04i_cusp_graph_scattering.tex` (the discrete Lax--Phillips construction `D_+ = O_+ cap O_-^perp` and the review's
  hypothesis H-MINIMAL in `notes/reviews/cusp-graph-2026-09-20.md`, finding 2).

## 7. Format of `notes/h-theta/astra-proofs.md`

Header (author line, brief, date 2026-09-21). Then Sections T1--T6 with every sub-item numbered exactly as above,
each a statement, a proof or citation, and a status line. Then "Correction ledger against the brief" (a table:
item, what the brief says, what is true). Then "Verdict on conj:h-theta" (one paragraph) and "Restated
conjectures to register" (each with a name, a precise statement, and a status you can defend). Then "What the
numerics lane should check" (a list of concrete numerical assertions with expected values to 8 digits where you
can compute them). Keep every formula in the conventions of Section 1. If you find that Section 1's sign
convention for the outgoing half is inconsistent with `04h`, say which one the notebook must adopt and use it
consistently. Do not write anything you cannot defend to a hostile reviewer.
