# Brief for the prover: the genus-two hedgehog. The quotient graph GL_2(R) \ T for R the coordinate ring of y^2 = x^5 + x^3 + x^2 - 2 over F_5 minus its point at infinity, its fifteen cusps, its finite core, and the H-CLASS test: is the equal-depth scattering matrix a Pic^0-group matrix whose Fourier blocks carry the fourteen L(T, chi)?

You are the prover in a mathematical research notebook (git repo, current directory). Author line for
everything you write: `codex:gpt-6-astra`. Write ONLY the file `notes/genus-two-quotient-graph/astra-proofs.md`
(create it; overwrite if present) and, if you want scratch computations, python files under
`notes/genus-two-quotient-graph/checks/`. Do not edit anything else. Do not run git. python3 with numpy, sympy is
installed; exact arithmetic over `F_5` and its extensions in python is expected. Read first:

1. `notes/genus-two-bond/astra-proofs.md` (this morning's lane; binding): E1 the curve `C: y^2 = x^5 + x^3 + x^2 - 2`
   over `F_5`, `P_0 = infinity`, `Pic^0(F_5) = Z/15` generated by `G = [(1,1) - P_0]`, the fifteen reduced Mumford pairs,
   `h^0` table, `K_C ~ 2 P_0`; E2 the fourteen `L_k(T) = 1 + A_k T + 5T^2`; E6: "there are fifteen cusp classes for
   `GL_2(O(C \ P_0))` (Lorscheid `1012.3513:hecke.tex:992`, Arends–Peterson–Weich 2026); if the genus-two scattering
   matrix has the form `S = M P_inv`, `M_ab = f(b - a)`, postulated at `04k:290-304`, its Fourier transform is one
   scalar block and seven blocks `[[0, m_k],[m_{15-k}, 0]]`, `m_k = sum_j f(j) zeta^{-kj}`, eigenvalues
   `+-sqrt(m_k m_{15-k})`; H-CLASS predicts `L`-ratios ... Proving that this actual genus-two quotient has that
   scattering matrix ... is OPEN."
2. The notebook's cusp cavities: `report/sections/04i_cusp_graph_scattering.tex`, `04j_cusp_graph_renewal.tex`
   (a finite graph with a cusp: the scattering matrix of a diagram = core + rays, `z = q^{s-1/2}`-type variable),
   `04k_elliptic_cavity_scattering.tex` (the elliptic diagrams D2 (`y^2 + y = x^3 + x + 1` over `F_2`) and D3
   (`y^2 = x^3 + x + 1` over `F_3`, Takahashi's Figure 5), `thm:multi-exit-scattering`, `det S = (-1)^h p/ptilde`,
   **H-CLASS at lines 290–304**: the equal-depth-cut scattering matrix is a `Pic`-group matrix times the inversion
   pairing), `04l_elliptic_cavity_channel.tex`, `04m_elliptic_cavity_sources.tex` (`cit:lorscheid-cusp-count`,
   `cit:apw-cusps-pic-no-funnels`: cusps = `Pic(R)`, no funnels), `04p_gl1_bond.tex` (`prop:cusps-are-class-group-bond`,
   `prop:d3-group-matrix`, `thm:d3-height-diagonalisation`, `obs:h-class`), `02d_definitions_complexes.tex`.
3. Sources in `refs/src`: `1012.3513` and `1012.3223` (Lorscheid: graphs of Hecke operators; the structure of
   `GL_2(R) \ T` for a curve of genus `g`: a finite core and `h = |Pic^0|`-many cusps (rays), vertices = rank-two
   vector bundles / `Pic`-classes of lattices; his description of the cusps via `Pic` and of the "nucleus"), the
   APW source if present (search `arends`, `peterson`, `weich`), Serre's Trees II.2 (not in the repo: cite precisely,
   not byte-cited), Takahashi (search `takahashi`). Quote by file and line.

Be explicit and computational: the target is an explicit finite graph with rays (vertex list, edge list with
weights/multiplicities, ray attachments) that the numerics lane can rebuild and whose scattering matrix it can
compute with the notebook's `scripts/cusp_graph.py`/`scripts/elliptic_cavity.py` machinery (read them). Label
PROVED, REFUTED, SHARPENED or OPEN. Keep a correction ledger. Do not pad.

## 0. Why this round

The notebook's D2 and D3 cavities are genus one, where every class character carries a constant `L`-function and
H-CLASS is a `2 x 2` shadow. Genus two is the first case where the fifteen cusps carry fourteen nonconstant quadratic
`L`-factors, so H-CLASS (the scattering matrix at equal depth is a group matrix on `Pic^0` block-diagonalised by
Fourier into inverse-character `2 x 2` blocks whose entries are `L`-ratios) becomes a real prediction. The author of
this brief (`claude:fable-5.1`) asks for the graph and the test.

## 1. Claims

**Q1 (the quotient graph).** With `R = O(C \ P_0)` (the affine coordinate ring, `F_5[x, y]/(y^2 - f)`), `K = F_5(C)`, and
`T` the Bruhat--Tits tree of `PGL_2(K_{P_0})` (degree `q + 1 = 6`, since `P_0` is a rational place), describe
`GL_2(R) \ T` (or `PGL_2(R) \ T`; fix the convention of 04k) as a finite graph with rays: by Serre/Lorscheid the
vertices are the isomorphism classes of rank-two vector bundles on `C` (or `R`-lattices in `K^2` up to scaling), the
graph has `h = 15` cusps (one ray per class of `Pic^0`, since `Pic(R) = Pic^0(C)` when `P_0` is rational), and a finite
core (Lorscheid's "nucleus"). Construct it explicitly for this curve: enumerate the vertices of the core (rank-two
bundles that are not "cusp-like", i.e. not of the form `L (+) L'` far out), the vertex stabiliser orders (the
`S(v)` of 04k's stabiliser-index convention), the edges with their indices, and the point at which each ray attaches
(the "cusp-like" bundles `O(D) (+) O(D')` with `deg D - deg D'` large, indexed by `[D] in Pic^0` once normalised). Give
the counts (core vertices, core edges), the stabiliser data, and a drawing-free adjacency list. If exhaustive
enumeration of rank-two bundles is infeasible by pure thought, give the algorithm precisely (e.g. Lorscheid's
lattice/`Pic`-parametrisation, or the double-coset description `GL_2(R) \ GL_2(K_{P_0}) / K_{P_0}`-stabiliser via
strong approximation and the class set) and compute what you can; state the expected structure from the Euler
characteristic / mass formula (the sum of `1/|Stab|` over vertices is a known invariant: give it).

**Q2 (the scattering matrix and H-CLASS).** With the diagram of Q1 (core plus fifteen standard rays after the
notebook's normalisation of couplings `c_a^2 = S(v)/S(e)`, 04k), write the scattering matrix `S(z)` of
`thm:multi-exit-scattering` and its determinant `(-1)^h p/ptilde`; prove or refute H-CLASS: in the basis of cusps
indexed by `Pic^0`, at equal depth, `S = M P_inv` with `M` a group matrix (`M_ab = f(b - a)`) times the inversion
pairing; if yes, compute the Fourier blocks and identify `m_k` in terms of the fourteen `L_k(T)` of the morning lane
(the expectation, by analogy with the level-one and prime-level `Q` results of `notes/deninger-cusp/astra-proofs.md`
D3–D4 and the function-field `H-ARITH` lane `notes/h-arith-fqt/`: `m_k m_{15-k}` is a ratio
`L_k(1/(qz^2)-ish)/L_k(...)`-type of the completed `L`-functions in the tree variable, and the trivial block carries
the zeta ratio with the Perron poles); if H-CLASS fails, say exactly how (which symmetry of `Pic^0` acts on the graph
and how the cusps are permuted; whether the group-matrix form needs the "equal depth" cut or an "equal height" cut).
The expected divisor: `p(z)` should have the `4 + 14 * 2 = 32` Hasse--Weil-type zeros of `P_Y` (the genus-16 cover's
numerator, E2) split among the channels, plus the local/delay factors of the core, with the `2 x 2` inverse-pair
blocks pairing `chi_k` with `chi_{15-k}`; check the count against `deg det Theta` and the number of exits.

**Q3 (what the automorphism does).** The Riemann--Roch involution `D -> K_C - D` (`K_C ~ 2 P_0`) acts on `Pic^0` as
`[D] -> -[D]` (E6: `(j, n) -> (-j, 2 - n)`); on the graph it should be the involution exchanging the cusp `[D]` with
the cusp `[-D]` (the "fold" of the two ends); identify it as an automorphism of the diagram (does the core admit it?)
and its effect on `S` (the inverse-pair blocks). Say whether this is the geometric fold that E6 left open.

**Q4 (what this changes).** One page: H-CLASS at genus two settled or not; the diagram as the first cavity whose
character channels carry zeros; the count of exits (`15`) against modes; what the numerics lane must build.

## 2. Output format

`notes/genus-two-quotient-graph/astra-proofs.md`: ledger (Q1–Q4, verdicts); the explicit graph data as tables; full
proofs with hierarchical steps; external theorems stated precisely and marked "not byte-cited"; repo sources
quoted by file and line; a section "Numerical checks for the blind lane" (vertex/edge/stabiliser lists, the
scattering matrix at two values of `z`, the determinant against `p/ptilde`, the Fourier blocks against `L_k`); a
section "Corrections to the brief"; a closing "What this changes in the notebook" with statuses and one-sentence
statements.

## 3. Durability (the network is unreliable today; this is mandatory)

Write your output file **incrementally**: create `astra-proofs.md` with the ledger table (all verdicts `PENDING`)
before you start, and after finishing each claim rewrite the file with that claim's section and its updated ledger
row. If you are resumed after an interruption, read your own `astra-proofs.md` first, keep everything already
written, and continue from the first `PENDING` row. Keep a one-line `notes/genus-two-quotient-graph/progress.txt`
with the claim you are working on.
