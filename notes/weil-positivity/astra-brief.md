You are the prover in a mathematical research notebook (git repo, current directory). Author line for everything you write: codex:gpt-6-astra. Write ONLY the file notes/weil-positivity/astra-proofs.md (create it; overwrite if present). Do not edit anything else. No web access is needed; do not assume any. You may run python3 (numpy, mpmath, sympy) with `timeout` for sanity checks, writing scratch scripts only under notes/weil-positivity/scratch/.

CONTEXT (read these first): notes/quantum-ihara-general.md (the Lamport proof style and status tags we use; its Theorem 1 is the Ihara--Bass formula for the non-backtracking superoperator T of an arbitrary family of superoperators E_1..E_D on a finite-dimensional space V with a fixed-point-free reversal i -> ibar; Corollary 2 is the Kraus case E_i = Ad(A_i), A_{ibar} = A_i^dagger, with the trace formula Tr T^l = sum over cyclically non-backtracking words w of length l of |Tr A_w|^2 >= 0; Corollary 3 is the inverse-paired case E_{ibar} = E_i^{-1}, where det(1 - uT) = (1-u^2)^{N(D-2)/2} det(1 - u Sigma + (D-1) u^2) with Sigma = sum_i E_i, N = dim V). Also notes/riemann-channel-note.md sections 3 and 5 (compressed semigroup Z(t) with one mode per zeta zero, eigenvalue exp(-sigma_n t/2 + i gamma_n t/2) for the zero rho_n = sigma_n + i gamma_n; distributional trace supported at t = 2 log n with weights Lambda(n) n^{-1/2}; RH iff all modes decay at the single rate 1/4).

THE QUESTION. In the notebook every zeta function has side A (a nonnegative count of rings, e.g. Tr T^l = sum_w |Tr A_w|^2) and side B (a transfer operator whose eigenvalues are the zeros). RH for zeta is "every zero rho is fixed by the reflection rho -> 1 - conj(rho)", and Weil's criterion says RH holds iff the form sum_rho ghat(rho) conj(ghat(1 - conj rho)) is >= 0 for all test functions g, the sum being computable from the primes by the explicit formula. We want the same structure stated and proved for a COMPLETELY ARBITRARY finite-dimensional transfer operator (any Kraus family, any superoperator family, and the continuous/Lindblad case), in particular: what "the mode at rho paired with the mode at 1 - conj(rho), RH says every mode is its own partner" means when there is NO functional equation, and exactly which extra structure (a duality of the spectrum) turns a one-sided bound into the two-sided "all on one circle" statement. I have drafted the statements below. If any is false as drafted, or needs a different hypothesis or constant, say so plainly, correct it, and prove the corrected version. Do not round up. Keep a CORRECTION LEDGER at the end listing every drafted statement you changed and why.

CONVENTIONS. X is a linear operator on a finite-dimensional complex vector space; spec(X) is its spectrum as a MULTISET (algebraic multiplicities), so Tr X^l = sum_{mu in spec X} mu^l for l >= 0. A "trivial set" S is a sub-multiset of spec(X). r > 0 is a "critical radius". Define for l >= 0 the rescaled trace sequence nu_l = nu_l(X; S, r) := r^{-l} ( Tr X^l - sum_{mu in S} mu^l ) = sum_{mu in spec(X) minus S} (mu/r)^l, and extend to l < 0 by nu_{-l} := conj(nu_l). A sequence (nu_l)_{l in Z} with nu_{-l} = conj(nu_l) is POSITIVE DEFINITE if sum_{l,m=0}^{L} c_l conj(c_m) nu_{l-m} >= 0 for every L and every c in C^{L+1}. The WEIL FORM of X (relative to S, r) is W(c) := sum_{l,m} c_l conj(c_m) nu_{l-m}. For a finitely supported c write f_c(z) = sum_l c_l z^l. The REFLECTION in the critical circle is J(mu) := r^2 / conj(mu) (inversion in the circle |mu| = r; its fixed points are exactly that circle). The MODE-PAIRING FORM is M(c) := sum_{mu in spec(X) minus S} f_c(mu/r) conj( f_c( J(mu)/r ) ), defined when 0 is not in spec(X) minus S.

TASKS. For each give the statement as a Theorem/Proposition/Lemma with a label, hypotheses listed explicitly, a Lamport-structured proof (<1>1, <1>2, ... each with PROOF; sub-steps <2>k where needed), and a status line: proved-here | sketched | conditional-on <hypothesis>. Cite only textbook facts (Herglotz, Bochner, Fourier series of the Poisson kernel) and state each such fact verbatim as an explicit hypothesis H-* with its standard name.

T1 (Weil positivity is the one-sided bound). Theorem: nu(X; S, r) is positive definite iff |mu| <= r for every mu in spec(X) minus S (multiset; a zero eigenvalue outside S is allowed and should be handled). Suggested route: (<=) each z = mu/r with |z| < 1 contributes to sum_{l,m} c_l conj(c_m) z^{[l-m]} (where z^{[k]} := z^k for k >= 0 and conj(z)^{-k} for k < 0) the value (1/2pi) int |f_c(e^{i theta})|^2 P_z(theta) d theta with P_z the Poisson kernel (1 - |z|^2)/|1 - z e^{-i theta}|^2 > 0 (state and verify the Fourier series of P_z, including the case z = 0), and each |z| = 1 contributes |f_c(z)|^2; sum of nonnegative terms. (=>) a positive definite sequence satisfies |nu_l| <= nu_0 (prove it from the 2x2 minors); if some |z| > 1 then nu is unbounded: prove a GROWTH LEMMA: for distinct unimodular e^{i theta_j} and positive integers m_j, the sequence a_l = sum_j m_j e^{i l theta_j} does not tend to zero (e.g. via (1/L) sum_{l<L} |a_l|^2 -> sum_j m_j^2), and then separate the eigenvalues of maximal modulus R > r from the rest.

T2 (A duality makes it two-sided, and turns the Weil form into a mode pairing). Theorem: suppose the multiset spec(X) minus S is invariant under J (mu -> r^2/conj(mu)), in particular contains no 0. Then (a) W(c) = M(c) for every finitely supported c (this is the "inflow identity" in its finite form: the Weil form built from traces equals the pairing of each mode with its reflected partner); (b) nu is positive definite iff |mu| = r for every mu in spec(X) minus S, i.e. iff every mode is its own partner under J; (c) if the invariance is only under the linear map mu -> r^2/mu (the "functional equation") then (a) still holds provided spec(X) minus S is also closed under complex conjugation, and (b) holds under mu -> r^2/mu alone (prove both). Also state and prove the elementary fact: for a J-invariant multiset, W(c) = sum over J-orbits, and an orbit {mu, J mu} with mu not on the circle contributes 2 Re( f(z) conj f(1/conj z) ), which can be made negative by a choice of c (this gives a second, direct proof of the "only if" in (b), and exhibits the analogue of Weil's off-line pair argument).

T3 (Kraus dichotomy: which pairing gives which half of RH). Setting of Theorem 1 of notes/quantum-ihara-general.md: V = M_n with Hilbert-Schmidt inner product, D = 2m, E_i = Ad(B_i) for matrices B_1..B_D with a fixed-point-free reversal i -> ibar, T the non-backtracking superoperator, Sigma = sum_i E_i. Prove: (a) ADJOINT PAIRING (B_{ibar} = B_i^dagger): Tr T^l = sum_w |Tr B_w|^2 >= 0 (this is Corollary 2; quote it, do not reprove), AND spec(T) is closed under complex conjugation, because the antilinear map C(v tensor |i>) = v^dagger tensor |i> is an antiunitary involution on W = M_n tensor C^D commuting with T (prove this). (b) INVERSE PAIRING (B_{ibar} = B_i^{-1}, all B_i invertible, D >= 3): with S_0 := the multiset consisting of +1 and -1 each with multiplicity N(D-2)/2 (N = n^2), the multiset spec(T) minus S_0 is invariant under mu -> (D-1)/mu (from Corollary 3: the roots of det(1 - u Sigma + (D-1) u^2) pair as mu mu' = D-1; make the multiset bookkeeping precise, including how the eigenvalues of Sigma map to pairs of eigenvalues of T and what happens when u^2 = 1/(D-1) is a double root). Also: is spec(T) minus S_0 closed under conjugation in the inverse-paired case? Answer precisely (it is iff spec(Sigma) is closed under conjugation; give a 1x1 or 2x2 counterexample where it is not). (c) BOTH pairings hold simultaneously iff every B_i is a nonzero scalar multiple of a unitary (prove: Ad(B^dagger) = Ad(B)^{-1} iff B^dagger B is a scalar). (d) Assemble: for an adjoint-paired Kraus family, Weil positivity of nu(T; S, r) is equivalent to the ONE-SIDED bound |mu| <= r off S, for any choice of S and r (T1); for unitary-up-to-scalar families with r = sqrt(D-1) and S = S_0 union {the pairs coming from the eigenvalues +-1 of Phi = Sigma/D, i.e. mu in {D-1, 1} and, if -1 is an eigenvalue of Phi, mu in {-(D-1), -1}, with multiplicities}, Weil positivity is equivalent to Hastings' Ramanujan bound: every eigenvalue lambda of Phi other than +-1 satisfies |lambda| <= 2 sqrt(D-1)/D, and equivalently every nontrivial mode is its own partner under J. Be explicit about why Phi is Hilbert--Schmidt self-adjoint in the unitary case (so lambda is real) and about the bookkeeping of multiplicities. (e) State the negative fact as a proposition: for an adjoint-paired family that is not unitary-up-to-scalar, there is in general no duality of spec(T), so "every mode is its own partner" has no partner to refer to; give an explicit small example (n = 1 or 2, D = 4) with numerically or symbolically computed spectrum of T whose nontrivial part is not invariant under mu -> c/mu for any c, and also show that the Weil form is nevertheless well defined and its positivity is exactly the one-sided bound.

T4 (Continuous case: Bochner replaces Herglotz). (a) Theorem: L a linear operator on a finite-dimensional space, S a trivial sub-multiset of spec(L), a in R. Put F(t) := e^{-a t} ( Tr e^{tL} - sum_{rho in S} e^{t rho} ) for t >= 0 and F(-t) := conj(F(t)). Then F is a continuous function on R, and F is positive definite (sum_{j,k} c_j conj(c_k) F(t_j - t_k) >= 0 for all finite families) iff Re rho <= a for every rho in spec(L) minus S. Route: each rho with Re(rho - a) = -b < 0 gives e^{-b|t| + i omega t}, whose Fourier transform is the positive Lorentzian 2b/(b^2 + (xi - omega)^2) (compute it), Re = a gives a point mass, and positive definite implies |F(t)| <= F(0) which rules out Re rho > a via a continuous growth lemma. (b) Duality: if spec(L) minus S is invariant under rho -> 2a - conj(rho) (reflection in the line Re = a), then F positive definite iff Re rho = a off S, and the Weil form sum c_j conj c_k F(t_j - t_k) equals the mode pairing sum_rho ghat(rho) conj(ghat(2a - conj rho)) for g = sum c_j delta_{t_j}, where ghat(rho) := sum_j c_j e^{t_j (rho - a)}; state and prove the exact identity with all signs. (c) Side A positivity for the continuous Kraus (cMPS / Lindblad-type) generator: for L(rho) = K rho + rho K^dagger + sum_j R_j rho R_j^dagger on M_n (arbitrary K, R_j in M_n, no trace preservation assumed), prove via the Dyson expansion that Tr_{M_n tensor M_n^*} e^{tL} (the trace of e^{tL} as a linear map on M_n) equals sum_{k>=0} sum_{j_1..j_k} int_{0<t_1<...<t_k<t} |Tr( e^{(t-t_k)K} R_{j_k} ... R_{j_1} e^{t_1 K} )|^2 dt_1...dt_k >= 0, the continuous ring norms, and that this generator commutes with the antiunitary rho -> rho^dagger, so spec(L) is closed under conjugation. (d) Remark (no proof needed, but state precisely): for a Hamiltonian generator L = -i[H, .] every eigenvalue is purely imaginary, so with a = 0 the two-sided statement holds trivially; this is the Hilbert--Polya situation, and the content of RH in this language is the existence of a presentation in which the transfer generator is a scalar plus i times a Hermitian operator.

T5 (Hilbert--Polya inner product). Proposition: let X act on the finite-dimensional space V, let V' be the sum of the generalised eigenspaces for the eigenvalues in spec(X) minus S (assume S is a union of full eigenvalue classes so V' is X-invariant). Then the following are equivalent: (i) X|_{V'} is diagonalisable and |mu| = r for all its eigenvalues; (ii) there is an inner product on V' in which X|_{V'}/r is unitary. Continuous version: L|_{V'} - a is skew-adjoint in some inner product iff L|_{V'} is diagonalisable with Re rho = a. Then the REMARK that Weil positivity (T1/T2) is blind to Jordan structure: give the explicit 2x2 example of a Jordan block on the circle for which nu is positive definite but no inner product makes X/r unitary; hence "RH <=> Weil positivity" holds with multiplicities, but "RH <=> a Hilbert--Polya inner product exists on the modes" needs semisimplicity in addition.

T6 (The zeta instance, formally). Without proving anything about zeta: state precisely what T4(b) becomes for the compressed semigroup Z(t) of notes/riemann-channel-note.md, i.e. identify L (the generator of Z(t), modes (-sigma_n + i gamma_n)/2), a = -1/4, S (the two trivial modes coming from the poles of xi at s = 0 and s = 1, and where the archimedean Gamma-factor terms sit: they are not discrete modes), the reflection (rho -> 1 - conj(rho) becomes which map on the modes of Z), the test-function form (Bochner--Schwartz for distributions in place of Bochner), and the identity of the Weil form with the autocorrelation of the trace of Z: sum_rho ghat(rho) conj(ghat(1 - conj rho)) = int int g(t) conj(g(s)) e^{(t-s)/4} Tr Z(t-s) dt ds, formally. State explicitly which steps are formal (infinitely many modes, distributional trace, convergence) and what hypothesis would make them rigorous. Status: sketched / conditional. This section is a dictionary entry, not a theorem.

T7 (Correction ledger and what is established). List every drafted statement you corrected, the hypotheses H-* you introduced, and a final RESULT: line summarising which propositions are proved-here, which conditional, which sketched.

FORMAT: Markdown with LaTeX math ($...$ and $$...$$), one section per task, Lamport steps, explicit hypotheses. Be complete rather than brief; this file will be reviewed adversarially by another model and then distilled into a lab-book section, so every constant, sign and multiplicity must be right and every step justified.
